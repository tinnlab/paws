{"angle":"root-cause-verification","finding":"BUG_ANALYSIS's diagnosed cause re-derived from live state, not from the doc","verdict":"CONFIRMED","evidence":"curl -r 0-15 of ggml-base-q5_1.bin from ggerganov/whisper.cpp -> '6c6d 6767  lmgg'; pre-fix has_whisper_magic (git show 2ab8d9004^) compares &bytes[..4] == b\"ggml\" (67 67 6d 6c). Live rig :29500 GET /api/voice/models -> [], GET /models/catalog -> base-q5_1 size_bytes 59707625 (=56.94 MB) installed:false; psql ziee_live_view voice_models -> 0 rows; voice-models/ empty.","action":"none — CONFIRM"}
{"angle":"root-cause-verification","finding":"the 'download produced no valid whisper model bytes' site (pre-fix model.rs:452) does NOT emit the same string as the first-chunk site, contrary to BUG_ANALYSIS section 4","verdict":"CONFIRMED (doc imprecision only)","evidence":"pre-fix source: first-chunk site -> 'file is not a whisper ggml/GGUF model (bad magic)'; post-stream site -> 'download produced no valid whisper model bytes'. Both carry code VOICE_MODEL_INVALID.","action":"corrected the sentence in BUG_ANALYSIS.md; the substantive claim (empty body conflated into the same REJECTION/code) stands"}
{"angle":"correctness","finding":"corrected has_whisper_magic is a strict WIDENING of the pre-fix accept set","verdict":"CONFIRMED","evidence":"accepts GGML_MAGIC_LE | GGML_MAGIC_BE | GGUF_MAGIC; GGML_MAGIC_BE == b\"ggml\" and GGUF_MAGIC == b\"GGUF\" are exactly the pre-fix pair, so no previously-accepted input can now fail.","action":"none"}
{"angle":"correctness","finding":"ModelRejection::classify ordering (Empty -> Truncated -> BadMagic) is right for both call sites","verdict":"CONFIRMED","evidence":"download passes (head, downloaded); upload passes (upload.head, upload.size). total_len==0 short-circuits before any head inspection, so an empty 200 / empty upload can never be reported as bad magic.","action":"none"}
{"angle":"resource-lifecycle","finding":"finalize_download's cross-device copy fallback returns the error WITHOUT removing the temp, and a part-way copy leaves a TRUNCATED destination that installed_model_path (exists + len>0) reports as an installed model","verdict":"CONFIRMED — REAL DEFECT","evidence":"pre-fix body: std::fs::copy(tmp,dest).map_err(...)? then remove_file(tmp) — the ? returns before the removal. installed_model_path (model.rs) returns the first file that is_file() && len()>0.","action":"FIXED in fix-round 2; TEST-14 pins it and was verified RED against the pre-fix body ('the temp must not leak on a failed publish')"}
{"angle":"resource-lifecycle","finding":"no reclamation of *.tmp orphaned by SIGKILL/OOM-kill mid-transfer under voice-models/","verdict":"CONFIRMED — REAL GAP","evidence":"every error exit removes its own temp and the upload path has TempGuard, but a killed process cannot run either. Nothing else deletes them: the library list is DB-backed and installed_model_path only matches ggml-<name>.{bin,gguf}. Cap is 5 GiB per file. CODING_GUIDELINES section 5 requires startup orphan-reclamation.","action":"FIXED in fix-round 2 — model::sweep_stale_temps at voice init, 6h min-age guard; TEST-15"}
{"angle":"resource-lifecycle","finding":"download temp cleanup on the stream/cancel/cap/sha failure exits","verdict":"CONFIRMED CORRECT","evidence":"async-block error -> remove_file(tmp); classify rejection -> remove_file(tmp); sha mismatch -> remove_file(tmp). Integration TEST-3 asserts no file and no .tmp survive the empty / bad-magic / truncated exits, and it PASSED.","action":"none"}
{"angle":"error-handling","finding":"three distinct conditions now carry three distinct codes and no failure is silently swallowed","verdict":"CONFIRMED","evidence":"VOICE_MODEL_EMPTY_DOWNLOAD / VOICE_MODEL_TRUNCATED / VOICE_MODEL_INVALID; unit test asserts the three codes are pairwise distinct. finalize_download preserves context ('publish model file: {e}').","action":"none"}
{"angle":"security","finding":"the new message surfaces the observed leading bytes — check for information disclosure","verdict":"CONFIRMED SAFE","evidence":"describe_head caps at head[..4] (4 bytes), renders hex + printable with non-printables as '.'. Credentials in an arbitrary-URL download are already stripped by redact_url before any message/log. No path, host, or token reaches the message.","action":"none"}
{"angle":"security","finding":"the arbitrary-URL SSRF boundary is untouched by this diff","verdict":"CONFIRMED","evidence":"no change to the outbound client or url_validator policy in the diff; unit test arbitrary_url_ssrf_policy_rejects_internal_targets still passes.","action":"none"}
{"angle":"api-contract","finding":"no OpenAPI/TS regeneration is required","verdict":"CONFIRMED","evidence":"the only derive added anywhere in src-app/server/src is #[derive(Debug, Clone, Copy, PartialEq, Eq)] on ModelRejection — no JsonSchema/ToSchema type changed. Error codes are payload strings, not schema members.","action":"none"}
{"angle":"api-contract","finding":"the two NEW error codes could break a client that switches on VOICE_MODEL_INVALID","verdict":"REFUTED","evidence":"grep over src-app/ui/src, src-app/desktop, src-app/server: the codes appear only in model.rs and its unit test. No client branches on any voice model error code.","action":"none"}
{"angle":"test-faithfulness","finding":"every 'valid model' fixture is now built from the format, not from the implementation","verdict":"CONFIRMED","evidence":"grep 'b\\\"ggml\\\"' in tests/voice/model_management_test.rs -> 0; all sites route through tests/voice/mod.rs GGML_MAGIC = [0x6c,0x6d,0x67,0x67] (hand-transcribed from a real file) via ggml_bytes(); stage_model also uses it.","action":"none"}
{"angle":"test-faithfulness","finding":"D2 property VERIFIED BY RUNNING, not asserted","verdict":"CONFIRMED","evidence":"reverted has_whisper_magic to the pre-fix b\"ggml\" comparison: 3 unit tests RED (accepts_the_real_on_disk_whisper_ggml_magic, whisper_magic_accepts_ggml_and_gguf_rejects_junk, rejection_classify_...); integration bad_magic_fix_real_format_catalog_install_succeeds RED with the shipped message verbatim ('the downloaded file is not a whisper model: it starts with `6c 6d 67 67` (\"lmgg\")'). Restored -> 15/15 unit green.","action":"none"}
{"angle":"test-faithfulness","finding":"the fixture-faithfulness guard in tests/voice/mod.rs is deliberately NOT #[cfg(test)]","verdict":"CONFIRMED CORRECT","evidence":"cfg(test) is not set for an integration-test target, so the attribute would have compiled the guard out silently. Observed running: 'voice::fixture_faithfulness::shared_fixtures_use_the_real_on_disk_ggml_magic ... ok'.","action":"none"}
{"angle":"test-coverage-claims","finding":"TESTS.md TEST-11 claims the failed-install presentation is asserted in BOTH the models card and the runtime-versions card; the spec only drove the models card","verdict":"CONFIRMED — REAL GAP","evidence":"voice-model-mgmt.spec.ts TEST-11 (pre-fix-round-2) touches only voice-available-model-* testids; no voice-version-* assertion existed anywhere for the failure row.","action":"FIXED in fix-round 2 — TEST-11b + the failVersionDownloadWith mock hook"}
{"angle":"test-coverage-claims","finding":"TEST-10's claim that the three staged-model suites still resolve and activate the fixture","verdict":"CONFIRMED","evidence":"observed in the run: lifecycle_test 2 tests, transcribe_test 2, streaming_real_test 1 — all ok, with stage_model now writing real-magic bytes.","action":"none"}
{"angle":"ui-presentation","finding":"INV-6 holds on both cards — a failure that transferred nothing renders no byte text at all","verdict":"CONFIRMED","evidence":"progressByteLabel returns null for status==='failed' && received===0 and for received===0 with an unknown total; both DownloadProgressLine call sites render the <Text> only when the label is non-null. Unit-tested for 0 / partial / complete.","action":"none"}
{"angle":"ui-presentation","finding":"INV-1/INV-2 are structural, not cosmetic — a failure on a not-installed row can only read as a failed ATTEMPT","verdict":"CONFIRMED","evidence":"both cards route the terminal error through DownloadFailureRow, which always prefixes a bold 'Install failed' and offers Retry; the backend catalog payload carries no per-model error at all (live API + integration TEST-9 over the WHOLE catalog).","action":"none"}
{"angle":"accessibility","finding":"the failure row is announced, and the corrective control is named","verdict":"CONFIRMED","evidence":"DownloadFailureRow renders role=\"alert\"; the Retry Button carries aria-label={retryLabel} ('Retry installing <name>'). Asserted in the e2e (toHaveAttribute('role','alert')).","action":"none"}
{"angle":"design-system","finding":"no hardcoded color, no physical direction property introduced","verdict":"CONFIRMED","evidence":"DownloadFailureRow uses tone=\"destructive\" (semantic token) and align=\"start\"/justify=\"between\"/gap=\"small\" (logical). npm run check — which chains lint:colors + lint:logical-direction + lint:guardrails + check:kit-manifest — passed to its last script.","action":"none"}
{"angle":"plan-fidelity","finding":"PLAN's 'Files to touch' lists src-app/ui/src/modules/voice/gallery.tsx (a failed-install cell so the design-critic pass reviews the real failure layout); it was never implemented and DRIFT-1 does not record the omission","verdict":"CONFIRMED — REAL GAP","evidence":"git show 2ab8d9004 --stat contains no gallery.tsx; DRIFT-1 has 11 entries, none about the gallery.","action":"FIXED in fix-round 2 — seeded surface 'seeded-available-models-failed-install' mirroring the llm-local-runtime precedent; recorded as DRIFT-2.3"}
{"angle":"plan-fidelity","finding":"PLAN's downloadProgress.helpers.ts extraction was an unplanned shape change","verdict":"CONFIRMED, already recorded","evidence":"DRIFT-1.5 records it as impl-wins with the PLAN amendment.","action":"none"}
{"angle":"docs-accuracy","finding":"DRIFT-1.6's fixture-site count (8, correcting PLAN's 7) is accurate","verdict":"CONFIRMED","evidence":"grep -c 'b\\\"ggml\\\"' model_management_test.rs -> 0 after the change; the pre-fix file had 7 literal sites plus the default_fixtures closure.","action":"none"}
{"angle":"dead-code","finding":"every symbol introduced by the diff has a production or test consumer","verdict":"CONFIRMED","evidence":"GGML_FILE_MAGIC/GGML_MAGIC_LE/BE/GGUF_MAGIC -> has_whisper_magic + tests; describe_head -> ModelRejection::message; ModelRejection::{classify,code,message,to_error} -> both call sites; STALE_TEMP_MIN_AGE/sweep_stale_temps -> voice::init + TEST-15; DownloadFailureRow -> both cards; progressByteLabel -> both DownloadProgressLine.","action":"none"}
{"angle":"concurrency","finding":"sweep_stale_temps at init could race a download in flight in another process sharing the data dir","verdict":"CONFIRMED — MITIGATED","evidence":"the sweep only removes entries whose mtime is at least STALE_TEMP_MIN_AGE (6h) old; the largest whisper model is ~3.1 GB, so a healthy transfer is orders of magnitude inside that window. TEST-15 asserts a fresh temp is NOT reclaimed.","action":"documented in the doc-comment; no further change"}
{"angle":"concurrency","finding":"the SSE per-key subscribe dedupe is untouched by this diff","verdict":"CONFIRMED","evidence":"claimSubscription/percentOf unchanged; only progressByteLabel was added to downloadProgress.helpers.ts.","action":"none"}
{"angle":"cross-platform","finding":"no OS-specific code or dependency introduced","verdict":"CONFIRMED","evidence":"sweep_stale_temps uses std::fs + SystemTime only; no cfg(target_os), no new crate. Extension matching is on OsStr -> str, which behaves identically on Windows.","action":"none"}
{"angle":"ui-presentation","finding":"a failed download with 0 bytes still renders a 0%-width error-toned Progress bar with no text","verdict":"CONFIRMED — ACCEPTED","evidence":"DownloadProgressLine renders whenever a snapshot exists; tone='error' and showInfo=false. It reads as a failure indicator, not as a byte count, and INV-6 is about the numeric label.","action":"no change — recorded as an accepted residual"}
{"angle":"resource-lifecycle","finding":"the upload path streams up to 5 GiB to a temp before the magic check runs","verdict":"CONFIRMED — DELIBERATELY UNCHANGED","evidence":"stream_upload_to_temp captures head as bytes arrive but the handler validates after the multipart field completes. Aborting the request-body read early would in practice surface to the browser as a connection reset instead of the 400 + actionable message, i.e. worse UX for the exact case the branch is improving; the 5 GiB cap and TempGuard already bound the cost.","action":"no change — recorded as an accepted trade-off (see DECISIONS/DRIFT-2)"}
{"angle":"root-cause-verification","finding":"round-3 independent re-derivation of the root cause from live state","verdict":"CONFIRMED","evidence":"curl -r 0-15 of ggml-base-q5_1/q8_0/base/tiny.en -> '6c6d 6767 lmgg' (4/4); git show origin/feat/agent-core:model.rs:63 -> == b\"ggml\"; live GET /api/voice/models -> [], /models/catalog -> base-q5_1 size_bytes 59707625 installed:false with NO error field, /models/downloads -> []; psql ziee_live_view count(voice_models) -> 0; voice-models/ empty. Real GGUF head -> '4747 5546 GGUF' so metadata.rs's b\"GGUF\" check is correct.","action":"none - BUG_ANALYSIS CONFIRMED; no live cleanup needed"}
{"angle":"permission-gating","finding":"the Retry control introduced by ITEM-5/ITEM-12 renders OUTSIDE the <Can permission={VoiceAdminManage}> that wraps the sibling Install button, and is reachable by a voice::admin::read-only user","verdict":"CONFIRMED","evidence":"AvailableModelsCard.tsx:273-280 + AvailableVersionsCard.tsx:217-224 render DownloadFailureRow outside the Can wrapper; list_active_model_downloads (model_handlers.rs:363) and runtime_version::list_active_downloads (handlers.rs:200) both take RequirePermissions<(VoiceAdminRead,)>; loadActive() self-gates on VoiceAdminRead and seeds terminal tasks into activeByKey without re-subscribing. e2e TEST-16 goes RED without the gate.","action":"FIXED - Can wrapper moved inside DownloadFailureRow so both cards inherit it; PLAN amended with INV-8/ITEM-13; TEST-16 added (FIX_ROUND-4 F4-1, DRIFT-2.9)"}
{"angle":"ui-presentation","finding":"INV-6's catalog-side 0-size suppression was applied to AvailableModelsCard only; AvailableVersionsCard could still render a naked zero for a release whose asset size is Some(0)","verdict":"CONFIRMED","evidence":"AvailableVersionsCard.tsx:182 was `v.size_bytes != null && !v.installed`; AvailableVersion.size_bytes is Option<u64> (runtime_version/models.rs:154) so Some(0) is representable. TEST-11b with the fixture seeded size_bytes:0 goes RED without the guard.","action":"FIXED - same `> 0` guard (FIX_ROUND-4 F4-2, DRIFT-2.10)"}
{"angle":"cosmetic-assertions","finding":"TEST-11b's INV-6 assertion on the versions card forbade a string that card can never render, so it could not fail","verdict":"CONFIRMED","evidence":"the assertion was not.toMatch(/\\b0 Bytes\\b/) but AvailableVersionsCard has its own local formatBytes (n<1024 -> `${n} B`), so 0 renders as '0 B'. Discovered by RUNNING the F4-2 negative control: with the size guard removed TEST-11b still PASSED.","action":"FIXED - assert the string the card actually renders (/\\b0 B\\b/), reason recorded inline; the duplicated formatter itself left alone as out-of-scope cleanup (FIX_ROUND-4 F4-3)"}
{"angle":"base-health","finding":"cargo check --workspace --tests fails on agent-core/tests/real_llm_loop.rs, unrelated to this branch","verdict":"CONFIRMED (inherited)","evidence":"cargo check -p agent-core --tests -> EXIT=101, E0063 missing fields isolate_children/schedule at :143 and :221; git diff --name-only origin/feat/agent-core...HEAD lists no agent-core file.","action":"reported to the orchestrator, not worked around"}
{"angle":"build-gate","finding":"gate:ui's visual layer failed on chat-collapse-borders TEST-3 (light+dark) in the first run","verdict":"REFUTED (flake / stale dev server)","evidence":"that run reused a 12h-old vite on :20143. A/B in the same worktree with only the one spec: src-app/ui at origin/feat/agent-core -> 2 passed; src-app/ui at HEAD -> 2 passed. The branch touches no chat file.","action":"stale vite killed, gate:ui re-run against a freshly booted server; recorded in TEST_RESULTS, not silenced"}
