# DRIFT-1 — implementation vs. plan / design

Authored during phase 5, item by item, as each landed.

- **DRIFT-1.1** — verdict: impl-wins — ITEM-12 assumed the gallery gates were purely GENERATED. Two HAND-maintained files also gate on tsc: `src/dev/gallery/coverage.ts` (`satisfies Record<GallerySurface, Coverage>`) and `src/dev/gallery/stateCoverage.ts` (`satisfies Record<RequiredState, StateCoverageEntry>`). Deleting the page made three of their keys stale, and the three new surfaces (`chat-extension/extension`, `BackgroundRunsFooter`, `BackgroundRunsPanel`) plus five new state keys became REQUIRED. Both files edited; PLAN ITEM-12 amended to name them.
- **DRIFT-1.2** — verdict: plan-wins — the footer read `convId ? BackgroundRuns.runsByConversation[convId] : undefined`. `npm run check`'s `lint:hooks` (H2 ternary-branch) rejected it: a store-proxy field read IS a hook, so a conditionally-evaluated one varies hook order between renders. Re-implemented to hoist the map read above the condition; the per-conversation lookup is then plain indexing. This is exactly the PLAN's store-discipline rule, so the plan wins and the code was corrected.
- **DRIFT-1.3** — verdict: resolved — PLAN_AUDIT recorded "no index on `workflow_runs.conversation_id`" as a standing concern. Verification found one already exists: `idx_workflow_runs_conv … WHERE (conversation_id IS NOT NULL)` (`202607140230_workflow_schema.sql:87`). DEC-6 and the audit entry were both corrected; still no migration.
- **DRIFT-1.4** — verdict: resolved — the panel's Load-more used `<Button size="sm">`; the kit's `size` union is `'icon' | 'default' | 'lg'` only. Switched to the default size.
- **DRIFT-1.5** — verdict: impl-wins — the workspace has TWO unit runners: Vitest (scoped by `vitest.config.ts` to `src/**/*.store.test.ts`, for specs needing `vi.mock` + jsdom) and `node:test` via `npm run test:unit` (for pure helpers). The store spec was authored for Vitest and the pure-predicate spec for `node:test`, matching each runner's documented remit. **Separately: `npm run test:unit` is RED on the base itself** — its loader resolves only `@/` aliases, so every spec importing a relative/extensionless path or `@ziee/framework/*` fails (e.g. the untouched `WorkflowRun.store.test.ts`). Not caused by this branch and not fixed here (B3: the shared loader is not this feature's to change); both new specs are authored so they DO run and pass. Recorded for the owner.
- **DRIFT-1.6** — verdict: impl-wins — the panel's EMPTY branch has no in-app entry point (no runs ⇒ no footer ⇒ no way to open the tab), which risked shipping it as dead code. Two real deliveries added instead of an allow-list: the e2e reaches it through the PERSISTED panel-tab rehydrate (delete the runs, reload, the restored tab refetches and renders empty), and the gallery reaches it through a conversation-aware `Background.listRuns` cassette that answers one designated id with zero runs (`deep-chat-background-empty`). PLAN ITEM-12 amended.
- **DRIFT-1.7** — verdict: none — `MessageList` early-returns for a message-less conversation and that branch does not render `message_list_footer`, so the footer is structurally invisible on a conversation with no turns. Correct in production (no turns ⇒ no sub-agents); no code change. Consequence recorded: every e2e fixture seeds one real message (`seedConversationWithMessage`).
- **DRIFT-1.8** — verdict: none — `just openapi-regen` produced an 802-line raw diff in `src-app/ui/openapi/openapi.json` (and similar for desktop) that is almost entirely key-ORDER churn. Content delta verified by diffing both files re-serialized with `sort_keys=True`: exactly 30 lines, all of them the new `conversation_id` parameter/schema plus the endpoint description. No unintended spec change.
- **DRIFT-1.9** — verdict: plan-wins — the plan enumerated six e2e specs to reconcile; implementation confirmed the seventh (`sync/subagent-completion-sync.spec.ts`) needs NO change because it reaches `/notifications/background` by `goto`, not by the nav entry, and that route is kept (DEC-2). Left untouched, as planned.
- **DRIFT-1.10** — verdict: resolved — `notification/module.tsx` kept an unused `Inbox` lucide import after the nav entry was removed (§15 / unused-import rule). Removed.

**Unresolved drifts:** 0
