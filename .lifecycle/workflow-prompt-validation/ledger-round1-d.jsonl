{"angle":"design-conformance","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":17,"severity":"medium","finding":"Half-converted RTL: the addon variants (lines 47-50) were converted to logical padding (ps/pe), but their COMPANION rules on the InputGroup root are still physical - `has-[>[data-align=inline-end]]:[&>input]:pr-1.5` and `has-[>[data-align=inline-start]]:[&>input]:pl-1.5`. `data-align` is expressed in logical terms and `order-first`/`order-last` flip under RTL, so in RTL the input's clearance padding lands on the side AWAY from the addon. Measured live (dir=rtl, align=inline-start, addon visually on the right): computed input padding-left=6px / padding-right=0px - 6px of dead padding on the far edge and 0px where the addon actually is. Same for inline-end mirrored. This is exactly the inconsistency the change's own comment claims to fix ('ml/mr/pl/pr do not flip under RTL'), left half-done one line above. Fix: pl-1.5 -> ps-1.5, pr-1.5 -> pe-1.5.","status":"confirmed"}
{"angle":"responsive-fidelity","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":47,"severity":"medium","finding":"NEW RTL regression: the gap between the input's text/caret edge and the addon's child collapses to 0px in EVERY RTL case. Measured (400px group, dir=rtl, old inline styles vs new classes) - inline-start AND inline-end both: button 3.2px -> 0px, kbd 5.61px -> 0px, plain icon/text 8.00px -> 0px. Cause: under RTL the old physical `pl-2`/`pr-2` accidentally landed on the addon's INNER side and supplied that separation; the new `ps-2`/`pe-2` correctly moves it to the OUTER side, and the inner separation that should replace it (the root's input pl-1.5/pr-1.5, finding line 17) is still physical and lands on the far side. Net RTL: the addon is no longer flush against the group border (measured child inset 0px -> 4/6/8px, a real fix) but the child now abuts the input content edge with zero clearance. LTR is unaffected (input->child gap measured at exactly 6.00px old and new, all three child shapes, both aligns).","status":"confirmed"}
{"angle":"design-conformance","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":36,"severity":"low","finding":"The explanatory comment quotes the removed class verbatim (`has-[>button]:mr-[-0.3rem]`, plus `-0.3rem`/`-0.15rem`), and Tailwind v4's content scanner is a raw-text candidate extractor that does NOT skip comments - so the utilities the change deletes are still emitted. Verified in the LIVE bundle at :5397: `.has-\\[\\>button\\]\\:mr-\\[-0\\.3rem\\]{margin-right:-0.3rem}`, `.has-\\[\\>button\\]\\:ml-\\[-0\\.3rem\\]`, `.has-\\[\\>kbd\\]\\:mr-\\[-0\\.15rem\\]` and `.has-\\[\\>kbd\\]\\:ml-\\[-0\\.15rem\\]` are all still present, and a repo-wide grep shows the ONLY remaining occurrences of those strings are this comment plus two parent-spec docblocks. Consequence: dead off-grid CSS ships, and the exact footgun remains applicable by class name (a consumer `className=\"has-[>button]:mr-[-0.3rem]\"` still reproduces the overflow with no CSS change). Rephrase the comment to break the candidate (e.g. 'a negative inline margin of -0.3rem' without the class syntax).","status":"confirmed"}
{"angle":"a11y","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":47,"severity":"low","finding":"The addon's own click-to-focus hit area shrinks in the inline axis. InputGroupAddon carries an onClick that focuses the sibling input, so its padding gutter is an interactive surface; measured addon border-box width 32px -> 28px with a button child (-4px) and 23.2px -> 21.2px with a kbd child (-2px), unchanged (24px/22px/8px) for icon/text/empty. The controls themselves are untouched (button measured 24x24 in every old/new x LTR/RTL x align combination, so WCAG 2.2 SC 2.5.8 is unaffected) and part of the lost 4px previously sat OUTSIDE the group's border, so this is minor - but it is a real reduction of the 'click the gutter to focus' strip and worth a conscious accept.","status":"confirmed"}
{"angle":"responsive-fidelity","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":26,"severity":"low","finding":"Containment is fixed only in the inline axis; the addon still overflows its group VERTICALLY and the change leaves it. Measured on all 9 live gallery input-groups at 390/768/1400px: group height 32px (h-8, 30px content box after the 1px borders) vs addon height 36px -> 4px of overflow (2px top + 2px bottom), from `py-1.5` (12px) + a 24px `icon-xs` button. Cosmetically invisible today (the addon is transparent and the button stays optically centred) and it cannot scroll (group overflow-x/y computed `visible`), but it breaks the same 'the addon sits inside its group' invariant this change asserts, and the new spec only probes the inline axis so it would never go red. `py-1` or `h-full` on the addon would close it.","status":"confirmed"}
{"angle":"responsive-fidelity","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":47,"severity":"low","finding":"The inline-start half of the fix is real but unguarded. Measured old vs new for align=inline-start with a button child: the addon's border box used to start 4.8px before the group's inner content edge, i.e. 3.8px past the 1px border (overhang 4.80 -> 0.00 after the change; kbd 2.39 -> 0.00). That start-side bleed produces NO `scrollWidth` growth in LTR (browsers do not make start-side overflow scrollable), which is why it was never noticed - and the new Layer-A spec only queries `[data-align=\"inline-end\"]` and never toggles the gallery's DIRECTION control, so a future regression of the `inline-start` variant (or of either variant under RTL) is invisible to it. Widen the probe to both aligns.","status":"confirmed"}
{"angle":"design-conformance","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":47,"severity":"low","finding":"REJECTED: off-grid / arbitrary values introduced. `ps-2`/`pe-2` = 8px, `ps-1`/`pe-1` = 4px, `ps-1.5`/`pe-1.5` = 6px - all on the Tailwind scale, with 6px being the 2px half-step the kit already uses elsewhere in this same file (`py-1.5`, `px-2.5`). The change strictly IMPROVES grid conformance: it removes the only two genuinely arbitrary values in the variants (`-0.3rem` = 4.8px, `-0.15rem` = 2.4px). No color token, radius, or arbitrary value is introduced by the diff.","status":"rejected"}
{"angle":"responsive-fidelity","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":49,"severity":"medium","finding":"REJECTED: `has-[>button]:pe-1` fails to override the base `pe-2`, leaving 8px and shifting the button 4px inward. Verified it wins: `:has(> button)` adds a type-selector's worth of specificity (0,1,1 vs 0,1,0), and all 9 live gallery addons compute `padding-inline-end: 4px` with `padding-inline-start: 0px`. Both `.has-\\[\\>button\\]\\:ps-1` and `.has-\\[\\>kbd\\]\\:ps-1\\.5` are present in the shipped bundle even though no in-repo addon has a kbd child.","status":"rejected"}
{"angle":"responsive-fidelity","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":49,"severity":"low","finding":"REJECTED: an addon containing BOTH a button and a kbd resolves ambiguously / differently than before. `has-[>button]:pe-1` and `has-[>kbd]:pe-1.5` have IDENTICAL specificity (0,1,1) so the winner is Tailwind's emitted source order - measured 6px (kbd wins). That is exactly the old behaviour, which had the same same-property tie between `ml-[-0.3rem]` and `ml-[-0.15rem]` (measured old net inset 5.61px vs new 6.00px, delta +0.39px). Not a regression. Worth knowing that in the `inline-start`+both case the padding the rule is meant to tighten against the BUTTON (first child) is the kbd's 6px, not 4px - but again, unchanged from before.","status":"rejected"}
{"angle":"responsive-fidelity","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":47,"severity":"low","finding":"REJECTED: perceptible LTR visual shift. Measured child inset from the group's inner content edge, old -> new: button 3.20px -> 4.00px (+0.80), kbd 5.61px -> 6.00px (+0.39), plain icon 8.00px -> 8.00px (0.00), text 8.00 -> 8.00 (0.00), empty addon 8px padding unchanged. The input->child gap is byte-identical at 6.00px in all six LTR combinations, and the input merely gains 0.8px of width where the addon stopped hanging out. Sub-pixel-class; the commit's '<=0.8px' claim is accurate.","status":"rejected"}
{"angle":"a11y","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":49,"severity":"low","finding":"REJECTED: focus ring clipped or tap target reduced on the control. The button measures 24x24 CSS px in every old/new x LTR/RTL x align x child-shape combination (WCAG 2.2 SC 2.5.8 minimum met, unchanged). The group computes `overflow-x: visible` so nothing clips the ring, and the button now sits 4.00px from the group's inner edge instead of 3.20px, so ring clearance marginally IMPROVES. No aria/role/label surface is touched by the diff.","status":"rejected"}
{"angle":"design-conformance","file":"sdk/packages/kit/src/shadcn/input-group.tsx","line":47,"severity":"low","finding":"REJECTED: tailwind-merge override fragility - a consumer passing `className=\"px-2\"` to InputGroupAddon would have tw-merge drop the base `ps-2` but NOT the differently-modified `has-[>button]:ps-1`, leaving an asymmetric 4px/8px result that the old margin-based code could not produce. Real but purely theoretical here: the two in-repo consumers (combobox.tsx:79 ComboboxInput, command.tsx:82 CommandInput) pass no className at all to the addon, and a repo-wide grep finds no third consumer of InputGroupAddon.","status":"rejected"}
