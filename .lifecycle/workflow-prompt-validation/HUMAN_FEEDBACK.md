# HUMAN_FEEDBACK — workflow-prompt-validation

The task brief is the human input this branch has received; the feature has not
yet been reviewed running by the owner. Recorded verbatim from the brief, with
how each was addressed.

- **FB-1** [status: resolved] — "Decide deliberately which side is correct (should `prompt: ""` be rejected at validate, or tolerated at run?) and make the two agree. Record the decision and its rationale." → DEC-1 records the decision (TOLERATED at run; `validate.rs`'s "an empty prompt is not a prompt" is canonical and `dispatch` moves to it) with three independent reasons, and DEC-2/DEC-3 record the two boundary choices it forces. The two sides now derive from ONE shared rule (`prompt_source` / `prompt_file_ref` / `read_prompt_file`), so the class is unrepresentable rather than patched. [generalizable: yes — when two code paths disagree, the fix is to delete the second decision site, not to make the two decisions match; a matched pair drifts again on the next edit]
- **FB-2** [status: resolved] — "write a test that reproduces the validate-green/run-red split BEFORE fixing, so you can prove which side changed. A fix without a red-first test is not accepted." → Both defects were reproduced red first and the output is transcribed. Backend: `validator said OK but the run said REJECTED … findings=[] run=Err("read prompt_file '': Is a directory")`. Kit: `overflow: 5, addonPastEnd: 3.8` at 390px AND 1280px, before any change. `REPRO.md` holds the measurements; `DRIFT-1.md` and the FIX_ROUND files hold the transitions. [generalizable: yes — a red-first run is also the only way to learn the defect's real shape; the 1280px measurement is what revealed the kit defect was width-independent, which the original report had framed as a mobile-only issue]
- **FB-3** [status: resolved] — "Note the repo's design system forbids hardcoded colors and off-grid spacing (4px base); fix this on-system, not with a magic offset." → The negative margins (`-0.3rem`/`-0.15rem`, both off the 4px scale) are gone, replaced by grid-aligned logical-direction padding (8/4/6px). No offset, no arbitrary value, and the conversion exposed and fixed a latent RTL bug in the group root plus a physical `!important` override in `command.tsx`. [generalizable: yes — converting one utility to logical properties can UNMASK a physical rule elsewhere that was accidentally compensating; check the whole component family, and render RTL to see it]
- **FB-4** [status: resolved] — "commit in the submodule and say so clearly; do not push — the orchestrator publishes sdk before the parent pointer." → Three commits on `sdk/agent-core-and-perf` (`753cc9e`, `b5a55b9`, `e0abf06`); nothing pushed, in either repo. DEC-8 records it, and the pointer situation the orchestrator needs is called out explicitly in the hand-off (the line has since moved sdk to `675a8ac`; `c6f5d8c` is an ancestor and the three commits rebase cleanly, but only together with the parent merge — doing it early breaks `tsc`, which was verified).
- **FB-5** [status: resolved] — "run ONLY the tests covering the files you actually touched, plus a typecheck. Run the full enumerated set exactly ONCE, at the end." → Adopted from the moment it arrived; iteration used scoped runs and the full enumerated set was run once at the end. It also exposed the failure mode of scoping badly: `cargo test -- workflow::validate::tests workflow::dispatch::tests` hid a RED guard living in a sibling module (`validate::humanisation_contract`), and the branch was broken for two rounds because of it. Scoping is now by MODULE (`-- workflow::`), never by test-module path. [generalizable: yes — scope a test run to the module, not to the test modules you edited: the tests that COVER a change are frequently not the ones next to it]
- **FB-6** [status: resolved] — "Never write a PASS line you did not observe." → `gate:ui` does not pass and is recorded FAIL in `TEST_RESULTS.md`, with the failing specs named, the branch ruled out by an A/B measurement (the spec fails MORE with the kit reverted), and a baseline run on pristine `origin/feat/agent-core` showing 19 visual failures against this branch's 4. TEST-10 is recorded FAIL for the same reason even though the fact it asserts was observed. [generalizable: yes — when a shared gate is red for pre-existing reasons, the honest artifact is the observed failure PLUS a measured baseline; "known flaky" without the baseline is indistinguishable from an excuse]
