{"angle":"security","file":"src-app/server/src/modules/workflow/dispatch.rs","line":123,"severity":"medium","finding":"`load_raw_prompt` does an UNCONFINED `bundle_root.join(rel)` + `read_to_string` with no canonicalize/starts_with of its own — it borrows its entire safety from `validate::check_prompt_files` having run first. That trust is not universal: `spawn_run` (runner.rs:1248) and `resume_run` (runner.rs:1552) call `validate_for_install`, but `POST /api/workflows/{id}/test` (handlers/dev.rs:1115) only calls `parse_workflow_yaml` and never validates, and it is gated by the SAME `WorkflowsExecute` + `user_can_access` as /run. A `real_llm` fixture leaves llm/agent steps un-mocked (runner.rs:758 only short-circuits when a mock exists), so those steps reach this join with zero path checking. Concrete: on a workflow whose bundle contains `prompts/task.md`, replace that file on disk with a symlink to the server's `config/dev.yaml` (or add a bundle asset after install); `/run` and `/resume` reject it with WORKFLOW_PROMPT_FILE_ESCAPE, `/test` reads it, sends it to the model, and writes it verbatim to the owner-readable step prompt log (`log_io::write_text_log`, dispatch.rs:172). The diff also WIDENS what reaches this join: `prompt: Some(\"\") + prompt_file: Some(rel)` used to hit the `_ => Err` arm and now resolves to `PromptSource::File(rel)` and reads. Fix: re-run the confinement check (canonicalize + starts_with(bundle_root)) inside `load_raw_prompt`, or make `/test` call `validate_for_install` like its siblings.","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/dispatch.rs","line":123,"severity":"low","finding":"TOCTOU between validation and use survives the refactor. `check_prompt_files` (validate.rs:1163-1177) canonicalizes `bundle_root.join(p)` and proves confinement at spawn/resume time; `load_raw_prompt` later re-joins the RAW relative string and reads it, with an arbitrary gap (a long-running workflow, a `waiting`/durable-resume gate) in between and no re-check. Anything with write access to `extracted_path` during that window (another server-side feature, a shared-tmp neighbour, a crashed `.staging-*`/`.old-*` swap left behind by `update_user_workflow_definition`) can swap a validated regular file for a symlink out of the bundle and the run will read it. The narrowed `(step_id, &Path)` signature introduced here was the natural place to fold the confinement in and it was not taken.","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1150,"severity":"low","finding":"The textual shape reject `p.contains(\"..\") || p.starts_with('/')` is the ONLY prompt_file check that runs when `bundle_present == false` (validate.rs:1158 `continue`), and this diff re-plumbed its input (`prompt_file_ref`) without widening it. It does not recognise Windows-absolute or UNC paths: `prompt_file: \"C:/ProgramData/ziee/config/dev.yaml\"` or `\"\\\\\\\\host\\\\share\\\\x\"` contains no `..` and does not start with `/`, yet `Path::join` on Windows DISCARDS the bundle root for both, so the escape is caught only by the bundle-present ESCAPE branch — i.e. not at all on the draft `/validate` and `/validate-def` surfaces, and not at all in `load_raw_prompt` (which has no shape check whatsoever). Pre-existing, but the block is in the diff and the fix is one predicate (`Path::new(p).is_absolute() || p.contains(':') || p.starts_with('\\\\\\\\')`).","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1175,"severity":"low","finding":"`canon.is_file()` conflates 'not a regular file' with 'stat failed': `Path::is_file` swallows every io::Error and returns false. Any EACCES/EIO/ENFILE on the bundle path now emits a BLOCKING `WORKFLOW_PROMPT_FILE_MISSING` (severity Error, validate.rs:471), and because `spawn_run`/`resume_run` call `validate_for_install` on EVERY run, a transient FS error turns a healthy workflow into a hard 400 instead of a retryable read failure at the step. Use `canon.metadata()` and distinguish `Ok(m) if !m.is_file()` from `Err(_)`.","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1146,"severity":"low","finding":"REJECTED: 'filtering the empty prompt_file (`prompt_file_ref`) lets an unchecked path reach the read'. Ruled out: `PromptSource::File(f)` is produced only when `prompt_file_ref(prompt_file)` is `Some(f)` (validate.rs:1090), and `check_prompt_files` selects its `pf` through the SAME `prompt_file_ref` (validate.rs:1146). The set of strings that reach `load_raw_prompt`'s join is therefore exactly the set the validator shape/escape-checks — the refactor closed a divergence rather than opening one. The only string newly skipped is `\"\"`, which the old code resolved to `bundle_root.join(\"\")` == the bundle dir and never rejected.","status":"rejected"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1175,"severity":"low","finding":"REJECTED: 'the new `else if !canon.is_file()` breaks the canonicalize/confinement ordering'. Ruled out: the new arm is chained AFTER `!canon.starts_with(&root_canon)` and evaluates the same fully-resolved `canon`, so no escaping path can reach it and no path that reaches it can be outside the root. It is strictly additive (a subset of previously-accepted inputs is now rejected); no previously-rejected path becomes accepted.","status":"rejected"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1175,"severity":"low","finding":"REJECTED: '`canon.is_file()` follows a symlink and re-opens symlink escape'. Ruled out: `canon` is the output of `resolved.canonicalize()`, which resolves every symlink component; there is nothing left to follow, and the ESCAPE test already ran on the identical value. The author's comment ('a symlink to a real file still passes') is accurate.","status":"rejected"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1135,"severity":"low","finding":"REJECTED: 'the diff newly skips the confinement half when the bundle root is absent (`bundle_present`)'. Ruled out by `git show HEAD~1:src-app/server/src/modules/workflow/validate.rs` — `let bundle_present = bundle_root.is_dir();` and the `if !bundle_present { continue; }` are byte-identical to the parent commit. The diff did not change WHEN the existence/confinement half is skipped; it only changed which strings reach it (empty ones no longer do) and added a check after it.","status":"rejected"}
{"angle":"security","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":1043,"severity":"low","finding":"REJECTED: 'the falsifiability control injects CSS via `page.addStyleTag` and is a script-injection surface'. Ruled out: it runs only in Playwright against the backend-free local gallery (`/gallery.html`), the content is a constant literal, and nothing is persisted or served.","status":"rejected"}
{"angle":"perms-authz","file":"src-app/server/src/modules/workflow/handlers/dev.rs","line":1115,"severity":"low","finding":"The tightened validator this diff ships is not applied uniformly across execution surfaces reachable with the SAME permission. `test_workflow` (dev.rs:1094) is gated by `RequirePermissions<(WorkflowsExecute,)>` + `user_can_access` — identical to `/run` — but parses `workflow.yaml` and dispatches without ever calling `validate_for_install`, whereas `spawn_run` (runner.rs:1248) and `resume_run` (runner.rs:1552) do. So the security-layer findings `WORKFLOW_PROMPT_FILE_UNSAFE`/`WORKFLOW_PROMPT_FILE_ESCAPE` (and the new not-a-file check) gate one execution path and not its sibling. This diff strengthened the validator without closing the surface that ignores it.","status":"confirmed"}
{"angle":"perms-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":1082,"severity":"low","finding":"REJECTED: 'making `prompt_source` / `prompt_file_ref` / `PromptSource` `pub` widens reachable capability'. Ruled out: they are pure, side-effect-free functions over two `Option<String>`s; they hold no handle, no pool, no path capability, and the `workflow` module is not re-exported outside the crate in a way that grants anything. No extractor, route, permission constant, or `Audience` is touched anywhere in either diff.","status":"rejected"}
{"angle":"perms-authz","file":"src-app/server/tests/workflow/builder_validate_def_test.rs","line":660,"severity":"low","finding":"REJECTED: 'the new `/validate-def` test exercises a weakened gate'. Ruled out: it authenticates with the same `workflow_user` bearer token as the pre-existing test in the file and hits the same route; no route registration, permission constant, or handler extractor is modified in the diff.","status":"rejected"}
{"angle":"perms-authz","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":236,"severity":"low","finding":"REJECTED: 'the `promptSuppliedByFile` tightening is an authorization gate change'. Ruled out: it only decides whether a form field is marked required and whether a helper note renders; every definition still goes through `validate_for_install` server-side on save (`update_user_workflow_definition`, dev.rs:564) and on every run.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":1178,"severity":"medium","finding":"`WORKFLOW_PROMPT_FILE_MISSING` is now overloaded onto a SECOND, semantically different condition (the path exists inside the bundle but is not a regular file) with a new message string, and the code comment claims 'the builder's author-facing copy needs no new entry'. That is wrong in effect: `validationCopy.ts:104` maps this code to exactly one sentence — \"This step points at a prompt file that isn't in the workflow — add the file, or type the prompt in directly.\" For `prompt_file: \"prompts/adir\"` the file IS in the workflow, and the remedy the author is given ('add the file') does not clear the finding, so the author is stuck on a blocking error with unactionable copy. Same for `workflow_mcp/tools.rs` and any client keyed on the code. Either emit a distinct code (e.g. WORKFLOW_PROMPT_FILE_NOT_A_FILE) or make the copy fn discriminate.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":1090,"severity":"medium","finding":"`WORKFLOW_PROMPT_BOTH` silently STOPS firing for a wire shape it used to reject. Old rule: `has_file = prompt_file.is_some()`, so `{prompt: \"hi\", prompt_file: \"\"}` produced a blocking WORKFLOW_PROMPT_BOTH and a 400 from install / `PUT /definition`. New rule routes that through `prompt_file_ref`, which filters `\"\"` to absent, so `prompt_source` returns `Inline(\"hi\")` and NO finding is emitted at all — a previously-rejected definition now validates clean and runs. This loosening is undocumented in the doc comment (which only argues the empty-`prompt:` direction) and is UNCOVERED by the new tests: TEST-2 asserts `(\"\",\"p.md\")`, `(\"\",None)`, `(None,\"\")`, `(\"\",\"\")`, `(\"   \",\"p.md\")` and `(\"hi\",\"p.md\")`, but never `(\"hi\",\"\")`; TEST-1's matrix pairs `Some(\"hi\")` with `Some(\"\")` only through an implication assertion that both sides now satisfy, so it cannot see the change either.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/runner.rs","line":1248,"severity":"medium","finding":"The two new blocking findings are enforced on EVERY run, not just at install, so they retro-break already-installed workflows with no migration or back-compat path. `spawn_run` (runner.rs:1248) and `resume_run` (runner.rs:1552) both call `validate_for_install`, which returns the FIRST Error-severity finding as a 400. After this diff: (a) a workflow carrying `prompt_file: \"\"` now 400s at `POST /api/workflows/{id}/runs` with WORKFLOW_PROMPT_MISSING where it previously created a run and failed only that step; (b) a workflow whose `prompt_file:` names a directory now 400s with WORKFLOW_PROMPT_FILE_MISSING; and worst, (c) an IN-FLIGHT durable run of either kind can no longer be resumed — `resume_run` hard-errors before `preflight`, so a `waiting`/`resumable` run is stranded rather than continuing. The status-code contract for `/runs` changes from 'run created, step-level failure' to 'request rejected'.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/dispatch.rs","line":128,"severity":"low","finding":"Runtime behaviour change not called out anywhere in the diff: `{prompt: \"\", prompt_file: absent}` used to match the old `(Some(p), None)` arm and return `Ok(\"\")` — the step ran with an empty prompt. It now returns `Err(\"step '<id>' has invalid prompt config\")`, surfaced to the client as a step failure `prompt render: step '<id>' has invalid prompt config`. Defensible, but it is a behaviour flip for any pre-existing (e.g. `is_dev`, or pre-check-era) workflow that relied on an empty prompt, and TEST-6 pins the new behaviour without a note that it is a change rather than a fix.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/components/builder/LlmStepForm.tsx","line":19,"severity":"low","finding":"The client-side documentation of the client/server contract is now stale and actively misleading — it is the only place the rule is written down for the next reader. `LlmStepForm.tsx:19-36`, `LlmMapStepForm.tsx:22-24,66` and `stepForms.ts:193` still state \"dispatch.rs's load_raw_prompt, which matches only (Some,None)/(None,Some), fails the RUN with 'invalid prompt config'\" — the exact behaviour this diff removed — and `stepForms.ts:193` still says \"WORKFLOW_PROMPT_MISSING fires only on `!has_prompt && !has_file`\" using the old `has_file = is_some()` definition. Only the `promptSuppliedByFile` doc block was updated.","status":"confirmed"}
{"angle":"api-contract","file":"sdk/packages/kit/src/shadcn/command.tsx","line":73,"severity":"low","finding":"The kit change converts the addon variants from physical `pl/ml`/`pr/mr` to logical `ps/pe`, but the one in-kit CONSUMER that overrides them was not converted: `command.tsx:73` still forces `*:data-[slot=input-group-addon]:pl-2!`. Two consequences. (1) In LTR the `!important` physical `padding-left:0.5rem` now beats the new `has-[>button]:ps-1`, so a command-palette addon holding a button gets 8px instead of the intended 4px — the visual result the diff's comment computes ('within 0.8px of the old optical result') does not hold there. (2) In RTL, `pl-2!` sets `padding-left` while `ps-*` sets `padding-inline-start` (= `padding-right`), so BOTH apply and the addon gets asymmetric padding — a new RTL defect created by the half-migration. The new `input-group-overflow.spec.ts` cannot catch either: it only asserts the addon does not extend PAST the group.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":1051,"severity":"low","finding":"REJECTED: 'this change needs an OpenAPI / types.ts regeneration that was not done'. Ruled out: `PromptSource` carries no `JsonSchema`/`Serialize` derive and appears in no request/response type; `ValidationError`'s shape, fields and `Severity` are untouched; no `#[debug_handler]` route signature changed. The only wire-visible delta is WHICH existing `code` strings appear in the existing `errors[]` array, which needs no schema regen (it does need the copy-map fix filed separately).","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":236,"severity":"low","finding":"REJECTED: 'the desktop workspace carries a divergent copy of stepForms.ts/promptSuppliedByFile that was left on the old rule'. Ruled out: `src-app/desktop/ui/src/modules/` contains only chat, desktop-base, file-dialog, host-mount, layouts, memory, remote-access, tunnel-auth, tunnelAuth, updater, window — no workflow module, and `grep -rn promptSuppliedByFile src-app/desktop/ui/src` is empty.","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":106,"severity":"low","finding":"REJECTED: 'the new not-a-file finding breaks `UNDECIDABLE_BY_DEF_CHECK` / `findingStillPresent` error-retirement'. Ruled out: the new emit reuses `WORKFLOW_PROMPT_FILE_MISSING`, which is already listed in `UNDECIDABLE_BY_DEF_CHECK` (store.ts:106), and `/validate-def` passes a non-existent bundle root so `bundle_present` is false and the branch is unreachable there. Retirement logic is unaffected.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":1178,"severity":"low","finding":"REJECTED: 'the new emit site breaks the crate-wide ValidationError drift guard (validate.rs tests ~2200-2620), which cross-checks every emitted code against the registry and against validationCopy.ts'. Ruled out: the guard keys on the `code` literal passed to `ValidationError::{err,at,warn}`; the new site reuses an already-registered, already-copied code and does not post-assign `code`/`layer`/`severity`, so the scanner is satisfied. (The guard checking only the CODE and not the message is precisely why the copy mismatch filed above slips through it.)","status":"rejected"}
