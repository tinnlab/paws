{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1296,"severity":"high","finding":"stat-before-open FIFO guard is TOCTOU-bypassable, and the comment at :1296-1301 asserts it is not. `std::fs::metadata(&canon)` (:1302) and `open_no_follow(&canon)` (:1312) are two INDEPENDENT path resolutions with no shared fd. For run_from_workspace / validate_from_workspace the bundle_root IS the sandbox's RW bind mount (workflow_mcp/tools.rs:571 sets extracted_path = resolve_conversation_workspace_dir() = workflow_workspace_root()/<conv_id> = code_sandbox workspace_root/<conv_id>, RW-bind-mounted at /home/sandboxuser per code_sandbox/tools/files.rs:33). Concrete input: model writes workflow.yaml with `prompt_file: p.md` plus a regular p.md, then runs in the sandbox `while :; do rm -f p.md; mkfifo p.md; rm -f p.md; cp orig p.md; done` while repeatedly calling the workflow MCP validate/run verb. On the interleaving where metadata() sees the regular file and open() sees the FIFO, open(2) O_RDONLY on a FIFO with no writer BLOCKS FOREVER (O_NOFOLLOW does not affect FIFOs). Each win parks one thread permanently. Fix: open with O_NONBLOCK|O_NOFOLLOW first, then fstat(2) the fd and reject non-regular, then clear O_NONBLOCK — i.e. make the guard operate on the fd, not on a re-resolved path.","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1312,"severity":"high","finding":"O_NOFOLLOW guards only the FINAL path component, so the confinement check is still TOCTOU-defeatable via an INTERMEDIATE directory swap — contradicting the doc at :1174-1178 which claims the window is closed. After canonicalize() (:1287) and the starts_with confinement test (:1293), both metadata() (:1302) and open_no_follow() (:1312) re-walk the whole path from `/`, following symlinks on every component except the last. Concrete state: bundle_root is the sandbox-writable conversation workspace (see the previous finding); model creates `prompts/passwd` (regular, so canonicalize+starts_with pass) and `prompt_file: prompts/passwd`; a racing sandbox process does `mv prompts prompts.bak; ln -s /etc prompts` between the canonicalize and the open. The server (running OUTSIDE the sandbox, as the server uid, on the HOST filesystem view) then opens /etc/passwd: O_NOFOLLOW is satisfied because the final component `passwd` is a real file. Contents land in the LLM prompt, the step prompt log (dispatch.rs:196 log_io::write_text_log) and any validator finding. Fix: openat2(RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS) against a dirfd of the canonical root, or a per-component O_NOFOLLOW dirfd walk.","status":"confirmed"}
{"angle":"concurrency","file":"src-app/server/src/modules/workflow_mcp/tools.rs","line":539,"severity":"high","finding":"read_prompt_file is synchronous, blocking file I/O and is now reached from `validate_for_install` on plain tokio WORKER threads with no spawn_blocking. dispatch.rs:137 correctly wraps its call, but every validator caller does not: workflow_mcp/tools.rs:539 (`async fn load_and_validate_workspace`), workflow/runner.rs:1248 (`spawn_run`), workflow/runner.rs:1552 (`resume_run`), workflow/handlers/dev.rs:564, hub/handlers.rs:2187 — all async fns calling it directly. Before this change check_prompt_files only did canonicalize(); it now does metadata + open + read of up to 1 MiB per prompt_file. Combined with the FIFO race above, a handful of wins park the runtime's worker threads permanently and the whole server stops scheduling. Even benignly, a slow/NFS-backed bundle now stalls a worker for the duration of the read.","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1188,"severity":"medium","finding":"The non-unix branch of open_no_follow is NOT equivalent: `std::fs::File::open` follows symlinks and NTFS reparse points, so on Windows even the FINAL-component swap (the one case the unix branch does close) escapes confinement, and read_prompt_file's doc at :1257-1282 makes an unqualified confinement claim. The `#[cfg(not(unix))]` fallback is silent — no runtime warning, no compensating check (e.g. FILE_FLAG_OPEN_REPARSE_POINT / re-verifying the opened handle's final path via GetFinalPathNameByHandleW). The repo ships and supports Windows (CLAUDE.md, wsl2 sandbox backend), so this is a live platform, not a theoretical one.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/validate.rs","line":1401,"severity":"medium","finding":"MAX_PROMPT_FILE_BYTES bounds ONE file, but the loop that consumes it is unbounded, so the doc's claim (:1163-1169) that the cap prevents 'unbounded work' on every launch does not hold in aggregate. check_prompt_files iterates every step (:1401) and validate_collecting (:582-586) runs check_steps_shape and check_prompt_files unconditionally — the >50-step cap at :656 only PUSHES a finding, it never short-circuits. Concrete input: a workflow.yaml with 100k steps each carrying `prompt_file: big.md` (a 1 MiB file) makes one validate call perform 100 GB of blocking reads plus 100k × 1 MiB of transient allocation, on a tokio worker (see the concurrency finding). Author-controlled, and reachable from the MCP workspace verbs with no install required.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/validate.rs","line":1249,"severity":"medium","finding":"`rel.contains('\\\\')` is a NEW rejection with no migration path, and it silently breaks existing data on Windows. Previously check_prompt_files rejected only `..` and a leading `/`; a Windows-authored bundle with `prompt_file: \"prompts\\\\task.md\"` validated AND ran correctly on Windows (PathBuf::join honours backslash there). It now hard-fails WORKFLOW_PROMPT_FILE_UNSAFE at install, at spawn_run (runner.rs:1248) AND at resume_run (runner.rs:1552) — an already-installed, previously-working workflow becomes unlaunchable, and a suspended run becomes unresumable. The rationale comment (:1239-1242) argues for cross-platform determinism but does not acknowledge that it retroactively invalidates installed bundles.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/runner.rs","line":1552,"severity":"medium","finding":"Four new failure classes now block resume_run of an IN-FLIGHT run, which previously only required the prompt file to exist. resume_run re-runs validate_for_install, which now goes through read_prompt_file, so a durably-suspended run (e.g. parked on an elicit gate) refuses to resume if its prompt file has since become zero-byte (PromptFileError::Empty, validate.rs:1325), >1 MiB (TooLarge, :1309), non-UTF-8 (:1323), or a non-regular file (:1304). For run_from_workspace runs the bundle root is the model-writable sandbox workspace, so the model's own later `>` truncation of its prompt file kills the resume of its own suspended run. A zero-byte prompt file previously produced a (degenerate but successful) run; this is a behaviour change on existing data with no compatibility shim.","status":"confirmed"}
{"angle":"concurrency","file":"src-app/server/src/modules/workflow/dispatch.rs","line":137,"severity":"medium","finding":"The spawn_blocking await is outside the module's own stated cancellation contract, and dropping its JoinHandle does not abort the blocking task. dispatch.rs:4-8 states every dispatcher 'wraps the blocking await in tokio::select! { _, cancel }'; resolve_prompt is awaited bare at dispatch.rs:186 (LlmDispatcher), :397 (LlmMapDispatcher) and agent_dispatch.rs:1101 — none under a cancel select. So (a) a run cancelled while the prompt file is being read cannot interrupt it, and (b) when the future IS dropped (client disconnect / run cancel / RUN_WALL_CLOCK timeout at runner.rs:1128), tokio does not cancel a spawn_blocking task: a read hung on the FIFO race above leaks a blocking-pool thread for the process lifetime, invisibly, with no run left to report it.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/validate.rs","line":1196,"severity":"medium","finding":"PromptFileError::code() (:1196) and ::layer() (:1207) are `pub` with ZERO callers anywhere in the tree (verified by grep across src-app/**/*.rs — the only hits are the prose at :1337/:1341). prompt_file_finding (:1345) deliberately re-writes the same variant→code/layer mapping as literals. That is two independent copies of one rule with nothing asserting they agree — exactly the 'two places decide separately' shape this whole change exists to eliminate, reintroduced inside the change itself. Also CODING_GUIDELINES §15 (pub fn with no consumer; the `pub` also suppresses the dead_code lint that would otherwise flag it). If the textual drift guard truly needs literals, code()/layer() should be deleted or a unit test should pin `prompt_file_finding(&e,..).code == e.code()` for every variant.","status":"confirmed"}
{"angle":"correctness","file":".github/workflows/visual-snapshots.yml","line":42,"severity":"medium","finding":"The submodule fix was applied to only one of the two visual workflows. visual-snapshots.yml:42 is a bare `actions/checkout@v4` (no `submodules: recursive`) followed by `npm ci`, while root package.json declares workspaces ['src-app/ui','src-app/desktop/ui','sdk/packages/*']. By the diff's own reasoning (visual-tests.yml:37-43) sdk/ therefore checks out empty, the sdk/packages/* workspace links cannot resolve, and `npm ci` fails before anything runs. This is the Layer B (pixel-baseline) job — i.e. the workflow a maintainer must use to bless baselines for the very kit regression this change is about is structurally unable to run.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/validate.rs","line":1247,"severity":"low","finding":"`rel.contains(\"..\")` is a SUBSTRING test, not a component test, so it rejects legitimate bundle-relative paths whose filenames merely contain two dots: `prompts/v1..2/task.md`, `task..md`, `spec..draft.md`. Because read_prompt_file is now also on the RUN path, such a workflow is not just un-installable but reports a SECURITY finding (WORKFLOW_PROMPT_FILE_UNSAFE, whose UI copy at validationCopy.ts:105 says 'written as a location elsewhere on the machine') for a file that is plainly inside the bundle — an unactionable false accusation. A Component::ParentDir scan (as workflow/workspace.rs:73-83 already does for `dir`) would be both correct and consistent with the sibling confinement helper in this same crate.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/server/src/modules/workflow/validate.rs","line":1290,"severity":"low","finding":"`bundle_root.canonicalize().unwrap_or_else(|_| bundle_root.to_path_buf())` silently swallows the error (CODING_GUIDELINES §6) and then compares a fully-canonical `canon` against a possibly non-canonical `root_canon` at :1293. The two operands are no longer in the same normalisation space, so `starts_with` answers a different question than the one intended. It happens to fail SAFE today (a relative or unresolvable root yields a false Escape, not a false pass) only because :1287 would already have returned for the same reason — i.e. the fallback is load-bearing-looking dead code whose safety is incidental, not established. Either propagate the error or assert the invariant.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/server/src/modules/workflow/validate.rs","line":1321,"severity":"low","finding":"The two TooLarge sites report incomparable numbers. :1310 reports the file's real size; :1321 reports `buf.len()`, which the take(MAX+1) at :1316 has clamped to exactly 1048577. So the growth path always tells the author 'prompt_file X is 1048577 bytes, over the 1048576-byte limit' regardless of whether the file is 1 MiB + 1 byte or 5 GiB — the message reads like a trivial 1-byte overage and misdirects the remedy. Report the stat size, or say 'over the limit' without a fabricated count.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":99,"severity":"low","finding":"The rewritten WORKFLOW_PROMPT_FILE_MISSING copy enumerates four causes ('missing, be a folder rather than a file, be empty, or not be text') but the code now also covers a FIFTH: PromptFileError::TooLarge (validate.rs:1201 maps TooLarge to this same code). An author whose prompt file exceeds 1 MiB is told to check whether it is a folder or empty — neither is true, and the actual remedy (shrink the file) is not offered. The humanised copy replaces the backend `message()`, so the byte-count detail from validate.rs:1225 never reaches them.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/validate.rs","line":1235,"severity":"low","finding":"read_prompt_file and check_prompt_file_shape are both `pub` and neither rejects an EMPTY `rel`, even though 'empty means absent' is the central rule this change introduces. `read_prompt_file(root, \"\")` reaches `bundle_root.join(\"\")` == bundle_root, canonicalizes and confinement-passes, and is only stopped by the incidental `is_file()` at :1304 (yielding a misleading 'cannot be read from the bundle: not a regular file'). The emptiness rule lives solely in the two callers (prompt_file_ref at :1137 and prompt_source at :1114), so a third caller of this pub API silently reintroduces the exact bug the change fixed. Reject `rel.is_empty()` inside check_prompt_file_shape.","status":"confirmed"}
{"angle":"correctness","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":2096,"severity":"low","finding":"The controlClearanceDelta assertion (`misplaced = groups.filter(g => delta !== null && delta >= 0)` must be empty) is coupled to an element-TYPE selector it does not control, so it will false-fail the moment a legitimate consumer appears. The tightening rule on the InputGroup root is `has-[>[data-align=inline-end]]:[&>input]:pe-1.5` (sdk/packages/kit/src/shadcn/input-group.tsx:17) — an `input` type selector — while the probe selects `[data-slot=\"input-group-control\"]`, which InputGroupTextarea (input-group.tsx:167) also carries on a `<textarea>`. Any InputGroup pairing an inline addon with a textarea control gets delta == 0, trips `>= 0`, and reports 'physical padding keyed off a logical attribute' — a diagnosis that is simply false for that cell. It passes today only because InputGroupTextarea has zero consumers in the tree.","status":"confirmed"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1314,"severity":"low","finding":"REJECTED: 'the size check can be defeated by growth between stat and read'. It cannot. meta.len() is checked against MAX at :1309 BEFORE Vec::with_capacity(meta.len()) at :1314, so the pre-allocation is bounded at 1 MiB; the read itself is bounded independently by Read::take(MAX+1) at :1316; and the post-read re-check at :1320 converts any growth into TooLarge. A file that grows from 100 bytes to 10 GiB between the stat and the read yields TooLarge, not an OOM. The only defect here is the misleading byte count in the message (logged separately).","status":"rejected"}
{"angle":"error-handling","file":"src-app/server/src/modules/workflow/validate.rs","line":1215,"severity":"low","finding":"REJECTED: 'the author-facing error text leaks a host path'. PromptFileError::message() interpolates only `rel` (the author's own bundle-relative string) and never `canon`, `resolved` or `bundle_root`. The `Unreadable(why)` payloads come from std::fs io::Error Display, which for canonicalize/metadata/open/read carries the errno text only ('No such file or directory (os error 2)') — std::fs deliberately does not attach the path (unlike fs_err/anyhow). String::from_utf8's error carries a byte offset. Verified across all five construction sites (:1288, :1303, :1305, :1312, :1319, :1324).","status":"rejected"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1243,"severity":"medium","finding":"REJECTED: 'the shape check can be bypassed with unicode / normalisation / empty-segment / dot-segment / Windows-absolute tricks'. Probed: U+2024 and other dot homoglyphs are not `..` to any kernel; overlong UTF-8 is unrepresentable in a Rust &str so `rel` is always well-formed; `.`/empty segments (`a/./b`, `a//b`) are inert; the drive-letter test at :1243-1246 is byte-indexed and correct (`C:` at index 0-1, alphabetic first byte); UNC and `\\\\?\\` forms are caught by the backslash reject. On Linux only `/` and NUL are structurally special, and NUL yields an InvalidInput io::Error at the syscall boundary. Crucially, the shape check is not the confinement boundary anyway — canonicalize + component-wise starts_with at :1287-1295 is, and Path::starts_with is component-based, so the classic `/root/bundle-evil` string-prefix bypass does not apply.","status":"rejected"}
{"angle":"security","file":"src-app/server/src/modules/workflow/validate.rs","line":1293,"severity":"medium","finding":"REJECTED: 'a symlinked bundle root, or a relative path that canonicalizes back inside via a symlink, defeats confinement'. Both operands of the starts_with test are produced by canonicalize (:1287, :1290), so a symlinked bundle root resolves identically on both sides and compares correctly; a bundle-internal symlink that resolves back inside the root is intentionally allowed and is genuinely inside. Separately, a symlink or FIFO cannot arrive in an INSTALLED bundle at all: hub/bundle.rs:567-575 rejects every tar entry that is not Regular or Dir ('symlinks / devices / FIFOs / hardlinks rejected'). The live risk is only the sandbox-writable workspace root, covered by the two TOCTOU findings above.","status":"rejected"}
{"angle":"correctness","file":"src-app/server/src/modules/workflow/dispatch.rs","line":92,"severity":"medium","finding":"REJECTED: 'a caller of the changed signatures was missed'. load_raw_prompt's signature change (StepDef+RunContext -> &str+&Path) has exactly two call sites, both updated: dispatch.rs:92 (resolve_prompt) and dispatch.rs:397 (LlmMapDispatcher). resolve_prompt itself keeps its signature and its third consumer, agent_dispatch.rs:1101, is therefore correct unchanged. All three prompt-bearing kinds (Llm, LlmMap, Agent) route through it; StepConfig::prompt_fields (validate.rs:285) is exhaustive over StepConfig with no `_` arm, so a new prompt-bearing kind is a compile error. check_prompt_files' old inline match was the only other reader of the pair and it was migrated (:1404).","status":"rejected"}
