{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":281,"severity":"high","finding":"`StepConfig::prompt_source(&self)` has ZERO callers — production or test — anywhere in server/, desktop/ or tests/. rustc confirms it: `cargo test --lib -p ziee --no-run` now emits `warning: method `prompt_source` is never used --> server/src/modules/workflow/validate.rs:281:12`, a NEW warning this diff introduces (the other 5 dead-code warnings are in chat/control_mcp/scheduler and pre-date it). Violates CODING_GUIDELINES §15 ('every new public fn with a side effect has a production caller'). Worse, its own doc-comment sells it as the safe wrapper — 'Preferred over calling `prompt_source(a, b)` directly: the two arguments there are adjacent, same-typed and opposite in meaning, so swapping them compiles and turns the prompt text into a file path' — yet BOTH real call sites (dispatch.rs:130 `match prompt_source(prompt, prompt_file)` and validate.rs:714 `match prompt_source(prompt, prompt_file)`) call the free function with exactly that hazardous shape. The mitigation was written and then not adopted.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":1196,"severity":"medium","finding":"`PromptFileError::code()` and `PromptFileError::layer()` (validate.rs ~1196 and ~1207) have zero callers. `prompt_file_finding` deliberately re-writes the SAME variant→code and variant→layer mapping with string literals, and its doc explains why (the humanisation_contract scanner only reads literal constructor args). So the diff ships two independent encodings of one mapping, with nothing pinning them to each other and no test on either — precisely the 'two places decide separately' shape the whole change exists to eliminate. Either delete both methods or make `prompt_file_finding` the only mapping and teach the scanner about it.","status":"confirmed"}
{"angle":"coverage-gaps","file":"src-app/server/src/modules/workflow/validate.rs","line":1309,"severity":"medium","finding":"`check_prompt_file_shape` grew two NEW rejection clauses — the Windows drive-letter test (`rel.as_bytes().get(1) == b':' && [0].is_ascii_alphabetic()`, line 1309) and `rel.contains('\\\\')` (line 1315) — and NEITHER has a single test. Grepped server/src and server/tests: no test string contains a backslash path or a `C:` path. Deleting either clause turns nothing red. The diff's own justification for them is cross-platform correctness ('a bundle authored on one OS is validated and run on another'), i.e. exactly the case no test on this platform will otherwise reach.","status":"confirmed"}
{"angle":"coverage-gaps","file":"src-app/server/src/modules/workflow/validate.rs","line":1170,"severity":"medium","finding":"The new DoS ceiling is entirely untested. `MAX_PROMPT_FILE_BYTES` (1170), the `meta.len() > MAX` stat guard, the `Read::take(MAX+1)` bound, the post-read `buf.len() > MAX` re-check, and `PromptFileError::TooLarge` have no test anywhere (`grep -rn 'TooLarge|MAX_PROMPT_FILE_BYTES' tests/ src/modules/workflow | grep -i test` → nothing). `PromptFileError::TooLarge` is therefore a variant that is constructible but never observed, and every one of those four lines could be deleted with the suite still green. Same for `open_no_follow`/`O_NOFOLLOW`, whose whole stated purpose is a TOCTOU window no test opens. These are the security-shaped additions in the diff and they are the least covered.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/tests/workflow/builder_validate_def_test.rs","line":90,"severity":"medium","finding":"`validate_def_prompt_source_verdicts` — the FIRST assertion (`prompt: \"\"` beside `prompt_file: \"prompts/task.md\"` ⇒ neither WORKFLOW_PROMPT_BOTH nor WORKFLOW_PROMPT_MISSING) has NO production revert that turns it red. It was already green on the pre-fix code: the old rule was `has_prompt = prompt.as_ref().filter(|s| !s.is_empty()).is_some()` (false for `\"\"`) and `has_file = prompt_file.is_some()` (true), so neither code fired — that GREEN verdict beside a failing run is literally the bug being fixed. Reverting `prompt_source`, `prompt_file_ref`, `check_prompt_files` or `load_raw_prompt` all leave it passing. It documents intent; it guards nothing.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/tests/workflow/builder_validate_def_test.rs","line":83,"severity":"medium","finding":"Same test asserts a claim strictly wider than what it exercises. Its doc-comment says 'INV-1: a def this endpoint reports clean must run, and one it rejects must not quietly run' and headlines 'the two prompt states that used to validate GREEN and then fail the RUN' — but the test only POSTs to /workflows/validate-def and reads `errors[].code`. It never dispatches, never calls `load_raw_prompt`, and /validate-def has no materialized bundle, so it is structurally incapable of observing either half of a validate↔run disagreement. Only the SECOND assertion (`prompt_file: \"\"` ⇒ WORKFLOW_PROMPT_MISSING) is falsifiable here, and it is falsifiable against the validator alone.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/tests/workflow/builder_validate_def_test.rs","line":123,"severity":"medium","finding":"The block labelled 'Control:' does NOT inject the defect that was fixed. It posts `prompt: \"inline\"` + `prompt_file: \"prompts/task.md\"` and asserts WORKFLOW_PROMPT_BOTH — a verdict that is byte-identical before and after the change (old rule: has_prompt=true, has_file=true ⇒ BOTH). Its stated job is 'so the first assertion above is not passing because the check stopped firing', but the first assertion is itself unfalsifiable (see the prior finding), so the control guards a non-guard. A control that injected the actual defect would be a def with `prompt: \"\"` + `prompt_file:` asserted to RUN, which this endpoint cannot express.","status":"confirmed"}
{"angle":"coverage-gaps","file":"src-app/server/src/modules/workflow/dispatch.rs","line":92,"severity":"medium","finding":"No integration or E2E test ever executes a workflow RUN with a `prompt_file:` step. `grep -rln prompt_file src-app/server/tests/` returns exactly ONE file (builder_validate_def_test.rs), which never runs a workflow. The whole runner half of INV-1 is unit-only: TEST-1/TEST-6 call `load_raw_prompt` directly against a tempdir, never through `resolve_prompt` (dispatch.rs:92), `LlmDispatcher::dispatch`, `LlmMapDispatcher` (dispatch.rs:397) or `agent_dispatch.rs:1101` — all three of which this diff re-pointed at the new loader. A regression in the re-plumbing (wrong bundle root passed, `spawn_blocking` result mis-mapped, agent path missed) would be invisible.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2144,"severity":"medium","finding":"`client_prompt_file_predicate_mirrors_prompt_source` is one-directional but documented as bidirectional. It text-scans `promptSuppliedByFile`'s TS body for `length > 0` / `!== ''` / `length !== 0` and for the absence of `.trim()` — i.e. it pins the TS side to hard-coded literals. It never reads `prompt_source` or `prompt_file_ref`. Its doc claims it will 'fail the BACKEND suite on drift' between the two implementations, but adding `.trim()` to `prompt_source` (the Rust side) leaves it green. (TEST-2 does catch that specific change, so the CLASS is covered — the guard's own claim is what is false, and a reader relying on it will not add the Rust-side pin.) Secondary brittleness: the assertion is substring-based, so `pf.length>0` (no spaces, e.g. after a formatter change) fails it spuriously, and `pf.trimEnd()` passes the no-trim check.","status":"confirmed"}
{"angle":"coverage-gaps","file":"src-app/server/src/modules/workflow/dispatch.rs","line":1973,"severity":"medium","finding":"`prompt_codes_list_covers_every_prompt_verdict_the_validator_emits` enforces only half of what its own doc demands. It asserts `VALIDATION_CODES ∩ prefix('WORKFLOW_PROMPT') == PROMPT_CODES`; the doc says 'add the new code to PROMPT_CODES *and* give the matrix an input that produces it', but nothing checks the second clause. Adding a code to both lists satisfies the guard while TEST-1's matrix never produces it, which is the exact silent-shrink the guard was written to prevent. A cheap fix: have TEST-1 collect the union of codes it observed and assert it equals PROMPT_CODES. Secondary: the filter is the literal prefix `WORKFLOW_PROMPT`, so a future prompt verdict named e.g. `WORKFLOW_STEP_PROMPT_*` escapes the guard entirely. Also note WORKFLOW_PROMPT_FILE_ESCAPE is produced by the matrix only on unix (the `prompts/escape.md` symlink is `#[cfg(unix)]`); on Windows that row degrades to FILE_MISSING and the code is never exercised.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":37,"severity":"medium","finding":"The spec's coverage note overstates the population it actually measures, and its two anti-vacuity guards do not catch the shortfall. Measured live against the running gallery (:5397, 390px and 1280px, ltr and rtl): the page contains exactly TWO `[data-slot=\"input-group\"]` elements, BOTH carry only an `inline-end` addon, and BOTH have a `> button` child. So there are ZERO `inline-start` rows and ZERO bare (no-button/no-kbd) rows. The doc claims measurement 'for BOTH `inline-start` and `inline-end` addons ... measured in the WRITING direction — the two variants are written as a symmetric pair, so a guard on one of them lets the other rot' and that the spec 'exercises the `has-[>button]` branch and the bare branch'. Neither is true today. `expect(groups.length).toBeGreaterThan(0)` and `expect(groups.filter(hasButtonChild).length).toBeGreaterThan(0)` are both satisfied by that same 2-row population, so the `ps-2` bare branch and the entire `inline-start` variant (including its `[&>input]:ps-1.5` clearance rule) could be reverted to a negative physical margin with this spec still green.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":250,"severity":"low","finding":"The falsifiability control covers the overflow + containment probes only; the `controlClearanceDelta` probe — the one the spec says 'is the part an RTL render is uniquely able to observe' and 'without this the RTL rows would merely re-assert the LTR overflow property' — has no injected control. Re-injecting REVERTED_RULE (a negative addon margin) does not move `controlClearanceDelta` at all, so the assertion that carries the RTL rows' entire independent value is never demonstrated to be able to fail. I verified by hand that it IS falsifiable: injecting the physical equivalent of the reverted `[&>input]:pr-1.5` (`[data-slot=input-group-control]{padding-left:10px;padding-right:6px}`) leaves LTR at delta −4/−4 (0 misplaced) and flips RTL to +4/+4 (2 misplaced ⇒ red). So the probe works; the spec just does not prove it.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":29,"severity":"low","finding":"`REVERTED_RULE` is documented as 'The exact rule that was removed' and the control's comment says 'exactly as the kit had it', but it injects `margin-inline-end:-0.3rem` (LOGICAL) whereas the removed kit rule was `has-[>kbd]/has-[>button]:mr-[-0.3rem]` (PHYSICAL `margin-right`). They coincide in LTR, which is the only direction the control runs, so the control is sound today — but the physical/logical distinction is the diff's headline rationale, and the constant asserts a byte-equality that does not hold. A physical `margin-right` revert is in fact NOT caught by the containment probe in RTL (the margin lands on the addon's inner side), only by the clearance probe.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":140,"severity":"low","finding":"`openWithAddons` deliberately replaces a wall-clock settle with two positive DOM signals, but drops the one the shared helper `openGallery` (tests/e2e/visual/_gallery.ts:64) waits on: `root.dir === dir`. It waits only for `gallery-root` visible plus 'some addon with a > button child exists'. Since `probeGroups` derives `rtl` from `getComputedStyle(el).direction`, a run that measured before ThemeProvider applied `dir` would silently produce LTR measurements under an RTL test name and still pass every assertion — the RTL rows would degrade to duplicate LTR rows with nothing to report it. Latent, not live: I confirmed `documentElement.dir === 'rtl'` and computed direction rtl at probe time today. Add `root.dir === dir` to the waitForFunction (or assert `groups.every(g => g.rtl)` in the RTL rows).","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/dispatch.rs","line":2150,"severity":"low","finding":"TEST-6 asserts `err.contains(\"without '..'\")` for BOTH `prompts/../prompts/real.md` and `/etc/passwd`, pinning an author-facing message ('prompt_file '<rel>' must be a bundle-relative path without ..') that names the wrong defect for an absolute path — and, after this diff, for a backslash path and a `C:`-drive path too, since all four collapse into `PromptFileError::Unsafe` with one message. The test now makes that mis-description load-bearing: splitting the message per cause would fail it. The paired UI copy (`WORKFLOW_PROMPT_FILE_UNSAFE`, validationCopy.ts) is generic enough to cover it, so this is a message-precision issue the test freezes rather than a user-visible break.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/dispatch.rs","line":2090,"severity":"low","finding":"`assert_eq!(ran_ok, 18, \"the matrix must contain rows that legitimately RUN, or the implication holds vacuously\")` is an exact-equality tripwire doing double duty as an anti-vacuity guard AND an undeclared behavioural assertion. The count is correct for the diff as reviewed (per kind: {absent,\"\"}×prompts/real.md = 2, plus {\"   \",\"hi\"}×{absent,\"\"} = 4, so 6 × 3 kinds = 18) and it does defeat vacuity. But a deliberate, correct behaviour change — e.g. deciding a whitespace-only `prompt_file:` is absent, or accepting a zero-byte file — fails here with a message about vacuity, pointing the next author at the wrong thing. `assert!(ran_ok >= 3)` plus an explicit per-row expectation table would separate the two jobs. (Confirmed live: a concurrent fix round has already bumped the sibling `checked` constant from 108 to 120 while leaving `ran_ok` at 18, which is the tripwire behaving as designed but also shows how easily the two drift apart in meaning.)","status":"confirmed"}
{"angle":"coverage-gaps","file":".github/workflows/visual-tests.yml","line":31,"severity":"high","finding":"The newly-enabled trigger fires a job that cannot go green, so it cannot signal the regression class it was added for. The `layer-a` job's test step is `npx playwright test -c playwright.visual.config.ts` with NO spec filter, and `playwright.visual.config.ts` sets `testDir: './tests/e2e/visual'` — 26 spec files, not the 6 curated in `gallery.config.json → visualSpecs`. Ran that exact command against the live gallery: 7 failed / 106 passed / 54 skipped at --workers=2, and re-running the failures at --workers=1 still gives 5 deterministic failures — chat-scroll-stability TEST-7/TEST-10/TEST-13 (`expect(body).toBeVisible()` → 'element(s) not found'), tabular-viewer TEST-27 (clipboard leaks 'Banana' from the preceding test, expected ''), user-profile-label TEST-4 (tooltip never visible). None of these specs is touched by the diff. The redness is pre-existing — but it was previously unobservable because, without `submodules: recursive`, `npm ci` could not resolve the `sdk/packages/*` workspaces and the job died at install. Adding the `sdk` trigger + the submodule fix converts a job that never ran into a job that always fails, which is the state in which a real kit regression gets waved through. Either scope this step to `${{ visualSpecs }}` (as `gate:ui` does) or fix/quarantine the 5 specs in the same change.","status":"confirmed"}
{"angle":"coverage-gaps","file":"src-app/ui/tests/e2e/workflows/builder-responsive.spec.ts","line":90,"severity":"low","finding":"`MAX_TOLERATED_OVERFLOW_PX` is tightened 4 → 1 in a full-stack spec (needs Postgres + cargo + the app, tests/e2e/workflows) that is not runnable from, and was not re-run in, this evidence set. The reasoning is sound (the measured addon overhang was 3.80px and the panel reported scrollWidth 370 / clientWidth 366; the kit overhang is now 0.00px, verified live) but the residual for `wf-builder-step-config` at 390px has not been measured post-fix, and the new bound leaves 1px of slack for everything else in that panel. If any other sub-pixel contributor sits between 1 and 4px the workflows E2E turns red on a spec nobody in this change ran.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":396,"severity":"low","finding":"`VALIDATION_CODES`'s doc-comment is now self-contradicting after the visibility bump. It still argues '**`#[cfg(test)]` on purpose.** Nothing in production reads this list — the guard derives the emitted set from the source itself, so a `pub` const here would be public API with no caller (CODING_GUIDELINES §15) whose `pub` also suppresses the `dead_code` lint.' The declaration immediately below it is now `pub(crate) const`. The substance survives (it is still `#[cfg(test)]`, so it is still not production API and the §15 argument still holds), but the paragraph now reads as a prohibition on the line it annotates and will be cited by the next reader as a reason the bump was wrong.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/models.rs","line":403,"severity":"low","finding":"Environment note, NOT caused by this diff: `cargo test --lib -p ziee -- workflow::` is RED on this branch. 187 passed / 1 failed — `modules::workflow::models::tests::job_kind_parses_round_trips_and_is_orthogonal` panics at models.rs:403 with `unknown variant `subagent`, expected one of `workflow`, `sandbox_exec`, `sub_agent``. Reproduced deterministically in isolation. `src-app/server/src/modules/workflow/models.rs` appears zero times in parent3.diff, so this is pre-existing and outside the change — but it means the module's own unit gate does not currently pass, and every new test in this diff was verified against a suite that is already failing on one case. All eight new/changed Rust tests themselves PASS: validate_and_dispatch_agree_on_every_prompt_state, validator_verdicts_on_the_empty_and_directory_prompt_states, prompt_source_treats_empty_as_absent_but_not_whitespace, client_prompt_file_predicate_mirrors_prompt_source, prompt_codes_list_covers_every_prompt_verdict_the_validator_emits, load_raw_prompt_reads_the_file_when_the_prompt_box_was_cleared, plus the pre-existing prompt_file_* guards and humanisation_contract.","status":"confirmed"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/dispatch.rs","line":1962,"severity":"low","finding":"REJECTED: the PROMPT_CODES guard is NOT self-referential (list-compared-to-itself). I checked the chain it leans on. `VALIDATION_CODES` is independently pinned by `validate::humanisation_contract::validation_codes_are_registered_and_humanised` (validate.rs:3907), which walks every `.rs` under `server/src` (plus `../desktop/tauri/src` when present, with a loud assert if the desktop workspace exists but that dir moved), lexes out comments / string literals / raw strings / char literals / `#[cfg(test)]` item bodies, extracts the literal `(layer, code)` args of `ValidationError::{err,at,warn}` and `Self::…` inside `impl ValidationError`, fails on any construction it cannot read (`scan.problems`), fails if the scan finds zero codes, and then asserts set equality in BOTH directions (`unregistered` and `stale`). So the new guard compares a hand list to a SOURCE-DERIVED set, not to itself. It passes today (`humanisation_contract::validation_codes_are_registered_and_humanised ... ok`), and `prompt_file_finding` deliberately uses literal args so the three file codes stay visible to it.","status":"rejected"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/dispatch.rs","line":2085,"severity":"low","finding":"REJECTED: the hard-coded `checked` count is right. Recomputed from the fixture and the code as reviewed: 3 kinds (llm, llm_map, agent) × 4 prompts ([None, \"\", \"   \", \"hi\"]) × 9 prompt_files ([None, \"\", prompts/real.md, prompts/adir, prompts/nope.md, prompts/empty.md, prompts/binary.bin, prompts/../prompts/real.md, prompts/escape.md]) = 108, which is the asserted value. It is a strict-equality tripwire, so ADDING a case fails it and forces the author to re-derive — which is the intended behaviour for a 'the matrix must actually have been walked' guard, not a defect. (The concurrent fix round has since added a 10th file case and correctly updated it to 120.)","status":"rejected"}
{"angle":"tests-quality","file":"src-app/server/src/modules/workflow/dispatch.rs","line":2090,"severity":"low","finding":"REJECTED: `ran_ok == 18` is arithmetically correct and does defeat vacuity. Enumerated every row against the real code: with inline absent ({None, \"\"}) only `prompts/real.md` reads Ok — adir is rejected by the `!meta.is_file()` branch, nope.md by canonicalize, empty.md by PromptFileError::Empty, binary.bin by the from_utf8 map_err, `prompts/../…` by check_prompt_file_shape, escape.md by the starts_with(root_canon) confinement — giving 2 Ok rows; with inline present ({\"   \", \"hi\"}) only the two absent-file values ({None, \"\"}) yield Inline, giving 4 Ok rows; every other pairing is PromptSource::Both. 6 per kind × 3 kinds = 18. And in all 18 the validator emits no prompt finding, so the implication is genuinely exercised in the non-trivial direction.","status":"rejected"}
{"angle":"tests-quality","file":"src-app/ui/tests/e2e/visual/input-group-overflow.spec.ts","line":52,"severity":"low","finding":"REJECTED: the RTL rows are NOT vacuous duplicates of the LTR rows. Verified against the live gallery that `?dir=rtl` really is applied (`document.documentElement.dir === 'rtl'`, `getComputedStyle(body).direction === 'rtl'`, and per-group `getComputedStyle(el).direction === 'rtl'`), and that the RTL rows carry an assertion the LTR rows cannot make: simulating the pre-fix physical control padding (`padding-left:10px; padding-right:6px` on `[data-slot=input-group-control]`) leaves LTR at controlClearanceDelta −4/−4 (0 misplaced, still green) and flips RTL to +4/+4 (2 misplaced, red). So the 'physical rule keyed off a logical attribute' defect is detectable only by the RTL rows, exactly as the spec claims.","status":"rejected"}
{"angle":"tests-quality","file":"src-app/ui/gallery.config.json","line":16,"severity":"low","finding":"REJECTED: the newly-listed spec really is executed by the gate. Traced the whole chain: `src-app/ui/package.json:192` `\"gate:ui\": \"node ../../sdk/packages/gallery/scripts/gate-ui.mjs\"` → gate-ui.mjs calls `resolveGalleryConfig(process.cwd())` (sdk/packages/gallery/scripts/lib/gallery-config.mjs:78, merging `gallery.config.json` from the `src-app/ui` cwd over DEFAULTS) → gate-ui.mjs:185 spreads `...CFG.visualSpecs` into `npx playwright test -c ${CFG.visualConfig} <specs>`. Playwright treats each bare filename as a path filter, and `input-group-overflow.spec.ts` resolves under `testDir: './tests/e2e/visual'`. Ran the identical command (`GALLERY_PORT=5397 npx playwright test -c playwright.visual.config.ts input-group-overflow.spec.ts --workers=1`): 5 tests collected, 5 passed. Layer B is not pulled in (visualSnapshotSpecs is gated on VISUAL_SNAPSHOTS).","status":"rejected"}
{"angle":"coverage-gaps","file":".github/workflows/visual-tests.yml","line":44,"severity":"low","finding":"REJECTED: `submodules: recursive` is not cosmetic — the diff's justification is literally true and it fixes a job that could not previously install. Root `package.json` declares `workspaces: [\"src-app/ui\", \"src-app/desktop/ui\", \"sdk/packages/*\"]`, and `package-lock.json` carries 15 `\"resolved\": \"sdk/packages/...\", \"link\": true` entries (e.g. `node_modules/@ziee/gallery` → `sdk/packages/gallery`). `npm ci` therefore hard-fails with an empty `sdk/`, before any test runs. It is also required at runtime: `playwright.visual.config.ts` imports `@ziee/gallery/scripts/lib/run-key.mjs` to derive the port. Separately rejected: the `- 'sdk'` path filter DOES match a submodule bump — a gitlink change presents as the single changed path `sdk`, which the literal pattern matches exactly.","status":"rejected"}
{"angle":"coverage-gaps","file":".github/workflows/visual-tests.yml","line":64,"severity":"low","finding":"REJECTED: the unfiltered CI run does NOT drag Layer B in without baselines. `tests/e2e/visual/gallery.spec.ts:25` has a file-level `test.skip(!SNAPSHOTS_ENABLED, ...)` and `_gallery.ts:19` defines `SNAPSHOTS_ENABLED = !!process.env.VISUAL_SNAPSHOTS`, which the workflow never sets; in my full-dir run all 54 snapshot cells reported as skipped. The header comment's 'LAYER B ... is NOT run here' is accurate. (The job's real problem is the 5 Layer-A specs that fail — filed separately.)","status":"rejected"}
{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":1079,"severity":"low","finding":"REJECTED: the `PromptSource` enum has no dead variants. All four are constructed in `prompt_source` (validate.rs:1109-1125) and consumed exhaustively in BOTH production consumers — `check_steps_shape` (validate.rs:714-727, mapping Both→WORKFLOW_PROMPT_BOTH, Missing→WORKFLOW_PROMPT_MISSING, Inline/File→no finding) and `load_raw_prompt` (dispatch.rs:130-155, mapping Inline→Ok, File→read_prompt_file, Missing/Both→Err with the shared message consts). Likewise `PROMPT_BOTH_MESSAGE`/`PROMPT_MISSING_MESSAGE`, `prompt_file_ref`, `check_prompt_file_shape`, `read_prompt_file`, `open_no_follow` and `MAX_PROMPT_FILE_BYTES` all have real production callers (grepped each).","status":"rejected"}
