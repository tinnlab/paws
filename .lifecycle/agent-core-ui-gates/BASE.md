# BASE — conflict-surface scoping

Branch: `fix/agent-core-ui-gates`, cut from `origin/feat/agent-core`
(NOT from `origin/main` — this fixes breakage that lives on the agent-core base;
it will merge back onto agent-core, per the task).

- **Migrations:** none touched. This is a frontend-only + SDK-submodule fix; no
  new migration, no `src-app/server` change → no migration-number collision
  surface.
- **OpenAPI regen:** NOT implied. No Rust handler/type change, no
  `openapi.json` / `api-client/types.ts` edit.
- **Files main/agent-core is actively changing that this branch also touches:**
  - `sdk` submodule (`packages/framework/app-seam.ts`, `packages/shell/*`) — the
    agent-core SDK pin is on branch `sdk/agent-core-and-perf`. TWO sibling
    branches (entry-slimming, e2e-speedup) ALSO carry sdk-submodule commits that
    must all reconcile onto `sdk/agent-core-and-perf` at merge. My sdk changes
    are additive (a new `peek()` method + 4 reader call-site swaps) — low
    conflict surface, but the pointer bump must be reconciled with the siblings.
  - `src-app/desktop/ui/package.json` — a single script-line edit; unlikely to
    collide.
  - `src-app/ui/src/modules/chat/**`, `loader.desktop.ts` — chat is the
    heaviest-churn area on agent-core; my edits are surgical (one registry method
    + one accessor push + 4 no-op loader exports) to minimize collision.
- **Generated files:** `src-app/ui/src/dev/gallery/RUNTIME_FINDINGS.{md,jsonl}`
  are regenerated by `runtime-health`; they are restored/regenerated at phase 8
  and NOT hand-edited.
