# HUMAN_FEEDBACK — notification-popover-responsive

- **FB-1** [status: resolved] — "the render of the notification popover is also broken, not responsive" → Reproduced on the live rig with 11 seeded notifications and then, as a controlled A/B, on an isolated dev server: the popover PANEL was the kit's fixed `w-72` (288px) while the bell's content wrapper carried an inline `style={{ width: 340, maxHeight: 460 }}`, so the mark-read/delete controls and "Mark all read" painted OUTSIDE the popover background at every width, and at 320px the fixed width pushed `document.scrollWidth` to 358 vs a 320 viewport — the page body scrolled sideways. Fixed by moving BOTH bounds onto the panel (`w-[min(21.25rem,calc(100vw-2rem))]` + `max-h-(--available-height)`, via the kit `Popover`'s existing `className` seam — the shared popover primitive is untouched), scrolling only the LIST inside the shared `ScrollArea` with the header and "View all" footer pinned outside it, and adding `wrap-anywhere` to the content column so a long unbroken token wraps instead of overflowing. Verified 320/390/1440 by geometric e2e + a mounted component harness.

- **FB-2** [status: resolved] — Coordinator, mid-task, verbatim: "I told you (and four other concurrent agents) to symlink `node_modules` from the main repo. That means every worktree shares ONE Vite optimized-deps cache … Setting GALLERY_PORT does NOT protect you from this … This matters doubly for you because your defect is a RENDERING/layout one. A failed module load makes a component silently not render, which looks exactly like 'popover broken, not responsive'. Confirm your reproduction is real." → Confirmed real, three ways. (a) The original reproduction ran against the live rig, which serves a **prebuilt static dist** with no Vite and no dep cache at all, so the artifact class cannot apply to it. (b) Re-ran the defect and the fix back-to-back on ONE isolated harness (worktree-local `node_modules` via `cp -al`, own `.vite`): the popover MOUNTED with 8 rows and identical 1254-char content in both states, with **0 console errors and 0 `ERR_NETWORK_CHANGED`**; only the geometry differed (panel `scrollWidth` 350-vs-288 → equal; doc `scrollWidth` 358-vs-320 → 320; token box 635.5px → 208px). (c) Discarded two `gate:ui` runs as VOID after the validity check (2328/2875 and 8456/9157 findings were `ERR_NETWORK_CHANGED`) rather than reporting them; the durable fix was per-worktree `node_modules` **plus warming the Vite dep optimizer before the health pass**, which took a run from 9157 findings to 332. [generalizable: yes — before trusting ANY gallery/`gate:ui` result, count `ERR_NETWORK_CHANGED` in `RUNTIME_FINDINGS.jsonl` and discard the run if it is non-trivial; give each worktree a real `node_modules` (`cp -al`) rather than a symlink, and WARM the Vite dep optimizer against the gallery before the runtime-health pass — port isolation alone does not prevent shared-dep-cache invalidation, and an un-warmed optimizer re-optimizes mid-run and poisons the run on its own]

- **FB-3** [status: wontfix] — NOT human feedback; recorded here so it is not lost at merge. `npm run check` is RED on pristine `origin/main` (`check:testid-registry`, then `check:state-matrix`), verified in a clean `origin/main` worktree. This branch adds ZERO drift to either generated file, but the required `npm run check (ui): PASS` line therefore cannot be produced without laundering four other teams' surfaces into this diff — and regenerating `stateMatrix.generated.ts` additionally breaks `tsc`, because `stateCoverage.ts` still maps `modules/onboarding/OnboardingRedirect:delayed`, a key the regen drops. → **wontfix ON THIS BRANCH**, deliberately. Fixing it means regenerating both files (pulling 32 foreign testids in), deleting a now-dead key from `stateCoverage.ts`, and inventing gallery-coverage dispositions for four surfaces this branch does not own — i.e. laundering another team's debt through a popover bugfix, into exactly the shared registries a concurrent workstream is also editing. It needs its own regen commit on main by the owners of the citations / llm-provider / mcp / workflow surfaces. Escalated to the orchestrator rather than silently absorbed or silently ignored; the consequence is that the phase-8 `npm run check (ui): PASS` line cannot honestly be written, and this branch is reported as 7/9 rather than claimed green.
