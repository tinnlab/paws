# BASE — conflict surface

- Branched from `origin/main` @ `dca29493f`; submodule `sdk` @ `0ba62538`
  (tip of branch `chat`), `agent-kit` @ `8dbe4999`.
- Files this branch touches: exactly one spec file
  (`src-app/ui/src/modules/file-rag/pages/FileRagAdminPage.test.tsx`).
  No other live worktree is editing it (it was last touched by
  `6e1db0623 test(ui): deterministic repro for the SettingsPageContainer
  hook-order crash`, already merged).
- No migration added; highest server migration prefix untouched.
- No `openapi.json` / `api-client/types.ts` regen implied.
- `sdk` pointer NOT moved: the confirmed mechanism is not in the sdk (DEC-3).
