{"angle":"correctness","file":"src-app/server/src/modules/llm_repository/utils.rs","line":726,"severity":"high","finding":"A 401/403 on the DERIVED capability URL is classified `Unhealthy`, which `record_test_outcome`/`run_startup_health_check` turn into an auto-disable. Unlike step 1's endpoint, this URL is invented by ziee (`https://api.github.com/` for GitHub-kind, `{origin}/api/models?limit=1` for Unknown), not configured by the operator. GitHub answers **403** when the anonymous 60-req/hr/IP budget is exhausted (the exact shared-egress condition this same branch documents in release_cache.rs), and a self-hosted host behind an auth proxy answers 403 for unknown paths. A user-added `https://github.com` row with auth_type `none` passes `has_credential()` (\"none\" => true), so it IS probed at boot, unauthenticated, and gets auto-disabled on a rate-limited box. This is precisely the outcome INV-4 (documented 20 lines above) says must never happen; only step 1's operator-configured credential endpoint has earned 401/403 => unhealthy.","status":"confirmed"}
{"angle":"correctness","file":"src-app/ui/src/modules/llm-local-runtime/components/AvailableVersionsCard.tsx","line":146,"severity":"medium","finding":"`handleCheckForUpdates` still derives its toast purely from `result.versions`. The server no longer 500s on an unreachable release feed — it answers 200 with `source:\"unavailable\"`, empty `versions` and an `unavailable_reason` — so the catch branch never fires and the user is told `No new <engine> versions — you're up to date.` when the truth is \"we could not reach upstream\". The card BODY was updated for this case (feedUnreachable/ErrorState at line 199) but the toast was not, so the explicit user-initiated action reports the exact lie the branch exists to remove.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/llm_local_runtime/engine/release_cache.rs","line":156,"severity":"low","finding":"Every lock acquisition uses `if let Ok(guard) = CACHE.read()/write()`, so a poisoned RwLock silently disables the cache forever: `write()` failing skips the insert (line 156), `read()` failing makes every fast-path lookup miss (line 140) and defeats retain-on-failure (line 177). The failure mode is exactly what the module exists to prevent — unbounded GitHub traffic — and it is invisible: no `tracing::warn!` on any of the three `Err` arms. Recover with `unwrap_or_else(|e| e.into_inner())` (the pattern the module's own tests use) or log the poisoning.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/llm_local_runtime/engine/release_cache.rs","line":153,"severity":"low","finding":"`get_or_refresh` has no single-flight: the read guard is (correctly) dropped before `fetch().await`, so N concurrent discovery requests on a cold/expired entry all miss and all issue their own upstream `GET /repos/{repo}/releases`. The module doc claims discovery \"costs one GitHub request per TTL\"; with the two-engine `list_available_versions` path and multiple admin tabs this can still burn several requests per TTL boundary against the 60/hr anonymous budget.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/llm_local_runtime/binary_manager.rs","line":339,"severity":"low","finding":"`.unwrap_or(DEFAULT_RELEASE_CACHE_TTL_SECS)` swallows the settings-row read error with no log at all. A persistent failure (missing row, unapplied migration, pool exhaustion) permanently and silently ignores the admin-configured TTL while the settings UI keeps showing the stored value — the admin's change appears accepted and has no effect, with nothing in the logs to explain it. Log the error before falling back.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/llm_local_runtime/engine/download.rs","line":165,"severity":"low","finding":"`parse_asset_variant` accepts any non-empty backend segment, but `POST /versions/download` validates the backend with `is_valid_backend` (`^[a-z][a-z0-9.]*$`, runtime_version/handlers.rs). An asset whose backend segment contains `_`, `+` or uppercase (e.g. `llama-server-linux-x86_64-cpu_avx512.tar.gz`) is therefore advertised by the discovery endpoint as an installable variant and then rejected with a 400 by the download endpoint — contradicting the endpoint's own doc (\"the exact tuples POST /local-runtime/versions/download accepts\"). Apply the same predicate here.","status":"confirmed"}
{"angle":"correctness","file":"src-app/server/src/modules/llm_repository/utils.rs","line":523,"severity":"low","finding":"`read_capped_body` truncates at 1 MiB mid-buffer and returns the partial bytes as if complete; the caller then fails `serde_json::from_slice` and reports `... answered 200 but the response is not JSON`. A self-hosted mirror that ignores `?limit=1` and serves its full model listing (>1 MiB) is therefore reported `unverified` with a misleading reason, even though its first array element would satisfy `is_model_listing`. Either flag truncation distinctly in the reason or parse incrementally, since only the first element is inspected.","status":"confirmed"}
{"angle":"correctness","file":"src-app/ui/src/modules/llm-local-runtime/components/AvailableVersionsCard.tsx","line":199,"severity":"low","finding":"The `feedUnreachable && readyUpstream.length === 0` branch claims \"the installable versions are unknown\" even when a cached catalogue WAS served and simply contains no `binary_ready` rows for this host. `readyUpstream` is `versions.filter(binary_ready)`, so a stale-but-real catalogue with only other-platform builds renders the wrong statement; the honest message there is the existing \"No published binaries found for {platform}/{arch}\" plus the stale notice. Gate on `updateCheck.versions.length === 0` instead.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_repository/utils.rs","line":404,"severity":"high","finding":"INV-4 still violated for one of the three URLs the DESIGN names as reported defects. `capability_probe_url(HuggingFace, repo_url)` ignores `repo_url` entirely and probes the fixed `hf_api_base()/api/models?limit=1`, so ANY row whose host is huggingface.co — including the reported `https://huggingface.co/custom` — is confirmed `healthy` on the strength of huggingface.co's own catalogue, not that URL's capability. It cannot serve models: `GitService::build_repository_url` (src-app/server/src/utils/git/service.rs:699) builds the HF clone as `{url}/{path}`, i.e. `https://huggingface.co/custom/org/model`, which is not a repository. The author recognised this exact class for GitHub and added `is_clonable_github_origin` (utils.rs:355) so `api.github.com` degrades to `unverified`; the symmetric HF origin/path guard was never written. Reachability of a DIFFERENT url is being reported as this url's confirmed capability.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/tests/llm_repository/capability_probe_test.rs","line":357,"severity":"high","finding":"Test that CERTIFIES the unfixed defect above. `a_hugging_face_host_is_probed_against_the_hugging_face_listing_surface` creates a repository at `https://huggingface.co/org-<uuid>` — an arbitrary, non-existent HF path with the same shape as the reported `huggingface.co/custom` — and its positive-control half asserts `good_result[\"status\"] == \"healthy\"` and `good_status == \"healthy\"` (lines 401-414). The paired unit test `capability_url_targets_the_kinds_listing_surface` (src-app/server/src/modules/llm_repository/utils.rs:983) asserts literally `capability_probe_url(HuggingFace, \"https://huggingface.co/custom\") == \"https://huggingface.co/api/models?limit=1\"`, pinning the reported URL to the confirming path. Both tests would go RED if INV-4 were actually satisfied for this input, so they lock the defect in.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_repository/utils.rs","line":726,"severity":"high","finding":"INV-4 ('a repository whose capability could not be confirmed is never auto-disabled') is violated by the new capability step's status mapping. `if status == UNAUTHORIZED || status == FORBIDDEN { Unhealthy }` — and `Unhealthy` is the ONLY verdict that auto-disables (connection_health.rs:301-320, run_startup_health_check ~line 366). GitHub answers its anonymous primary AND secondary rate limits with **403**, and the same branch's DESIGN §1 documents this very deployment as continuously exhausting the 60-req/hour anonymous GitHub budget from a shared IP. A GitHub-kind repository probed while rate-limited therefore records `unhealthy` and is auto-disabled, even though nothing about its capability was determined. Same shape for a gated/rate-limited HF response. A rate-limit 403 is definitionally 'could not confirm', not 'credentials rejected'.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_repository/utils.rs","line":405,"severity":"medium","finding":"INV-4: the GitHub capability probe targets a DIFFERENT host than the repository. `capability_probe_url(Github, _) = \"https://api.github.com/\"`, while the repository (and the clone the download path performs) lives on `github.com`. A transport failure on that second host maps to `map_transport_error` -> `ProbeVerdict::Unhealthy` (utils.rs:717-722) -> auto-disable. So a network/proxy that reaches github.com perfectly well but blocks api.github.com now auto-disables a working repository. The probe introduced a new auto-disable trigger that is not tied to the repository's own reachability, which is precisely the 'do not disable working custom deployments' constraint the DESIGN calls out in §3.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/tests/llm_local_runtime/release_cache_test.rs","line":97,"severity":"medium","finding":"Vacuous half of an [acceptance] test — it would still pass if the behaviour it names were reverted. `discovery_is_cached_and_degrades_honestly_when_upstream_is_gone`'s second half sets the TTL to 60s ('so the next read is forced to attempt a refresh') and then calls `take_upstream_down()`. But the cache entry is only seconds old and 60s is the CHECK-constraint FLOOR (migration 202607200500), so `get_or_refresh`'s fresh fast path (release_cache.rs:136-150) returns BEFORE `fetch` is ever invoked: the downed upstream is never contacted and the retain-on-failure branch (release_cache.rs:174, which the module docs call 'the load-bearing part') is never entered. Every assertion in that block (200, versions survive, source != unavailable, checked_at present) is satisfied by the ordinary in-TTL cache read already asserted 20 lines earlier. Reverting retain-on-failure to evict-on-failure would not turn this test red. (The branch IS covered by the in-crate unit test `failed_refresh_retains_previous_catalog`, so the behaviour is not unguarded — but no integration test proves the stale-but-labelled response ever reaches the wire.)","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_local_runtime/engine/release_cache.rs","line":130,"severity":"medium","finding":"INV-2 partial: `get_or_refresh` has no single-flight/coalescing. The read guard is dropped before `fetch().await`, so every request that arrives after the TTL lapses issues its OWN `list_releases` upstream call before any of them writes the entry. With the discovery card mounted in several tabs/devices (and both `/versions/available` and `/{engine}/check-updates` sharing the entry, plus one entry per engine), a TTL boundary produces a burst of N GitHub requests against the same 60/hr budget the cache exists to protect. Steady state is fixed; the burst case is not, and it is unmeasured — `mock_release`'s `release_list_hits` counter would make a concurrency test trivial to write.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_local_runtime/engine/download.rs","line":233,"severity":"low","finding":"INV-1 latent gap: discovery and the download endpoint use two different, unshared notions of a valid backend. `parse_asset_variant` performs no character validation on the backend segment (only non-empty), while `is_valid_backend` in runtime_version/handlers.rs:67 requires `^[a-z][a-z0-9.]*$` and rejects `-`, `_` and uppercase. An asset such as `llama-server-linux-x86_64-cuda-12.9.tar.gz` or `...-CUDA12.tar.gz` would be advertised by `/versions/available` as an installable variant and then rejected with 400 INVALID_BACKEND by `/versions/download` — i.e. the 'exact set of installable combinations' promise would be false. No current trigger (every published asset is lowercase `[a-z0-9.]`), but nothing enforces that the two predicates agree.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_repository/utils.rs","line":944,"severity":"low","finding":"Duplicate `#[test]` attribute on `only_a_clonable_github_origin_can_be_verified`: `#[test]` at line 944, doc comment, then `#[test]` again at line 953. rustc accepts it but emits `warning: duplicated attribute` (`duplicate_macro_attributes`). Verified by compiling the same shape in isolation. Cosmetic, but it is the signature of an attribute added without re-reading the item.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/tests/llm_repository/test_connection_user_agent.rs","line":55,"severity":"low","finding":"Dead test setup + an untested production path. The diff adds `LLM_REPOSITORY_GITHUB_API_BASE` to this spec's server env 'so this regression test stays offline', but the repository URL is `https://api.github.com`, which `is_clonable_github_origin` rejects — `test_repository_connectivity` returns `Unverified` at utils.rs:706-716 BEFORE `capability_probe_url` (and hence `github_api_base()`) is ever consulted. The seam is never read. Consequence: the GitHub capability branch (`capability_probe_url(Github, ..)` + `is_github_api_catalog` against a real response) has NO integration coverage anywhere in the diff — only the pure predicate is unit-tested.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/api-client/apiEndpoints.ts","line":325,"severity":"low","finding":"Wired-and-behaving: the new discovery endpoint has no in-app consumer. `RuntimeVersion.listAvailable` appears only in the generated api-client; a grep of src-app/ui/src and tests for `listAvailable` / `versions/available` finds no call site outside generated code. The UI's AvailableVersionsCard (and the new e2e spec) still drive the surface from `/{engine}/check-updates` — the 'endpoint named for a different verb' the DESIGN §1 complains about. INV-1 is stated at the API level and IS satisfied (the route is registered in routes.rs:96 and covered by tests/llm_local_runtime/available_versions_test.rs), so this is not an invariant break — but the endpoint is production-registered and app-unconsumed, and the per-variant platform/arch data it exists to expose reaches no user surface.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/src/modules/llm_model/repository.rs","line":925,"severity":"low","finding":"AFFIRMATION (no defect): INV-3 holds in every reachable state I could find. `DownloadStatus::Completed` is written at exactly one place — `update_download_status` — and the terminal UPDATE now merges `DownloadProgressData::completed()` with the row's own stored total via a `jsonb_typeof` + range-guarded `||`, so a NULL/absent/non-numeric/<=0 total falls back to 100/100 rather than aborting. Every reader inherits it: the REST read and the SSE frame both project `progress_data` off the same row (llm_model/handlers/downloads.rs:64-85), and handlers/uploads.rs:1345 is the only production writer of the completed status. Failed/cancelled correctly keep their frozen snapshot, and `failed_download_freezes_progress_while_completed_reports_full` (tests/llm_model/download_progress_test.rs) drives BOTH terminal transitions on two identically-seeded rows through the real chokepoint with an explicit negative control — the strongest test in the diff.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/server/tests/llm_local_runtime/available_versions_test.rs","line":74,"severity":"low","finding":"AFFIRMATION (no defect): INV-1 holds and is honestly tested. `discovery_alone_yields_an_installable_five_tuple` names no tag, platform, arch or backend of its own — it reads all five out of the discovery response, POSTs them verbatim, and waits for the install to actually complete; the paired negative asserts an undiscovered tag fails with an error naming the discovery endpoint. `discovery_requires_versions_read_permission` carries a real 401 + a real 403 (a user holding `llm_local_runtime::read` but not `versions_read` — the gate is `versions_read`, permissions.rs:48) AND a 200 positive control. The `mock_release` fixture was extended with genuinely non-host assets (windows/zip, macos/metal), so the 'non-host variants are listed' assertion is not idle.","status":"confirmed"}
