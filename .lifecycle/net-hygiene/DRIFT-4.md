# DRIFT-4 — implementation vs plan, round 4 (re-baselined onto `a02c09a04`)

- **DRIFT-4.1** — verdict: impl-wins — the branch was merged onto the NEW `origin/feat/agent-core` tip `a02c09a04` (27 commits on from the previous re-baseline), and the `sdk` submodule onto `origin/sdk/agent-core-and-perf` @ `ebe3ff8262`. The only conflict was the submodule pointer (resolved to this branch's merged sdk tip) and the two generated gallery indexes, resolved by REGENERATING (`npm run gen:state-matrix`) rather than by hand-picking a side. Two other features' `.lifecycle/` dirs arrived with the merges and were stripped (a branch carries exactly one). BASE.md and every gate's diff base move to `a02c09a04`.
- **DRIFT-4.2** — verdict: none — `lint:hooks` (`d22d22941`) is now part of `npm run check`. This branch's diff passes it clean: `0 violations across 2454 file(s)`. Nothing in the diff adds a reactive store read in a loop/conditional — the two new app modules (`meFreshness`, `bootSessionVerify`) are plain functions with no store reads at all.
- **DRIFT-4.3** — verdict: none — RE-MEASURED against the new tip with a backend built from THIS branch, because the shared `:29185` instance (owned by another session) went down mid-lifecycle. That turned out to be an improvement in rigour, not a compromise: one backend now serves both builds, it is seeded deterministically (1 admin + 50 conversations), and its per-IP rate limiter is disabled so the audit's settings sweep measures the diff rather than the token bucket. The earlier measurements against the shared instance are superseded.
- **DRIFT-4.4** — verdict: none — the re-measurement CHANGED the story for the better and the results were rewritten accordingly. On the previous baseline the waterfall dimension was noise-dominated (it flipped sign with run order); on this one it does not: the longest serial `/api` run falls 20→9, 9→5, 10→5, and the `/auth/me`-vs-`setup/status` overlap goes from NEGATIVE on all three routes (strictly serial) to positive on all three. The reason is that the other branch's landed N+1 fix removed the ~19-request run that had been swamping the metric. The earlier "this metric is unusable here" note in RESULTS is therefore replaced with the real numbers, not carried forward.
- **DRIFT-4.5** — verdict: plan-wins — TEST_RESULTS had recorded `gate:ui (ui): PASS` on the strength of a background task's exit code, which was the exit code of the `tail` in the pipeline, NOT of `npm run gate:ui`. The real output said `❌ GATE FAILED — runtime-health`. Corrected: the gate was re-run in the foreground on BOTH this branch and the baseline so the finding could be classified rather than asserted. Recording a gate result from a pipeline's exit code is precisely the "trust the artifact you re-ran" failure the lifecycle's P1 rule exists for.

- **DRIFT-4.6** — verdict: plan-wins — TEST-9's diff bases were HARDCODED shas, and the re-baseline made that a real defect rather than a style point: the widened range swept in the other branch's landed `testIds.generated.ts` and `stateMatrix.generated.ts` changes and reported them as THIS branch's INV-4 violations. Both bases are now resolved by `git merge-base` against their upstream branch, so the scan always sees exactly this branch's own work. Its generated-index exemption was also tightened from "every changed line is a pure line-number shift" (which broke on a regenerated header COUNT) to "no line was added or removed once integers are normalized" — which is the property that actually matters, since an added or removed surface/testid leaves an unpaired line.

**Unresolved drifts:** 0
