{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/LlmStepForm.tsx","line":38,"severity":"high","finding":"VERIFIED CLOSED — round 8's `patch({ prompt: v || null })` reaches the backend as None. EXECUTED serde probe against a faithful replica of validate.rs's StepDef (#[serde(flatten)] over an internally-tagged StepConfig with `#[serde(default, skip_serializing_if = \"Option::is_none\")] prompt: Option<String>`): input `{\"id\":\"llm_1\",\"kind\":\"llm\",\"prompt\":null,\"prompt_file\":\"p/a.md\"}` -> `prompt=None prompt_file=Some(\"p/a.md\")`, no deserialize error. serde ACCEPTS an explicit JSON null for Option<String> even inside an internally-tagged enum reached through flatten (confirmed by execution, not assumed). validate.rs:681-698 then gives PROMPT_BOTH=false / PROMPT_MISSING=false and dispatch.rs:106-115 load_raw_prompt takes the (None, Some(rel)) arm -> 'OK file'. The pre-round-8 write is reproduced as the defect: `prompt:\"\"` + prompt_file -> BOTH=false, MISSING=false, dispatch='ERR invalid prompt config'. The fix is real and correct.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/handlers/dev.rs","line":570,"severity":"medium","finding":"VERIFIED CLOSED — `null` is not merely tolerated on the wire, it is normalised OUT of persistence. Same executed probe: re-serialising the parsed struct gives JSON `{\"id\":\"llm_1\",\"depends_on\":[],\"kind\":\"llm\",\"prompt_file\":\"p/a.md\"}` and YAML `id: llm_1 / depends_on: [] / kind: llm / prompt_file: p/a.md` — the `prompt` key is absent because of skip_serializing_if. dev.rs:570 (`serde_norway::to_string(&def)`) is what writes workflow.yaml, so a cleared prompt is written ABSENT. Under the pre-round-8 write the same path emitted `prompt: ''` beside `prompt_file:`, which is precisely the unrunnable state.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":207,"severity":"high","finding":"VERIFIED CLOSED — `prompt: null` with NO prompt_file still produces the required marker AND the exact copy. EXECUTED against the real module through the repo's node-test loader: agent -> {\"prompt\":\"A task description is required\"}, llm -> {\"prompt\":\"A prompt is required\"}, llm_map -> {\"prompt\":\"A prompt is required\"}; `promptSuppliedByFile` false in all three, so `required={!fromFile}` (AgentStepForm.tsx:81, LlmStepForm.tsx:41, LlmMapStepForm.tsx:71) renders the asterisk. `promptField(label, false)` = `z.string({error: required}).trim().min(1, required)` — null hits the TYPE branch, which carries the SAME authored copy, so no zod diagnostic leaks (INV-1). With a prompt_file present all three return {} and fromFile is true (silent client, PROMPT_FROM_FILE_NOTE shown).","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":691,"severity":"high","finding":"VERIFIED CLOSED — the backend still emits WORKFLOW_PROMPT_MISSING for the null case. Executed probe case C (`prompt:null`, no prompt_file): has_prompt=false, has_file=false -> PROMPT_MISSING=true. So WorkflowBuilderPage.tsx:60 `hasErrors` is true and Save stays disabled; dev.rs:564 `validate_for_install` rejects the PUT. No path where `null` is WORSE than `\"\"`: the only behavioural divergence is at dispatch for a def that could never install (C: null -> fail-fast 'invalid prompt config'; D: \"\" -> silently runs an empty prompt), and null is the safer of the two.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/builderFields.tsx","line":121,"severity":"medium","finding":"VERIFIED CLOSED — no controlled/uncontrolled React warning is possible from the null write. `PromptField.value` is typed `string` and every call site passes `step.prompt ?? ''` (AgentStepForm.tsx:70, LlmStepForm.tsx:30, LlmMapStepForm.tsx:60), so the Textarea's `value` is never undefined/null. `npx tsc --noEmit` EXECUTED: exit 0. RefInsertMenu's insert path (builderFields.tsx:114) always passes a non-empty token, so `v || null` cannot null out an insert.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":392,"severity":"medium","finding":"VERIFIED CLOSED — `Object.assign(step, patch)` writes the null through verbatim (immer draft, no key stripping) and `toWorkflowDef` (line 56-68) passes `def.steps` unchanged, so `JSON.stringify` ships `\"prompt\":null`. EXECUTED: wire = {\"inputs\":[],\"steps\":[{\"id\":\"llm_1\",\"kind\":\"llm\",\"prompt\":null,\"prompt_file\":\"p/a.md\",\"output_format\":\"text\"}]}. The generated wire type already permits it — api-client/types.ts declares `prompt?: string | null` on the llm/llm_map/agent arms — so the client is not lying to the type system.","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/api-client/types.ts","line":6180,"severity":"medium","finding":"VERIFIED CLOSED — round 8 introduces no OpenAPI/type drift. `prompt?: string | null` is the generated type for all three prompt-bearing arms (6180 llm, 6191 llm_map, 6213 agent), matching `Option<String>` + `skip_serializing_if` on the Rust side; no regeneration is required because no Rust type changed in round 8 (`git show 21f86a534 --stat` touches only three .tsx files plus one .ts test). `cargo check -p ziee --tests` EXECUTED: exit 0, no warnings from any workflow file (the 6 dead-code warnings are pre-existing, in scheduler/models.rs, control_mcp/handlers.rs, mcp/repository.rs and tests/common/harness_inner.rs).","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":100,"severity":"medium","finding":"VERIFIED CLOSED — WORKFLOW_PROMPT_BOTH's remedy copy ('clear the prompt box here to use the file') is now TRUE end to end, which is what round 8 was fixing. Traced: copy -> author clears -> LlmStepForm.tsx:38 writes null -> validate.rs:683 BOTH stops firing AND validate.rs:691 MISSING does not start firing (has_file is true) -> dev.rs:564 install passes -> dispatch.rs:108 reads the file. The store's single humanisation boundary (describeRequestError, WorkflowBuilder.store.ts:512/354) is untouched by round 8; its 91 unit assertions were re-run and pass.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":209,"severity":"low","finding":"NOT A ROUND-8 DEFECT (pre-existing, unchanged by the diff) — the client trims and the backend does not, so a WHITESPACE-ONLY prompt with no prompt_file shows the red 'A prompt is required' under the field while validate.rs:681 (`filter(|s| !s.is_empty())`) sees a prompt, reports nothing, and Save stays enabled. Verified pre-existing: `git show origin/feat/agent-core:...stepForms.ts` already used `z.string().trim().min(1)` for the prompt field, and round 8 cannot produce this state (`' ' || null` is `' '`, unchanged before and after). Contrast validate.rs:718, which DOES reject a whitespace-only sandbox `run:`. Out of DESIGN 3 scope (it would need a backend severity/rule change); recorded so the divergence is not re-derived in a future round.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":263,"severity":"low","finding":"NOT A DEFECT (bounded, no user-visible effect) — the 400ms `validateTimer` is never cancelled on unmount: `init` (line 532) registers only EventBus listeners and never calls the `onCleanup` hook store-kit provides (sdk/packages/framework/src/store-kit.ts:386). Leaving the builder within 400ms of a keystroke therefore fires one stray `POST /validate-def`. Traced the consequence: `__destroy__` (store-kit.ts:395-405) only unsubscribes listeners — the zustand api object is NOT destroyed — so the late `set` writes into a detached store and cannot throw or resurrect UI. Round 8 did not touch this path. Registering `onCleanup(() => clearTimeout(validateTimer))` would close it if a future round wants to.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":260,"severity":"medium","finding":"VERIFIED CLOSED — the session cache's generation guard is coherent. A response landing after `invalidate` is discarded (line 260 / 321) and the pending promise it would otherwise have left behind is already removed by `invalidate` itself (line 341 / 356), so no stale promise can be re-awaited; `toolsPending` is deleted on the winning path (line 261). `createValidateRunner`'s newest-run-only rule (WorkflowBuilder.store.ts:188/203) is likewise intact. Both files' unit suites were EXECUTED under vitest: 61/61 pass (WorkflowBuilder.store.test.ts + ToolCatalog.store.test.ts).","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/SandboxStepForm.tsx","line":38,"severity":"low","finding":"VERIFIED CLOSED — round 8 brings the prompt field INTO the house pattern rather than inventing one: `patch({ stdin: v || null })` (SandboxStepForm.tsx:38) and `patch({ system: e.target.value || null })` (AgentStepForm.tsx:161) already normalised a cleared optional-string field to absent. All three prompt call sites now match. Verified there is no fourth writer: grep over builder/*.tsx + stores/*.ts finds exactly three `prompt:` writes, all `v || null`. `createStep`'s `prompt: ''` seed (stepForms.ts:103/116/122) is correctly left alone — a new step has no prompt_file (the builder renders no such control), so it is caught by PROMPT_MISSING, not by the BOTH hazard.","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.test.ts","line":349,"severity":"low","finding":"NOT A DEFECT (speculative to call one) — the round-8 regression test hardcodes ['AgentStepForm.tsx','LlmStepForm.tsx','LlmMapStepForm.tsx'] rather than deriving the set, so a hypothetical FOURTH prompt/prompt_file step kind would not be covered. This matches the already-shipped round-7 test one block above (line 308, same three-file list) and the backend's own three arms (validate.rs:671-679), and a new kind would require a new StepConfig variant, so the gap is future-conditional, not a reachable defect today. Note the branch's own INV-3 guard (noFreeTextEntityRef.test.ts:47) DOES derive its file set via readdirSync — the negative half of the R8 assertion could be generalised the same way at no cost.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/AgentStepForm.tsx","line":78,"severity":"high","finding":"VERIFIED CLOSED — round 8 broke no other field or kind, and did not weaken the Save gate. EXECUTED: `npx tsc --noEmit` exit 0; the builder's node:test suite 91/91 pass (stepForms, validationCopy, toolSchemaForm, refInsert, agentStepForm, noFreeTextEntityRef); the workflow vitest store suites 61/61 pass; `cargo check -p ziee --tests` exit 0; lint:colors, lint:guardrails and lint:logical-direction all clean. `LlmMapStepForm`'s sibling `for_each` write is correctly still `patch({ for_each: v })` — `for_each`/`item_var` are non-Option Strings on the wire (validate.rs:166-167), so a null there WOULD be a deserialize error; the change was applied only to the Option fields.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/AgentStepForm.tsx","line":52,"severity":"low","finding":"VERIFIED CLOSED — the agent read-back tolerates the new null. `agentReadback({ prompt: step.prompt, … })` types `prompt?: string | null` and does `(config.prompt ?? '').trim()` (agentStepForm.ts:49/61), so a cleared task renders 'run the task you describe above' rather than crashing or printing 'null'.","status":"rejected"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":1090,"severity":"high","finding":"VERIFIED CLOSED — the branch's one production change to check_prompt_files (skip the existence/confinement half when `bundle_root.is_dir()` is false) does NOT open a fail-open at any install or run path. Enumerated every caller: dev.rs:268 (extraction.extracted_path), dev.rs:564 (live bundle_root), runner.rs:1248 and runner.rs:1552 (workflow.extracted_path), workflow_mcp/tools.rs:528 and :672 (the resolved workspace root), hub/handlers.rs:2187 (extraction.extracted_path). Every one of them reads `<root>/<entry_point>` off disk IMMEDIATELY BEFORE validating and errors out if that read fails, which proves the root exists, so `bundle_present` is true and WORKFLOW_PROMPT_FILE_MISSING/ESCAPE still run there. The textual `..`/absolute-path reject (validate.rs:1082-1090) is unconditional and runs even on the bundle-less draft surfaces. The skip is reachable only from the two draft endpoints, which deliberately pass a never-created unique path so a WorkflowsRead caller cannot probe the filesystem (dev.rs:944-952).","status":"rejected"}
{"angle":"security-authz","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":128,"severity":"medium","finding":"VERIFIED CLOSED — the schema interpreter cannot be turned into a prototype-pollution or non-termination vector by a hostile MCP `input_schema`. `lookupRef` uses `Object.prototype.hasOwnProperty.call` (line 128) so `#/$defs/__proto__` does not resolve against Object.prototype; every merge is an object SPREAD (lines 174/185/208), which uses CreateDataProperty and therefore cannot set a prototype; recursion is cut by an explicit ref STACK plus SCHEMA_BUDGET.maxRefDepth (line 170) and field count is capped at maxFields with the remainder surfaced by name (line 378-379). Unrelated to round 8; re-verified as part of the whole-diff sweep.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":514,"severity":"low","finding":"VERIFIED CLOSED — no dead exports introduced or left. Every ToolCatalog export has a production consumer in ToolStepForm.tsx (entryForServerName:150, failureTitle:369, failureMessage:370/243, isRetryableFailure:374, load:171, resolveServer:164, invalidate/invalidateServer:250-256); every toolSchemaForm export is consumed by ToolArgumentsForm.tsx or ToolStepForm.tsx; PROMPT_FROM_FILE_NOTE and promptSuppliedByFile are used by all three prompt forms. `cargo check -p ziee --tests` reports no new dead-code warning from any workflow file. Round 8 added no symbol.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/LlmMapStepForm.tsx","line":68,"severity":"medium","finding":"VERIFIED CLOSED — full-diff regression sweep found no newly-broken behaviour. Read every scoped file (three step forms, builderFields, stepForms, validationCopy, both stores, ToolStepForm, ToolArgumentsForm, BuilderValidationPanel, toolSchemaForm, StepConfigPanel, SandboxStepForm, RefInsertMenu, WorkflowBuilderPage) plus validate.rs/dispatch.rs/dev.rs. No spec asserts the old `patch({ prompt: v })` shape (grep clean); the three new e2e specs drive the REAL backend (a loopback node MCP mock in helpers/builder-tools-mock-server.ts, the established resource-link-mock-server pattern) with no `page.route` API mocking and no `.only`/unconditional skip; the backend test file is registered (tests/mcp/mod.rs:34) and compiles.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/stores/WorkflowRun.store.test.ts","line":56,"severity":"low","finding":"PRE-EXISTING, NOT THIS BRANCH — two test-infrastructure facts observed while executing the suites, both reproducible on origin/feat/agent-core and both outside the diff. (1) `npm run test:unit`'s glob `src/**/*.test.ts` sweeps in the vitest-authored `*.store.test.ts` files, which then fail under node:test with ERR_MODULE_NOT_FOUND (the app tree's extensionless relative specifiers are deliberately not resolved by node-test-hooks.mjs); vitest.config.ts owns those files. (2) Under vitest, WorkflowRun.store.test.ts fails with 'registerLazyStore is not a function' at stores/workflowRun/index.ts:17 — that file is untouched by this branch (absent from `git diff --stat`). The two suites this branch DOES own (WorkflowBuilder.store.test.ts, ToolCatalog.store.test.ts) pass 61/61.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/dispatch.rs","line":114,"severity":"low","finding":"RESIDUAL, NOT A ROUND-8 DEFECT — a workflow that ALREADY carries `prompt: \"\"` beside `prompt_file:` (hand-authored YAML, a hub bundle, or a def saved by a pre-round-8 build) still validates GREEN in the builder and then fails the RUN at dispatch.rs:114. Executed probe case B proves the state: BOTH=false, MISSING=false, dispatch='ERR invalid prompt config'. Round 8 correctly closes the WRITE — the builder can no longer create it — but not the READ: the box already renders empty (`step.prompt ?? ''`), so clearing it fires no onChange and the normalisation never runs (the author can only fix it by typing a character and deleting it, which nothing prompts them to do). The root cause is a BACKEND disagreement — validate.rs:681 treats Some(\"\") as no-prompt while dispatch.rs:106 treats it as invalid — and DESIGN 3 puts backend validation rules, codes and severities explicitly out of scope for this branch, so this is not actionable here. Recorded, not counted as a confirmed finding.","status":"rejected"}
