{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":1934,"severity":"medium","finding":"`let window = &rest[..rest.len().min(400)]` slices a &str by BYTE index. If byte 400 of the window lands inside a multi-byte UTF-8 char the test PANICS with 'byte index 400 is not a char boundary' instead of reporting a drift. validate.rs currently holds 161 non-ASCII bytes and ref_check.rs 79 (em dashes / ellipses in doc comments and messages); I verified none currently sits in the 380..420 window of any `ValidationError::` occurrence, so it passes today, but any future message or doc-comment edit that shifts one there converts the humanisation guard into an unrelated panic. Use floor_char_boundary / char_indices().take_while instead.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":1916,"severity":"low","finding":"VALIDATE_SRC is `include_str!(\"validate.rs\")`, i.e. the scanner also scans this file's own `mod tests` and the humanisation_contract module. A future test fixture that constructs `ValidationError::at(\"semantic\", \"WORKFLOW_X\", ...)` inside a #[cfg(test)] block would be counted as a production emit site, forcing a bogus VALIDATION_CODES + validationCopy.ts entry for a code the server can never emit. I confirmed the two existing self-references (the doc comment at 1926 and the assert string at 2014) do NOT produce false positives, because their first literal is not one of schema/semantic/security. Failure mode is noisy (test fails), not silent.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":1927,"severity":"low","finding":"no findings beyond those logged: I re-implemented `emitted_codes` byte-for-byte in Python and ran it over the real validate.rs + ref_check.rs. It yields exactly 41 codes, identical to VALIDATION_CODES (0 unregistered, 0 stale), and the escape handling (`(true,'\\\\')` skip-next) correctly survives the escaped-quote text inside the assert messages. The scanner is not silently matching nothing.","status":"rejected"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"no findings for this angle: the whole diff is a `pub const` string array plus a `#[cfg(test)]` module. Nothing touches a handler, extractor, permission, or any runtime input path. The three `include_str!` reads are compile-time reads of repo-controlled files and, being under cfg(test), are not compiled into the release binary at all, so no source text is shipped and no untrusted input is parsed.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"no findings for this angle: `git diff origin/feat/agent-core...HEAD -- validate.rs` is 231 insertions / 0 deletions. Every existing `layer`/`code`/`message` literal, the `Severity` enum, the `ValidationError` struct and its serde/JsonSchema derives, `validate_for_install`, and therefore the `/validate` response, the install/import/run AppError text and the `workflow_mcp::validate_workflow` payload are byte-identical to base. The new const is not a schema type and does not appear in OpenAPI.","status":"rejected"}
{"angle":"error-handling","file":"src-app/server/src/modules/workflow/validate.rs","line":1920,"severity":"low","finding":"no findings for this angle beyond the byte-slice panic logged under correctness-logic: the cross-file `include_str!` of validationCopy.ts fails at COMPILE time if the file is moved or renamed (loud), and if the file is emptied or truncated the `unhumanised` assertion lists all 41 codes and fails loudly. There is no swallowed I/O and no `unwrap` on a runtime value in the added code.","status":"rejected"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2046,"severity":"medium","finding":"The humanisation half of the guard is a raw substring search over the WHOLE file: `!UI_COPY_SRC.contains(&format!(\"\\\"{code}\\\"\"))`. It does not check that the code is a key of `HUMAN_COPY`. Concrete failure: delete `\"WORKFLOW_TOOL_NO_TOOL\": () => ...` from HUMAN_COPY but leave the code quoted anywhere else in the file (a comment, a deprecated list, a doc block) and this test stays green; `humaniseFinding` then hits its `if (!copy) return finding.message` fallback (validationCopy.ts:172-174) and the raw wire message reaches the author — exactly the property the test claims to guarantee. The TS-side test cannot catch it either, because it iterates `HUMANISED_CODES = Object.keys(HUMAN_COPY)`, which no longer contains the code. A structural check (parse the keys, or have the TS test assert against a generated list) would close it.","status":"confirmed"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2011,"severity":"low","finding":"no vacuity finding: I specifically checked for the 'cross-file test that silently scans zero items' failure. The test has an explicit `assert!(!emitted.is_empty(), ...)` self-check AND, more importantly, the bidirectional `stale` assertion (registered - emitted must be empty) means a scanner that degrades to finding a SUBSET of emit sites also fails loudly with the missing codes named. Verified empirically that it currently finds all 41. Rejecting my initial suspicion that this is a vacuous guard.","status":"rejected"}
{"angle":"extensibility","file":"src-app/server/src/modules/workflow/validate.rs","line":1916,"severity":"medium","finding":"The scanner's source set is a hardcoded two-file list (VALIDATE_SRC + REF_CHECK_SRC at 1916-1917) while the emitters `ValidationError::{err,at,warn}` are `pub(crate)` (441/450/466) and the struct's fields are `pub`. Any OTHER module in the crate that starts emitting workflow findings (a future policy_check.rs, or a direct `ValidationError { code: .., .. }` struct literal) is invisible to `emitted_codes`: its code is neither registry-checked nor humanisation-checked, so it silently reaches the builder as a raw wire message. There is no compile-time seam forcing a new emitting file into the scan — the next contributor has to know to edit line 1917.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/server/src/modules/workflow/validate.rs","line":1965,"severity":"medium","finding":"Two closed filters silently DROP emit sites rather than failing: the layer allowlist `matches!(literals[0].as_str(), \"schema\"|\"semantic\"|\"security\")` (1965) and the SCREAMING_SNAKE charset filter on the code (1966). Failure mode: introduce a fourth layer (say \"policy\"), emit `WORKFLOW_NEW_THING` from it and forget the registry — `emitted` never sees the code, `unregistered` is empty, `unhumanised` is empty, the test passes, and the builder shows the raw validator message. The `stale` direction only rescues the case where the author DID register the code, which is not the case the guard exists for. The comment at 1961-1963 states the drop-rather-than-false-positive tradeoff but not that it re-opens the silent hole.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"Adding one validation code now requires edits in three places: the emit site, `VALIDATION_CODES` (355), and `HUMAN_COPY` in validationCopy.ts. The registry is a middleman that adds no guarantee the test could not get directly — `emitted` is already derived from source, so comparing `emitted` against the UI copy would enforce the same INV-1 property with one fewer hand-maintained closed list. Its only extra value is the duplicate/shape check in `validation_codes_registry_is_well_formed` and the grouping comments.","status":"confirmed"}
{"angle":"maintainability","file":"src-app/server/src/modules/workflow/validate.rs","line":1934,"severity":"low","finding":"The `400` window is a magic constant justified only as 'a generous bound'. Nothing detects the case where a call's first two string literals fall OUTSIDE it (e.g. a long `//` comment inserted between `ValidationError::at(` and the layer argument): the site is then silently skipped, and the miss only surfaces if the code was already registered (via `stale`). Either derive the bound (scan to the matching close paren) or assert that a window ending mid-call is an error.","status":"confirmed"}
{"angle":"maintainability","file":"src-app/server/src/modules/workflow/validate.rs","line":363,"severity":"low","finding":"no further findings for this angle: the per-group comments in VALIDATION_CODES (`// validate.rs — prompts`, `// ref_check.rs — type-aware reference checking`) are hand-maintained and unchecked, so a code that moves between files leaves a stale comment. I verified every grouping is currently ACCURATE (all 8 ref_check codes really are emitted from ref_check.rs, the other 33 from validate.rs), and a stale comment has no behavioural consequence — rejecting as style.","status":"rejected"}
{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"`pub const VALIDATION_CODES` has no production consumer: `grep -rn VALIDATION_CODES src-app/ --include=*.rs --include=*.ts --include=*.tsx` matches only validate.rs itself (the const, its `use super::` in the test module, and the two test bodies). It is a `pub` API-surface item read exclusively by #[cfg(test)] code, and because it is `pub` the dead_code lint will never flag it. CODING_GUIDELINES §15 requires a new public item to have a production caller; either make it `pub(crate)`/private with `#[cfg(test)]`-adjacent scope, or wire it into a real consumer (e.g. have `workflow_mcp::validate_workflow` document the code set from it).","status":"confirmed"}
{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":1937,"severity":"low","finding":"`let mut chars = window[open..].char_indices().peekable();` — `peek()` is never called anywhere in the loop (the only consumption is `chars.next()` at 1940 and 1955). The `.peekable()` adapter is dead and misleads a reader into looking for lookahead logic that does not exist.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":264,"severity":"high","finding":"`onChange={v => patch({ tool: v, arguments: {} })}` wipes ALL arguments on EVERY Combobox change event, with no `v === step.tool` guard. Base UI's single-select path calls `setSelectedValue(itemValue)` unconditionally on item press (node_modules/@base-ui/react/combobox/root/AriaCombobox.js:563 — no equality check), and the kit's `useControllableState.set` also always fires `onChange` (sdk/packages/kit/src/kit/use-controllable-state.ts:24-26). So re-picking the tool that is ALREADY selected (a natural thing to do when re-opening the picker to check the description, then clicking the highlighted row / pressing Enter) destroys every argument the author entered — both the generated fields and the free rows — with no confirmation and no undo. The kit Combobox is also rendered with `showClear`, so the inline X is a second one-click path to the same silent wipe.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":258,"severity":"medium","finding":"When the picker is active, a saved `step.tool` that is NOT in the fetched tool list renders as EMPTY. The kit Combobox resolves `selected = current ? byValue.get(current) ?? null : null` (sdk/packages/kit/src/kit/combobox.tsx:73) and passes that to Base UI, so an unknown value shows the `placeholder` (\"Search this server's tools…\"). Failure mode: a workflow saved against tool `search`, which the server later renamed to `web_search`, opens with an apparently BLANK required Tool field while `step.tool` is still `'search'`; `configErrors.tool` (stepForms.ts:209) only checks non-empty so no inline error fires and Save is not blocked. The author cannot see what is stored, and the only way to make the field non-blank is to pick a tool — which (see the finding above) also wipes the arguments.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":150,"severity":"medium","finding":"Non-string enums are handled as strings in both directions. `toolSchemaForm.ts:146` builds options with `value: String(v)`, and this Select commits that string verbatim (`onChange={v => onChange(v)}`, line 152) — so a property declared `{\"type\":\"integer\",\"enum\":[1,2,3]}` (the enum branch at toolSchemaForm.ts:157 runs BEFORE the type switch, so it always becomes a select) is saved as the STRING \"2\" and the tool receives a type it declared it does not accept. The reverse is also broken: the `value` prop is `typeof value === 'string' && value ? value : undefined`, so a workflow loaded with `{\"page\": 2}` renders the Select as UNSELECTED (\"Choose a value\") while the argument is in fact set. The same stringification applies to `multiselect` (line 162, `value.map(String)`).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":127,"severity":"medium","finding":"`checked={value === true}` renders a boolean field OFF whenever the key is absent — including when the schema declares `\"default\": true`. The description below it simultaneously reads \"Defaults to true.\" (line 73), so the author sees a switch in the OFF position next to text saying the default is on, and reasonably concludes the tool will be called with `false`. The control cannot express the three real states (absent/true/false), and there is no way to explicitly send `false` distinct from \"leave unset\" other than toggling on then off (which does write `false`, since `commitField`'s delete test at ToolStepForm.tsx:213 is strict `=== ''`).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":126,"severity":"low","finding":"The name→id resolution passes `(servers ?? []).map(...)` UNFILTERED, while the sibling Server picker filters to `s.enabled` (capabilities.tsx:17). A step whose `server` names a DISABLED accessible server therefore renders a Server `Select` showing the placeholder (its option was filtered out, and the kit Select drops an unknown value) while the Tool field below it is a fully populated picker of that same disabled server's tools. The two adjacent controls contradict each other about whether the server exists.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":81,"severity":"low","finding":"REJECTED (verified, not a defect): I suspected `severityOf` (validationCopy.ts:196-198) would mis-bucket entries from the `warnings` array, because `ValidationError.severity` is OPTIONAL in the generated TS type (api-client/types.ts:7443) and the helper defaults an absent value to 'error' — which would paint a warning with the red XCircle and inflate the red per-step badge in StepList. Checked the wire: `validate.rs:429` declares `severity` as a non-Option field with only `#[serde(default)]` (deserialize-side) and no `skip_serializing_if`, and handlers/dev.rs:951-953 splits the vector BY that field. So every entry landing in `warnings` always carries `severity: \"warning\"` on the wire. Not reachable.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":111,"severity":"high","finding":"Template mode is derived from the CURRENT value (`isTemplateValue`), and the plain `Input` used in that mode commits the raw string on every keystroke (line 116). So any PARTIAL edit of a reference flips the field out of template mode mid-typing and silently corrupts the argument. Concrete: a `number`/`integer`/`select`/`switch`/`multiselect` field holding `{{ inputs.limit }}`; the author backspaces the trailing `}}` (or selects-all and types `5`). The value is now the plain string `\"{{ inputs.limit \"` (or `\"5\"`), `isTemplateValue` is false, and the control swaps to `InputNumber`/`Select`, whose value guards (`typeof value === 'number'`, line 136; `typeof value === 'string' && value`, line 150) do not match — so the field renders EMPTY. The corrupt string is still in `step.arguments`, invisible and un-editable from the form, and the 'Use a value instead' undo button (line 83) has also disappeared because it only renders while `templated`. The workflow saves with a broken argument the author was shown as blank.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":140,"severity":"medium","finding":"`onChange={v => onChange(v ?? '')}` turns the kit InputNumber's 'still typing' signal into a DELETE. InputNumber emits `undefined` for an intermediate string (`isIntermediate`, sdk/packages/kit/src/kit/input-number.tsx:24/48) — which includes a lone `-` and `1e`. That reaches `commitField` (ToolStepForm.tsx:213), which deletes the key, so the `value` prop becomes `null`, and InputNumber's own sync effect then fires `if (value == null) setBuf('')` (input-number.tsx:33-36) and erases the buffer. Failure mode: in any generated number field the author cannot type a negative value — the `-` vanishes as soon as it is typed. The sibling builder forms avoid this by never letting the value go null (`onChange={v => patch({ max_steps: v ?? 1 })}`, AgentStepForm.tsx:163).","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":177,"severity":"medium","finding":"The `json`/`textarea` control runs `coerceToDeclared` on EVERY keystroke, so as soon as the text becomes parseable JSON the stored value becomes an object and `value={valueToText(value)}` re-renders it as `JSON.stringify(...)` — a different string from what the author typed. Typing `{ \"a\": 1 }` rewrites the textarea to `{\"a\":1}` the instant the closing brace lands, collapsing the whitespace and moving the caret to the end (React only preserves selection when the controlled value is unchanged). Continuing to type then appends in the wrong place. Coercion belongs on blur, not on change.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":191,"severity":"low","finding":"Typing a DECLARED property name into a free 'Additional arguments' row makes the row disappear under the author's cursor. `commitRows` (line 203) merges rows last, so the moment the key matches a schema property the value lands in `known`; `splitArguments` then puts it outside `extra`, the resync effect sees `argsSnapshot(extra)` differ from `lastPushed`, and `setRows(argsToRows(extra))` drops the row. The value is not lost (it reappears in the generated field above), but the input the author is typing into is destroyed mid-keystroke with no explanation.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":203,"severity":"low","finding":"REJECTED (verified sound): I traced the buffer-vs-prop protocol for clobbering. `lastPushed` is written from `rowsToArgs(next)` in `commitRows` while `patch` sends `{...known, ...rowObj}`; the resync effect compares against `argsSnapshot(extra)`, and `extra` is rebuilt in the same key order the merge produced, so `JSON.stringify` equality holds and a self-commit never re-seeds the rows. `commitField` (which does NOT touch `lastPushed`) leaves `extra` byte-identical, so it also does not trip the effect. The generated half and the free half genuinely cannot clobber each other, including across the `useGenerated` false→true transition when the catalog resolves. No finding.","status":"rejected"}
{"angle":"reactive-read-in-loop","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":39,"severity":"low","finding":"REJECTED (verified clean). `defineLocalStore`'s proxy calls `useStore(...)` on every non-function property read (store-kit.ts:589-593), so a reactive read IS a hook here. I checked every read in the five files: StepList reads `store.def`/`store.selectedStepId`/`store.validation` unconditionally at the top and hoists `findingsByStep` above the `.map()`; BuilderValidationPanel reads `store.validation`/`store.validating`/`store.def` at the top and calls `attributeFindings` before any map; ToolStepForm reads only `catalog.byServerId` (line 122) plus `McpServer.servers`, both unconditional; ToolArgumentsForm/GeneratedField only FORWARD `store` (no property read) and RefInsertMenu's `store.def` read is at the top of its own component, so per-field menus are separate hook scopes. Action reads (`store.selectStep`, `store.updateStep`, `catalog.load`) return function-valued props and take the hook-free branch. Hook counts are stable in all files.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":160,"severity":"medium","finding":"A server that is REACHED but serves ZERO tools degrades SILENTLY — the one case the module's own INV-6 comment (line 111) says must never happen. `entry = {tools: [], loading:false, failure:null}`, so `blockingFailure` is null (no Alert renders) and `usePicker` is false only because `toolOptions.length > 0` fails. The author gets the plain hand-entry `Input` with the generic placeholder 'e.g. search' and the generic description 'The exact name of the tool to call on that server.' — pixel-identical to the pre-picker experience and indistinguishable from 'the picker just hasn't been built for this'. Nothing tells them the server answered and offers nothing (an MCP server whose tool set is permission-filtered to empty, or a misconfigured server, is the realistic case). The store even models the distinction correctly; the component collapses it.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":93,"severity":"medium","finding":"A FAILED validation call is invisible in this panel. `runValidate` (WorkflowBuilder.store.ts:99-107) writes the reason to `store.error` and deliberately keeps the prior `validation` — but `store.error` is not rendered by this panel, nor anywhere else in the builder (WorkflowBuilderPage.tsx renders only `loadError`, `deletedExternally` and save errors). Two wrong states result: (a) if the FIRST validate fails, `validation` is null and this panel asserts 'Add steps to validate the workflow.' while the workflow has steps; (b) if a later validate fails, the panel keeps rendering the stale result — including the green 'No blocking errors.' at line 99 — with no staleness marker, while `saveDisabled` (WorkflowBuilderPage.tsx:68) is computed from that same stale `validation`. In both cases the author is told the workflow validated when it did not.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":198,"severity":"low","finding":"`LabeledControl` accepts an `error` prop (builderFields.tsx:26) and GeneratedField never passes one, so `required` on a generated field is purely decorative: a missing required argument shows only the red `*`. The backend `POST /validate-def` cannot check arguments against a tool's schema (it never fetches the tool), so nothing else catches it either — the omission surfaces only when the workflow RUNS. The client already knows `field.required` and the current value; the check is one line.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":236,"severity":"low","finding":"REJECTED (defensively sound). I checked whether a malformed `input_schema` could render an empty or throwing generated section: `describeToolSchema` narrows every access with `isRecord`, returns null for a missing/non-object schema or zero properties (toolSchemaForm.ts:206-212), keeps an unresolvable `$ref` property as a JSON field rather than dropping it (line 233-234), and cuts `$ref` cycles with an explicit stack. So `spec` is either null (caller renders the free-row fallback with the 'Arguments' label) or has at least one field. `ToolArgumentsForm` can never render an empty container.","status":"rejected"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":59,"severity":"low","finding":"`data-testid={`wf-builder-finding-goto-${finding.stepId}`}` is not unique: a step with two findings (very common — e.g. a tool step missing both server and tool emits WORKFLOW_TOOL_NO_SERVER and WORKFLOW_TOOL_NO_TOOL, both located at the step id) renders two buttons with the identical testid. Any Playwright `getByTestId(...)` on it fails with a strict-mode violation, so the very affordance this diff adds is not reliably assertable. Index the testid (or key it on the finding code).","status":"confirmed"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":116,"severity":"low","finding":"The problem/caution badge carries its full phrasing only in a native `title` on a non-focusable `<span>` plus an `sr-only` span. A sighted keyboard user therefore never gets the expansion (`title` shows on hover only, and the span is not tabbable), so for them the badge is a bare glyph and a digit with no stated unit. Screen-reader users are covered by the sr-only text; pointer users by the title; keyboard-only sighted users by neither.","status":"confirmed"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/RefInsertMenu.tsx","line":66,"severity":"low","finding":"REJECTED (verified named + operable). The new `compact` icon-only trigger passes both `aria-label=\"Insert reference\"` and `tooltip`, and the kit Button's type union FORCES a name on `size=\"icon\"` (button.tsx:59-60) with `aria-label` winning over the tooltip string (button.tsx:97) — so the trigger has an accessible name and is a real `<button>` inside a `Dropdown`. Same for the `Undo2` clear-ref button (ToolArgumentsForm.tsx:89). The empty-refs case still renders a disabled 'No references available yet' item rather than an empty menu (line 38-43). No finding.","status":"rejected"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":82,"severity":"low","finding":"REJECTED (pre-existing, not introduced by this diff). The row `<li>` is a clickable non-button with `onClick` and no `role`/`tabIndex`/keyboard handler, which is a genuine a11y gap — but that markup is unchanged from origin/feat/agent-core; the diff only adds the validation badges inside it. Out of scope as a finding against this branch.","status":"rejected"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":117,"severity":"low","finding":"REJECTED (conformant). Checked every class added by the diff across all five files: colors are semantic tokens only (`text-destructive`, `text-warning`, `text-muted-foreground`, `bg-muted`, `border-primary`, `bg-accent`) — no raw hue, no arbitrary color value, no inline style color. Direction props are logical throughout (`text-start`, `ps-0`, `justify-start`, `items-start`); no `pl/pr`, `ml/mr`, `text-left/right` appears in the diff. Spacing stays on the 4px scale plus the kit's sanctioned 2px half-steps (`mt-0.5`, `py-1`). `SectionHeader` is already used for the Steps title+actions row (StepList.tsx:53), form fields go through `LabeledControl` rather than raw flex-gap, and every control is a kit component — no raw `<button>`/`<input>`.","status":"rejected"}
{"angle":"responsive-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":82,"severity":"low","finding":"The label-row action group has no `shrink-0`, and `LabeledControl`'s row is `flex items-center justify-between gap-2` with a `<label>` that has neither `min-w-0` nor truncation (builderFields.tsx:57-62). The labels here come from the tool's schema `title` (toolSchemaForm.ts:238) and can be long. At ~358px (the config column at a 390px viewport) a long single-token label pushes against the two icon buttons, which — having only `w-8`-style width and default `flex-shrink: 1` — compress rather than hold their size. The `compact` variant addresses the width of the trigger but not its shrink behaviour; `shrink-0` on the wrapper is the fix.","status":"confirmed"}
{"angle":"responsive-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":102,"severity":"low","finding":"REJECTED (holds up). Traced the new kind-Tag + badge row at the narrowest real case. The page stacks below `md` (WorkflowBuilderPage.tsx:184-187) so at 390px the list is full-width, and at `md` the column is `w-80` (320px) minus the grip and three icon buttons. The wrapper carries `min-w-0`, the Tag carries `min-w-0 truncate`, and both badges carry `shrink-0 whitespace-nowrap` — so the kind label truncates and the counts stay intact rather than wrapping to a second line. No overflow.","status":"rejected"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":4,"severity":"low","finding":"REJECTED as a coupling violation: `import { McpServer } from '@/modules/mcp/stores/mcpServer'` matches the established house pattern here — the sibling `capabilities.tsx:3` does exactly this and its header calls `McpServer` 'the sanctioned cross-module accessor', and `AgentStepForm.tsx:8` imports it too. Not a new cross-module reach. (The real problem is that ToolStepForm re-derives the server list itself instead of reusing the module's own helper, which is what causes the enabled-filter divergence logged separately under correctness-logic.)","status":"rejected"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":38,"severity":"low","finding":"REJECTED. `ToolArgumentsForm` takes the whole `WorkflowBuilderStore` purely to forward it to `RefInsertMenu`, which is a wider dependency than the component needs — but it is the same threading pattern `PromptField` already uses (builderFields.tsx:76-77), and `RefInsertMenu`'s own contract requires the store. Not worth changing, and not a coupling violation.","status":"rejected"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":120,"severity":"medium","finding":"The tool catalog is refetched over the network on EVERY step selection. `ToolCatalogStoreDef.use()` is a `defineLocalStore`, whose `use()` builds a fresh store per component INSTANCE (store-kit.ts:634-648: `useRef` seeded once per mount), and `StepConfigPanel` remounts the form on every switch via `<StepForm key={step.id}>` (StepConfigPanel.tsx:88). So `byServerId` starts empty on each mount, the store's own cache short-circuit (`existing && !existing.loading && existing.failure == null`, ToolCatalog.store.ts:59) can never hit, and clicking between two tool steps on the same server issues a `GET /api/mcp/servers/{id}/tools` each time — a call that for a stdio server can mean spawning/handshaking the server process. It is also visible: during each refetch `usePicker` is false, so the Tool field renders as a DISABLED plain Input and then swaps back to a Combobox, and the 'Additional arguments' section briefly shows every declared argument as a free key/value row before the effect re-partitions them.","status":"confirmed"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":81,"severity":"low","finding":"REJECTED (not a real cost). `attributeFindings` runs unmemoized twice in this panel and twice more in StepList on every render, and `commitRows`/`commitField` write the store on every keystroke. But `attributeFindings` is O(findings × steps) over a list the backend caps at 50 steps and a handful of findings, and the per-keystroke store write is the pre-existing builder pattern used by every other step form. No re-render storm and no expensive derive.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":239,"severity":"low","finding":"The 'Tool list unavailable' Alert is terminal — it offers no retry, so an `unreachable` failure (a server that was briefly down) can only be cleared by navigating away and back. The store ships `invalidate(serverId)` specifically for this, documented as 'used by \"try again\"' (ToolCatalog.store.ts:117-118), and it has no caller anywhere in the app; `markUnknown` and the `unknownNames` state it writes (ToolCatalog.store.ts:47/111) likewise have no production reader — grepped both UI workspaces. Either wire a retry action into this Alert or drop the unreachable store surface.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":247,"severity":"low","finding":"`spec.overflowNames` is only ever length-checked; the names themselves are never rendered. The note says 'Showing N of M arguments this tool accepts — the rest can be set below', but 'below' is a free key/value editor and `splitArguments` only creates a row for an overflow property that ALREADY has a saved value — so for a tool declaring >24 properties the author is told extra arguments exist and given no way to learn a single one of their names. The data to fix it is already computed and carried into the component.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/RefInsertMenu.tsx","line":17,"severity":"low","finding":"REJECTED. The new `compact` prop is reached on a production path — `ToolArgumentsForm.tsx:104` passes it for every generated field — and the labelled default branch is still used by `PromptField` (builderFields.tsx:111-116). Both branches live. Likewise `AttributedFinding.stepIndex`/`stepLabel` are consumed by `findingStepTitle`, and `WORKFLOW_LEVEL` is used inside `indexFindingsByStep`. No dead additions in these files.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":300,"severity":"high","finding":"coerceToDeclared runs the `isTemplateValue(raw)` early-return BEFORE the kind switch, so a json/textarea field whose text merely CONTAINS a reference is returned as a raw STRING instead of being JSON-parsed. Trigger: tool schema {\"properties\":{\"filters\":{\"type\":\"object\"}}} -> fieldKind='json' -> ToolArgumentsForm.tsx:177 is the only caller; the author types {\"userId\": \"{{ inputs.user }}\"}. TEMPLATE_RE matches, line 300 returns the raw string, so arguments.filters is persisted as the STRING '{\"userId\": \"{{ inputs.user }}\"}' rather than an object. The value silently changes type on the first keystroke, and the existing test only covers the whole-value case coerceToDeclared('{{ inputs.limit }}', ...). Partially-templated object/array arguments are the normal authoring pattern for a tool that takes structured input.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":145,"severity":"high","finding":"enumOptions (and choiceOptions, line 141) stringify every enum value with String(v) and ToolField.options carries no way back to the declared type, so a non-string enum is silently written to the workflow as a string. Trigger: {\"properties\":{\"level\":{\"type\":\"integer\",\"enum\":[1,2,3]}}}. fieldKind checks `enum` FIRST (line 157) so kind='select', not 'integer'; ToolArgumentsForm.tsx:151 does onChange(v) with the option's string value and nothing coerces it -> arguments.level === \"2\" where the tool declares an integer. Identical for a boolean enum ([true,false] -> \"true\") and for multiselect (ToolArgumentsForm.tsx:162 value.map(String); coerceToDeclared's multiselect arm is never reached from that control).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":175,"severity":"medium","finding":"Neither standard spelling of a NULLABLE property is understood, so both degrade to a raw JSON textarea. (a) `type` as an array: FieldSchema.type is typed `string` (workflowElicitSchema.ts:47) and the switch at 175 has no array branch, so {\"q\":{\"type\":[\"string\",\"null\"]}} falls to `default:` -> 'json'; the line-164 `field.type === 'array'` test likewise misses [\"array\",\"null\"]. (b) anyOf-null: {\"q\":{\"anyOf\":[{\"type\":\"string\"},{\"type\":\"null\"}]}} (what FastMCP/pydantic emit for Optional[str], i.e. the most common real MCP tool schema) fails the line-161 `choices.every(c => 'const' in c)` test, then hits switch(undefined) -> 'json'. The author gets a 4-row JSON box for a plain optional string, and typing 1234 into it yields the NUMBER 1234 via coerceToDeclared's JSON.parse where the schema requires a string.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":160,"severity":"medium","finding":"`allOf` is not handled anywhere. Trigger: {\"properties\":{\"level\":{\"allOf\":[{\"$ref\":\"#/$defs/Level\"}],\"default\":\"info\"}}} — pydantic v1's spelling for an enum-typed field, and the standard way to attach a description to a $ref. There is no enum, no anyOf/oneOf and no `type` on the node, so fieldKind falls to `default:` -> 'json'. The referenced $defs entry (which DOES declare the enum) is never consulted because resolveSchema only follows a `$ref` that is a DIRECT key of the node, not one nested inside allOf. A closed value set renders as a free JSON box.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":127,"severity":"medium","finding":"resolveSchema discards the referencing node's OWN sibling keywords: when `$ref` is present it returns resolveSchema(target,...) and never merges the node back. Trigger: {\"properties\":{\"color\":{\"$ref\":\"#/$defs/Color\",\"title\":\"Line colour\",\"description\":\"Colour of the trend line\",\"default\":\"red\"}}} with $defs.Color = {\"type\":\"string\",\"enum\":[\"red\",\"blue\"]}. describeToolSchema:236-243 then reads title/description/default off the RESOLVED def, so the field is labelled from the shared def (or falls back to the raw key `color`), shows no description, and loses `default: \"red\"` entirely. $ref-alongside-annotations is legal and common in JSON Schema 2020-12.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":303,"severity":"medium","finding":"coerceToDeclared's `integer`, `number`, `switch` and `multiselect` arms are unreachable on the production path. The function has exactly ONE caller — ToolArgumentsForm.tsx:177 — inside the `case 'textarea': case 'json':` Textarea branch. Every other kind is edited by a typed control that calls onChange directly: InputNumber (line 140), Switch (128), Select (151), MultiSelect (163), and the default Input (190, which passes e.target.value uncoerced). So only kinds 'textarea' and 'json' ever reach the switch. The unit tests exercise the dead arms (toolSchemaForm.test.ts:156-158: coerceToDeclared('5', f('limit')) === 5, coerceToDeclared('true', f('verbose')) === true), which reads as coverage of a type-coercion step that never runs — and masks finding #2 above.","status":"confirmed"}
{"angle":"api-friendliness","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":227,"severity":"medium","finding":"ToolFormSpec.overflowNames is computed but its VALUES are never read — only `.length` (ToolArgumentsForm.tsx:247). The overflow note says 'the rest can be set below', yet the free key/value section below only pre-creates rows for keys that ALREADY have a value (ToolStepForm.tsx:174/179 -> splitArguments -> `extra`, and splitArguments:281 builds `declared` from spec.fields, i.e. the KEPT 24 only). So for a tool declaring >24 properties the author is told N more arguments exist, is NOT told their names — which this spec is holding in overflowNames — and must type each key from memory. That is precisely the 'never make the author type what the system can enumerate' failure the module header (lines 6-11) says it exists to prevent.","status":"confirmed"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":221,"severity":"low","finding":"SCHEMA_BUDGET.maxFields bounds the RENDERED fields but the cap is applied after the whole property set has been materialised, and the unbounded remainder is retained. describeToolSchema builds Object.keys(props), then two full `filter` passes, then `ordered.slice(24)` into overflowNames — which is kept alive on the spec for the lifetime of the memo despite never being read (see the api-friendliness row). A tool returning a 5,000-property input_schema allocates four full-length arrays per re-derivation. Bounded in practice by the HTTP response size, hence low, but the cap is in the wrong place: it should bound the enumeration, not just the render.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":183,"severity":"low","finding":"The textarea heuristic promotes a field to a multi-line control based on its DESCRIPTION length, not the value's: `(field.description?.length ?? 0) > 160`. Trigger: {\"name\":{\"type\":\"string\",\"maxLength\":40,\"description\":\"<180 chars of prose>\"}} — a 40-char single-line name renders as a 3-row Textarea (ToolArgumentsForm.tsx:170-175) purely because the tool author wrote a thorough description. maxLength (40) explicitly says otherwise and is ignored in that branch.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":110,"severity":"low","finding":"lookupRef uses `name in bucket`, which walks the prototype chain, so a $defs pointer at an inherited key resolves to a prototype member. Trigger: {\"$defs\":{},\"properties\":{\"x\":{\"$ref\":\"#/$defs/__proto__\"}}} -> bucket['__proto__'] is Object.prototype, which isRecord() accepts, so it is returned as the field's FieldSchema and rendered (harmlessly, as 'json'). '#/$defs/constructor' and '#/$defs/toString' return functions and are rejected by isRecord, so nothing throws and there is no pollution — but the lookup should be Object.prototype.hasOwnProperty.call(bucket, name) so an attacker-influenced pointer cannot silently resolve to something the tool never declared.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":208,"severity":"low","finding":"A root-level `$ref` is not resolved. describeToolSchema reads inputSchema['properties'] directly, so {\"$ref\":\"#/$defs/Input\",\"$defs\":{\"Input\":{\"type\":\"object\",\"properties\":{...},\"required\":[...]}}} yields props === undefined -> returns null -> the whole tool falls back to hand-typed key/value pairs. The schema is fully self-contained and this module's own resolveSchema could dereference it in one call; the fallback is taken for a schema it is capable of reading.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":54,"severity":"low","finding":"Adding a ToolFieldKind requires edits in three places and only two are type-checked. `fieldKind` (line 152) returns the kind and `ToolArgumentsForm.tsx:121` switches on it — but that switch ends in a `default:` (line 185) returning a plain Input, so a new union member compiles clean and silently renders as an untyped text box. coerceToDeclared (line 303) likewise has a `default:`. A `satisfies`-exhaustive mapping (or removing the two `default:` arms in favour of explicit cases) would make a new kind a compile error rather than a silent degradation.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":191,"severity":"medium","finding":"resolveFindingStep takes loc.split('.')[0], but the location for WORKFLOW_BAD_STEP_ID (validate.rs:624) is the RAW step id — which by definition may be malformed. Trigger: a YAML-authored workflow with `- id: report.final` alongside `- id: report`, loaded into the builder (WorkflowBuilder.store.ts:159 load -> runValidate). The finding's location is 'report.final', head = 'report', so the copy \"This step's internal name must start with a lowercase letter...\" is attached to the WRONG step (or, with no 'report' step, to 'Whole workflow'). StepList.tsx:68 then flags the wrong row as invalid. The one finding whose job is to point at a bad id is the one that cannot.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":190,"severity":"low","finding":"The outputs guard is `loc.startsWith('outputs[')`, but WORKFLOW_DUPLICATE_OUTPUT_NAME (validate.rs:857) passes the BARE output name as its location (&o.name), not an outputs[...] path — only ref_check.rs:189 uses the bracketed form. So an output named the same as a step (the canonical pattern: outputs: [{name: summary, from: '{{ summary.output }}'}] next to `- id: summary`) would attribute a whole-workflow output finding to that step. LATENT ONLY: BuilderDef/toWorkflowDef (WorkflowBuilder.store.ts:25-63) never send `outputs`, so this code cannot fire from the builder today — it is guarded by an omission elsewhere, not by this function.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":126,"severity":"medium","finding":"WORKFLOW_TEMPLATE_SYNTAX interpolates the RAW backend message into author-facing copy, violating this file's own stated rules at lines 52-56 ('never name a YAML key, a Rust field, or a wire type') and the drift guard's stated purpose. The interpolated text is TemplateError's Display impl (template.rs:46-63), e.g. \"unknown filter 'foo' (supported: json, raw)\" or \"cannot access field 'x' on a non-object value (path 'a.b')\", so the person reads: \"A reference in this step isn't written correctly (cannot access field 'x' on a non-object value (path 'a.b')).\" The backend test only checks that the CODE key exists in this file, so it cannot catch a humanisation that re-emits the wire message verbatim.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":93,"severity":"low","finding":"WORKFLOW_PROMPT_FILE_UNSAFE (line 93) and WORKFLOW_PROMPT_FILE_ESCAPE (line 95) carry BYTE-IDENTICAL copy, erasing the distinction the two codes exist to make. Backend: UNSAFE (validate.rs:1045) fires on a literal '..' or leading '/' in prompt_file — the author typed a bad path; ESCAPE (validate.rs:1059) fires when a syntactically fine relative path CANONICALIZES outside the bundle — a symlink out of the bundle. The fix differs (edit the path vs. remove the symlink) but the sentence is the same.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":77,"severity":"low","finding":"WORKFLOW_STEP_DESCRIPTION_TOO_LONG says 'shorten it to under 200 characters', but validate.rs:645 rejects only `desc.chars().count() > MAX_STEP_DESCRIPTION_CHARS` (=200, validate.rs:272) — exactly 200 characters is VALID. The instruction is off by one and tells the author to delete a character that is allowed.","status":"confirmed"}
{"angle":"api-friendliness","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":73,"severity":"low","finding":"WORKFLOW_BAD_STEP_ID (\"This step's internal name must start with a lowercase letter...\") and WORKFLOW_DUPLICATE_STEP_ID (\"rename one of them\", line 75) instruct the author to edit a step id, but the builder exposes no step-id control anywhere: StepConfigPanel.tsx renders description/depends_on plus the per-kind form, stepForms.ts mints ids in createStep (line 95-96), and no component patches `id`. Both findings are only reachable for a YAML-imported workflow and are unfixable from the surface that displays them — the copy asks for an action the UI does not offer.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":61,"severity":"low","finding":"Four backend limits are hardcoded into English sentences here with no drift guard: '50 is the limit' (line 62 vs validate.rs:610), 'under 200 characters' (78 vs MAX_STEP_DESCRIPTION_CHARS=200, validate.rs:272), 'the longest allowed wait is 30 minutes' (107 vs ELICIT_TIMEOUT_HARD_CAP_MS=1_800_000, validate.rs:279), 'the maximum is 20' (109 vs MAX_PARALLEL_HARD_CAP=20, validate.rs:270). All four are CORRECT today (verified), but validation_codes_are_registered_and_humanised (validate.rs:2004) only asserts the quoted CODE literal is present in this file — it never compares the numbers. Bumping any cap silently makes the author-facing copy lie, in the one file whose whole purpose is a checked humanisation contract.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":196,"severity":"low","finding":"severityOf defaults an ABSENT severity to 'error', while the generated contract declares it optional (api-client/types.ts:7440 `severity?: Severity`). StepList.tsx:39-41 concatenates attributeFindings(validation.errors) with attributeFindings(validation.warnings) and then relies solely on this field for the per-step badge counts, discarding the authoritative signal (which array the finding arrived in). If the backend ever adds skip_serializing_if to ValidationError.severity — legal under the generated type — every warning would be counted and badged as a blocking error. Passing the known severity into attributeFindings would make the wrong state unrepresentable.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":161,"severity":"low","finding":"Two exports have no production consumer. HUMANISED_CODES (line 161) is imported only by validationCopy.test.ts:7 — the Rust drift guard (validate.rs:2046) greps this file's raw TEXT for `\"CODE\"` literals and never touches this export, so its stated role ('drift-guard surface') is not what the guard actually uses. WORKFLOW_LEVEL (line 234) is written by indexFindingsByStep (241) but that bucket is never read: StepList.tsx:68 only does findingsByStep.get(step.id), and BuilderValidationPanel.tsx:81-82 re-derives from attributeFindings + findingStepTitle. The workflow-level counts are computed on every render and discarded.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":85,"severity":"medium","finding":"A server-side 403 is misclassified as 'unreachable'. mcp/handlers/runtime.rs:79-87 returns 403 USER_NO_ACCESS 'You do not have access to this server' when the caller HOLDS mcp_servers::read but lacks per-server access — a condition hasPermissionNow(Permissions.McpServersRead) at line 61 structurally cannot detect. The catch at 85 labels every throw 'unreachable', so failureMessage:167 renders the self-contradicting sentence: `Couldn't reach \"web_search\" (You do not have access to this server) — enter the tool name and arguments by hand.` The 'no-permission' variant exists for exactly this case and is only ever reached via the client-side global-permission pre-check. Branching on `(error as {status?: number}).status === 403` (the api-client stamps it, core.ts:729) would classify it correctly.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":167,"severity":"medium","finding":"failureMessage's 'unreachable' arm interpolates a raw api-client Error.message into an author-facing sentence. sdk/packages/framework/src/api-client/core.ts:678 defaults errorMessage to `HTTP error! status: ${response.status}` and line 712 to `HTTP error! status: 502 - ${textResponse}` when the body is not JSON. So a proxy returning an HTML error page produces: `Couldn't reach \"web_search\" (HTTP error! status: 502 - <!DOCTYPE html><html>...) — enter the tool name and arguments by hand.` — a machine code plus an unbounded third-party document pasted into an Alert description (ToolStepForm.tsx:244). The store's own doc (line 22-23) says every failure maps to VISIBLE author-facing copy; this one maps to a transport dump.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":12,"severity":"medium","finding":"The store's stated contract — 'refetching once per builder mount is CORRECT' (lines 12-15) — does not hold. Its only consumer is ToolStepForm, which StepConfigPanel.tsx:88 mounts as `<StepForm key={step.id} ...>`; defineLocalStore.use (store-kit.ts:632-645) creates the zustand api inside a useRef that is re-initialised on every remount. So a FRESH catalog instance is created on every step selection, not once per builder. Repro: a workflow with two tool steps on the same server — clicking step A, step B, step A issues three GET /api/mcp/servers/{id}/tools calls, each of which may spawn/handshake a stdio MCP server. Both byServerId and the `inflight` Set (line 52) are per-instance, so neither the cache nor the coalescing guard can dedupe across selections, and the byServerId map (documented at 41-43 as letting 'two names for one server share a fetch') can only ever hold one entry.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":111,"severity":"medium","finding":"Three declared members are never used anywhere in either UI workspace (verified by repo-wide grep for markUnknown / unknownNames / .invalidate on this store). `markUnknown` (111) has no caller and `unknownNames` (47) is read by nothing — the 'unknown-server' failure it was meant to feed is produced independently by entryForServerName:148-153. `invalidate` (118) also has no caller, and its doc comment cites a \"try again\" affordance that does not exist: ToolStepForm renders the failure as a static Alert (line 239-246) with no retry control. Roughly a fifth of the store's surface is unreachable, and the invalidate doc documents behaviour the UI does not offer.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":118,"severity":"low","finding":"invalidate() deletes the cache entry but does NOT clear `inflight`, so calling it during a fetch leaves that serverId registered: the next load() short-circuits at line 56 and returns with no fetch, and the still-running pre-invalidation request then writes its (by definition stale) result into byServerId at 78-101. The repo's own core/coalescedLoader.ts documents this exact bug and solves it with a generation counter ('Clearing only the cache would hand the next reader the still-registered pre-invalidation promise', lines 16-20). Currently LATENT because invalidate has no callers — it becomes live the moment the documented 'try again' button is added.","status":"confirmed"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":52,"severity":"low","finding":"The store hand-rolls cache + in-flight coalescing that src-app/ui/src/core/coalescedLoader.ts already provides — a module explicitly extracted for reuse ('Reusable by any other \"several stores want the same list on boot\" case', lines 6-9) — and re-implements it with strictly weaker semantics: no TTL, no generation guard on a superseded request (see the invalidate row), and a second caller gets `undefined` back rather than the shared promise (line 56 returns nothing, so `await catalog.load(...)` resolves before the data exists). Either adopt the loader per serverId or state in the header why per-server keying rules it out.","status":"confirmed"}
{"angle":"api-friendliness","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":55,"severity":"low","finding":"load(serverId, serverName) takes the name purely to embed it in a failure message, and nothing links the two arguments. The caller must independently keep them consistent — ToolStepForm.tsx:137 passes `serverId` (derived by entryForServerName from step.server) and `step.server` separately; entryForServerName already resolved BOTH from the same match object and could return the pair. A caller that passes a stale name after the author renames a server produces a failure Alert naming the wrong server, with no compile-time or runtime check.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":165,"severity":"low","finding":"Apostrophe style is inconsistent inside this one change. failureMessage uses ASCII apostrophes throughout — \"You don't have permission to list this server's tools\" (165), \"Couldn't reach\" (167), \"which isn't one of the servers available to you\" (163) — while the sibling component added in the same diff writes the same possessive typographically: ToolStepForm.tsx:275 'Loading this server\\u2019s tools\\u2026'. The two sentences appear in the same control within seconds of each other. ASCII is the dominant style in the workflow module (only 4 U+2019 occurrences module-wide), so ToolStepForm is the outlier — but the pair should be settled one way before this ships.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":242,"severity":"low","finding":"REJECTED — falsy defaults. Checked default:false / default:0 / default:\"\" end to end. ToolField.default is `unknown` and assigned unguarded (`default: schema.default`, line 242), the consumer gates on `field.default !== undefined` (ToolArgumentsForm.tsx:73), and valueToText(false)==='false' / valueToText(0)==='0' (line 327-331 falls to JSON.stringify for non-strings). Falsy defaults survive intact. Only default:\"\" renders the empty sentence 'Defaults to .', which is cosmetic and lives in the consumer, not here.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":129,"severity":"low","finding":"REJECTED — self-referential $ref infinite recursion. Verified terminating: with $defs.Node = {\"$ref\":\"#/$defs/Node\"} the first hop pushes the ref onto `stack`, the second hits `stack.includes(ref)` (line 129) and returns null; a long non-cyclic chain is cut by stack.length >= maxRefDepth. Mutual recursion (A->B->A) is cut identically. resolveSchema also never recurses into nested `properties`, so there is no second unbounded axis. The explicit-stack claim in the header holds.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":206,"severity":"low","finding":"REJECTED — hostile/malformed input throwing. Constructed and traced: inputSchema as null/string/array/number; properties as a string or array; required containing non-strings ([1,null]); enum as a non-array; anyOf/oneOf as a string; items as a tuple ARRAY (`\"items\":[{...}]`); items.enum a non-array; enumNames shorter than enum or a string; maxLength a string. Every one is guarded by isRecord / Array.isArray / optional chaining, and none throws or returns a shape ToolArgumentsForm crashes on — the documented 'degrade to the escape hatch, never throw' contract (lines 199-205) holds.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":254,"severity":"low","finding":"REJECTED — TEMPLATE_RE regex statefulness. The literal has no `g`/`y` flag, so RegExp.prototype.test carries no lastIndex across calls; isTemplateValue is deterministic per input despite the shared module-level regex. Also checked the lazy `[\\s\\S]*?` against a value containing many `{{`: no catastrophic backtracking (no nested quantifier).","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":59,"severity":"low","finding":"REJECTED — 'a failed fetch is cached forever'. The cache-hit guard requires `existing.failure == null` (line 59), so a failed OR no-permission entry always falls through and re-fetches on the next load(). Also checked for a permanently-loading entry: inflight is cleared in `finally` (102-104) and both the success and catch paths set loading:false, so no path leaves loading:true. What actually re-triggers the retry is a remount or a serverId change (ToolStepForm.tsx:136-139 deps), which per the confirmed lifecycle row happens on every step switch — so retries are frequent, not absent.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":78,"severity":"low","finding":"REJECTED — set() after unmount writing to a dead consumer. defineLocalStore (store-kit.ts:632-658) holds the zustand api in a useRef and its unmount cleanup only runs __destroy__, which detaches EventBus listeners; it does not tear down the store api. A late set() therefore writes to a store with no remaining subscribers — no React state-update-on-unmounted warning, no leak beyond the instance itself being GC'd with the component.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":57,"severity":"low","finding":"REJECTED — cache-hit check running BEFORE the permission check (57-59 vs 61). Order does matter in principle: an entry fetched while permitted is still served after a mid-session permission loss. But the entry was legitimately fetched, contains only tool names/descriptions the user was already shown, and the whole store instance is destroyed on the next step switch. No leak of anything not already disclosed; not worth a change.","status":"rejected"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":209,"severity":"low","finding":"REJECTED — attributeFindings scanning `steps` twice per finding (steps.find inside resolveFindingStep at 192, then steps.findIndex at 211). O(findings x steps), and the backend caps a workflow at 50 steps (validate.rs:610), so the worst case is a few thousand string comparisons per validation round, re-run on a 400ms debounce. Measurably free; a Map<id,index> would be premature.","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":78,"severity":"low","finding":"REJECTED — response-shape mismatch. Checked against the generated contract: ListToolsResponse.tools is NON-optional (api-client/types.ts:3082-3084), so `res.tools ?? []` (line 80) is redundant but harmless, not a nullable-treated-as-non-null bug. Tool.input_schema is `unknown` (types.ts:6542-6546) and describeToolSchema's parameter is `unknown` — an exact match, and every narrowing is done defensively inside. Tool.description is optional and is handled with a ternary at ToolStepForm.tsx:145. No contract violation found in these three files.","status":"rejected"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":1,"severity":"low","finding":"REJECTED — vocabulary duplication in toolSchemaForm. It imports FieldSchema/TitledChoice from ../workflowElicitSchema rather than declaring a second JSON-Schema type set, imports nothing outside the workflow module, and has no React/store dependency (it is pure and directly unit-tested). The one real consequence of that reuse is that FieldSchema.type is `string` and so cannot express a type ARRAY — logged separately under correctness-logic, not as a coupling defect.","status":"rejected"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":58,"severity":"low","finding":"REJECTED — mixed voice in HUMAN_COPY. Entries alternate between declarative ('This step needs a prompt.') and imperative ('Choose the server this step should call.'), but the split tracks a real distinction — describing a broken state vs. naming the single action that fixes it — and every entry stays one sentence with a consistent em-dash-before-the-remedy pattern. Style preference, not a defect. Also checked every entry for a leaked machine code or YAML/Rust key: none found except WORKFLOW_TEMPLATE_SYNTAX, logged separately.","status":"rejected"}
{"angle":"extensibility","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":58,"severity":"low","finding":"REJECTED — HUMAN_COPY as a closed map every new code must edit. Verified this is the intended, well-supported design, not drift-prone: adding a code is a ONE-place edit here, an unmapped code degrades to the backend message (humaniseFinding:172-174) rather than to nothing, and validate.rs:2004 fails the backend suite in BOTH directions (emitted-but-unregistered, and registered-but-unhumanised). Cross-checked all 41 registered VALIDATION_CODES against HUMAN_COPY's 41 keys and the /validate-def handler (handlers/dev.rs:938-963, which only calls validate_collecting, i.e. validate.rs + ref_check.rs): every code this surface can emit is mapped. No gap.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":42,"severity":"low","finding":"REJECTED — SCHEMA_BUDGET / ToolFormSpec / ToolField as unused exports. SCHEMA_BUDGET is exported for toolSchemaForm.test.ts:129-135 (which derives the overflow case from it rather than hardcoding 24) — a legitimate test seam, and it is read on the production path at lines 226-227 and 129. ToolField/ToolFormSpec are imported by ToolArgumentsForm.tsx:16-17. isTemplateValue, splitArguments, valueToText and describeToolSchema all have production callers (ToolArgumentsForm.tsx, ToolStepForm.tsx:15/152/174). Only the coerceToDeclared arms and overflowNames' contents are genuinely unreached, logged separately.","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":160,"severity":"high","finding":"INV-6 promises the free-text fallback is 'never the default and never silent'; in the state 'server chosen, reachable, serves ZERO tools' the fetch SUCCEEDS so entry.failure stays null, blockingFailure is null (no Alert), and usePicker is false purely because toolOptions.length === 0 — so the code silently renders the free-text Tool Input (line 269) with the generic description 'The exact name of the tool to call on that server.' and no stated reason. The author cannot tell an empty-catalog server from a working picker.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":292,"severity":"high","finding":"INV-6 names 'a tool may declare no schema' as one of the TWO cases that must carry a documented, visible reason; in the state 'tool chosen but describeToolSchema returns null' (absent schema, non-object schema, or zero declared properties — toolSchemaForm.ts:206-212) the code sets useGenerated=false and falls back to the generic key/value 'Arguments' editor with description 'Key/value pairs passed to the tool…' and NO explanation of why the generated form is absent. The unreachable-server case gets an Alert (line 239); the no-schema case gets nothing.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":258,"severity":"high","finding":"INV-3 makes the Tool field a picker and INV-6 forbids silent degradation; in the state 'saved workflow whose step.tool is NOT among the fetched tools' (renamed/removed upstream tool) the kit Combobox resolves selection as `byValue.get(current) ?? null` (sdk/packages/kit/src/kit/combobox.tsx:73), so the field renders EMPTY showing the placeholder while step.arguments/step.tool still hold the stale name. The stored value is neither shown nor flagged, and validation cannot catch it (WORKFLOW_TOOL_NO_TOOL only fires on an EMPTY tool), so the author sees a blank Tool field on a workflow that will still call the old tool.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/WorkflowBuilderPage.tsx","line":83,"severity":"high","finding":"INV-1 promises 'no raw schema/YAML key language ever reaches the person building a workflow'; the humanisation is applied ONLY inside BuilderValidationPanel. On the Save path the server runs validate_for_install (validate.rs:501-524) and returns AppError message '[semantic/WORKFLOW_PROMPT_MISSING] agent_1: step has neither prompt: nor prompt_file:', which the page surfaces verbatim via message.error(e.message) (and the store stores it raw at WorkflowBuilder.store.ts:338-343). Reachable whenever store.validation is null or stale — never-validated, a transient /validate-def failure (the store deliberately keeps the prior validation, line 106-109), the 400ms debounce race, or the prompt_file divergence the /validate handler documents (handlers/dev.rs:98-105 resolves prompt_file against a throwaway temp dir while install resolves against the real bundle root). humaniseFinding is never called on this path.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":147,"severity":"medium","finding":"INV-6 requires the fallback's stated reason to be TRUE; entryForServerName resolves against McpServer.servers, whose initial state is `[]` (modules/mcp/stores/mcpServer/state.ts:6). While that list is still loading, an existing tool step's valid server resolves to the `unknown-server` failure and the author is shown 'This step points at a server called \"X\", which isn't one of the servers available to you' (line 163) plus the free-text fallback — a false, alarming reason in a transient state that then flips to the picker. The store already distinguishes a loading catalog (entry.loading); it does not distinguish a loading SERVER LIST.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":63,"severity":"medium","finding":"INV-1's rule for HUMAN_COPY is 'say what the PERSON must do'; the new copy for WORKFLOW_SANDBOX_FLAVOR_REQUIRED ('the workflow needs a sandbox environment chosen for it') and WORKFLOW_UNKNOWN_FLAVOR ('pick one of the installed environments') direct an action the builder cannot perform: BuilderDef has no `sandbox` field and toWorkflowDef never emits one (WorkflowBuilder.store.ts:25-30, 51-63), while AddStepMenu still offers the 'Run code' step (stepForms.ts:59). Every builder workflow containing a sandbox step therefore validates with a blocking error whose humanised instruction points at a control that does not exist, permanently blocking Save. Root cause is pre-existing; the branch's new copy asserts the missing affordance as if it were present.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":158,"severity":"medium","finding":"INV-6 says the fallback is 'never the default'; in the ordinary INITIAL state (no server chosen) blockingFailure deliberately excludes the 'no-server' failure, so no Alert renders and toolOptions is empty — the author's FIRST view of the Tool field is the free-text Input, i.e. the escape hatch IS the default. The store already authored the correct copy for this state ('Pick a server first to see the tools it offers.', ToolCatalog.store.ts:161) but the component never renders it; a disabled picker carrying that sentence would satisfy the invariant.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":295,"severity":"medium","finding":"INV-4 promises one typed field per declared property and INV-6 requires the fallback's reason to be stated correctly; in the state 'tool declares more than SCHEMA_BUDGET.maxFields (24) properties', the overflow properties spill into the section labelled 'Additional arguments' whose description reads 'Extra values this tool did not declare. Usually empty.' — which is false for those keys, since the tool DID declare them. The overflow note in ToolArgumentsForm.tsx:248-251 says 'the rest can be set below', contradicting the section's own description.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":168,"severity":"rejected","finding":"Candidate: humaniseFinding falls back to the raw backend message for an unknown code (line 173), leaking wire vocabulary. REJECTED as a finding in its own right — the design explicitly accepts this as the test-prevented case, and the backend drift guard (validate.rs:1985-2062) fails the suite when a registered/emitted code has no copy. The residual hole is logged separately under wired-and-behaving (the scanner's layer filter).","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":69,"severity":"rejected","finding":"Candidate: INV-5 (templating survives) not upheld in typed controls. REJECTED — verified upheld in every generated kind: isTemplateValue (toolSchemaForm.ts:263) is type-agnostic, a templated value renders as a text Input for switch/number/select/multiselect/json alike (lines 111-120), coerceToDeclared passes a template through untouched (toolSchemaForm.ts:300), a compact RefInsertMenu is attached to every field's label row, and the switch back to a typed control is one click (lines 83-94).","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":275,"severity":"rejected","finding":"Candidate: a saved workflow with schema-undeclared arguments loses them on edit. REJECTED — splitArguments preserves them in `extra`, the free rows are seeded from `extra` when a schema exists (ToolStepForm.tsx:178-180), and both commit paths merge known+rows so neither half clobbers the other (lines 203-216). State verified upheld.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":111,"severity":"medium","finding":"`markUnknown` and the `unknownNames` state it writes (line 47) have NO production caller and no test caller — a repo-wide grep finds only the definition. The 'unknown server name' case is instead handled purely by entryForServerName's derived failure, so this action and its state are dead.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":118,"severity":"medium","finding":"`invalidate` is documented as 'used by \"try again\"' but has no caller: the unreachable-server Alert (ToolStepForm.tsx:239-246) is a bare warning with no retry action, and the entry is cached only for the component's lifetime. The design's INV-6 escape hatch therefore has no recovery path — an author whose server was momentarily down must unmount and remount the step form to get the picker back.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":12,"severity":"medium","finding":"The store documents itself as a 'PRIVATE, per-builder catalog' whose id-keying means 'two names for one server share a fetch' and whose `inflight` guard exists because 'several fields read the same server in one render pass'. But defineLocalStore().use() allocates a store per COMPONENT INSTANCE (sdk/packages/framework/src/store-kit.ts:629-645) and StepConfigPanel remounts the step form on every selection via `key={step.id}` (StepConfigPanel.tsx:88) — so the catalog is per-tool-step-mount, not per-builder: listTools refires every time the author clicks between steps, and no cache or in-flight coalescing ever spans two steps.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/server/src/modules/workflow/validate.rs","line":1965,"severity":"medium","finding":"The design calls the humanisation drift guard 'the guard that makes this real'. The emit-site scanner only records a code when the FIRST string literal in the call is one of \"schema\"|\"semantic\"|\"security\"; a future ValidationError emitted on any other layer contributes nothing and fails no assertion, so it would ship with the raw wire message reaching the author. The test asserts the scanner found >0 codes overall, which does not detect a single silently-skipped emit site.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":265,"severity":"low","finding":"`loading={entry.loading}` on the Combobox is unreachable: usePicker (line 160) already requires `!entry.loading`, so the picker only renders when loading is false. The loading state is handled entirely by the fallback Input's disabled/placeholder branch (lines 274-277), leaving the Combobox's loading skeleton dead.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":161,"severity":"low","finding":"`HUMANISED_CODES` is exported as 'the drift-guard surface' but has no production consumer; the actual guard is the Rust test's TEXTUAL scan of the file for `\"CODE\"` literals (validate.rs:2040-2046), which never reads this export. Its only consumer is validationCopy.test.ts.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":76,"severity":"rejected","finding":"Candidate: the per-step invalid markers are computed but never reached. REJECTED — verified wired: StepList reads store.validation, derives counts through the same attributeFindings/indexFindingsByStep the panel uses, and renders them at lines 115-138; the populated + problems gallery cells exercise them.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/dev/gallery/coverage.ts","line":487,"severity":"rejected","finding":"Candidate: the coverage reasons cite builder-tool-picker.spec.ts, which is absent from the committed diff. REJECTED — the file exists on disk (src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts, currently untracked alongside builder-validation-attribution.spec.ts and the tools mock-server helper), so the claim is true of the working tree; committing them is a process matter outside this audit's product-diff scope.","status":"rejected"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":236,"severity":"low","finding":"DESIGN_SYSTEM.md's titled-section convention (SectionHeader for a titled block; the module's own StepList uses it) is applied to one half of the arguments UI only: the generated group is a bare `div className=\"flex flex-col gap-4\"` with no title, while its sibling fallback block is a titled LabeledControl. The two halves of the same concept render at different structural levels.","status":"confirmed"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":1,"severity":"rejected","finding":"Candidate: hardcoded colors / raw Tailwind hues / arbitrary values / inline style colors in the new UI. REJECTED — a sweep of ToolStepForm, ToolArgumentsForm, BuilderValidationPanel, StepList, RefInsertMenu and gallery.tsx finds zero raw hues, zero hex literals, zero arbitrary bracket values and zero inline style colors; every color is a semantic token (text-destructive / text-warning / text-success / text-muted-foreground / bg-muted / border-primary / bg-accent).","status":"rejected"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":99,"severity":"rejected","finding":"Candidate: physical direction properties / off-grid spacing. REJECTED — the same sweep finds no pl-/pr-/ml-/mr-/text-left/text-right/left-/right- utilities (StepList uses ps-0, the finding rows use text-start), and every spacing utility is on the 4px scale or the kit's sanctioned 2px half-step (mt-0.5).","status":"rejected"}
{"angle":"design-in-context","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":293,"severity":"medium","finding":"In the schema-driven state the author reads: untitled typed fields, then a section titled 'Additional arguments'. 'Additional' presupposes a preceding section called 'Arguments' that is never rendered (ToolArgumentsForm.tsx:236 has no heading) — the label is stranded from the title it refers to, and the same block silently renames itself to 'Arguments' in the fallback state, so the two states of one surface disagree about what the section is.","status":"confirmed"}
{"angle":"design-in-context","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":334,"severity":"low","finding":"One form offers two different templating affordances side by side: every generated field carries a compact RefInsertMenu on its label row (ToolArgumentsForm.tsx:95-105), while the key/value rows immediately below — which take the same {{ }} references — offer only a placeholder hint ('value or {{ reference }}') and no menu. In the fallback state the menu disappears entirely, so the discoverable way to insert a reference exists exactly where the schema happened to be readable.","status":"confirmed"}
{"angle":"design-in-context","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":39,"severity":"rejected","finding":"Candidate: the finding rows duplicate the step-list's identity chrome (index + label), so a count/label belongs in the container title instead. REJECTED — the design explicitly requires each finding to NAME its step because the panel sits away from the step list (DESIGN §2.2); repeating 'Step 3 · <label>' is the invariant, not duplication.","status":"rejected"}
{"angle":"precedent-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":152,"severity":"medium","finding":"The design names WorkflowElicitForm + workflowElicitSchema as the generated form's twin. Only the FieldSchema/TitledChoice TYPES were reused; the schema→control derivation was re-implemented: fieldKind (lines 152-195), choiceOptions (141) and enumOptions (145) duplicate the already-shipped isMultiSelectField / isTitledSelectField / selectOptions in WorkflowElicitForm.tsx (lines ~35-63), which handle the identical enum / anyOf / oneOf / items-enum cases. Two independent readings of the same JSON-Schema subset now exist in one module and can drift.","status":"confirmed"}
{"angle":"precedent-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":141,"severity":"low","finding":"The design names CapabilitySelect (capabilities.tsx) as the tool picker's twin. The Tool picker was instead inlined in ToolStepForm — options built at lines 141-148 and the control at 258-268 — rather than added as a sibling export next to CapabilitySelect. The two entity pickers stacked on the same form are also different primitives with different label conventions: Server is a Select labelled `display_name || name`, Tool is a Combobox labelled `name — description`, so adjacent fields for the same class of value read and behave differently.","status":"confirmed"}
{"angle":"precedent-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":54,"severity":"rejected","finding":"Candidate: the findings list hand-rolls a clickable row instead of mirroring StepList's rows. REJECTED — it is a real kit Button (semantic, keyboard-reachable), which is if anything stricter than the twin: StepList's rows are `li` elements with an onClick (StepList.tsx:71-88). Diverging toward the accessible primitive is not a precedent violation.","status":"rejected"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":463,"severity":"high","finding":"The cell's own comment claims 'Seeded through the tool CATALOG store so no network call is made — the same shape listTools returns', but the surface never touches ToolCatalogStoreDef: it imports describeToolSchema/splitArguments and renders ToolArgumentsForm in isolation with a hand-derived spec (lines 465-497). Consequence for a design review: the schema-driven state of the ACTUAL tool-step surface (picker + generated form + 'Additional arguments' together) has no gallery cell, and INV-3's Tool PICKER is never rendered in the gallery at all — the only ToolStepForm cell is the unreachable-server fallback (line 506), which deliberately renders the free-text branch. The gallery therefore shows the escape hatch and never the primary path the design exists to establish.","status":"confirmed"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/dev/gallery/stateMatrix.generated.ts","line":4086,"severity":"medium","finding":"New conditional render states registered by this branch have no gallery cell: ToolStepForm's `usePicker` fork (signal at ToolStepForm.tsx:258 — the picker itself), its `entry.loading` fallback (lines 274-277, not even signalled), and ToolArgumentsForm's `spec.overflowNames.length > 0` note (signal at line 247). Every one of them is a state the design's invariants turn on, and none can be seen in a design review.","status":"confirmed"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":365,"severity":"low","finding":"The two `errors[]` entries omit `severity`, which the backend ALWAYS serializes (ValidationError.severity is a plain field, validate.rs:429-430; ::at/::err set Severity::Error). The panel's severityOf defaults correctly so nothing renders wrong, but the fixture is still not byte-shaped like a real /validate-def response — the same class of drift the branch's own comment says it is fixing.","status":"confirmed"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":355,"severity":"rejected","finding":"Candidate: the validation fixtures carry invented codes / pre-humanised prose. REJECTED — verified faithful against the backend: WORKFLOW_PROMPT_MISSING + 'step has neither prompt: nor prompt_file:' at location `agent_1` matches validate.rs:683-687 (`&s.id`); WORKFLOW_TOOL_NO_TOOL + 'tool step has empty tool:' matches validate.rs:728-732; WORKFLOW_REF_FIELD_UNRESOLVED's message and warning severity match ref_check.rs:362-370 (::warn) with a `<step>.<field>` location. The branch removed the previously-invented `unresolved_reference`/layer `graph` fixture.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/dev/gallery/coverage.ts","line":487,"severity":"info","finding":"SCOPE NOTE — codes/layers/messages of the two error fixtures were cross-checked line-by-line against src-app/server/src/modules/workflow/validate.rs and ref_check.rs; results in the rows below.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":366,"severity":"low","finding":"VERIFIED REAL: fixture {code:'WORKFLOW_PROMPT_MISSING', layer:'semantic', message:'step has neither prompt: nor prompt_file:'} matches validate.rs:682-687 verbatim (ValidationError::at(\"semantic\",\"WORKFLOW_PROMPT_MISSING\",\"step has neither prompt: nor prompt_file:\",&s.id)); code is registered in VALIDATION_CODES validate.rs:368. No finding.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":372,"severity":"low","finding":"VERIFIED REAL: fixture {code:'WORKFLOW_TOOL_NO_TOOL', layer:'semantic', message:'tool step has empty tool:'} matches validate.rs:726-733 verbatim; code registered validate.rs:377. Location 'tool_1' is a bare step id, which is what ValidationError::at(&s.id) emits. No finding.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":382,"severity":"low","finding":"VERIFIED REAL: warning location 'summarize.prompt' matches the loc format ref_check.rs:139/149/174 `format!(\"{}.prompt\", s.id)`; code WORKFLOW_REF_FIELD_UNRESOLVED registered validate.rs:403 and emitted with layer 'semantic' + severity Warning at ref_check.rs:362-371. No finding on code/layer/location.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":384,"severity":"low","finding":"Warning message is NOT verbatim despite the doc-comment claim at gallery.tsx:355-356 ('copied verbatim from validate.rs + ref_check.rs'). Fixture: \"'agent_1.output' accesses field '.title' but the object shape is unknown; ...\". The backend's first interpolation is render_expr(expr) (ref_check.rs:366-367), and render_expr (ref_check.rs:434-450) renders the FULL expression including every access — so for {{ agent_1.output.title }} the server emits \"'agent_1.output.title' accesses field '.title' ...\". Impact is nil on screen (WORKFLOW_REF_FIELD_UNRESOLVED has HUMAN_COPY at validationCopy.ts:150, so finding.message is never rendered), but the 'verbatim' claim is false and this fixture is the template others will copy.","status":"confirmed"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":366,"severity":"medium","finding":"Seeded def CONTRADICTS the findings it is paired with, so the combination is one the backend cannot produce. errorValidation asserts WORKFLOW_PROMPT_MISSING ('step has neither prompt: nor prompt_file:') on 'agent_1', but agentStepFixture carries a full prompt (gallery.tsx:239-240); it asserts WORKFLOW_TOOL_NO_TOOL ('tool step has empty tool:') on 'tool_1', but toolStepFixture sets tool:'search_trials' (gallery.tsx:257). validate.rs:681 and :726 emit these ONLY when the field is empty/whitespace. Consequence: seeded-wf-builder-problems marks agent_1/tool_1 broken in the step list while their real config is complete — a reviewer who opens either step (or wires StepConfigPanel into the surface) sees a fully-filled form under a red marker. Only defensible as a 'validation is stale relative to the edit' transient, which nothing in the fixture says.","status":"confirmed"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":384,"severity":"medium","finding":"The warning references a field access that does not exist in the seeded def. Fixture message says 'agent_1.output' accesses field '.title'; the seeded summarize step's prompt is 'Summarise the key points from {{ agent_1.output }} in five bullets.' (gallery.tsx:315) — no .title access anywhere in builderFourStepDef. ref_check.rs:343-372 emits WORKFLOW_REF_FIELD_UNRESOLVED only from an actual Access::Field walk over a parsed reference, so this finding could never be produced for this def.","status":"confirmed"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":365,"severity":"low","finding":"REJECTED as a finding: the two error entries omit `severity`, while the Rust ValidationError serializes it unconditionally (validate.rs:429-430 uses serde `default` for DESERIALIZATION only). TS `severity?: Severity` (types.ts:7443) makes it legal, and severityOf() (validationCopy.ts:196-198) treats absent as 'error' — identical rendering. Not worth a change.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":268,"severity":"low","finding":"REJECTED: toolInputSchema (required string / integer+default / boolean / closed enum) is a plausible MCP `Tool.input_schema` and exercises describeToolSchema's text/integer/switch/select arms (toolSchemaForm.ts:157-194). No invented construct; nothing to compare it against on the backend since input_schema is an opaque serde_json::Value.","status":"rejected"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/coverage.ts","line":487,"severity":"high","finding":"FALSE coverage claim the gate cannot detect. The reason says ToolStepForm is rendered '...AND, via StepConfigPanel, by seeded-wf-builder-populated whose def now contains a tool step'. StepConfigPanel renders ONLY the selected step (StepConfigPanel.tsx:47 `steps.find(s => s.id === selectedStepId)`, dispatched at :88), and populatedBuilderSurface seeds selectedStepId:'agent_1' (gallery.tsx:440) — so that surface renders AgentStepForm, never ToolStepForm. The gallery comment at gallery.tsx:294-297 states the same wrong premise ('without one, ToolStepForm is never rendered by the surface whose coverage entry claims it'): adding the tool step to the def is necessary but NOT sufficient; it must also be selected. gen-gallery-coverage.mjs only checks that a KEY exists, never the reason prose, so this stands unchallenged.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/RUNTIME_FINDINGS.md","line":9,"severity":"high","finding":"The committed runtime-health record is a garbage run and now asserts a false product state. Base said '🔴 HIGH (gating) | 0' + 'None — every surface is runtime-clean of gating HIGH findings'; the branch commits '🔴 HIGH (gating) | 2393' and a 16-row 'Surfaces with HIGH findings (gate-failing)' table. Every listed cause is the stale-Vite harness artifact — 'net::ERR_NETWORK_CHANGED' (31 occurrences) and 'Failed to fetch dynamically imported module: http://localhost/modules/...' — including a `crash` finding on deep-chat-tool-group caused by ConversationPage failing to import. request-failed 2620/2635 were auto-muted as harness noise while their console-error twins were not, which is the signature of a dev-server restart mid-run, not a regression. Additionally the entire 'Baselined (documented pre-existing — non-gating)' section and its triage note were DELETED (2 → 0), losing the seeded-s3-group-widget-error triage. Committing this both slanders 16 unrelated surfaces and destroys the baseline the gate subtracts from, so the next real regression has nothing to be measured against. Re-run on a quiet worktree Vite and recommit.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/coverage.ts","line":488,"severity":"medium","finding":"Both new/changed coverage reasons (lines 487 and 488) cite 'builder-tool-picker.spec.ts' as supporting evidence, but that file is NOT committed on this branch — `git ls-files --error-unmatch src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts` fails and `git status` lists it as untracked (alongside builder-validation-attribution.spec.ts and helpers/builder-tools-mock-server.ts). As committed, the coverage record points at a test that does not exist in the repository. (The file does exist in the working tree and does exercise TEST-4/5/6/7 over the picker + generated form, so the substance is real — only the commit is missing.)","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":461,"severity":"medium","finding":"The toolStepSchemaSurface doc comment describes a surface that is not what renders. It claims 'the Tool field is a picker over the server's real tools' and 'Seeded through the tool CATALOG store so no network call is made — the same shape `listTools` returns'. The surface renders ONLY <ToolArgumentsForm> (gallery.tsx:489-495); ToolArgumentsForm.tsx has no Tool field at all (it maps spec.fields, :237-246), and neither ToolCatalogStoreDef nor listTools is imported or touched anywhere in the surface — the schema is the local literal toolInputSchema (gallery.tsx:268). A reader trusting the comment would believe INV-3 (the picker) has a gallery cell; it does not. The registered `note` at gallery.tsx:924 is, by contrast, accurate.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":933,"severity":"low","finding":"Title/slug say 'server unreachable' but the rendered failure is a DIFFERENT kind. The step's server is 'a_server_that_is_not_available' (gallery.tsx:515), which no entry in McpServer.servers matches, so entryForServerName returns {kind:'unknown-server'} (ToolCatalog.store.ts:148-153) and the Alert shows the unknown-server copy ('...which isn't one of the servers available to you...', :163) — not the 'unreachable' copy ('Couldn't reach \"X\" (detail)...', :167). The store's own comment (:86-89) calls `unreachable` the COMMON case, and it has no gallery cell.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/stateMatrix.generated.ts","line":4041,"severity":"low","finding":"REJECTED: spot-checked every changed signal line number against source and all match — BuilderValidationPanel !finding.stepId:46 / validating:90 / cost:122; StepList from==null:47, steps.length===0:59, problems>0:115, problems===0&&cautions>0:128; ToolArgumentsForm templated:83,111,203 + overflowNames:247; ToolStepForm blockingFailure:239, usePicker:258, useGenerated&&spec:282, rows.length===0:301. Generated files are faithful, not hand-edited.","status":"rejected"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/galleryCoverage.generated.ts","line":419,"severity":"low","finding":"REJECTED: the added GALLERY_SURFACES key 'modules/workflow/components/builder/ToolArgumentsForm' and the 419→420 count are exactly what gen-gallery-coverage.mjs's collectSurfaces() produces for the one new .tsx under src/modules; STATE_MATRIX.md summary (338→339 surfaces, 2067→2077 signals) is consistent with the ToolArgumentsForm addition + the 2 new StepList branches + the RefInsertMenu compact branch.","status":"rejected"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":258,"severity":"medium","finding":"The tool step's PRIMARY state has no gallery cell — only its degraded fallback does. `usePicker` (:258, the Combobox over the server's real tools) and `useGenerated && spec` (:282, the embedded generated arguments form) both require a resolvable server whose catalog carries the selected tool. seeded-wf-builder-tool-fallback deliberately uses an unresolvable server, so both are false there; seeded-wf-builder-populated never selects the tool step; seeded-wf-builder-tool-schema-form renders ToolArgumentsForm standalone, bypassing ToolStepForm entirely. Net: no screenshot in the gallery ever shows what a correctly-configured tool step looks like — every design review of this form sees the escape hatch. (Functionally covered by the untracked builder-tool-picker.spec.ts TEST-4/TEST-5; this is a visual-review gap, not a behavioural one.)","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":46,"severity":"medium","finding":"The panel's whole-workflow finding branch has no gallery cell. `if (!finding.stepId)` (:46) renders the finding as plain <li> text with the title 'Whole workflow' (validationCopy.ts:252) instead of a clickable Button — the component's doc comment (:21-22) calls this out as a designed state. All three fixture findings in errorValidation resolve to real steps (agent_1 / tool_1 / summarize), in BOTH surfaces that use it (seeded-wf-builder-validation-error and seeded-wf-builder-problems), so the unattributed rendering is never shown. Real backend codes routinely land here: WORKFLOW_NO_STEPS, WORKFLOW_CYCLE, WORKFLOW_TOO_MANY_STEPS and every `outputs[...]` location (validationCopy.ts:190) have no step.","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":301,"severity":"medium","finding":"ToolStepForm's declared required state `empty` (`rows.length === 0` → 'No arguments', :301-303) is still unreached by any gallery cell even though the branch added a ToolStepForm surface. seeded-wf-builder-tool-fallback seeds toolStepFixture.arguments with three keys (gallery.tsx:258-262) and, with spec null, argsToRows consumes all of them (:178-180) → 3 rows. stateCoverage.ts:459 still excuses this as 'via surface — rendered within its page; empty branch proven by Part 2 runtime coverage', which was written before any ToolStepForm cell existed and is now a stale excuse next to a real cell that could have delivered it (arguments: {}).","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":247,"severity":"low","finding":"The overflow notice `spec.overflowNames.length > 0` (:247-252, 'Showing N of M arguments this tool accepts') has no gallery cell: toolInputSchema declares 4 properties (gallery.tsx:271-291) against SCHEMA_BUDGET.maxFields = 24 (toolSchemaForm.ts:48), so overflowNames is always []. A 25+-property schema fixture would cover it in the same surface.","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":485,"severity":"low","finding":"The schema-driven form is only HALF populated, so representative-data layout is only half proven. splitArguments puts {query, limit} in `known` (legacy_flag goes to `extra`), leaving include_completed (Switch) and phase (Select) with no value — they render unchecked / placeholder. A cell with all four fields carrying values (including a long enum label and a long string) is what would expose real-data layout bugs in the label row, which already carries two action buttons in template mode (ToolArgumentsForm.tsx:81-107).","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":115,"severity":"low","finding":"REJECTED: both new StepList branches ARE covered by seeded-wf-builder-problems. `problems > 0` (:115) lights on agent_1 and tool_1 (two errors); `problems === 0 && cautions > 0` (:128) lights on summarize (the sole warning, location 'summarize.prompt' resolving via resolveFindingStep's head split). RefInsertMenu's new `compact` branch (:66) is covered by the schema-form cell (ToolArgumentsForm.tsx:104 passes compact) and non-compact by the existing builder cells; ToolArgumentsForm's three `templated` branches (:83,:111,:203) are covered by limit:'{{ inputs.since_year }}' in the integer field.","status":"rejected"}
{"angle":"patterns-conformance","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":911,"severity":"low","finding":"REJECTED: the three new seeded entries conform to SeededSurfaceEntry (sdk/packages/gallery/src/registry/types.ts:62-81) and to the house pattern seen in e.g. src/modules/citations/gallery.tsx — slug/title/note/path:'/'/initialPath:'/'/component, no `surface` field (that is overlay-only), unique `seeded-wf-*` slugs (assertUniqueSlugs, registry-core.ts:62). The hand-rolled `lazy(async () => ...)` store-seeding wrappers match the pre-existing agentFormSurface/populatedBuilderSurface/validationErrorSurface in the same file rather than the lazyNamed/lazyProps helpers, which is correct for store-seeded surfaces. `fullHeight: true` on the two small max-w-xl form cells matches the pre-existing seeded-wf-builder-agent-form. No new module gallery.tsx is needed (gen-gallery-seed-registry gates module ownership, already satisfied).","status":"rejected"}
{"angle":"maintainability","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":298,"severity":"low","finding":"Identifier `builderFourStepDef` now holds FIVE steps (toolStepFixture appended at :340). The doc comment (:294) and the surface title (:903, '(5 steps)') were both updated; the identifier was not, and it is referenced from three surfaces (:439, :549, :584). A stale count in a name is exactly the thing the next author will trust instead of counting.","status":"confirmed"}
{"angle":"maintainability","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":251,"severity":"low","finding":"toolStepFixture (:251) and toolInputSchema (:268) are untyped object literals, unlike the file's other fixtures which are pinned to generated wire types (cannedTestResult: TestRunResponse :57, cannedDryRunResult: DryRunResult :81, errorValidation: ValidateDefResponse :363). Nothing ties toolInputSchema to `Tool.input_schema` or toolStepFixture to the tool StepDef, so a wire-shape change will not fail tsc here — the fixture just silently stops resembling reality. (builderFourStepDef's `as unknown as BuilderDef` at :342 has the same effect for the step, and predates this branch.)","status":"confirmed"}
{"angle":"maintainability","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":363,"severity":"low","finding":"errorValidation is an INLINE fixture that no gate checks against the backend. check-gallery-fixtures.mjs validates only src/dev/gallery/fixtures/recorded/*.json against openapi.json (its MANIFEST, scripts/check-gallery-fixtures.mjs:28-33), and the Rust drift guard validation_codes_are_registered_and_humanised reads validationCopy.ts, not this file — so a backend message rewording or a code rename leaves this fixture silently wrong (which is precisely how the pre-branch invented codes survived). Cheapest durable fix: assert these three codes against HUMANISED_CODES (validationCopy.ts:161) in a unit test, so a renamed code fails.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":486,"severity":"low","finding":"`if (!spec) return null` is unreachable. describeToolSchema returns null only for a non-record schema, a missing/non-record `properties`, or zero properties (toolSchemaForm.ts:206-212); toolInputSchema is a record with four properties, so spec is always truthy. It is also placed AFTER `splitArguments(toolStepFixture.arguments, spec)` at :485, which already accepts null — so the guard protects nothing even in principle.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":494,"severity":"low","finding":"`onChange={noop}` makes every control on the schema-form surface inert: typing in `query`, toggling `include_completed`, picking a `phase`, the RefInsertMenu insert, and the 'Use a value instead' undo (ToolArgumentsForm.tsx:81-107) all no-op. DEC-5's reversible template mode — the property those two buttons exist to provide — therefore cannot be demonstrated or interaction-tested from this cell, and a runtime interaction recipe pointed at it would silently observe nothing. The sibling fallback cell (:519-526) wires the real store instead; wiring commitField the same way here costs one closure.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":900,"severity":"low","finding":"REJECTED: no dead registrations. All three new seeded entries have unique slugs and reachable lazy components; every new module-level fixture (toolStepFixture, toolInputSchema) has at least one consumer; `noop` and `cleanValidation` remain used; gallery.tsx exports only `gallery`, consumed by the runtime registry (@/dev/gallery/support), and gallery.tsx is excluded from the surface denominator by gen-gallery-coverage.mjs's walk() filter.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":107,"severity":"high","finding":"Sticky `templateMode` LATCHES ACROSS A TOOL CHANGE and cannot be reset by the value going away. `GeneratedField` is keyed by `field.name` (ToolArgumentsForm.tsx:341-350) and `ToolStepForm` is only remounted per STEP (`<StepForm key={step.id}>`, StepConfigPanel.tsx:88), so changing the Tool inside one step reuses the SAME GeneratedField instance for any property whose name is unchanged. The effect at :106-108 only ever sets the latch to true; nothing clears it when `value` becomes undefined. Given: tool A declares `days` (integer) and the author inserts `{{ inputs.horizon_days }}` (latch=true); they then pick tool B on the same server, which also declares `days` — `patch({tool, arguments:{}})` (ToolStepForm.tsx:329) clears the value but the latch survives, so the integer field renders as a free-text Input with the caption 'Using a reference — resolved when the workflow runs.' over an EMPTY value. Typing 5 then commits the STRING \"5\" for an integer argument. Violates INV-4 (typed field replaced by free text) and INV-6 (undocumented, silently-defaulted fallback with a FALSE stated reason), plus a real type corruption.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":132,"severity":"high","finding":"`entryForServerName` is fed `McpServer.servers`, which is `[]` until the LAZY global store's `loadMcpServers()` resolves (mcp/stores/mcpServer/index.ts:44 fires it from `init`, state.ts:6 starts empty) and nothing on this page awaits it or reads its `loading`/`isInitialized` flags. Given: open `/settings/workflows/:id/edit` on a saved tool step — for the whole first render window `servers` is empty, so `entryForServerName` (ToolCatalog.store.ts:243-249) returns `{kind:'unknown-server'}`, the blocking Alert renders 'This step points at a server called \"X\", which isn't one of the servers available to you' and the Tool field falls back to a free-text Input. Worse: if `loadMcpServers` ever fails (network / perm), `servers` stays `[]` FOREVER and that false accusation is permanent, with no retry affordance (`isRetryableFailure` excludes `unknown-server`, ToolCatalog.store.ts:278-280). Violates INV-3 (a picker degrades to free text for an enumerable entity) and INV-6 (the stated reason is wrong).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":383,"severity":"medium","finding":"`optionKeyForValue` returns `undefined` when a stored enum value matches no declared option, so the `Select` (ToolArgumentsForm.tsx:236-249) renders its 'Choose a value' PLACEHOLDER while `step.arguments[name]` still holds the old value. This is exactly the defect the round-1 fix closed for the Tool picker (the synthetic '… — not offered by this server any more' option, ToolStepForm.tsx:179-195) and it was NOT closed for enum arguments. Given: a saved workflow with `units: \"kelvin\"` and an upstream schema that tightened the enum to [\"c\",\"f\"] — the author sees an unset-looking field, believes nothing is configured, and the workflow keeps sending \"kelvin\". Silent, and the required-check can't catch it because the key is present.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":394,"severity":"medium","finding":"`optionKeysForValues` DROPS any stored array entry that matches no declared option (`.filter(v => v !== undefined)`), and the MultiSelect commits back through `optionValuesForKeys` (ToolArgumentsForm.tsx:250-263). Given: a saved arg `tags: [\"a\",\"legacy\"]` where the current schema declares only [\"a\",\"b\"] — the control shows only 'a', and the moment the author toggles ANY option the commit writes `[\"a\", …]` and \"legacy\" is silently erased from the definition. This is genuine data loss, and it contradicts the module's own stated principle that undeclared data must survive editing (`splitArguments`, toolSchemaForm.ts:445-451, preserves undeclared KEYS but not undeclared VALUES inside a declared key).","status":"confirmed"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":105,"severity":"medium","finding":"The new `d.error = null` on a SUCCESSFUL `runValidate` wipes a concurrent save/install failure. `save()` writes the install failure into the SAME `d.error` slot (:347) and the panel's new error Alert reads it (BuilderValidationPanel.tsx:98). Given: the author edits a step (`updateStep` → `scheduleValidate`, 400 ms debounce, :237) and immediately clicks Save; the POST/PUT fails at ~200 ms (name collision, or `validate_for_install` diverging on `prompt_file:` against the real bundle root) and sets `d.error`; the debounce fires at 400 ms, `/validate-def` succeeds at ~600 ms and clears `d.error` — the persistent failure Alert the round-1 fix added specifically so the save failure stays visible disappears on its own, leaving a green 'No blocking errors.' (BuilderValidationPanel.tsx:137) while the workflow is in fact unsaved.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":92,"severity":"medium","finding":"`runValidate` has NO generation/in-flight guard: two overlapping calls both write `d.validation` (and now both write `d.error = null`) in completion order, not issue order. Both callers are live — `load()` fires one immediately (:192) and every mutation schedules another (:227/237/256/267/275). Given: the mount-time validate is slow (1 s) and a post-edit validate is fast (100 ms), the STALE result lands last and becomes the panel's findings, the StepList's per-step invalid markers (StepList.tsx:39-42) and the Save gate (`hasErrors`, WorkflowBuilderPage.tsx:61) — all describing a definition the author no longer has. The `d.error=null` added this round widens the blast radius to the save-error slot.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":383,"severity":"medium","finding":"`humaniseInstallError` falls back to the RAW api-client message for anything that is not a parseable validator finding, and both consumers render that raw string as prominent copy: an Alert TITLE (BuilderValidationPanel.tsx:115-126) and a toast (WorkflowBuilderPage.tsx:93). `core.ts:715` builds `HTTP error! status: ${status} - ${textResponse}` from the whole response body, so a 502 from a proxy or a 500 with an HTML body puts an entire error page into the Alert title. This is precisely the leak the same round-1 round guarded against for the tool catalog (`describeFetchError`, ToolCatalog.store.ts:68-93, which explicitly cites this exact string) — the mitigation was not applied at the save/validate boundary, so INV-1's 'no machine string reaches the author' still fails on the most common failure of all.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":134,"severity":"medium","finding":"The `inflight` Set and `generation` Map live in the LOCAL store's `actions` closure, i.e. one per ToolStepForm MOUNT, while the cache they protect (`sessionCatalogs`) is module-level and per builder SESSION. `publish` only writes the cache on COMPLETION (:140-145), never on start. Given: step A's form starts `listTools` for server S; the author clicks step B before it resolves — `StepConfigPanel` remounts the form (`key={step.id}`), the fresh catalog store has an empty `inflight`, empty `byServerId` and (still) an empty session cache, so it issues a SECOND `GET /api/mcp/servers/{id}/tools` for the same server. That re-handshakes a stdio MCP server, which is the exact cost the WeakMap was introduced to eliminate (header note :16-24). Marking the entry in the session cache at request start (or hoisting `inflight` into the scope) is what would actually close it.","status":"confirmed"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":228,"severity":"medium","finding":"The `pendingNumberClear` blur-deferral DELETES a previously-committed argument when the author abandons the field on a NaN-intermediate buffer. The kit emits `onChange(undefined)` for both 'cleared' and 'partial' (input-number.tsx:47-50), which sets the flag (:221-223); on blur the kit's own handler takes the `Number.isNaN` branch and RESTORES the display without re-emitting (input-number.tsx:56-67), then calls the consumer `onBlur`, which unconditionally `commit(undefined)` (:228-232) and `commitField` deletes the key (ToolStepForm.tsx:261). Given: `days: 5`, the author selects-all and types `-` (intending -3) then tabs away — the display flashes 5 and the `days` argument is REMOVED from the definition rather than left at 5. Same for `1e` / `1e-` / `.`. The flag needs to distinguish 'buffer empty' from 'buffer unparseable'.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":239,"severity":"medium","finding":"A free 'Additional arguments' row silently VANISHES mid-edit the moment its key matches a declared property. `commitRows` patches `{...known, ...rowObj}` (:255) and sets `lastPushed` to the ROW object (:254); `splitArguments` then classifies that key into `known`, so `extra` no longer contains it, `incoming !== lastPushed.current` and the resync effect (:239-247) rebuilds `rows` WITHOUT it. Given: the author types 'location' (a declared field) into an Additional-arguments key box — at the keystroke that completes the name the whole row disappears from under the cursor and the half-typed value is transplanted into the generated field above. Reachable on every schema whose property names an author might plausibly retype, and it also fires when the author is trying to override a spilled `overflowNames` property that IS declared.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":136,"severity":"medium","finding":"The round-1 addition `.filter(s => s.enabled)` makes a step that points at a DISABLED (but perfectly accessible, registered) MCP server resolve to `unknown-server`, whose copy asserts it 'isn't one of the servers available to you' (ToolCatalog.store.ts:267). Given: an admin temporarily disables server 'weather' and the author opens a tool step that uses it — the builder tells them the server does not exist, drops them into hand-entry, offers no 'Try again' (`isRetryableFailure` excludes it) and offers no hint that the fix is to re-enable the server. INV-6 requires the escape hatch to carry a TRUE stated reason; this one is false. The `unknown-server` variant needs a `disabled-server` sibling.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":227,"severity":"medium","finding":"`optionKeyOf` collides for distinct declared enum values that stringify identically: `1` → \"1\" and `\"1\"` → \"1\"; `null` → \"null\" and `\"null\"` → \"null\". `enumOptions`/`choiceOptions` (:236-250) therefore emit two options with the SAME `value`, and `optionValueForKey` (:389-391) resolves a key by `find(o => o.value === key)` — always the FIRST. Given a tool declaring `{\"enum\": [1, \"1\"]}`, choosing the second (string) option commits the NUMBER 1; the picker also has a duplicate React key. Bounded (mixed-type enums are uncommon) but the round-trip is silently lossy and `matchOption`'s lenient second pass (:371) actively depends on this key equality, so it cannot simply be tightened without also fixing the option keys.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":134,"severity":"medium","finding":"INV-3 is violated for any user with more than one page of MCP servers. `McpServer.servers` is a PAGINATED slice (`pageSize: 10`, mcp/stores/mcpServer/state.ts:12) and `entryForServerName` resolves the step's server name only against that slice, so an accessible server on page 2+ resolves to `unknown-server` and the Tool field degrades to free text with a false reason. The code documents this (ToolCatalog.store.ts:227-233) as inherited from the Server picker and defers it, but a documented invariant violation in a reachable state is still a violation — and the Server picker below it will also be missing the same servers, so the author has no way to re-pick correctly either.","status":"confirmed"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2217,"severity":"medium","finding":"The drift guard's emit-site scanner matches only the literal identifier `ValidationError` (:2217) behind a `src.contains(\"ValidationError\")` pre-filter (:2372), so a construction reached through an ALIAS is invisible to it — and invisible SILENTLY, unlike every other hole (which routes into `scan.problems` and fails loudly). Given a future `use crate::modules::workflow::validate::ValidationError as VE;` followed by `VE::err(\"semantic\", \"WORKFLOW_NEW_THING\", …)`: the file still contains the string 'ValidationError' (from the `use`), so it is scanned, but no code is extracted; the new code is neither in `emitted` (so `unregistered` passes) nor added to `VALIDATION_CODES` (so `stale` passes) nor required to have `HUMAN_COPY` — and it reaches the author as the raw wire message, which is the precise failure INV-1 exists to prevent. Verified the guard is NOT vacuous today: I independently recovered 41 emit sites → 41 codes, `VALIDATION_CODES` has exactly those 41, and `HUMAN_COPY` has exactly those 41 keys. Rejecting a `use … as …` alias in the scanner (a `problems` entry) would close it.","status":"confirmed"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":1952,"severity":"low","finding":"`scan_roots` adds the desktop crate only `if desktop.is_dir()` while asserting `is_dir()` for the server root inside `scan_emitted_codes` (:2350-2355). Given the desktop crate is moved or renamed (`src-app/desktop/tauri/src` → anything else), that root is silently dropped and any `ValidationError` it emits stops being guarded — with no failure. Every other drift-guard hole in this module is deliberately loud; this one is not. Low because the desktop crate emits no findings today (verified: zero `ValidationError::` sites under `src-app/desktop/tauri/src`).","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":106,"severity":"low","finding":"INV-2's 'can take the user to that step' is implemented as `store.selectStep(stepId)` only — there is no scroll/focus move (`grep scrollIntoView` over modules/workflow: none). WorkflowBuilderPage stacks StepList/StepConfigPanel ABOVE BuilderValidationPanel and only goes side-by-side at `md` (:195-204). Given: a 390 px viewport with several steps, the author reads a finding at the bottom of the page and clicks it — the config panel, which is what changed, is entirely off-screen above, so the button appears to do nothing. The invariant is technically satisfied and visibly not.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":295,"severity":"low","finding":"The `serverId &&` half of the 'Try again' guard is dead. `isRetryableFailure` is true only for `unreachable` / `no-tools` (ToolCatalog.store.ts:278-280), and both are produced exclusively inside `load`, which is only ever called with a non-null `serverId` (:145). So the condition can never be the thing that hides the button, and a reader is led to believe there is a retryable-but-unresolvable state that does not exist.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":302,"severity":"low","finding":"`WORKFLOW_LEVEL` is exported with a doc comment claiming it is 'part of the returned map's contract', but the only reader in the tree is `indexFindingsByStep` two lines below (:309). No component, store or non-test module imports it — `StepList` looks up step ids only, as the comment itself concedes. CODING_GUIDELINES §15: a `pub`/`export` with no external consumer. Make it module-private, or give it a consumer.","status":"confirmed"}
{"angle":"test-quality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":679,"severity":"low","finding":"`toolStepNewSurface` (:679) and `toolStepFallbackSurface` (:704) render `<ToolStepForm store={store} step={newToolStepFixture|toolStepFixture} />` — the MODULE-LEVEL fixture object, not the step held in the store they just seeded. Every `patch()` those surfaces make lands in the store while the `step` prop stays frozen, so any interaction in the gallery (pick a server, type a tool name) produces no visible change and the runtime-health / design-critic passes over these two surfaces cannot exercise them. The sibling `toolStepSchemaSurface` (:658) does it correctly by going through `StepConfigPanel`; these two should too.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":389,"severity":"low","finding":"`humaniseInstallError` fabricates a wire type with `{...} as ValidationError` (:384-389), deliberately omitting `severity`. The `as` cast defeats excess/missing-property checking, so if the generated `ValidationError` ever gains a required field the OpenAPI regen + `tsc` will NOT flag this construction — the one place in the module that builds a wire object by hand rather than receiving it. A typed literal (with `severity` supplied explicitly) would restore the contract check that the rest of the file relies on.","status":"confirmed"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":57,"severity":"low","finding":"REJECTED — WeakMap leak / dropped live session. `defineLocalStore.use()` stores its proxy in a `useRef` and returns the SAME object for the mount's lifetime (sdk/packages/framework/src/store-kit.ts:631-654), so the scope key is stable for the whole editing session (cache hits work) and becomes unreachable at unmount (entry collectible). Two concurrent builders get two distinct proxies and therefore two distinct Maps — no collision. Nothing module-level holds a strong reference to a scope.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":183,"severity":"low","finding":"REJECTED — a stale response winning after `invalidate`. Traced both orders: `invalidate` bumps `generation` and clears `inflight`/cache/state (:211-218); the older promise's post-await checks (:183, :191) see the bumped generation and return without publishing, and its `finally` (:202) is generation-guarded so it does not delete the RETRY's `inflight` entry. Double-clicking 'Try again' is also safe (gen 2 wins, gens 0/1 discard). No path leaves an entry permanently `loading` either: the only writer of `loading:true` is `load` itself, which always either publishes or is superseded by an `invalidate` that deletes the entry outright.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":327,"severity":"low","finding":"REJECTED — the synthetic 'not offered by this server any more' option being re-committed as if real. The Combobox `onChange` returns early when the chosen value equals `step.tool` (:327-330), and the synthetic option's value IS `step.tool`, so pressing it is a no-op; no patch, no save-path difference. Its `storedToolMissing` gate also recomputes to false the instant a real tool is chosen, so it cannot linger in the option list.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":184,"severity":"low","finding":"REJECTED — the pending InputNumber clear being LOST on unmount (step switch / tool change / Save). Every unmount trigger here is a pointer or keyboard interaction with another control, and focus (hence `blur`) moves on mousedown, before the click handler that causes the unmount runs; the deferred `commit(undefined)` therefore lands first, and `updateStep`'s immer `set` is synchronous, so a subsequent `save()` reads the committed def. The real defect in this machinery is the OPPOSITE direction (an unparseable buffer deleting a good value) — logged separately.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":116,"severity":"low","finding":"REJECTED — `no-tools` mis-describing an UNREACHABLE server as one that 'answered but publishes no tools'. Checked the backend: `list_server_tools` (src-app/server/src/modules/mcp/handlers/runtime.rs:69-95) propagates `session.list_tools()` errors, so an unreachable/failed handshake is a non-2xx and lands in the `catch`, not in `failureForToolList`. A 200 with an empty array really does mean the server served zero tools, and the 403 → `no-permission` mapping matches the handler's `USER_NO_ACCESS` forbidden exactly.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":357,"severity":"low","finding":"REJECTED — `parseInstallError` being dead machinery. Verified the producer: `validate_for_install` formats `\"[{layer}/{code}] {location}: {message}\"` (validate.rs:523-532) and it IS on the builder's save path — `dev::update_user_workflow_definition` calls it before touching the bundle (handlers/dev.rs:562) and it is bound to `PUT /workflows/{id}/definition` (routes.rs:73-76). `AppError::bad_request` surfaces that message through `core.ts`'s `errorResponse.error`, so the regex matches real traffic.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":365,"severity":"low","finding":"REJECTED — `VALIDATION_CODES` changing from `pub const` to `#[cfg(test)] const` breaking a consumer. Grepped the whole server crate, the desktop crate and the integration tests: no reference outside `validate.rs` itself (the only user is `use super::VALIDATION_CODES` in the guard module). Not part of any serialized/OpenAPI surface either.","status":"rejected"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2582,"severity":"low","finding":"REJECTED — the drift guard passing VACUOUSLY today. Reproduced its three inputs independently: 41 `ValidationError::{err,at,warn}` emit sites outside `#[cfg(test)]` yielding 41 distinct codes; `VALIDATION_CODES` contains exactly those 41; `HUMAN_COPY` has exactly those 41 keys (zero in either difference). Its anti-vacuity asserts are all present and reachable: root `is_dir` (:2350), non-empty file list (:2358), non-empty `emitted` (:2598), missing/renamed `const HUMAN_COPY` → `Err` → panic (:2469, :2625), zero parsed keys (:2631), and `problems` failing loudly on any unreadable construction (:2588). The residual hole is the alias case, logged separately.","status":"rejected"}
{"angle":"vacuity","file":"src-app/ui/tests/e2e/workflows/builder-responsive.spec.ts","line":139,"severity":"high","finding":"The headline leg of TEST-20 ('no horizontal overflow at 390/768/1280px') can never fail. The builder renders inside SettingsPage.tsx:331 `<div className=\"flex-1 overflow-hidden\">` and inside SettingsPageContainer's DivScrollY (OverlayScrollbars scroll host), so no builder child can ever propagate width to the document: `document.scrollingElement.scrollWidth - clientWidth` is structurally 0 whatever the builder renders. A step list, header row, name Input or tool card 900px wide at a 390px viewport would be CLIPPED (invisible, unreachable) and this assertion still returns 0 <= 1. Only the finding boxes (line 160-167) and the four generated fields (line 194-196) are actually geometry-checked; every other element on the page is unverified. ITEM-10 ('the owner reported it doesn't fit') is therefore not proven by the assertion that names it.","status":"confirmed"}
{"angle":"acceptance-fidelity","file":"src-app/server/src/modules/workflow/validate.rs","line":2582,"severity":"medium","finding":"TEST-1 is the [acceptance] proof of INV-1 ('no raw schema/YAML key language ever reaches the person building a workflow'), but it only asserts that every registered code is a KEY of HUMAN_COPY (line 2626-2640). Key PRESENCE is not human language: an entry such as `\"WORKFLOW_X\": ({message}) => message`, or copy that names a wire field without a trailing colon ('This step's for_each must be a list', 'output_format must be json or text'), violates INV-1 and leaves this test GREEN. The complement lives in a NON-acceptance unit test (validationCopy.test.ts:72-95) whose screen is only a trailing-colon regex plus a four-token denylist (prompt_file|$defs|serde|snake_case), which those examples pass. So for a real, cheap-to-hit violation shape of INV-1, the invariant's designated acceptance test does not go RED.","status":"confirmed"}
{"angle":"test-reality","file":"src-app/ui/src/modules/workflow/components/builder/noFreeTextEntityRef.test.ts","line":84,"severity":"medium","finding":"Phantom leg. TESTS.md claims TEST-3 asserts 'no builder step form binds a FREE-TEXT control to a field whose values the system can enumerate'. `offendingBindings` actually clears a field as soon as ANY picker in the SAME FILE binds it (line 87-89), and ToolStepForm.tsx binds `step.tool` to BOTH a Combobox (:318-322) and an Input (:336-339). The tool field is therefore permanently exempt: a regression that makes free text the DEFAULT path (e.g. inverting `usePicker` at ToolStepForm.tsx:169-170, or the catalog silently failing) leaves both controls in the source and this scanner stays green. What is actually asserted is the much narrower 'no field is bound ONLY to a text box', not the claimed class rule.","status":"confirmed"}
{"angle":"test-reality","file":"src-app/ui/tests/e2e/workflows/builder-validation-attribution.spec.ts","line":239,"severity":"medium","finding":"TEST-21 silently drops fixture findings it cannot pattern-match. The regex requires `code:` then `layer:` then `location:` in that exact order, so the gallery's third seeded error — WORKFLOW_SANDBOX_FLAVOR_REQUIRED at gallery.tsx:509-515, which deliberately carries NO `location` — is never extracted and therefore never compared against the live backend, neither for its code/layer nor for its verbatim message. The `>= 3` guard at line 243-246 is satisfied by the other three findings, so that entry could drift back to a fabricated code and pre-humanised prose (exactly the defect TEST-21 exists to catch) with the test green. Any future locationless fixture finding is equally invisible.","status":"confirmed"}
{"angle":"negative-control","file":"src-app/server/src/modules/workflow/validate.rs","line":2138,"severity":"medium","finding":"The ~400-line hand-written Rust lexer added for the drift guard has no self-test: nothing proves `scan_rust_source` would actually FIND a newly added `ValidationError::at(\"semantic\", \"NEW_CODE\", …)` site, or that its comment/string/char-literal/`#[cfg(test)]` skipping is correct. The two directional set comparisons (line 2617-2631) make an under-scan of an EXISTING code fail loudly ('listed but no longer emitted'), which is the safe direction — but a NEW emit site the lexer fails to read is invisible to BOTH sets simultaneously (not emitted, not registered), so the sets still agree, no copy is demanded, and a code with no human copy ships green. Concrete desync vectors with zero coverage: `skip_char_literal_or_lifetime` (:2123) mis-advancing on `'\\u{1f600}'`-shaped input, `#[cfg(all(test, …))]` not matching the literal `#[cfg(test)]` check (:2180), and `raw_string_prefix` on `br#\"`. A fixture-string negative control (feed the scanner a synthetic source containing a known code and assert it is found; feed one with the code in a comment and assert it is NOT) would close this.","status":"confirmed"}
{"angle":"coverage-gap","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":184,"severity":"medium","finding":"The commit-on-blur machinery has zero coverage. (a) `pendingNumberClear` (:184-232) exists because InputNumber emits `undefined` for both 'cleared' and 'partial number being typed' — the named bug is 'a negative number could never be typed over an existing one'. No test types into an InputNumber character-by-character or blurs it; TEST-6 uses `fill('25')`, a single-shot set that never produces the `undefined` transition. (b) The Textarea `onBlur` -> `coerceToDeclared` + `sameValue` path (:275-278): `coerceToDeclared` is unit-tested in isolation, but the `json`/`textarea` control is never RENDERED by any test — the e2e mock server (builder-tools-mock-server.ts:153-208) declares no object/array/long-string property, so the json/textarea/multiselect/switch-with-default control arms and their blur wiring are never exercised end to end. A regression in either blur handler is undetectable.","status":"confirmed"}
{"angle":"coverage-gap","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":103,"severity":"medium","finding":"Sticky template mode (`templateMode` latch, :103-109) and its escape (`clear-ref` button, :138-152) are untested. TEST-6 asserts the clear-ref button is VISIBLE (builder-tool-picker.spec.ts:309-311, 328-331) but never CLICKS it, so DEC-5's promise 'the switch to template mode is reversible / never a trap' is unproven — `setTemplateMode(false); commit('')` has no test. Nor is the bug the latch was written for covered: backspacing the closing `}}` of an inserted reference must NOT flip the control back to the typed one. Both are the exact regressions the code comments describe.","status":"confirmed"}
{"angle":"coverage-gap","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":179,"severity":"medium","finding":"The synthetic 'not offered by this server any more' option (:179-198) has zero coverage anywhere — no unit test, no e2e, no gallery-driven assertion (grep for the literal string returns only the component). It is the guard against a saved-but-renamed upstream tool rendering as an EMPTY Combobox while the definition still carries the value — a silent data-loss shape the required-field check explicitly cannot catch. Related and equally uncovered: the 'Try again' button + `retryCatalog` (:205-210, :295-307) — the store's `invalidate` is unit-tested (ToolCatalog.store.test.ts:308-330) but the button that calls it is never clicked.","status":"confirmed"}
{"angle":"coverage-gap","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":117,"severity":"medium","finding":"The panel's new error Alert (`wf-builder-validation-error`, :115-124) and the save-toast humanisation (WorkflowBuilderPage.tsx:84-95) are never rendered by any test. `parseInstallError` / `humaniseInstallError` are thoroughly unit-tested (validationCopy.test.ts:285-366), but nothing proves the WIRING: that `store.error` reaches the Alert, that the green 'No blocking errors.' claim is really suppressed while the last check failed (the stale-green defect the comment names), or that the save toast actually shows the humanised sentence instead of the raw `[semantic/CODE] step: message`. INV-1 is claimed to be 'a property of the SURFACE, not of one code path' — the second surface is only proven at the pure-function layer.","status":"confirmed"}
{"angle":"coverage-gap","file":"src-app/ui/tests/e2e/workflows/builder-validation-attribution.spec.ts","line":122,"severity":"low","finding":"The WARNING half of the attribution surface is only ever asserted ABSENT. `wf-builder-step-warned-*` appears once, as `toHaveCount(0)` (:122), and `wf-builder-warnings` appears in no spec at all — so the warning badge (StepList.tsx:126-138) and the warnings list branch (BuilderValidationPanel.tsx:151-158) are never rendered positively by any test. `indexFindingsByStep` counts warnings correctly at the unit layer (validationCopy.test.ts:245-277), but a rendering regression in either warning surface would go undetected, and the `toHaveCount(0)` at :122 has no positive control that the badge can appear at all.","status":"confirmed"}
{"angle":"coverage-gap","file":"src-app/ui/tests/e2e/workflows/helpers/builder-tools-mock-server.ts","line":174,"severity":"low","finding":"The enum `raw` round-trip — the defect toolSchemaForm.test.ts:308-341 was written for (an integer enum committing `\"2\"` instead of `2`, and a stored `2` rendering unselected) — is never exercised through the live Select control. The mock server's only enum is a STRING enum (`mode: ['semantic','keyword','hybrid']`), for which `optionValueForKey`/`optionKeyForValue` are identity functions, so TEST-5's enum leg (builder-tool-picker.spec.ts:243-251) would pass identically against the old `String(v)` implementation. Declaring one non-string enum property in the mock would make the ToolArgumentsForm.tsx:236-249 wiring falsifiable.","status":"confirmed"}
{"angle":"vacuity","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.test.ts","line":73,"severity":"low","finding":"'no mapped copy leaks wire vocabulary to the author' loops over `HUMANISED_CODES` (= `Object.keys(HUMAN_COPY)`) with no assertion that the collection is non-empty. If HUMAN_COPY were emptied, renamed, or its export changed shape during a refactor, the loop body never runs and `assert.deepEqual(offenders, [])` passes vacuously. The Rust guard asserts `!humanised.is_empty()` (validate.rs:2632) but that is a different suite; this file should carry its own floor (the sibling scanner at noFreeTextEntityRef.test.ts:94-98 does exactly that).","status":"confirmed"}
{"angle":"test-reality","file":".lifecycle/workflow-builder-ux/TESTS.md","line":12,"severity":"low","finding":"TESTS.md's TEST-1 line describes the mechanism inaccurately in two ways: it says the codes scanned are those in 'validate.rs + ref_check.rs' (the guard scans the whole server crate plus the desktop crate — validate.rs:1950-1957) and that validationCopy.ts is 'read via include_str!' (it is read at RUNTIME with fs::read_to_string, validate.rs:1935-1939, deliberately so it cannot be a stale compiled-in snapshot). Both drifts are in the safe direction (the code is stronger than the claim), but the claim ledger is the artifact the merge gate reads, so it should describe what runs.","status":"confirmed"}
{"angle":"flake-risk","file":"src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts","line":69,"severity":"low","finding":"Every tool-step spec depends on the freshly-seeded MCP server appearing in `McpServer.servers`, which ToolCatalog.store.ts:236-238 documents as a PAGINATED slice ('a server beyond the loaded page resolves to unknown-server'). E2E specs share one backend/database per run and each of these four specs plus builder-step-kinds and builder-responsive seeds another server, so once the accumulated server count exceeds the loaded page the Server picker option `wf-builder-tool-server-opt-<name>` stops existing and the tool catalog degrades to the fallback. The failure direction is RED, not a false pass, but it is order-dependent across specs sharing the run.","status":"confirmed"}
{"angle":"flake-risk","file":"src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts","line":360,"severity":"low","finding":"TEST-7 waits 30000ms for the unreachable-server Alert while seeding that same server with `timeout_seconds: 30` (:351). The assertion budget equals the server's own configured upstream timeout, so any path where the backend actually spends its full timeout (rather than failing fast on DNS for `.invalid`) races the expect. Give the alert a budget strictly larger than the seeded `timeout_seconds`.","status":"confirmed"}
{"angle":"flake-risk","file":"src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts","line":117,"severity":"low","finding":"`generatedField()` locates the label/description block by XPath `ancestor::div[contains(@class,\"flex-col\")][1]`, coupling TEST-5's acceptance assertions (requiredness `*`, description, 'Defaults to 10.') to LabeledControl's Tailwind class string. A purely cosmetic change to that wrapper's classes retargets the ancestor and breaks the acceptance proof of INV-4. The failure direction is red, so this is polish, not vacuity — but a `data-slot`/testid anchor on the LabeledControl root would decouple it.","status":"confirmed"}
{"angle":"test-reality","file":"src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts","line":137,"severity":"low","finding":"REJECTED — considered coverage inflation (one spec file carrying four TEST-IDs). It is not: TEST-4/5/6/7 are four separate `test()` blocks with independent fixtures, independent mock lifecycles and disjoint assertions (option set / generated field shape / reference round-trip / unreachable-server fallback). No TEST-ID leans on another's assertion.","status":"rejected"}
{"angle":"vacuity","file":"src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts","line":171,"severity":"low","finding":"REJECTED — `expect(options).toHaveCount(MOCK_TOOL_NAMES.length)` is not an assertion over a possibly-empty collection: `openToolList` (:87-92) first waits for `[data-testid^=\"wf-builder-tool-name-opt-\"]`.first() to be VISIBLE, so a listbox that renders nothing fails before the count is taken, and the sorted testid comparison at :173-179 then pins identity, not just cardinality.","status":"rejected"}
{"angle":"negative-control","file":"src-app/ui/tests/e2e/workflows/builder-validation-attribution.spec.ts","line":203,"severity":"low","finding":"REJECTED — TEST-18's absence assertion `not.toMatch(/prompt_file|prompt:/i)` over `body.innerText()` carries an explicit positive control on the very same captured string (`expect(bodyText).toContain(HUMAN.agentPromptMissing)`, :210), so a page that failed to render, or an empty innerText, cannot make it pass.","status":"rejected"}
{"angle":"vacuity","file":"src-app/ui/tests/e2e/workflows/builder-responsive.spec.ts","line":148,"severity":"low","finding":"REJECTED — the per-finding clipping loop is NOT a loop over a possibly-empty collection: `expect(findingCount).toBeGreaterThan(0)` is asserted inside the viewport loop (:148-151), i.e. at every one of the three widths, and a pre-loop `expect(byTestId(page,'wf-builder-finding').first()).toBeVisible()` (:122-124) guards the setup. Same for the 390px column check, which asserts `boxes.length === SEARCH_PROPERTY_NAMES.length` (:198-200).","status":"rejected"}
{"angle":"negative-control","file":"src-app/server/tests/mcp/list_tools_for_builder_test.rs","line":278,"severity":"low","finding":"REJECTED — the two 403 tests were checked for the classic 'a 403 that proves nothing' shape. They do not have it: test 3 carries an explicit 200 positive control on a server the outsider DOES own (:278-284) before asserting 403 on the other user's server, and both tests discriminate the refusing layer by `error_code` (INSUFFICIENT_PERMISSIONS vs USER_NO_ACCESS) rather than by status alone. Test 2 also registers a REAL server id so the 403 cannot be a 'no such row' artifact.","status":"rejected"}
{"angle":"vacuity","file":"src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts","line":369,"severity":"low","finding":"REJECTED — TEST-7's 'an arbitrary name COMMITS' leg is asserted only as the ABSENCE of 'A tool name is required' with no prior positive assertion that the message was ever present, which looked vacuous. It is not, because the same commit is independently proven downstream: `waitBuilderValid` (:379) requires the backend to stop emitting WORKFLOW_TOOL_NO_TOOL, and the post-reload `toHaveValue('an_unlisted_tool')` (:388-391) proves the value actually persisted.","status":"rejected"}
{"angle":"acceptance-fidelity","file":"src-app/ui/tests/e2e/workflows/builder-validation-attribution.spec.ts","line":46,"severity":"low","finding":"REJECTED — TEST-2 was examined for the tautology shape (asserting the implementation rather than the design promise). It is not one: three steps, two broken for DIFFERENT codes, the THIRD and valid step selected, per-finding `data-step-id` counts (1 vs 2 vs 0), a repo-wide `toHaveCount(2)` on invalid markers, badge counts matching the per-step finding counts, and two goto clicks landing on two DIFFERENT step kinds. Every one of those goes red if attribution were hardcoded, derived from selection, or derived from 'the workflow is invalid'.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":134,"severity":"high","finding":"A user who holds workflows::manage/install but NOT mcp_servers::read is left with a PERMANENTLY disabled Tool field carrying a false 'Looking up this server…' placeholder and no stated reason. Chain: mcpServer/actions/loadMcpServers.ts:17 returns early without setting isInitialized OR error when hasPermissionNow(McpServersRead) is false, so serversInitialized=false and serversError=null forever; ToolStepForm.tsx:134 `serversSettled` is therefore false forever; entryForServerName (ToolCatalog.store.ts:428) returns {kind:'resolving-server', needsLookup:false} because listReady is false, so resolveServer is never called; ToolStepForm.tsx:191 `busy` stays true, :188-189 classifies resolving-server as transient so NO Alert renders, and :407 `disabled={busy}` disables the free-text escape hatch. Given a saved tool step and that permission set, the author can neither pick nor type a tool name, forever, with no explanation — the documented no-permission failure (ToolCatalog.store.ts:235-246 / :302-309) is unreachable because it needs a serverId the list can never supply. INV-6 violated (silent, non-degrading).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":2236,"severity":"high","finding":"The drift guard can still pass VACUOUSLY. The lexer keys on the literal identifier `ValidationError` (validate.rs:2236) and reports the two ways to rename the type (`use … as VE`, `type VE = …`), but not the idiomatic third: `Self`. `impl ValidationError` exists at validate.rs:450, so a future convenience constructor written inside it — e.g. `pub(crate) fn dead_tools(loc:&str)->Self{ Self::at(\"semantic\",\"WORKFLOW_NEW_CODE\",\"…\",loc) }` or a bare `Self { layer:…, code:\"WORKFLOW_NEW_CODE\", … }` — emits a real code that the scanner never sees. Because the code is also absent from VALIDATION_CODES and from HUMAN_COPY, all three assertions in validation_codes_are_registered_and_humanised agree: `unregistered` is empty (it was never scanned), `stale` is empty (it was never registered), `unhumanised` is empty (it was never registered). Guard green, raw wire message ships to the author. Same class as the alias hole round 2 closed; `<ValidationError>::err(…)` is a second (contrived) instance — the ident is followed by `>` not `::`, so scan_validation_error_use returns at :2385 treating it as a type mention.","status":"confirmed"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":471,"severity":"medium","finding":"Author-facing copy is humanised TWICE and the second pass truncates it. WorkflowBuilder.store.ts:410 already stores the humanised sentence (humaniseRequestError → humaniseValidatorFinding returns the full `Step N · label: <copy>` string, untidied). BuilderValidationPanel.tsx:98 then calls humaniseInstallError(store.error, steps) on that already-humanised string, and WorkflowBuilderPage.tsx:93 does the same on the re-thrown e.message for the toast. Second pass: no `[layer/CODE]` head → no status → falls to tidyMachineText (validationCopy.ts:447) which clips at MAX_ERROR_CHARS=160. Measured: WORKFLOW_BAD_STEP_ID copy is 190 chars, WORKFLOW_PROMPT_FILE_ESCAPE 165, WORKFLOW_PROMPT_FILE_UNSAFE 148 (+~26 for the 'Step N · label: ' prefix = 174). So a save that fails on any of these renders, in BOTH the Alert title and the toast, e.g. 'Step 2 · Fetch notes: The file this step reads its wording from ends up outside the workflow once the location is followed — point it at a file that is really …' — the actionable half is cut off. The docstring at :505 ('Idempotent: an already-humanised sentence passes through unchanged') is false for any copy over 160 chars.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":462,"severity":"medium","finding":"fieldForValue still ERASES a stored `null` element of a multiselect argument — the exact data-loss class it was added to close. staleOption returns early for `value === null` (toolSchemaForm.ts:462); that guard is correct for the single-select case ('no value ⇒ no synthetic') but wrong for the per-ELEMENT call at :505, where null is a real array member. Given `arguments: {tags: [\"a\", null]}` against a schema declaring `items.enum: [\"a\",\"b\"]` (hand-authored YAML, or an upstream schema that dropped null from the enum): no synthetic option is built for null, optionKeysForValues (:440-446) drops it via the `.filter(v => v !== undefined)`, the MultiSelect's value list becomes [\"a\"], and the next toggle rebuilds `arguments` from that list (ToolArgumentsForm.tsx:299) — null is gone from the saved workflow. Contradicts DEC-6's rule that a value the current schema does not declare must survive editing. (A DECLARED null enum member is fine: enumOptions gives it raw null and matchOption finds it.)","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":334,"severity":"medium","finding":"The SERVER picker silently drops a configured value it cannot offer — the defect this branch fixed for the Tool picker (storedToolMissing synthetic option, ToolStepForm.tsx:201-217) and left standing one field above. CapabilitySelect (capabilities.tsx:14-21) builds its options from McpServer.servers — a PAGE of 10 (mcpServer/state.ts:11 pageSize:10, shared server-side search/status filters with /settings/mcp-servers) — and additionally `.filter(s => s.enabled)`. The kit Select renders the placeholder when `value` is not in `options` (select.tsx:148-176: customDisplay = selectedOpt?.label → undefined → SelectValue falls back to the placeholder). So for a saved step whose server is DISABLED, or is on page 2+, or is filtered out by a search term the user left on the MCP settings page, the required Server field reads 'Select a server' with no error (configErrors passes: step.server is non-empty) — while the new catalog Alert immediately below names that very server ('\"X\" is one of your servers but it is currently disabled'). Self-contradicting surface, and with >10 accessible servers the author cannot select the ones past page 1 at all (INV-3's job-to-be-done step 1). capabilities.tsx is untouched by this diff, but the disabled-server state that makes the contradiction visible is new here.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":122,"severity":"medium","finding":"A stale SAVE error is never retired, so a provably-false finding sentence sits above a green 'No blocking errors.' createValidateRunner's success branch clears `error` only when `d.errorSource === 'validate'` (WorkflowBuilder.store.ts:122-126), and save() only clears it when the author retries the save (:370-374). Sequence: Save fails with `[semantic/WORKFLOW_PROMPT_MISSING] agent_1: …` → store.error = 'Step 1 · Research the topic: This step needs a task description…', errorSource='save'. The author then fills the task in → updateStep → scheduleValidate → the check succeeds with zero errors. BuilderValidationPanel now renders the red Alert with that (now untrue) sentence at :141-152 AND the green 'No blocking errors.' at :164-169, because checkFailed is false. The panel added a staleness qualifier for the failed-CHECK case but there is none for a superseded save failure, and Save is enabled — so the only actionable text on screen is wrong.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":192,"severity":"low","finding":"A one-frame ENABLED free-text fallback flashes on the server-resolution boundary. On the frame where serverId first resolves, `entry` is still `EMPTY` (ToolCatalog.store.ts:418 `byServerId[match.id] ?? EMPTY`) — no failure, loading:false — so ToolStepForm.tsx:192 computes usePicker=false and :191 busy=false, rendering the hand-entry Input enabled with the generic 'The exact name of the tool to call on that server.' description and 'e.g. search' placeholder. The `load` effect (:161-166) is a useEffect, so it runs after paint; only then does loading:true flip the field to disabled and finally to the picker. The author briefly sees the INV-6 escape hatch presented as the normal state, with no reason given, for a state that is not a failure at all. Same one-frame path on every remount of an already-cached step form (StepConfigPanel keys the form by step id, so each step switch remounts with an empty local byServerId).","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":91,"severity":"low","finding":"CHECKED, NOT A DEFECT: the stored in-flight promise cannot produce an unhandled rejection or a permanently-pending entry. fetchTools (:198-213) and lookupServer (:274-282) each wrap the whole body in try/catch and RESOLVE with an entry/verdict, so `toolsPending`/`serversPending` never hold a rejected promise, however many late joiners await them. The generation guard (:260, :321) discards a superseded response, and `invalidate`/`invalidateServer` (:335-361) delete the pending entry and the local state together, so the invalidating instance cannot be left stuck on loading:true. The only way to strand a store instance at loading:true is a SECOND live ToolStepForm mount invalidating while the first awaits — unreachable, since StepConfigPanel.tsx:88 mounts exactly one step form (`key={step.id}`).","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"sdk/packages/framework/src/store-kit.ts","line":630,"severity":"low","finding":"CHECKED, NOT A DEFECT: the WeakMap scope key is stable. defineLocalStore.use memoises {api, proxy} in a useRef (store-kit.ts:630-645) and returns the SAME proxy object for every render of a mount, so `sessions` (ToolCatalog.store.ts:98) is keyed once per builder editing session rather than churning per render; sessionState never reads a prop off the proxy, so no hook is invoked by using it as a key; and the entry is collectable when the page unmounts. One fetch per server per session, as documented.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":258,"severity":"low","finding":"CHECKED, NOT A DEFECT: the number commit-on-blur is correct, and no pending edit is lost on unmount. Verified against the kit: InputNumber forwards its ref through Input (input.tsx:23,47) to the DOM input, and handleBlur (input-number.tsx:54-67) restores the previous text via setBuf BEFORE calling onBlur — React has not flushed it, so numberInput.current.value still holds what the author typed. Abandoned partial buffer ('-', '1e') → the raw read is non-empty → the argument is left alone; genuinely emptied box → buf==='' takes neither kit branch, the raw read is '' → commit(undefined) deletes the key. Unmount-before-blur is not reachable through the builder's own affordances: the step rows, Save, the tool Combobox and the ref menu all move focus (or focus body), which fires blur — and its commit lands on the store by step id before the click handler re-keys the panel.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":318,"severity":"low","finding":"CHECKED, NOT A DEFECT: the JSON/textarea commit-on-blur cannot lose data. onChange already commits the author's exact text on every keystroke (:317), so an unmount before blur leaves the text intact in `arguments`; only the JSON.parse coercion is deferred, and coerceToDeclared keeps an unparseable buffer as the raw string rather than nulling it (toolSchemaForm.ts:591-601). sameValue (:69-72) keeps a bare blur from marking the workflow dirty.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":108,"severity":"low","finding":"CHECKED, NOT A DEFECT: template mode does not desync. The latch is seeded from the value on mount (:108-110), re-latched whenever the value becomes a reference, and cleared only on a BLANK value (:127) so a half-typed '{{ inputs.limit' still holds the mode. It cannot outlive a step switch or a tool switch: StepConfigPanel.tsx:88 remounts the form per step, and the field key is `${toolKey}::${field.name}` (ToolArgumentsForm.tsx:391) so a same-named property on a different tool gets a fresh instance. An external `arguments` replacement mid-edit cannot happen while the author has edits (WorkflowBuilder.store.ts:196 refuses to overwrite a dirty def); when it does happen the value change reaches the effect and clears/relatches correctly. Text/textarea/json never switch modes (canHoldTemplate, :97-98), which is the round-1 collapse fixed.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":461,"severity":"low","finding":"CHECKED, NOT A DEFECT (except the null case logged separately): a synthetic option cannot shadow a declared one nor be committed as declared. staleOption disambiguates its key against the list it will JOIN (`seen`, including earlier synthetics) with a suffix loop (:466-470), fieldForValue puts extras FIRST (:509) and matchOption's identity pass (`o.raw === value`, :408) resolves the stored value to the synthetic before any lenient key match, and optionValueForKey returns the option's `raw` (:434-437) so what is committed is the original stored value, unchanged. assignKeys (:257-270) keeps `enum:[1,\"1\"]` injective. Round-trips through save/reload because the value is never rewritten.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":107,"severity":"low","finding":"CHECKED, NOT A DEFECT: createValidateRunner's sequence guard has no ordering hole. `seq !== issued` is re-checked on BOTH the success and the failure path (:119, :129) so a superseded run can neither write a stale `validation` nor a stale error, and `validating` cannot stick — only the newest run clears it, and it is set true before every issue. errorSource routing is right in both directions: a successful check clears only a 'validate' error (:122-126) and a failed check refuses to overwrite a 'save' error (:135). The Save gate reads `validation` which can be up to one debounce window stale, but the backend re-validates on install and the failure is humanised, so no invalid workflow can be installed through the gap.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":486,"severity":"low","finding":"CHECKED, NOT A DEFECT (except the truncation logged separately): humaniseRequestError cannot blank a message, cannot leak an `HTTP error!` blob and does not mangle a validator finding. Every path ends in a non-empty string (`tidyMachineText(raw) || fallback`, :496); an HTTP-error string whose status maps to nothing returns the fallback rather than the response body (:495); parseInstallError's location regex (:364) requires an unspaced token followed by ': ', so a location-less message such as 'step has neither prompt: nor prompt_file:' cannot be mis-split (the anchored \\S+ cannot reach the colon past the first space) and degrades to workflow-level, which is what it already is; and a code with no HUMAN_COPY entry returns null so the raw text is never re-attributed to a step it does not belong to.","status":"rejected"}
{"angle":"reactive-read-in-loop","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":39,"severity":"low","finding":"CHECKED, NOT A DEFECT: no Rules-of-Hooks violation in the changed surfaces. StepList hoists attributeFindings/indexFindingsByStep above the `.map()` (:39-42) and reads no store field inside it; BuilderValidationPanel reads store.validation/validating/def/error/errorSource unconditionally at the top (:88-104) and attributes once before both lists; ToolStepForm reads McpServer.servers/isInitialized/error as three separate unconditional statements (:130-134) precisely so the hook count cannot vary with isInitialized; GeneratedField's hooks (useState/useEffect/useRef) are all unconditional and `control()` contains none; WorkflowBuilderPage destructures the store proxy at :49-59, before the `!canAccess` early return at :101.","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":179,"severity":"low","finding":"CHECKED, NOT A DEFECT: the by-name lookup's wire contract holds. ListAccessibleServersQuery (server/src/modules/mcp/handlers/user.rs:44-53) takes page/per_page/search/status with no upper clamp or rejection on per_page, so SERVER_LOOKUP_PAGE_SIZE=100 is accepted and repository::list_accessible applies it directly; `search` is a server-side ILIKE over name/display_name/description, and lookupServer still requires exact `s.name === serverName` (:275) so a substring hit cannot be mistaken for the server. listTools' `res.tools ?? []` matches the optional wire field, and ValidationError.severity's optionality is handled defensively in severityOf (validationCopy.ts:258).","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":235,"severity":"low","finding":"CHECKED, NOT LOGGED AS DEAD CODE: the `no-permission` branches in load (:235-246) and resolveServer (:302-309) are effectively unreachable — both require a resolved serverId / a listReady=true gate that only a user who already holds mcp_servers::read can produce, so they only fire on a mid-session permission revocation. They are legitimate defensive code, not dead weight; their unreachability is a symptom of the HIGH finding above (the permission-denied user never gets far enough to be told why), not a separate defect. Everything else new is wired: HUMANISED_CODES is consumed by humaniseValidatorFinding and by the gallery boot gate, WORKFLOW_LEVEL by indexFindingsByStep, ValidationSlice/ValidateRunnerDeps by the extracted runner, and all four new gallery surfaces are registered with the listTools cassette key.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":505,"severity":"low","finding":"CHECKED, GENUINELY WIRED: the tool-step surfaces run the production path, not a stub. toolStepSchemaSurface renders the real StepConfigPanel → ToolStepForm → ToolArgumentsForm against a resolvable server name, and the cassette adds the real `McpServerRuntime.listTools` key so ToolCatalogStoreDef.load actually fetches; the fallback surface points at a name the accessible list does not contain so entryForServerName reaches the stated-reason alert; and the fixture-code gate (throwing when a fixture code is absent from HUMANISED_CODES) makes the invented-code regression that hid the original defect impossible to reintroduce. The broken-def fixture's findings are consistent with the def they describe.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":387,"severity":"low","finding":"CHECKED, NOT A DEFECT: the Combobox re-select guard (`if (v === (step.tool ?? '')) return`, :387-390) does prevent Base UI's re-press of the highlighted row from wiping every filled-in argument, and the deliberate wipes (server change :336-341, genuine tool change) are correct — keeping server A's arguments on server B's tool would be worse. The free-row/generated-field merge order is also sound: commitField deletes the colliding row key before patching (:320) so the typed control wins while the row is still being edited, and absorbDeclaredRow drops the row on blur (:303-308) so the two editors never both own a key.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":298,"severity":"low","finding":"CONSIDERED AND DISMISSED as not worth logging: two theoretical option-mapping edges. (a) A schema declaring the empty string as an enum member cannot be committed — the kit Select treats '' as no-selection (select.tsx:92) and commitField deletes on '' — but an empty-string enum member is not a shape real MCP servers emit. (b) A stored multiselect array holding both 1 and \"1\" against `enum:[1,2]` maps both to the same control key, so the MultiSelect momentarily shows a duplicate chip until the next toggle normalises it to the declared type; that is the documented lenient re-selection working as intended, and requires an already-malformed stored value. Neither is a reachable user-facing failure.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":107,"severity":"low","finding":"ROUND-3 REGRESSION (bounded). `findingStillPresent` matches a save error to a fresh check by (code, location), but the location it compares against was PARSED out of the wire string by `parseInstallError` (validationCopy.ts:369), which mis-reads a location-LESS finding whose message begins `<token>: `. `validate_for_install` (validate.rs:511-533) prefixes a location only when the finding has one, so `WORKFLOW_TOO_MANY_STEPS` arrives as `[semantic/WORKFLOW_TOO_MANY_STEPS] workflow.yaml: 51 steps exceeds Phase-1 cap of 50` and `errorFinding.location` becomes the string `workflow.yaml`, while `/validate-def` returns the same finding with `location: undefined`. `(undefined ?? null) === 'workflow.yaml'` is false, so `spentSaveError` is TRUE on the very first check after the failed save and the error is retired with NOTHING fixed. Given: 50 valid steps, author clicks Add step and hits Save inside the 400 ms debounce (the exact race the design cites) -> install rejects with TOO_MANY_STEPS -> the Alert appears -> 400 ms later the debounced check lands and silently clears it. Same class via code-swap: a save rejected with WORKFLOW_PROMPT_FILE_ESCAPE (real bundle root, symlink) is reported by `/validate-def` as WORKFLOW_PROMPT_FILE_MISSING (nonexistent tmp root, validate.rs:1065-1084), so the codes differ and the true save error is retired. Bounded because the equivalent finding is still listed in the panel and `hasErrors` keeps Save disabled.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":518,"severity":"low","finding":"REJECTED — the round-3 `staleOption` null relaxation is correct in BOTH constructed cases. Multiselect with a stored `null` ELEMENT: `fieldForValue` line 523 calls `consider(null)`; `staleOption` (line 469) no longer bails on null, `matchOption` misses, `staleLabel(null)` (line 484-488) recovers the text `null` via `optionKeyOf` after `valueToText` returns '', and the synthetic carries `raw: null`, so `optionKeysForValues` -> `matchOption(o.raw === null)` binds it and the next toggle re-emits `null` instead of erasing it. Single select with `value === null`: line 524's `value !== null` guard skips `consider`, and `optionKeyForValue` (line 422) returns undefined, so the placeholder shows and NO phantom option is invented. `undefined` still short-circuits at line 469 for both kinds.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/handlers/dev.rs","line":946,"severity":"medium","finding":"`POST /validate-def` — the builder's ONLY validation feed — passes a guaranteed-NONEXISTENT temp dir as `bundle_root`, so `check_prompt_files` (validate.rs:1063-1084) fails `canonicalize()` for every `prompt_file:` step and always emits `WORKFLOW_PROMPT_FILE_MISSING`. The SAVE path validates against the real bundle root (handlers/dev.rs:562) and finds nothing. Given: an imported workflow whose agent/llm step uses `prompt_file:` is opened at /settings/workflows/:id/edit -> the branch now renders that false finding as an authoritative human sentence (validationCopy.ts:98-99 'This step points at a prompt file that isn't in the workflow'), StepList.tsx:115-127 paints the step red, and `hasErrors` (WorkflowBuilderPage.tsx:60,70) DISABLES Save permanently. No builder step form ever writes `prompt_file`, so the author cannot clear it from the UI, and typing a prompt instead produces WORKFLOW_PROMPT_BOTH. Origin is pre-existing backend behaviour (the divergence is even acknowledged at validationCopy.ts:334), but the branch is what turns it into a confident, step-attributed, Save-blocking claim.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/capabilities.tsx","line":17,"severity":"low","finding":"The SERVER picker never got the synthetic-option treatment the Tool picker has (ToolStepForm.tsx:220-239). `useCapabilityOptions` filters `s.enabled` and reads only the loaded page (`McpServer.servers`, page size 10 — mcpServer/state.ts:10), so a step pointing at a DISABLED server, or one on page 2+, or any server at all for a user without `mcp_servers::read`, has no matching option. Verified NOT data loss and NOT invisible: base-ui `SelectValue` falls through `resolveSelectedLabel` to `stringifyAsLabel` when no item matches (node_modules/@base-ui/react/internals/resolveValueLabel.js:65-105), so the trigger paints the raw `name` and `entryForServerName` states the reason in the Alert. Residual gap: the value shows as the raw `name` rather than its `display_name`, is not marked selected in the open dropdown, and cannot be re-chosen if the author changes away from it — while every other configured-but-unlisted value in this feature (stale tool, stale enum option, stale multiselect element) is kept in its list WITH a stated reason.","status":"confirmed"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/capabilities.tsx","line":1,"severity":"low","finding":"REJECTED (as a NEW defect) — the round-3 change described for `capabilities.tsx` / the SERVER picker NEVER LANDED: `git diff origin/feat/agent-core...HEAD -- '*capabilities*'` is empty and c70f695c7 does not touch the file. There is therefore no synthetic server value to commit, to collide with a real option, or to survive a save/reload, and `CapabilitySelect`'s prop shape is unchanged, so its other caller (`CapabilityMultiSelect` is separate; `CapabilitySelect` has exactly one caller, ToolStepForm.tsx:353) is unaffected. Checked the sibling risk too: `CapabilityMultiSelect` is SAFE — kit MultiSelect renders an unlisted value as `labelByValue.get(v) ?? v` and its `toggle` is a functional update over `prev`, so an off-page/disabled capability in a step's `servers` is both visible and preserved across toggles.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/server/src/modules/workflow/validate.rs","line":431,"severity":"low","finding":"Residual hole in the (now `Self::`-aware) drift guard, invisible to ALL THREE assertions at once. `ValidationError`'s fields are `pub` (line 430-441) and the scanner only reads CONSTRUCTION sites, so `let mut e = ValidationError::err(\"semantic\", \"WORKFLOW_CYCLE\", m); e.code = \"WORKFLOW_NEW\";` registers the already-known WORKFLOW_CYCLE and never surfaces WORKFLOW_NEW: the emitted set is unchanged, so `unregistered`/`stale` both pass, `HUMAN_COPY` is never asked for an entry, and `humaniseFinding` (validationCopy.ts:196-198) falls back to the raw wire message in front of the author — the exact silent-agreement failure mode the `Self::` fix was written to close. Every OTHER dialect is loud (`use … as`, `type X =`, struct literal, `Self { code: \"…\" }`, non-literal code arg). Closing it needs either private fields + accessors or a scan for `\\.code\\s*=` on a `ValidationError` binding.","status":"confirmed"}
{"angle":"regression-risk","file":"src-app/server/src/modules/workflow/validate.rs","line":2436,"severity":"low","finding":"REJECTED — `scan_self_use` does not produce the false positives asked about. `-> Self {` and `impl … for Self {` are excluded by the `prev_word` allowlist at line 2455-2458 (`->` is tokenised explicitly at line 2233-2239), so a return-type `Self` is never read as a struct literal; `Self` in an UNRELATED impl is unreachable because `impl_ve_from` is only armed when `scan_validation_error_use` returns true for `impl`/`for` + `ValidationError` (line 2380) and is popped on the exactly-matching depth (line 2220-2222); the type's OWN `err`/`at`/`warn` bodies are `Self { … }` literals threading a `code` PARAMETER, and `scan_self_struct_literal` only reports a LITERAL `code: \"…\"` (line 2522-2539). Note (not a defect): any `Self::<other>` inside `impl … ValidationError` — e.g. `Self::default()` in an `impl Default` — WOULD be reported, as would an associated `type Item = ValidationError;`. Both fail LOUD with an actionable message, which is the safe direction for a guard whose failure mode is vacuity.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":143,"severity":"low","finding":"REJECTED — the round-3 `usePermission(Permissions.McpServersRead)` gate is hook-safe and its stated reason is TRUE. `usePermission` IS a hook (sdk/packages/framework/src/permissions/usePermission.ts:14); it is called at ToolStepForm's top level, after the other unconditional store-proxy reads and BEFORE every `useMemo`/`useEffect`, with no early return anywhere in the component and no call inside a `.map()`/conditional — the hook count is invariant. The reason is verified true: `loadMcpServers` (mcp/stores/mcpServer/actions/loadMcpServers.ts) returns at `hasPermissionNow(McpServersRead)` without setting `isInitialized` or `error`, so `serversSettled` would never flip. The denied path is usable: `entryForServerName` returns `no-permission` with `serverId: null`, so `awaitingCatalog` and `busy` are both false and the Tool field renders as an ENABLED `Input` (line 428 `disabled={busy}`) under an Alert carrying the true sentence — INV-6 satisfied. Also re-verified the busy/settled fix: `awaitingCatalog` (line 207) cannot latch, because every `load` path writes `byServerId[serverId]` (cached / denied / loading:true) before it awaits.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":305,"severity":"low","finding":"The LOAD boundary is the one failure surface left outside the single humanisation boundary the branch established. `load`'s catch stores the raw `error.message`, and WorkflowBuilderPage.tsx:126-133 renders it as `ErrorState details={loadError}`. The api-client formats a transport failure as `HTTP error! status: 502 - <the entire response body>` — the exact unbounded machine artefact `describeRequestError` was written to stop (validationCopy.ts:413-425) and that the SAVE and VALIDATE catches now both route through. Given: the builder is opened behind a proxy returning a 502 HTML page, the whole page body lands in the error card. `describeRequestError(error, [], 'This workflow could not be loaded — try again.')` is a drop-in.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":505,"severity":"low","finding":"REJECTED — humanisation now happens at EXACTLY one boundary, with no raw and no double-clipped path. Traced all four routes: (a) save failure -> store.save catch -> `describeRequestError` -> `d.error` AND `throw new Error(msg)`; the page toast prints `e.message` verbatim (WorkflowBuilderPage.tsx:91-92) and the panel Alert prints `store.error` verbatim (BuilderValidationPanel.tsx:101,147), so the 160-char `tidyMachineText` clip is applied once, to machine text only; (b) validate failure -> `createValidateRunner` catch -> `describeRequestError` -> `d.error`, panel only, no toast; (c) name-collision and 'Give the workflow a name' bypass `describeRequestError` with copy written in place, no second pass; (d) no renderer calls `humaniseFinding`/`humaniseRequestError` on `store.error`. `attributeFindings` humanises the panel/steplist findings once, above both maps.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":534,"severity":"low","finding":"`humaniseRequestError` has NO production caller after round 3 — the store's two call sites moved to `describeRequestError` (WorkflowBuilder.store.ts:173,462) and it is now referenced only from validationCopy.test.ts. A `pub`-equivalent export with no consumer (CODING_GUIDELINES §15); either drop it and have the tests call `describeRequestError(...).text`, or keep it and note it is test-only.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":410,"severity":"low","finding":"`loading={busy}` on the Tool Combobox can never be true: the Combobox is only rendered when `usePicker` (line 211) holds, and `usePicker` requires `!busy`. Since round 3 widened `busy` to include `awaitingCatalog`, the busy state is exclusively served by the `Input` branch (line 415-429), so the Combobox's loading affordance is unreachable. Harmless, but it reads as live behaviour that no state can produce.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":248,"severity":"low","finding":"REJECTED — no ordering or lifetime defect found across the round-3 surface. Validation: `createValidateRunner`'s `seq !== issued` guard covers BOTH the success and the catch branch, so a stale response cannot overwrite a newer one or resurrect a retired error; the debounce timer is per store INSTANCE (WorkflowBuilder.store.ts:216) so concurrent builders cannot cross-fire. Catalog: `toolsGeneration`/`serversGeneration` are bumped by `invalidate`/`invalidateServer` before the retry re-issues, so an in-flight response is discarded rather than clobbering the retry; `toolsPending` holds the PROMISE, so the step-switch remount (StepConfigPanel keys each form by step id, giving a fresh `defineLocalStore` instance with an empty `byServerId`) joins the running `tools/list` instead of re-handshaking; the double `toolsPending.delete` from two awaiters of the same promise is a no-op. The WeakMap session is keyed on the builder store, so it dies with the editing session.","status":"rejected"}
{"angle":"reactive-read-in-loop","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":39,"severity":"low","finding":"REJECTED — no store-proxy read (which IS a hook) survives inside a loop. StepList hoists `store.def.steps`, `store.selectedStepId`, `store.validation` and computes `indexFindingsByStep` once above `steps.map` (line 34-42). BuilderValidationPanel hoists `store.validation`/`validating`/`def.steps`/`error`/`errorSource` and calls `attributeFindings` once above both `errors.map` and `warnings.map` (line 87-111). ToolArgumentsForm's `spec.fields.map` emits `<GeneratedField>` ELEMENTS keyed `${toolKey}::${field.name}`, so its `useState`/`useEffect`/`useRef` are per-component and the store read inside `RefInsertMenu` (`store.def`, line 33) is at that component's own top level. ToolStepForm's own reads (incl. the new `usePermission`) are all above the first `useMemo`.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":150,"severity":"low","finding":"REJECTED — walked the full state matrix the brief lists and each reaches its intended, stated surface. no server -> `no-server` (transient) -> generic blurb on the Tool field, no Alert. list still loading -> `resolving-server` + `needsLookup=false` until `listReady`, field disabled, 'Looking up this server…'. list load FAILED -> `serversError` flips `serversSettled`, the by-name lookup runs, `server-lookup-failed` is retryable. no mcp_servers::read -> `no-permission` immediately, hand entry ENABLED. disabled server -> `disabled-server` with its own Alert title ('This server is turned off'). server not on page 1 -> `resolveServer` (search + per_page 100 + exact-name equality) resolves it authoritatively. unreachable -> `unreachable` + 'Try again'. zero tools -> `no-tools`, tone info, retryable. rich schema -> generated fields. no schema -> key/value rows naming the tool as the reason (line 450-453). stored tool no longer offered -> synthetic option + explanatory description (line 220-239); `spec` is null so the saved arguments stay editable as free rows, no data loss. stored enum value no longer declared / stored null multiselect element -> `fieldForValue` synthetic. unmapped code -> documented raw-message fallback. failed validation call -> Alert + 'results below are from an earlier check' + the green line suppressed. install error on Save -> humanised + step-named in both toast and Alert.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":347,"severity":"medium","finding":"ROUND-4 REGRESSION at the LOAD boundary. `load`'s catch now routes through `humaniseRequestError`, which reuses `statusSentence` (validationCopy.ts:464-481) — copy written for a MUTATION boundary. Executed against the real module: a 400 renders 'The server rejected this change.', a 403 renders 'You do not have permission to make this change.' Loading a workflow is not a change, so both sentences describe an act the author never performed. Worse, `GET /workflows/{id}/definition` (handlers/mod.rs:126) calls `parse_workflow_yaml`, whose failure is `AppError::bad_request(\"WORKFLOW_INVALID_YAML\", \"workflow.yaml deserialization failed: <which step/field>\")` -> HTTP 400. Given a workflow whose on-disk workflow.yaml no longer deserializes, the author now reads 'The server rejected this change.' and the parse diagnostic — the only thing that says WHICH step is malformed — is discarded (500 likewise collapses to 'The server reported an internal error'). Before round 4 `d.loadError = error.message` put that diagnostic into ErrorState's `details` (WorkflowBuilderPage.tsx:130). Fix: a load-specific status->sentence map (read voice), and keep the server's `error` text as secondary detail for 4xx codes that carry a diagnostic.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":166,"severity":"medium","finding":"INV-1 leak in the 'imported workflow using prompt_file:' state — the state round 4 deliberately unblocked. `StepConfig::Llm/LlmMap/Agent` declare `prompt: Option<String>` with `skip_serializing_if = \"Option::is_none\"` (validate.rs:152-155), so a `prompt_file:` step arrives with NO `prompt` key. `configErrors` (stepForms.ts:216) runs `nonEmpty('A prompt')` = `z.string().trim().min(1, msg)`; the custom message is attached to the `min` check only, so an ABSENT value produces zod's raw type diagnostic. Verified with the project's zod: `{kind:'llm', prompt_file:'prompts/x.md'}` yields `prompt: Invalid input: expected string, received undefined`, which `LabeledControl` renders verbatim into the field error slot (builderFields.tsx:70), above an empty required Prompt textarea. So the author is shown machine text AND a false accusation: the backend accepts the step (check_steps_shape treats prompt_file as satisfying), and the builder has no prompt_file control at all — nothing on screen explains where the wording actually lives. The zod line predates the branch, but round 4 is what made this state load clean at the panel level, so the raw string is now the ONLY thing the author sees. Fix: `z.string(...).optional()`-aware kind schemas that treat a `prompt_file` step as satisfied (and surface the file), or at minimum give `nonEmpty` an `error` on the type check too.","status":"confirmed"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":1090,"severity":"high","finding":"REJECTED — the injection guard survives the no-bundle skip. The textual path-SHAPE reject (`p.contains(\"..\") || p.starts_with('/')` -> WORKFLOW_PROMPT_FILE_UNSAFE) is emitted at validate.rs:1078-1086, i.e. BEFORE the `if !bundle_present { continue }` at 1090, and it `continue`s on its own. Proven live: `modules::workflow::validate::tests::draft_validation_without_a_bundle_still_rejects_an_unsafe_prompt_path` passes (37/37 in `cargo test --lib -p ziee workflow::validate::`). No weakening of the security half.","status":"rejected"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":1089,"severity":"high","finding":"REJECTED — the INSTALL/RUN paths still perform the full existence + confinement check. Every `validate_for_install` / `validate_collecting` caller passes a bundle root that provably EXISTS at call time because each one has just read `workflow.yaml` out of it: handlers/dev.rs:268 (`extraction.extracted_path`), handlers/dev.rs:564 (`existing.extracted_path`), runner.rs:1248 + 1552 (`workflow.extracted_path`), hub/handlers.rs:2187 (`extraction.extracted_path`), workflow_mcp/tools.rs:528 + 672 (the conversation workspace `root`). `bundle_root.is_dir()` is therefore true on all of them and the WORKFLOW_PROMPT_FILE_MISSING / _ESCAPE half runs unchanged. Only the two draft surfaces in handlers/dev.rs (the deliberately-never-created unique path) take the skip.","status":"rejected"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":1074,"severity":"medium","finding":"REJECTED — `bundle_root.is_dir()` cannot be true-but-wrong in a way that matters. It follows symlinks, so a symlinked bundle dir answers true — but that was already the pre-round-4 behaviour and the confinement test that follows still canonicalizes BOTH sides (`canon.starts_with(&root_canon)`), so a symlinked root is compared against its own resolved path, not the link. TOCTOU: if the dir disappears between `is_dir()` and `canonicalize()`, canonicalize fails and the code emits WORKFLOW_PROMPT_FILE_MISSING — fail-CLOSED. The opposite race (dir absent at check, created after) cannot occur on any install/run path, since those already read the entry file from that dir. The one residual (dev.rs:564 with a bundle dir deleted out-of-band) skips the check but then fails downstream at the staging copy, so nothing is written.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":2352,"severity":"high","finding":"REJECTED — the drift guard cannot pass vacuously, and I could not construct a ValidationError invisible to all three assertions. The scanner now reports: struct literals (`ValidationError { … }` / `Self { … }`, 2538 + 2620), import aliases (2565), `Self::` inside `impl ValidationError` (2608), a non-`err/at/warn` constructor (2735), a non-string-literal layer or code argument (2762 + 2783), an unknown layer (2796), a non-SCREAMING_SNAKE code (2805) and, new in round 4, post-construction `code`/`layer`/`severity` assignment (2371 + scan_field_assignment 2321) with negative controls for read/compare/match/`Row { code: … }`. On top of that the test asserts `problems.is_empty()`, `!emitted.is_empty()`, `emitted - registered == {}` AND `registered - emitted == {}` (validate.rs:3418-3450), so the hand-curated VALIDATION_CODES canary makes any scanner regression that sees FEWER emit sites fail loudly rather than agree vacuously. `ValidationError::{err,at,warn}` are `pub(crate)`, and the only other crate that can reach the `pub` fields (`ziee-desktop`) is in `scan_roots`. The single residual evasion I found is `std::mem::replace(&mut e.code, \"X\")` (a field access followed by `,` rather than `=`); it is not a drift mode anyone reaches by accident on a `&'static str` field, and it cannot make the guard vacuous. `validation_codes_are_registered_and_humanised` passes.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":134,"severity":"high","finding":"REJECTED — the guessed-location fix neither leaves a false red Alert nor retires a still-true one. (a) Still-true: a location-less finding whose message merely opens `workflow.yaml: ` now parses with `locationCertain:false` (validationCopy.ts:435 keys it off `attributed.stepId != null`, the only real evidence the token is a location) and matches on CODE alone, so the original 'retired on the first check with nothing fixed' bug is closed. (b) Genuinely-gone: the code-only fallback still retires once the code is absent from the fresh result; the `locationCertain:true` path still discriminates the same code on a DIFFERENT step. (c) The new `UNDECIDABLE_BY_DEF_CHECK` list never leaves the author stuck: `saveDisabled` (WorkflowBuilderPage.tsx:68) is computed from `validation.errors`, not from `store.error`, and `save()` blanks `error` at entry (line 462) — so a WORKFLOW_PROMPT_FILE_* message the author has dealt with clears on the next Save. Every one of the three round-4 store tests plus the 58 others pass.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":557,"severity":"medium","finding":"REJECTED — a load failure can never render blank. Every branch of `describeRequestError` returns a non-empty string: `statusSentence` returns fixed sentences, the `HTTP error!`-with-unusable-status branch returns `fallback`, and the last line is `tidyMachineText(raw) || fallback` with `fallback = 'Failed to load workflow definition'`. Verified by execution across 400/403/404/500/502-html/TypeError. (The information LOSS half of the same boundary is the confirmed finding above; blankness is not a risk.)","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":410,"severity":"low","finding":"REJECTED — round 4's only code removal was the `loading={busy}` prop on the tool Combobox, and it was provably unreachable: `usePicker = !blockingFailure && !busy && serverToolOptions.length > 0` (line 211) gates this branch, so `busy` is false wherever the prop was passed. `git show f5f4a08e6 -- src-app/ui` confirms no export, function, type or interface was deleted anywhere in round 4; nothing needed a caller check. `npx tsc --noEmit` exits 0.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":42,"severity":"low","finding":"REJECTED — the exports with no non-test consumer outside their own file (SCHEMA_BUDGET, isWholeTemplateValue, parseInstallError, WORKFLOW_LEVEL, humaniseFinding, resolveFindingStep, ParsedInstallError, DescribedRequestError, describeFetchError, failureForToolList, classifyFetchFailure) are this workspace's sanctioned unit-test surface — each is exercised by toolSchemaForm.test.ts / validationCopy.test.ts / ToolCatalog.store.test.ts, and WORKFLOW_LEVEL carries an explicit doc note on why it is part of the returned map's contract. Not dead code under CODING_GUIDELINES §15 (which targets public fns with side effects and no production caller).","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":248,"severity":"medium","finding":"REJECTED — the session cache's concurrency is sound across the invalidate-mid-flight race. `load` captures `gen` before awaiting and drops its result when `toolsGeneration` has moved (line 260), and `invalidate` bumps the generation, deletes the pending promise AND deletes the cached entry (335-346), so the discarded continuation's early return leaves no stale pending handle — the next `load` starts a fresh fetch. A late joiner AWAITS the existing promise rather than firing a second `tools/list`. The session map is a WeakMap keyed on the builder store instance, so it is collected with the editing session. Same reasoning holds for `serversGeneration` / `serversPending`.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":263,"severity":"low","finding":"REJECTED (pre-existing, outside the diff) — `validateTimer` is never cleared on unmount, so navigating away from the builder within 400ms of the last keystroke fires one trailing `POST /validate-def` and a `set` on a torn-down local store. The timer predates this branch (it is unchanged from origin/feat/agent-core), the local store's `init` listeners do auto-unsubscribe, and one extra idempotent validate request is harmless. Noted, not charged to this branch.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/server/src/modules/workflow_mcp/tools.rs","line":672,"severity":"medium","finding":"REJECTED — the third consumer of `check_prompt_files` is unaffected by round 4. `workflow_mcp`'s `validate_workflow` tool passes `root` = the resolved conversation workspace dir, and it has already `read_to_string`'d `<root>/workflow.yaml` before the call, so `bundle_root.is_dir()` is true and the existence/confinement half runs exactly as before. The model-facing verdict is byte-identical; only the two `handlers/dev.rs` draft surfaces changed.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/server/src/modules/workflow/handlers/dev.rs","line":101,"severity":"low","finding":"REJECTED — round 4 left the whole product surface green. Evidence run in this worktree: `cargo test --lib -p ziee workflow::validate::` = 37 passed / 0 failed (including both new prompt-file tests and `validation_codes_are_registered_and_humanised`); `npx vitest run` over the builder specs = 61 passed; `npx tsc --noEmit` = exit 0; `check:state-matrix` (339 surfaces), `check:testid-registry` (1716 ids) and `check:gallery-coverage` all clean, so round 4's regenerated STATE_MATRIX line numbers (438/463) match the current ToolStepForm.tsx. dev.rs's round-4 change is comments only — `git show f5f4a08e6 -- .../dev.rs` contains no non-comment line. (The 3 `*.store.test.ts` failures under `npm run test:unit` are vitest-authored files swept up by the node:test glob and reproduce on the base branch — pre-existing, not this branch.)","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":397,"severity":"medium","finding":"REJECTED — round 4 introduced no wire-contract change. `FindingIdentity.locationCertain` is a UI-local interface never sent or received; no `#[derive(JsonSchema)]` type, handler signature, status code, permission or `_docs` entry was touched (dev.rs is comments only), so no OpenAPI regeneration is owed. The optional wire field `ValidationError.location` is read as `(e.location ?? null)` in `findingStillPresent`, matching the generated `string | null | undefined` shape.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":105,"severity":"medium","finding":"REJECTED — every round-4 addition has a live production consumer and a test that would fail if it were unplugged. `humaniseRequestError` is now called from `load` (was export-only before, so round 4 also closed that dead export); `UNDECIDABLE_BY_DEF_CHECK` and `locationCertain` are both read inside `findingStillPresent`, which is on the only validation-run path; `FINDING_FIELDS` + `scan_field_assignment` are reached from the live scanner loop and covered by three positive and six negative fixtures in `scanner_reads_awkward_source_shapes`, which passes.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":309,"severity":"low","finding":"REJECTED (pre-existing, outside the diff) — a user holding `workflows::manage` but not `workflows::read` reaches the edit route (`canAccess` gates on WorkflowsManage) while `load`'s `hasPermissionNow(Permissions.WorkflowsRead)` guard returns immediately, so the builder paints an empty step list with no loading state and no explanation. Not data loss: `workflowId` stays null, so `save()` takes the POST branch and stops at 'Give the workflow a name before saving' rather than overwriting the real definition. The guard line is unchanged from origin/feat/agent-core and outside this branch's diff.","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":211,"severity":"high","finding":"REJECTED — INV-3/4/5/6 hold across the enumerated states. INV-3: the Tool field is a Combobox whenever the list is enumerable, and `noFreeTextEntityRef.test.ts` enforces the general CLASS rule by scanning every `*StepForm.tsx` for an enumerable field bound to a free-text control. INV-4: `describeToolSchema` yields one typed control per declared property with required/type/default/description, `$ref`/`allOf`/`anyOf`-nullable resolved and recursion cut by an explicit stack. INV-5: `RefInsertMenu` is present on every generated field, template mode latches reversibly for the controls that cannot hold `{{ }}`, and `coerceToDeclared` passes a whole-value reference through untouched. INV-6: every non-picker path states its reason — no-server / resolving-server / unknown-server / disabled-server / server-lookup-failed / no-permission / unreachable / no-tools each map to a distinct `failureTitle` + `failureMessage`, `busy` disables the Input rather than silently inviting entry, a stored tool the server dropped keeps a labelled synthetic option, and a tool with no schema gets a named 'doesn't publish a list of the arguments it accepts' blurb. An unmapped validation code would fall back to the raw backend message, but the backend drift guard proves no such code exists.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":505,"severity":"medium","finding":"REJECTED — the stored-value-not-declared paths are correct in both directions. `fieldForValue` treats `undefined` (never expressible in JSON) as the only 'no value', keeps a stored `null` ELEMENT of a multiselect as a real member (so the commit rebuilt from the control's key list cannot erase it) and refuses to invent an option for a single picker's `null`; `staleOption` disambiguates its key against the list it joins so it can never shadow a declared choice; `assignKeys` makes `enum: [1, \"1\"]` non-colliding so `optionValueForKey`'s `find` cannot return the wrong type; and `optionValueForKey` uses an explicit match test rather than `?? key` so a legitimately-declared `null` choice is not committed as the string \"null\".","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":204,"severity":"medium","finding":"Round 5 fixed the LANGUAGE of the prompt_file: field error but not its TRUTH. `nonEmpty('A task description')` / `nonEmpty('A prompt')` are attached unconditionally on agent/llm/llm_map (stepForms.ts:204,210,215), and `prompt` is `skip_serializing_if = Option::is_none` (validate.rs:152-155,162-165,228-231), so an imported `prompt_file:` step arrives with NO `prompt` key. Verified by direct execution of `configErrors`: {agent → \"A task description is required\", llm → \"A prompt is required\", llm_map → \"A prompt is required\"} for a step carrying `prompt_file: 'prompts/task.md'`. The backend disagrees: `check_steps_shape` sets `has_file = prompt_file.is_some()` and emits WORKFLOW_PROMPT_MISSING only when `!has_prompt && !has_file` (validate.rs:682,691-697), and `check_prompt_files` skips its existence half on a draft (validate.rs:1099-1102, the round-4 change). So on ONE screen the field renders red 'A task description is required' (AgentStepForm.tsx:70 → builderFields.tsx:68), while BuilderValidationPanel renders green 'No blocking errors.' (BuilderValidationPanel.tsx:167-172), StepList shows no invalid marker (StepList.tsx:37-41) and Save is enabled (saveDisabled reads only `validation.errors`, WorkflowBuilderPage.tsx:60,68-72). The state is one round 4 DELIBERATELY unblocked; the builder exposes no prompt_file control, so the author is given a false requirement with no explanation of why the box is empty. Fix: make the client schema mirror the backend rule (skip the prompt requirement when `prompt_file` is present).","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":96,"severity":"low","finding":"Downstream of the above: acting on the false hint (typing into the prompt box of a `prompt_file:` step) triggers the real backend WORKFLOW_PROMPT_BOTH, whose author copy is 'This step has both a typed-in prompt and a prompt file — keep one of them.' (validationCopy.ts:94-97). Only one of the two is visible or removable in the builder — there is no prompt_file field anywhere under components/builder/ (grep: zero `prompt_file` render sites) — so half the stated instruction cannot be carried out on this surface. Recoverable (emptying the textarea satisfies `has_prompt = prompt.filter(|s| !s.is_empty())`, validate.rs:681), which is why this is low and not high, but the copy still tells the author to choose between two things when it shows them one.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":182,"severity":"low","finding":"REJECTED — the `nonEmpty` CLASS fix is complete for the raw-vocabulary defect it targeted. Executed `configErrors` over every kind × every nonEmpty call site (agent.prompt, llm.prompt, llm_map.{prompt,for_each,item_var}, sandbox.run, elicit.message, tool.{server,tool}) × {absent, '', '   \\t\\n ', null, 42}: all 45 cells return the authored sentence, identical across all five states. No zod default diagnostic ('Invalid input', 'expected string', 'received undefined') is reachable at any of them. `z.string({error}).trim().min(1, required)` covers invalid_type and too_small with the same string, and `.trim()` runs before `.min` in zod 4.4.3 so whitespace-only is rejected.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":191,"severity":"low","finding":"REJECTED — no OTHER zod validator in stepForms.ts can leak a default message. Executed every numeric/enum node: agent.max_steps {absent,null,'x',NaN,Infinity → 'Enter a number'; 1.5,1e21 → 'Enter a whole number'; 0,-3 → 'Must be at least 1'}; llm_map.max_parallel {…,999 → 'At most 20'}; llm_map.max_retries {-1 → 'Cannot be negative'}; sandbox.timeout_ms {0 → 'Must be at least 1 ms'}; elicit.timeout_ms {-1 → 'Cannot be negative'}; agent/llm.output_format {absent,null,'xml',5 → 'Choose a result format'}. `wholeNumber()` authors both the type error and `.int()`; `choice()` authors the enum error; every `.min`/`.max` carries an explicit string. The rendered path is `builderFields.tsx:68` (`{error && <span…>{error}</span>}`) — verbatim, so this enumeration is the complete surface.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":489,"severity":"low","finding":"REJECTED — the LOAD boundary is now read-voiced for every status. `statusSentence(status,'read')` returns act-neutral or read-specific copy for 401/404/408/429/5xx and read variants for 403 ('permission to open this workflow'), 409 ('try opening it again'), 413 ('for the server to send back'); every remaining 4xx (400/402/405/410/415/422…) falls to `rejectionCopy('read', …)` = 'This workflow could not be opened — …'. No branch on the read path can emit the word 'change'. Verified against the enumerated status set plus the fall-through, and the store passes `{ boundary: 'read' }` at the single load catch site (WorkflowBuilder.store.ts:354-357).","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":628,"severity":"low","finding":"REJECTED — the server diagnostic is preserved AND the unbounded blob is still clipped. `serverDetail = isTransportWrapper ? '' : tidyMachineText(raw)` (validationCopy.ts:620-628) rests on a correct reading of the generated client: `sdk/packages/framework/src/api-client/core.ts:678-717` sets `errorMessage = errorResponse.error` when the body parses as JSON carrying `error`, and only falls back to `HTTP error! status: N - <raw body>` when it does not — so the wrapper IS exactly the unbounded case and is suppressed, while a server-written sentence survives, markup-stripped and clipped to MAX_ERROR_CHARS=160. Cannot render blank: `rejectionCopy` returns a literal when `detail` is '', and the final line is `serverDetail || fallback` with a non-empty caller fallback.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":617,"severity":"low","finding":"REJECTED (considered and ruled out) — the widened mutation-boundary behaviour does not open an INV-1 hole today. Round 5 also made a save/validate 4xx paste the server's own message via `rejectionCopy('mutation', detail)` instead of flattening it. The only way a WIRE-vocabulary validator string could reach that branch is a code absent from HUMAN_COPY (`humaniseValidatorFinding` returns null at validationCopy.ts:415). Cross-checked the registry mechanically: VALIDATION_CODES = 41 codes, HUMAN_COPY = 41 keys, symmetric difference EMPTY in both directions, and every `ValidationError::at(\"layer\",\"CODE\"…)` emit site in validate.rs + ref_check.rs is registered — so the `validation_codes_are_registered_and_humanised` guard (validate.rs:3412) is non-vacuous and the branch is unreachable for findings. The remaining reachable 4xx bodies are server-authored English (WORKFLOW_TOOL_NAME_TOO_LONG) or transport wrappers that suppress themselves; the one genuinely YAML-flavoured message (`workflow.yaml deserialization failed: …`) is the READ case the round deliberately preserves, attributed to the server (INV-6 escape hatch) rather than passed off as our copy.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":200,"severity":"low","finding":"REJECTED — the `nonEmpty`/`wholeNumber`/`choice` rewrite changed no VALIDITY, only messages, so the Save gate is untouched. Field-by-field: `z.string()`→`z.string({error})` and `z.number({message})`→`z.number({error})` alter the message map, not acceptance; `.int()`→`.int(msg)` and `z.enum(v)`→`z.enum(v,{error})` likewise. `createStep` defaults still parse clean for all six kinds (stepForms.test.ts 'createStep yields a schema-VALID default for every kind' passes). Independently: `configErrors` feeds only inline field copy — `saveDisabled` (WorkflowBuilderPage.tsx:68) is computed from `validation.errors` + step count + name, and never from `configErrors` (grep: zero references outside the six step forms and the tests). So even a changed verdict could not move the gate.","status":"rejected"}
{"angle":"regression-risk","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":603,"severity":"low","finding":"REJECTED — the signature change is backward-compatible and every caller is accounted for. `describeRequestError`/`humaniseRequestError` widened their third parameter from `fallback: string` to `string | DescribeErrorOptions`, normalised at validationCopy.ts:605-608. Both remaining production callers still pass a bare string (WorkflowBuilder.store.ts:210 validate, :512 save) and keep the mutation voice; only the load site (:354) passes the object. Grepped the whole of src-app/ for these two symbols: no other production consumer exists. `npx tsc --noEmit` over the UI workspace exits clean with zero diagnostics.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.test.ts","line":381,"severity":"low","finding":"REJECTED — the source guard on the store's LOAD boundary is non-vacuous. `/load: async \\([\\s\\S]*?\\n      \\},/` first matches at WorkflowBuilder.store.ts:308 (`load: async (id: string) => {`) and terminates at the 6-space `},` on :360 — every inner closer in between is indented 8-12 spaces — so `boundary: 'read'` at :355 is genuinely inside the captured body, and `assert.ok(loadBody, …)` fails loudly if the shape drifts rather than passing empty. Ran the suites: builder component specs 86/86 pass under node:test; WorkflowBuilder.store + ToolCatalog.store 61/61 pass under vitest. (`npm run test:unit`'s glob also feeds the vitest-authored `*.store.test.ts` files to node:test, where they die on `vitest` imports — pre-existing and branch-independent: the untouched WorkflowRun.store.test.ts fails identically.)","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":485,"severity":"low","finding":"REJECTED — swept the full state list against INV-3/4/6 and found no silent or false fallback. no-server → 'Pick a server first'; list not settled / lookup in flight → resolving-server (never an accusation) with the Tool input DISABLED and 'Looking up this server…' (ToolStepForm.tsx:425-433); list load FAILED → serversSettled true → authoritative by-name lookup; no `mcp_servers::read` → `canList=false` short-circuits to no-permission immediately (entryForServerName:424) instead of hanging on a list that never settles; disabled server → disabled-server, kept rather than filtered; unreachable → unreachable + Try again; zero-tool server → no-tools with 'info' tone; tool with no schema → named-tool reason at ToolStepForm.tsx:455-458; stored tool no longer offered → synthetic option + explanatory description (:221-239); stored `null` multiselect element → preserved by `fieldForValue` (toolSchemaForm.ts:518-523) so a toggle cannot erase it. `awaitingCatalog` (:207) closes the one frame where an unasked server rendered as an enabled free-text box.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":108,"severity":"low","finding":"REJECTED — round 5 touched no component state, and the pre-existing per-field state machine holds under re-inspection. The template latch is sticky-on-reference and cleared only on BLANK (:111-128), so a partial `{{ inputs.limit` edit keeps the mode while an externally-emptied value releases it; fields are keyed `${toolKey}::${field.name}` (:391) so a same-named property on a different tool cannot inherit the latch/touched/pending-clear state; the InputNumber clear is deferred to blur and gated on the raw buffer (:250-265). `touched` gating means a freshly chosen tool does not paint required rows red, which is the same class of false-required the confirmed finding above describes — and it is handled correctly here.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":180,"severity":"low","finding":"REJECTED — no lifecycle regression from round 5 (it added no timer, listener, promise or effect). Re-verified the pre-existing machinery: `createValidateRunner`'s monotonic `seq !== issued` guard drops a stale response on BOTH the success and catch paths (:188,:203); the debounce handle is per-store-instance so concurrent builders cannot share it (:253); the session cache's `toolsGeneration`/`serversGeneration` counters discard an in-flight fetch that 'Try again' superseded (ToolCatalog.store.ts:260,321) and `toolsPending` makes a late joiner await the running request instead of re-handshaking a stdio server; `defineLocalStore` init listeners auto-unsubscribe on unmount. The load path's `set` runs after `await`, and a second `load(id)` cannot interleave destructively — the effect keys on `[id, canAccess]`.","status":"rejected"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":1084,"severity":"low","finding":"REJECTED — independently re-derived that the round-4 `bundle_present` skip does not weaken the path check. In `check_prompt_files` the textual reject (`p.contains(\"..\") || p.starts_with('/')` → WORKFLOW_PROMPT_FILE_UNSAFE + `continue`, validate.rs:1083-1092) runs UNCONDITIONALLY and strictly BEFORE `if !bundle_present { continue }` (:1093-1096), so the security half is never skipped. Only the two draft surfaces take the skip: `validate_workflow` (dev.rs:107) and `validate_workflow_def` (dev.rs:953) pass `temp_dir()/ziee-wf-validate-<uuid v4>`, a path never created; the authoritative callers pass a real root (dev.rs:268 `extraction.extracted_path` for create/import, dev.rs:564 `existing.extracted_path` for PUT /definition), where the existence + canonicalize-confinement pair still runs. The unique-uuid root also keeps the path-existence oracle closed for a `WorkflowsRead` caller. Round 5 changed nothing on the server.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":3412,"severity":"low","finding":"REJECTED — the backend↔UI humanisation contract survived round 5 intact. `human_copy_keys` (validate.rs:2938-2959) parses only the object literal initialising `const HUMAN_COPY`, bounded by its own brace depth, so the prose and new functions round 5 appended 280 lines below it (FailureBoundary, DescribeErrorOptions, rejectionCopy — which mention `workflow.yaml` and `HTTP error! status:` in doc comments) cannot reach the parser or the code set. HUMAN_COPY itself is byte-unchanged in this commit. Mechanically re-ran the guard's three comparisons out-of-band: emitted ⊆ registered, registered ⊆ emitted, registered ⊆ humanised — all empty differences at 41 codes. No wire type, endpoint, or generated `types.ts` symbol changed in round 5.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":473,"severity":"low","finding":"REJECTED — round 5 introduced no dead code. `FailureBoundary` is exported but never imported elsewhere; it is not dead, it is the type of `DescribeErrorOptions.boundary`, which IS the public parameter the store passes at WorkflowBuilder.store.ts:354, so the alias is part of the reachable signature (naming it is what makes the exported option object documentable). `rejectionCopy` and `statusSentence` are module-private with live callers; `DescribeErrorOptions` is structurally satisfied by the store's inline literal. Both new tests execute (86/86 node:test) rather than being `#[ignore]`-equivalent skips. No `allow(dead_code)`-style suppression added anywhere in the diff.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/stepForms.ts","line":250,"severity":"low","finding":"REJECTED (nit, not a defect) — `configErrors`' 'Never throws' contract is honoured for the input it can actually receive. An unknown `kind` returns `{}` via the round-4 `if (!schema) return {}` guard (verified by execution). A literal `null` step still throws on `step.kind`, but every call site reads a step object out of the typed `def.steps` inside a form that the panel only mounts for a selected, existing step (AgentStepForm.tsx:34, LlmStepForm.tsx:15, LlmMapStepForm.tsx:19, SandboxStepForm.tsx:15, ElicitStepForm.tsx:24, ToolStepForm.tsx:121) — `null` is not reachable, so this is a doc-comment overstatement rather than a failure mode.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/stores/WorkflowBuilder.store.ts","line":210,"severity":"low","finding":"REJECTED (considered and ruled out) — the validate boundary correctly stayed on the mutation voice, so round 5 did not cross-contaminate it. A `/validate-def` failure follows an edit the author actually made, so 'The server rejected this change.' / 'You do not have permission to make this change.' describes something real, and its fallback still names the right act ('The workflow could not be checked — try again.'). The save boundary likewise keeps `SAVE_FAILURE_FALLBACK` and the mutation branch of `rejectionCopy`, and the name-collision short-circuit (:507-511) still precedes humanisation. The `errorSource` partition and the `findingStillPresent` retirement rule are untouched by this round.","status":"rejected"}
