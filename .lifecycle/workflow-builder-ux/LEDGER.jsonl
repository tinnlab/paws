{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":1934,"severity":"medium","finding":"`let window = &rest[..rest.len().min(400)]` slices a &str by BYTE index. If byte 400 of the window lands inside a multi-byte UTF-8 char the test PANICS with 'byte index 400 is not a char boundary' instead of reporting a drift. validate.rs currently holds 161 non-ASCII bytes and ref_check.rs 79 (em dashes / ellipses in doc comments and messages); I verified none currently sits in the 380..420 window of any `ValidationError::` occurrence, so it passes today, but any future message or doc-comment edit that shifts one there converts the humanisation guard into an unrelated panic. Use floor_char_boundary / char_indices().take_while instead.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":1916,"severity":"low","finding":"VALIDATE_SRC is `include_str!(\"validate.rs\")`, i.e. the scanner also scans this file's own `mod tests` and the humanisation_contract module. A future test fixture that constructs `ValidationError::at(\"semantic\", \"WORKFLOW_X\", ...)` inside a #[cfg(test)] block would be counted as a production emit site, forcing a bogus VALIDATION_CODES + validationCopy.ts entry for a code the server can never emit. I confirmed the two existing self-references (the doc comment at 1926 and the assert string at 2014) do NOT produce false positives, because their first literal is not one of schema/semantic/security. Failure mode is noisy (test fails), not silent.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/server/src/modules/workflow/validate.rs","line":1927,"severity":"low","finding":"no findings beyond those logged: I re-implemented `emitted_codes` byte-for-byte in Python and ran it over the real validate.rs + ref_check.rs. It yields exactly 41 codes, identical to VALIDATION_CODES (0 unregistered, 0 stale), and the escape handling (`(true,'\\\\')` skip-next) correctly survives the escaped-quote text inside the assert messages. The scanner is not silently matching nothing.","status":"rejected"}
{"angle":"security-authz","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"no findings for this angle: the whole diff is a `pub const` string array plus a `#[cfg(test)]` module. Nothing touches a handler, extractor, permission, or any runtime input path. The three `include_str!` reads are compile-time reads of repo-controlled files and, being under cfg(test), are not compiled into the release binary at all, so no source text is shipped and no untrusted input is parsed.","status":"rejected"}
{"angle":"api-contract","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"no findings for this angle: `git diff origin/feat/agent-core...HEAD -- validate.rs` is 231 insertions / 0 deletions. Every existing `layer`/`code`/`message` literal, the `Severity` enum, the `ValidationError` struct and its serde/JsonSchema derives, `validate_for_install`, and therefore the `/validate` response, the install/import/run AppError text and the `workflow_mcp::validate_workflow` payload are byte-identical to base. The new const is not a schema type and does not appear in OpenAPI.","status":"rejected"}
{"angle":"error-handling","file":"src-app/server/src/modules/workflow/validate.rs","line":1920,"severity":"low","finding":"no findings for this angle beyond the byte-slice panic logged under correctness-logic: the cross-file `include_str!` of validationCopy.ts fails at COMPILE time if the file is moved or renamed (loud), and if the file is emptied or truncated the `unhumanised` assertion lists all 41 codes and fails loudly. There is no swallowed I/O and no `unwrap` on a runtime value in the added code.","status":"rejected"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2046,"severity":"medium","finding":"The humanisation half of the guard is a raw substring search over the WHOLE file: `!UI_COPY_SRC.contains(&format!(\"\\\"{code}\\\"\"))`. It does not check that the code is a key of `HUMAN_COPY`. Concrete failure: delete `\"WORKFLOW_TOOL_NO_TOOL\": () => ...` from HUMAN_COPY but leave the code quoted anywhere else in the file (a comment, a deprecated list, a doc block) and this test stays green; `humaniseFinding` then hits its `if (!copy) return finding.message` fallback (validationCopy.ts:172-174) and the raw wire message reaches the author — exactly the property the test claims to guarantee. The TS-side test cannot catch it either, because it iterates `HUMANISED_CODES = Object.keys(HUMAN_COPY)`, which no longer contains the code. A structural check (parse the keys, or have the TS test assert against a generated list) would close it.","status":"confirmed"}
{"angle":"test-quality","file":"src-app/server/src/modules/workflow/validate.rs","line":2011,"severity":"low","finding":"no vacuity finding: I specifically checked for the 'cross-file test that silently scans zero items' failure. The test has an explicit `assert!(!emitted.is_empty(), ...)` self-check AND, more importantly, the bidirectional `stale` assertion (registered - emitted must be empty) means a scanner that degrades to finding a SUBSET of emit sites also fails loudly with the missing codes named. Verified empirically that it currently finds all 41. Rejecting my initial suspicion that this is a vacuous guard.","status":"rejected"}
{"angle":"extensibility","file":"src-app/server/src/modules/workflow/validate.rs","line":1916,"severity":"medium","finding":"The scanner's source set is a hardcoded two-file list (VALIDATE_SRC + REF_CHECK_SRC at 1916-1917) while the emitters `ValidationError::{err,at,warn}` are `pub(crate)` (441/450/466) and the struct's fields are `pub`. Any OTHER module in the crate that starts emitting workflow findings (a future policy_check.rs, or a direct `ValidationError { code: .., .. }` struct literal) is invisible to `emitted_codes`: its code is neither registry-checked nor humanisation-checked, so it silently reaches the builder as a raw wire message. There is no compile-time seam forcing a new emitting file into the scan — the next contributor has to know to edit line 1917.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/server/src/modules/workflow/validate.rs","line":1965,"severity":"medium","finding":"Two closed filters silently DROP emit sites rather than failing: the layer allowlist `matches!(literals[0].as_str(), \"schema\"|\"semantic\"|\"security\")` (1965) and the SCREAMING_SNAKE charset filter on the code (1966). Failure mode: introduce a fourth layer (say \"policy\"), emit `WORKFLOW_NEW_THING` from it and forget the registry — `emitted` never sees the code, `unregistered` is empty, `unhumanised` is empty, the test passes, and the builder shows the raw validator message. The `stale` direction only rescues the case where the author DID register the code, which is not the case the guard exists for. The comment at 1961-1963 states the drop-rather-than-false-positive tradeoff but not that it re-opens the silent hole.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"Adding one validation code now requires edits in three places: the emit site, `VALIDATION_CODES` (355), and `HUMAN_COPY` in validationCopy.ts. The registry is a middleman that adds no guarantee the test could not get directly — `emitted` is already derived from source, so comparing `emitted` against the UI copy would enforce the same INV-1 property with one fewer hand-maintained closed list. Its only extra value is the duplicate/shape check in `validation_codes_registry_is_well_formed` and the grouping comments.","status":"confirmed"}
{"angle":"maintainability","file":"src-app/server/src/modules/workflow/validate.rs","line":1934,"severity":"low","finding":"The `400` window is a magic constant justified only as 'a generous bound'. Nothing detects the case where a call's first two string literals fall OUTSIDE it (e.g. a long `//` comment inserted between `ValidationError::at(` and the layer argument): the site is then silently skipped, and the miss only surfaces if the code was already registered (via `stale`). Either derive the bound (scan to the matching close paren) or assert that a window ending mid-call is an error.","status":"confirmed"}
{"angle":"maintainability","file":"src-app/server/src/modules/workflow/validate.rs","line":363,"severity":"low","finding":"no further findings for this angle: the per-group comments in VALIDATION_CODES (`// validate.rs — prompts`, `// ref_check.rs — type-aware reference checking`) are hand-maintained and unchecked, so a code that moves between files leaves a stale comment. I verified every grouping is currently ACCURATE (all 8 ref_check codes really are emitted from ref_check.rs, the other 33 from validate.rs), and a stale comment has no behavioural consequence — rejecting as style.","status":"rejected"}
{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":355,"severity":"low","finding":"`pub const VALIDATION_CODES` has no production consumer: `grep -rn VALIDATION_CODES src-app/ --include=*.rs --include=*.ts --include=*.tsx` matches only validate.rs itself (the const, its `use super::` in the test module, and the two test bodies). It is a `pub` API-surface item read exclusively by #[cfg(test)] code, and because it is `pub` the dead_code lint will never flag it. CODING_GUIDELINES §15 requires a new public item to have a production caller; either make it `pub(crate)`/private with `#[cfg(test)]`-adjacent scope, or wire it into a real consumer (e.g. have `workflow_mcp::validate_workflow` document the code set from it).","status":"confirmed"}
{"angle":"dead-code","file":"src-app/server/src/modules/workflow/validate.rs","line":1937,"severity":"low","finding":"`let mut chars = window[open..].char_indices().peekable();` — `peek()` is never called anywhere in the loop (the only consumption is `chars.next()` at 1940 and 1955). The `.peekable()` adapter is dead and misleads a reader into looking for lookahead logic that does not exist.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":264,"severity":"high","finding":"`onChange={v => patch({ tool: v, arguments: {} })}` wipes ALL arguments on EVERY Combobox change event, with no `v === step.tool` guard. Base UI's single-select path calls `setSelectedValue(itemValue)` unconditionally on item press (node_modules/@base-ui/react/combobox/root/AriaCombobox.js:563 — no equality check), and the kit's `useControllableState.set` also always fires `onChange` (sdk/packages/kit/src/kit/use-controllable-state.ts:24-26). So re-picking the tool that is ALREADY selected (a natural thing to do when re-opening the picker to check the description, then clicking the highlighted row / pressing Enter) destroys every argument the author entered — both the generated fields and the free rows — with no confirmation and no undo. The kit Combobox is also rendered with `showClear`, so the inline X is a second one-click path to the same silent wipe.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":258,"severity":"medium","finding":"When the picker is active, a saved `step.tool` that is NOT in the fetched tool list renders as EMPTY. The kit Combobox resolves `selected = current ? byValue.get(current) ?? null : null` (sdk/packages/kit/src/kit/combobox.tsx:73) and passes that to Base UI, so an unknown value shows the `placeholder` (\"Search this server's tools…\"). Failure mode: a workflow saved against tool `search`, which the server later renamed to `web_search`, opens with an apparently BLANK required Tool field while `step.tool` is still `'search'`; `configErrors.tool` (stepForms.ts:209) only checks non-empty so no inline error fires and Save is not blocked. The author cannot see what is stored, and the only way to make the field non-blank is to pick a tool — which (see the finding above) also wipes the arguments.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":150,"severity":"medium","finding":"Non-string enums are handled as strings in both directions. `toolSchemaForm.ts:146` builds options with `value: String(v)`, and this Select commits that string verbatim (`onChange={v => onChange(v)}`, line 152) — so a property declared `{\"type\":\"integer\",\"enum\":[1,2,3]}` (the enum branch at toolSchemaForm.ts:157 runs BEFORE the type switch, so it always becomes a select) is saved as the STRING \"2\" and the tool receives a type it declared it does not accept. The reverse is also broken: the `value` prop is `typeof value === 'string' && value ? value : undefined`, so a workflow loaded with `{\"page\": 2}` renders the Select as UNSELECTED (\"Choose a value\") while the argument is in fact set. The same stringification applies to `multiselect` (line 162, `value.map(String)`).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":127,"severity":"medium","finding":"`checked={value === true}` renders a boolean field OFF whenever the key is absent — including when the schema declares `\"default\": true`. The description below it simultaneously reads \"Defaults to true.\" (line 73), so the author sees a switch in the OFF position next to text saying the default is on, and reasonably concludes the tool will be called with `false`. The control cannot express the three real states (absent/true/false), and there is no way to explicitly send `false` distinct from \"leave unset\" other than toggling on then off (which does write `false`, since `commitField`'s delete test at ToolStepForm.tsx:213 is strict `=== ''`).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":126,"severity":"low","finding":"The name→id resolution passes `(servers ?? []).map(...)` UNFILTERED, while the sibling Server picker filters to `s.enabled` (capabilities.tsx:17). A step whose `server` names a DISABLED accessible server therefore renders a Server `Select` showing the placeholder (its option was filtered out, and the kit Select drops an unknown value) while the Tool field below it is a fully populated picker of that same disabled server's tools. The two adjacent controls contradict each other about whether the server exists.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":81,"severity":"low","finding":"REJECTED (verified, not a defect): I suspected `severityOf` (validationCopy.ts:196-198) would mis-bucket entries from the `warnings` array, because `ValidationError.severity` is OPTIONAL in the generated TS type (api-client/types.ts:7443) and the helper defaults an absent value to 'error' — which would paint a warning with the red XCircle and inflate the red per-step badge in StepList. Checked the wire: `validate.rs:429` declares `severity` as a non-Option field with only `#[serde(default)]` (deserialize-side) and no `skip_serializing_if`, and handlers/dev.rs:951-953 splits the vector BY that field. So every entry landing in `warnings` always carries `severity: \"warning\"` on the wire. Not reachable.","status":"rejected"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":111,"severity":"high","finding":"Template mode is derived from the CURRENT value (`isTemplateValue`), and the plain `Input` used in that mode commits the raw string on every keystroke (line 116). So any PARTIAL edit of a reference flips the field out of template mode mid-typing and silently corrupts the argument. Concrete: a `number`/`integer`/`select`/`switch`/`multiselect` field holding `{{ inputs.limit }}`; the author backspaces the trailing `}}` (or selects-all and types `5`). The value is now the plain string `\"{{ inputs.limit \"` (or `\"5\"`), `isTemplateValue` is false, and the control swaps to `InputNumber`/`Select`, whose value guards (`typeof value === 'number'`, line 136; `typeof value === 'string' && value`, line 150) do not match — so the field renders EMPTY. The corrupt string is still in `step.arguments`, invisible and un-editable from the form, and the 'Use a value instead' undo button (line 83) has also disappeared because it only renders while `templated`. The workflow saves with a broken argument the author was shown as blank.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":140,"severity":"medium","finding":"`onChange={v => onChange(v ?? '')}` turns the kit InputNumber's 'still typing' signal into a DELETE. InputNumber emits `undefined` for an intermediate string (`isIntermediate`, sdk/packages/kit/src/kit/input-number.tsx:24/48) — which includes a lone `-` and `1e`. That reaches `commitField` (ToolStepForm.tsx:213), which deletes the key, so the `value` prop becomes `null`, and InputNumber's own sync effect then fires `if (value == null) setBuf('')` (input-number.tsx:33-36) and erases the buffer. Failure mode: in any generated number field the author cannot type a negative value — the `-` vanishes as soon as it is typed. The sibling builder forms avoid this by never letting the value go null (`onChange={v => patch({ max_steps: v ?? 1 })}`, AgentStepForm.tsx:163).","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":177,"severity":"medium","finding":"The `json`/`textarea` control runs `coerceToDeclared` on EVERY keystroke, so as soon as the text becomes parseable JSON the stored value becomes an object and `value={valueToText(value)}` re-renders it as `JSON.stringify(...)` — a different string from what the author typed. Typing `{ \"a\": 1 }` rewrites the textarea to `{\"a\":1}` the instant the closing brace lands, collapsing the whitespace and moving the caret to the end (React only preserves selection when the controlled value is unchanged). Continuing to type then appends in the wrong place. Coercion belongs on blur, not on change.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":191,"severity":"low","finding":"Typing a DECLARED property name into a free 'Additional arguments' row makes the row disappear under the author's cursor. `commitRows` (line 203) merges rows last, so the moment the key matches a schema property the value lands in `known`; `splitArguments` then puts it outside `extra`, the resync effect sees `argsSnapshot(extra)` differ from `lastPushed`, and `setRows(argsToRows(extra))` drops the row. The value is not lost (it reappears in the generated field above), but the input the author is typing into is destroyed mid-keystroke with no explanation.","status":"confirmed"}
{"angle":"frontend-state","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":203,"severity":"low","finding":"REJECTED (verified sound): I traced the buffer-vs-prop protocol for clobbering. `lastPushed` is written from `rowsToArgs(next)` in `commitRows` while `patch` sends `{...known, ...rowObj}`; the resync effect compares against `argsSnapshot(extra)`, and `extra` is rebuilt in the same key order the merge produced, so `JSON.stringify` equality holds and a self-commit never re-seeds the rows. `commitField` (which does NOT touch `lastPushed`) leaves `extra` byte-identical, so it also does not trip the effect. The generated half and the free half genuinely cannot clobber each other, including across the `useGenerated` false→true transition when the catalog resolves. No finding.","status":"rejected"}
{"angle":"reactive-read-in-loop","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":39,"severity":"low","finding":"REJECTED (verified clean). `defineLocalStore`'s proxy calls `useStore(...)` on every non-function property read (store-kit.ts:589-593), so a reactive read IS a hook here. I checked every read in the five files: StepList reads `store.def`/`store.selectedStepId`/`store.validation` unconditionally at the top and hoists `findingsByStep` above the `.map()`; BuilderValidationPanel reads `store.validation`/`store.validating`/`store.def` at the top and calls `attributeFindings` before any map; ToolStepForm reads only `catalog.byServerId` (line 122) plus `McpServer.servers`, both unconditional; ToolArgumentsForm/GeneratedField only FORWARD `store` (no property read) and RefInsertMenu's `store.def` read is at the top of its own component, so per-field menus are separate hook scopes. Action reads (`store.selectStep`, `store.updateStep`, `catalog.load`) return function-valued props and take the hook-free branch. Hook counts are stable in all files.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":160,"severity":"medium","finding":"A server that is REACHED but serves ZERO tools degrades SILENTLY — the one case the module's own INV-6 comment (line 111) says must never happen. `entry = {tools: [], loading:false, failure:null}`, so `blockingFailure` is null (no Alert renders) and `usePicker` is false only because `toolOptions.length > 0` fails. The author gets the plain hand-entry `Input` with the generic placeholder 'e.g. search' and the generic description 'The exact name of the tool to call on that server.' — pixel-identical to the pre-picker experience and indistinguishable from 'the picker just hasn't been built for this'. Nothing tells them the server answered and offers nothing (an MCP server whose tool set is permission-filtered to empty, or a misconfigured server, is the realistic case). The store even models the distinction correctly; the component collapses it.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":93,"severity":"medium","finding":"A FAILED validation call is invisible in this panel. `runValidate` (WorkflowBuilder.store.ts:99-107) writes the reason to `store.error` and deliberately keeps the prior `validation` — but `store.error` is not rendered by this panel, nor anywhere else in the builder (WorkflowBuilderPage.tsx renders only `loadError`, `deletedExternally` and save errors). Two wrong states result: (a) if the FIRST validate fails, `validation` is null and this panel asserts 'Add steps to validate the workflow.' while the workflow has steps; (b) if a later validate fails, the panel keeps rendering the stale result — including the green 'No blocking errors.' at line 99 — with no staleness marker, while `saveDisabled` (WorkflowBuilderPage.tsx:68) is computed from that same stale `validation`. In both cases the author is told the workflow validated when it did not.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":198,"severity":"low","finding":"`LabeledControl` accepts an `error` prop (builderFields.tsx:26) and GeneratedField never passes one, so `required` on a generated field is purely decorative: a missing required argument shows only the red `*`. The backend `POST /validate-def` cannot check arguments against a tool's schema (it never fetches the tool), so nothing else catches it either — the omission surfaces only when the workflow RUNS. The client already knows `field.required` and the current value; the check is one line.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":236,"severity":"low","finding":"REJECTED (defensively sound). I checked whether a malformed `input_schema` could render an empty or throwing generated section: `describeToolSchema` narrows every access with `isRecord`, returns null for a missing/non-object schema or zero properties (toolSchemaForm.ts:206-212), keeps an unresolvable `$ref` property as a JSON field rather than dropping it (line 233-234), and cuts `$ref` cycles with an explicit stack. So `spec` is either null (caller renders the free-row fallback with the 'Arguments' label) or has at least one field. `ToolArgumentsForm` can never render an empty container.","status":"rejected"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":59,"severity":"low","finding":"`data-testid={`wf-builder-finding-goto-${finding.stepId}`}` is not unique: a step with two findings (very common — e.g. a tool step missing both server and tool emits WORKFLOW_TOOL_NO_SERVER and WORKFLOW_TOOL_NO_TOOL, both located at the step id) renders two buttons with the identical testid. Any Playwright `getByTestId(...)` on it fails with a strict-mode violation, so the very affordance this diff adds is not reliably assertable. Index the testid (or key it on the finding code).","status":"confirmed"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":116,"severity":"low","finding":"The problem/caution badge carries its full phrasing only in a native `title` on a non-focusable `<span>` plus an `sr-only` span. A sighted keyboard user therefore never gets the expansion (`title` shows on hover only, and the span is not tabbable), so for them the badge is a bare glyph and a digit with no stated unit. Screen-reader users are covered by the sr-only text; pointer users by the title; keyboard-only sighted users by neither.","status":"confirmed"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/RefInsertMenu.tsx","line":66,"severity":"low","finding":"REJECTED (verified named + operable). The new `compact` icon-only trigger passes both `aria-label=\"Insert reference\"` and `tooltip`, and the kit Button's type union FORCES a name on `size=\"icon\"` (button.tsx:59-60) with `aria-label` winning over the tooltip string (button.tsx:97) — so the trigger has an accessible name and is a real `<button>` inside a `Dropdown`. Same for the `Undo2` clear-ref button (ToolArgumentsForm.tsx:89). The empty-refs case still renders a disabled 'No references available yet' item rather than an empty menu (line 38-43). No finding.","status":"rejected"}
{"angle":"ux-accessibility","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":82,"severity":"low","finding":"REJECTED (pre-existing, not introduced by this diff). The row `<li>` is a clickable non-button with `onClick` and no `role`/`tabIndex`/keyboard handler, which is a genuine a11y gap — but that markup is unchanged from origin/feat/agent-core; the diff only adds the validation badges inside it. Out of scope as a finding against this branch.","status":"rejected"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":117,"severity":"low","finding":"REJECTED (conformant). Checked every class added by the diff across all five files: colors are semantic tokens only (`text-destructive`, `text-warning`, `text-muted-foreground`, `bg-muted`, `border-primary`, `bg-accent`) — no raw hue, no arbitrary color value, no inline style color. Direction props are logical throughout (`text-start`, `ps-0`, `justify-start`, `items-start`); no `pl/pr`, `ml/mr`, `text-left/right` appears in the diff. Spacing stays on the 4px scale plus the kit's sanctioned 2px half-steps (`mt-0.5`, `py-1`). `SectionHeader` is already used for the Steps title+actions row (StepList.tsx:53), form fields go through `LabeledControl` rather than raw flex-gap, and every control is a kit component — no raw `<button>`/`<input>`.","status":"rejected"}
{"angle":"responsive-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":82,"severity":"low","finding":"The label-row action group has no `shrink-0`, and `LabeledControl`'s row is `flex items-center justify-between gap-2` with a `<label>` that has neither `min-w-0` nor truncation (builderFields.tsx:57-62). The labels here come from the tool's schema `title` (toolSchemaForm.ts:238) and can be long. At ~358px (the config column at a 390px viewport) a long single-token label pushes against the two icon buttons, which — having only `w-8`-style width and default `flex-shrink: 1` — compress rather than hold their size. The `compact` variant addresses the width of the trigger but not its shrink behaviour; `shrink-0` on the wrapper is the fix.","status":"confirmed"}
{"angle":"responsive-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":102,"severity":"low","finding":"REJECTED (holds up). Traced the new kind-Tag + badge row at the narrowest real case. The page stacks below `md` (WorkflowBuilderPage.tsx:184-187) so at 390px the list is full-width, and at `md` the column is `w-80` (320px) minus the grip and three icon buttons. The wrapper carries `min-w-0`, the Tag carries `min-w-0 truncate`, and both badges carry `shrink-0 whitespace-nowrap` — so the kind label truncates and the counts stay intact rather than wrapping to a second line. No overflow.","status":"rejected"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":4,"severity":"low","finding":"REJECTED as a coupling violation: `import { McpServer } from '@/modules/mcp/stores/mcpServer'` matches the established house pattern here — the sibling `capabilities.tsx:3` does exactly this and its header calls `McpServer` 'the sanctioned cross-module accessor', and `AgentStepForm.tsx:8` imports it too. Not a new cross-module reach. (The real problem is that ToolStepForm re-derives the server list itself instead of reusing the module's own helper, which is what causes the enabled-filter divergence logged separately under correctness-logic.)","status":"rejected"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":38,"severity":"low","finding":"REJECTED. `ToolArgumentsForm` takes the whole `WorkflowBuilderStore` purely to forward it to `RefInsertMenu`, which is a wider dependency than the component needs — but it is the same threading pattern `PromptField` already uses (builderFields.tsx:76-77), and `RefInsertMenu`'s own contract requires the store. Not worth changing, and not a coupling violation.","status":"rejected"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":120,"severity":"medium","finding":"The tool catalog is refetched over the network on EVERY step selection. `ToolCatalogStoreDef.use()` is a `defineLocalStore`, whose `use()` builds a fresh store per component INSTANCE (store-kit.ts:634-648: `useRef` seeded once per mount), and `StepConfigPanel` remounts the form on every switch via `<StepForm key={step.id}>` (StepConfigPanel.tsx:88). So `byServerId` starts empty on each mount, the store's own cache short-circuit (`existing && !existing.loading && existing.failure == null`, ToolCatalog.store.ts:59) can never hit, and clicking between two tool steps on the same server issues a `GET /api/mcp/servers/{id}/tools` each time — a call that for a stdio server can mean spawning/handshaking the server process. It is also visible: during each refetch `usePicker` is false, so the Tool field renders as a DISABLED plain Input and then swaps back to a Combobox, and the 'Additional arguments' section briefly shows every declared argument as a free key/value row before the effect re-partitions them.","status":"confirmed"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":81,"severity":"low","finding":"REJECTED (not a real cost). `attributeFindings` runs unmemoized twice in this panel and twice more in StepList on every render, and `commitRows`/`commitField` write the store on every keystroke. But `attributeFindings` is O(findings × steps) over a list the backend caps at 50 steps and a handful of findings, and the per-keystroke store write is the pre-existing builder pattern used by every other step form. No re-render storm and no expensive derive.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":239,"severity":"low","finding":"The 'Tool list unavailable' Alert is terminal — it offers no retry, so an `unreachable` failure (a server that was briefly down) can only be cleared by navigating away and back. The store ships `invalidate(serverId)` specifically for this, documented as 'used by \"try again\"' (ToolCatalog.store.ts:117-118), and it has no caller anywhere in the app; `markUnknown` and the `unknownNames` state it writes (ToolCatalog.store.ts:47/111) likewise have no production reader — grepped both UI workspaces. Either wire a retry action into this Alert or drop the unreachable store surface.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":247,"severity":"low","finding":"`spec.overflowNames` is only ever length-checked; the names themselves are never rendered. The note says 'Showing N of M arguments this tool accepts — the rest can be set below', but 'below' is a free key/value editor and `splitArguments` only creates a row for an overflow property that ALREADY has a saved value — so for a tool declaring >24 properties the author is told extra arguments exist and given no way to learn a single one of their names. The data to fix it is already computed and carried into the component.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/RefInsertMenu.tsx","line":17,"severity":"low","finding":"REJECTED. The new `compact` prop is reached on a production path — `ToolArgumentsForm.tsx:104` passes it for every generated field — and the labelled default branch is still used by `PromptField` (builderFields.tsx:111-116). Both branches live. Likewise `AttributedFinding.stepIndex`/`stepLabel` are consumed by `findingStepTitle`, and `WORKFLOW_LEVEL` is used inside `indexFindingsByStep`. No dead additions in these files.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":300,"severity":"high","finding":"coerceToDeclared runs the `isTemplateValue(raw)` early-return BEFORE the kind switch, so a json/textarea field whose text merely CONTAINS a reference is returned as a raw STRING instead of being JSON-parsed. Trigger: tool schema {\"properties\":{\"filters\":{\"type\":\"object\"}}} -> fieldKind='json' -> ToolArgumentsForm.tsx:177 is the only caller; the author types {\"userId\": \"{{ inputs.user }}\"}. TEMPLATE_RE matches, line 300 returns the raw string, so arguments.filters is persisted as the STRING '{\"userId\": \"{{ inputs.user }}\"}' rather than an object. The value silently changes type on the first keystroke, and the existing test only covers the whole-value case coerceToDeclared('{{ inputs.limit }}', ...). Partially-templated object/array arguments are the normal authoring pattern for a tool that takes structured input.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":145,"severity":"high","finding":"enumOptions (and choiceOptions, line 141) stringify every enum value with String(v) and ToolField.options carries no way back to the declared type, so a non-string enum is silently written to the workflow as a string. Trigger: {\"properties\":{\"level\":{\"type\":\"integer\",\"enum\":[1,2,3]}}}. fieldKind checks `enum` FIRST (line 157) so kind='select', not 'integer'; ToolArgumentsForm.tsx:151 does onChange(v) with the option's string value and nothing coerces it -> arguments.level === \"2\" where the tool declares an integer. Identical for a boolean enum ([true,false] -> \"true\") and for multiselect (ToolArgumentsForm.tsx:162 value.map(String); coerceToDeclared's multiselect arm is never reached from that control).","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":175,"severity":"medium","finding":"Neither standard spelling of a NULLABLE property is understood, so both degrade to a raw JSON textarea. (a) `type` as an array: FieldSchema.type is typed `string` (workflowElicitSchema.ts:47) and the switch at 175 has no array branch, so {\"q\":{\"type\":[\"string\",\"null\"]}} falls to `default:` -> 'json'; the line-164 `field.type === 'array'` test likewise misses [\"array\",\"null\"]. (b) anyOf-null: {\"q\":{\"anyOf\":[{\"type\":\"string\"},{\"type\":\"null\"}]}} (what FastMCP/pydantic emit for Optional[str], i.e. the most common real MCP tool schema) fails the line-161 `choices.every(c => 'const' in c)` test, then hits switch(undefined) -> 'json'. The author gets a 4-row JSON box for a plain optional string, and typing 1234 into it yields the NUMBER 1234 via coerceToDeclared's JSON.parse where the schema requires a string.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":160,"severity":"medium","finding":"`allOf` is not handled anywhere. Trigger: {\"properties\":{\"level\":{\"allOf\":[{\"$ref\":\"#/$defs/Level\"}],\"default\":\"info\"}}} — pydantic v1's spelling for an enum-typed field, and the standard way to attach a description to a $ref. There is no enum, no anyOf/oneOf and no `type` on the node, so fieldKind falls to `default:` -> 'json'. The referenced $defs entry (which DOES declare the enum) is never consulted because resolveSchema only follows a `$ref` that is a DIRECT key of the node, not one nested inside allOf. A closed value set renders as a free JSON box.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":127,"severity":"medium","finding":"resolveSchema discards the referencing node's OWN sibling keywords: when `$ref` is present it returns resolveSchema(target,...) and never merges the node back. Trigger: {\"properties\":{\"color\":{\"$ref\":\"#/$defs/Color\",\"title\":\"Line colour\",\"description\":\"Colour of the trend line\",\"default\":\"red\"}}} with $defs.Color = {\"type\":\"string\",\"enum\":[\"red\",\"blue\"]}. describeToolSchema:236-243 then reads title/description/default off the RESOLVED def, so the field is labelled from the shared def (or falls back to the raw key `color`), shows no description, and loses `default: \"red\"` entirely. $ref-alongside-annotations is legal and common in JSON Schema 2020-12.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":303,"severity":"medium","finding":"coerceToDeclared's `integer`, `number`, `switch` and `multiselect` arms are unreachable on the production path. The function has exactly ONE caller — ToolArgumentsForm.tsx:177 — inside the `case 'textarea': case 'json':` Textarea branch. Every other kind is edited by a typed control that calls onChange directly: InputNumber (line 140), Switch (128), Select (151), MultiSelect (163), and the default Input (190, which passes e.target.value uncoerced). So only kinds 'textarea' and 'json' ever reach the switch. The unit tests exercise the dead arms (toolSchemaForm.test.ts:156-158: coerceToDeclared('5', f('limit')) === 5, coerceToDeclared('true', f('verbose')) === true), which reads as coverage of a type-coercion step that never runs — and masks finding #2 above.","status":"confirmed"}
{"angle":"api-friendliness","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":227,"severity":"medium","finding":"ToolFormSpec.overflowNames is computed but its VALUES are never read — only `.length` (ToolArgumentsForm.tsx:247). The overflow note says 'the rest can be set below', yet the free key/value section below only pre-creates rows for keys that ALREADY have a value (ToolStepForm.tsx:174/179 -> splitArguments -> `extra`, and splitArguments:281 builds `declared` from spec.fields, i.e. the KEPT 24 only). So for a tool declaring >24 properties the author is told N more arguments exist, is NOT told their names — which this spec is holding in overflowNames — and must type each key from memory. That is precisely the 'never make the author type what the system can enumerate' failure the module header (lines 6-11) says it exists to prevent.","status":"confirmed"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":221,"severity":"low","finding":"SCHEMA_BUDGET.maxFields bounds the RENDERED fields but the cap is applied after the whole property set has been materialised, and the unbounded remainder is retained. describeToolSchema builds Object.keys(props), then two full `filter` passes, then `ordered.slice(24)` into overflowNames — which is kept alive on the spec for the lifetime of the memo despite never being read (see the api-friendliness row). A tool returning a 5,000-property input_schema allocates four full-length arrays per re-derivation. Bounded in practice by the HTTP response size, hence low, but the cap is in the wrong place: it should bound the enumeration, not just the render.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":183,"severity":"low","finding":"The textarea heuristic promotes a field to a multi-line control based on its DESCRIPTION length, not the value's: `(field.description?.length ?? 0) > 160`. Trigger: {\"name\":{\"type\":\"string\",\"maxLength\":40,\"description\":\"<180 chars of prose>\"}} — a 40-char single-line name renders as a 3-row Textarea (ToolArgumentsForm.tsx:170-175) purely because the tool author wrote a thorough description. maxLength (40) explicitly says otherwise and is ignored in that branch.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":110,"severity":"low","finding":"lookupRef uses `name in bucket`, which walks the prototype chain, so a $defs pointer at an inherited key resolves to a prototype member. Trigger: {\"$defs\":{},\"properties\":{\"x\":{\"$ref\":\"#/$defs/__proto__\"}}} -> bucket['__proto__'] is Object.prototype, which isRecord() accepts, so it is returned as the field's FieldSchema and rendered (harmlessly, as 'json'). '#/$defs/constructor' and '#/$defs/toString' return functions and are rejected by isRecord, so nothing throws and there is no pollution — but the lookup should be Object.prototype.hasOwnProperty.call(bucket, name) so an attacker-influenced pointer cannot silently resolve to something the tool never declared.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":208,"severity":"low","finding":"A root-level `$ref` is not resolved. describeToolSchema reads inputSchema['properties'] directly, so {\"$ref\":\"#/$defs/Input\",\"$defs\":{\"Input\":{\"type\":\"object\",\"properties\":{...},\"required\":[...]}}} yields props === undefined -> returns null -> the whole tool falls back to hand-typed key/value pairs. The schema is fully self-contained and this module's own resolveSchema could dereference it in one call; the fallback is taken for a schema it is capable of reading.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":54,"severity":"low","finding":"Adding a ToolFieldKind requires edits in three places and only two are type-checked. `fieldKind` (line 152) returns the kind and `ToolArgumentsForm.tsx:121` switches on it — but that switch ends in a `default:` (line 185) returning a plain Input, so a new union member compiles clean and silently renders as an untyped text box. coerceToDeclared (line 303) likewise has a `default:`. A `satisfies`-exhaustive mapping (or removing the two `default:` arms in favour of explicit cases) would make a new kind a compile error rather than a silent degradation.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":191,"severity":"medium","finding":"resolveFindingStep takes loc.split('.')[0], but the location for WORKFLOW_BAD_STEP_ID (validate.rs:624) is the RAW step id — which by definition may be malformed. Trigger: a YAML-authored workflow with `- id: report.final` alongside `- id: report`, loaded into the builder (WorkflowBuilder.store.ts:159 load -> runValidate). The finding's location is 'report.final', head = 'report', so the copy \"This step's internal name must start with a lowercase letter...\" is attached to the WRONG step (or, with no 'report' step, to 'Whole workflow'). StepList.tsx:68 then flags the wrong row as invalid. The one finding whose job is to point at a bad id is the one that cannot.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":190,"severity":"low","finding":"The outputs guard is `loc.startsWith('outputs[')`, but WORKFLOW_DUPLICATE_OUTPUT_NAME (validate.rs:857) passes the BARE output name as its location (&o.name), not an outputs[...] path — only ref_check.rs:189 uses the bracketed form. So an output named the same as a step (the canonical pattern: outputs: [{name: summary, from: '{{ summary.output }}'}] next to `- id: summary`) would attribute a whole-workflow output finding to that step. LATENT ONLY: BuilderDef/toWorkflowDef (WorkflowBuilder.store.ts:25-63) never send `outputs`, so this code cannot fire from the builder today — it is guarded by an omission elsewhere, not by this function.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":126,"severity":"medium","finding":"WORKFLOW_TEMPLATE_SYNTAX interpolates the RAW backend message into author-facing copy, violating this file's own stated rules at lines 52-56 ('never name a YAML key, a Rust field, or a wire type') and the drift guard's stated purpose. The interpolated text is TemplateError's Display impl (template.rs:46-63), e.g. \"unknown filter 'foo' (supported: json, raw)\" or \"cannot access field 'x' on a non-object value (path 'a.b')\", so the person reads: \"A reference in this step isn't written correctly (cannot access field 'x' on a non-object value (path 'a.b')).\" The backend test only checks that the CODE key exists in this file, so it cannot catch a humanisation that re-emits the wire message verbatim.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":93,"severity":"low","finding":"WORKFLOW_PROMPT_FILE_UNSAFE (line 93) and WORKFLOW_PROMPT_FILE_ESCAPE (line 95) carry BYTE-IDENTICAL copy, erasing the distinction the two codes exist to make. Backend: UNSAFE (validate.rs:1045) fires on a literal '..' or leading '/' in prompt_file — the author typed a bad path; ESCAPE (validate.rs:1059) fires when a syntactically fine relative path CANONICALIZES outside the bundle — a symlink out of the bundle. The fix differs (edit the path vs. remove the symlink) but the sentence is the same.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":77,"severity":"low","finding":"WORKFLOW_STEP_DESCRIPTION_TOO_LONG says 'shorten it to under 200 characters', but validate.rs:645 rejects only `desc.chars().count() > MAX_STEP_DESCRIPTION_CHARS` (=200, validate.rs:272) — exactly 200 characters is VALID. The instruction is off by one and tells the author to delete a character that is allowed.","status":"confirmed"}
{"angle":"api-friendliness","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":73,"severity":"low","finding":"WORKFLOW_BAD_STEP_ID (\"This step's internal name must start with a lowercase letter...\") and WORKFLOW_DUPLICATE_STEP_ID (\"rename one of them\", line 75) instruct the author to edit a step id, but the builder exposes no step-id control anywhere: StepConfigPanel.tsx renders description/depends_on plus the per-kind form, stepForms.ts mints ids in createStep (line 95-96), and no component patches `id`. Both findings are only reachable for a YAML-imported workflow and are unfixable from the surface that displays them — the copy asks for an action the UI does not offer.","status":"confirmed"}
{"angle":"extensibility","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":61,"severity":"low","finding":"Four backend limits are hardcoded into English sentences here with no drift guard: '50 is the limit' (line 62 vs validate.rs:610), 'under 200 characters' (78 vs MAX_STEP_DESCRIPTION_CHARS=200, validate.rs:272), 'the longest allowed wait is 30 minutes' (107 vs ELICIT_TIMEOUT_HARD_CAP_MS=1_800_000, validate.rs:279), 'the maximum is 20' (109 vs MAX_PARALLEL_HARD_CAP=20, validate.rs:270). All four are CORRECT today (verified), but validation_codes_are_registered_and_humanised (validate.rs:2004) only asserts the quoted CODE literal is present in this file — it never compares the numbers. Bumping any cap silently makes the author-facing copy lie, in the one file whose whole purpose is a checked humanisation contract.","status":"confirmed"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":196,"severity":"low","finding":"severityOf defaults an ABSENT severity to 'error', while the generated contract declares it optional (api-client/types.ts:7440 `severity?: Severity`). StepList.tsx:39-41 concatenates attributeFindings(validation.errors) with attributeFindings(validation.warnings) and then relies solely on this field for the per-step badge counts, discarding the authoritative signal (which array the finding arrived in). If the backend ever adds skip_serializing_if to ValidationError.severity — legal under the generated type — every warning would be counted and badged as a blocking error. Passing the known severity into attributeFindings would make the wrong state unrepresentable.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":161,"severity":"low","finding":"Two exports have no production consumer. HUMANISED_CODES (line 161) is imported only by validationCopy.test.ts:7 — the Rust drift guard (validate.rs:2046) greps this file's raw TEXT for `\"CODE\"` literals and never touches this export, so its stated role ('drift-guard surface') is not what the guard actually uses. WORKFLOW_LEVEL (line 234) is written by indexFindingsByStep (241) but that bucket is never read: StepList.tsx:68 only does findingsByStep.get(step.id), and BuilderValidationPanel.tsx:81-82 re-derives from attributeFindings + findingStepTitle. The workflow-level counts are computed on every render and discarded.","status":"confirmed"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":85,"severity":"medium","finding":"A server-side 403 is misclassified as 'unreachable'. mcp/handlers/runtime.rs:79-87 returns 403 USER_NO_ACCESS 'You do not have access to this server' when the caller HOLDS mcp_servers::read but lacks per-server access — a condition hasPermissionNow(Permissions.McpServersRead) at line 61 structurally cannot detect. The catch at 85 labels every throw 'unreachable', so failureMessage:167 renders the self-contradicting sentence: `Couldn't reach \"web_search\" (You do not have access to this server) — enter the tool name and arguments by hand.` The 'no-permission' variant exists for exactly this case and is only ever reached via the client-side global-permission pre-check. Branching on `(error as {status?: number}).status === 403` (the api-client stamps it, core.ts:729) would classify it correctly.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":167,"severity":"medium","finding":"failureMessage's 'unreachable' arm interpolates a raw api-client Error.message into an author-facing sentence. sdk/packages/framework/src/api-client/core.ts:678 defaults errorMessage to `HTTP error! status: ${response.status}` and line 712 to `HTTP error! status: 502 - ${textResponse}` when the body is not JSON. So a proxy returning an HTML error page produces: `Couldn't reach \"web_search\" (HTTP error! status: 502 - <!DOCTYPE html><html>...) — enter the tool name and arguments by hand.` — a machine code plus an unbounded third-party document pasted into an Alert description (ToolStepForm.tsx:244). The store's own doc (line 22-23) says every failure maps to VISIBLE author-facing copy; this one maps to a transport dump.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":12,"severity":"medium","finding":"The store's stated contract — 'refetching once per builder mount is CORRECT' (lines 12-15) — does not hold. Its only consumer is ToolStepForm, which StepConfigPanel.tsx:88 mounts as `<StepForm key={step.id} ...>`; defineLocalStore.use (store-kit.ts:632-645) creates the zustand api inside a useRef that is re-initialised on every remount. So a FRESH catalog instance is created on every step selection, not once per builder. Repro: a workflow with two tool steps on the same server — clicking step A, step B, step A issues three GET /api/mcp/servers/{id}/tools calls, each of which may spawn/handshake a stdio MCP server. Both byServerId and the `inflight` Set (line 52) are per-instance, so neither the cache nor the coalescing guard can dedupe across selections, and the byServerId map (documented at 41-43 as letting 'two names for one server share a fetch') can only ever hold one entry.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":111,"severity":"medium","finding":"Three declared members are never used anywhere in either UI workspace (verified by repo-wide grep for markUnknown / unknownNames / .invalidate on this store). `markUnknown` (111) has no caller and `unknownNames` (47) is read by nothing — the 'unknown-server' failure it was meant to feed is produced independently by entryForServerName:148-153. `invalidate` (118) also has no caller, and its doc comment cites a \"try again\" affordance that does not exist: ToolStepForm renders the failure as a static Alert (line 239-246) with no retry control. Roughly a fifth of the store's surface is unreachable, and the invalidate doc documents behaviour the UI does not offer.","status":"confirmed"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":118,"severity":"low","finding":"invalidate() deletes the cache entry but does NOT clear `inflight`, so calling it during a fetch leaves that serverId registered: the next load() short-circuits at line 56 and returns with no fetch, and the still-running pre-invalidation request then writes its (by definition stale) result into byServerId at 78-101. The repo's own core/coalescedLoader.ts documents this exact bug and solves it with a generation counter ('Clearing only the cache would hand the next reader the still-registered pre-invalidation promise', lines 16-20). Currently LATENT because invalidate has no callers — it becomes live the moment the documented 'try again' button is added.","status":"confirmed"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":52,"severity":"low","finding":"The store hand-rolls cache + in-flight coalescing that src-app/ui/src/core/coalescedLoader.ts already provides — a module explicitly extracted for reuse ('Reusable by any other \"several stores want the same list on boot\" case', lines 6-9) — and re-implements it with strictly weaker semantics: no TTL, no generation guard on a superseded request (see the invalidate row), and a second caller gets `undefined` back rather than the shared promise (line 56 returns nothing, so `await catalog.load(...)` resolves before the data exists). Either adopt the loader per serverId or state in the header why per-server keying rules it out.","status":"confirmed"}
{"angle":"api-friendliness","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":55,"severity":"low","finding":"load(serverId, serverName) takes the name purely to embed it in a failure message, and nothing links the two arguments. The caller must independently keep them consistent — ToolStepForm.tsx:137 passes `serverId` (derived by entryForServerName from step.server) and `step.server` separately; entryForServerName already resolved BOTH from the same match object and could return the pair. A caller that passes a stale name after the author renames a server produces a failure Alert naming the wrong server, with no compile-time or runtime check.","status":"confirmed"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":165,"severity":"low","finding":"Apostrophe style is inconsistent inside this one change. failureMessage uses ASCII apostrophes throughout — \"You don't have permission to list this server's tools\" (165), \"Couldn't reach\" (167), \"which isn't one of the servers available to you\" (163) — while the sibling component added in the same diff writes the same possessive typographically: ToolStepForm.tsx:275 'Loading this server\\u2019s tools\\u2026'. The two sentences appear in the same control within seconds of each other. ASCII is the dominant style in the workflow module (only 4 U+2019 occurrences module-wide), so ToolStepForm is the outlier — but the pair should be settled one way before this ships.","status":"confirmed"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":242,"severity":"low","finding":"REJECTED — falsy defaults. Checked default:false / default:0 / default:\"\" end to end. ToolField.default is `unknown` and assigned unguarded (`default: schema.default`, line 242), the consumer gates on `field.default !== undefined` (ToolArgumentsForm.tsx:73), and valueToText(false)==='false' / valueToText(0)==='0' (line 327-331 falls to JSON.stringify for non-strings). Falsy defaults survive intact. Only default:\"\" renders the empty sentence 'Defaults to .', which is cosmetic and lives in the consumer, not here.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":129,"severity":"low","finding":"REJECTED — self-referential $ref infinite recursion. Verified terminating: with $defs.Node = {\"$ref\":\"#/$defs/Node\"} the first hop pushes the ref onto `stack`, the second hits `stack.includes(ref)` (line 129) and returns null; a long non-cyclic chain is cut by stack.length >= maxRefDepth. Mutual recursion (A->B->A) is cut identically. resolveSchema also never recurses into nested `properties`, so there is no second unbounded axis. The explicit-stack claim in the header holds.","status":"rejected"}
{"angle":"error-handling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":206,"severity":"low","finding":"REJECTED — hostile/malformed input throwing. Constructed and traced: inputSchema as null/string/array/number; properties as a string or array; required containing non-strings ([1,null]); enum as a non-array; anyOf/oneOf as a string; items as a tuple ARRAY (`\"items\":[{...}]`); items.enum a non-array; enumNames shorter than enum or a string; maxLength a string. Every one is guarded by isRecord / Array.isArray / optional chaining, and none throws or returns a shape ToolArgumentsForm crashes on — the documented 'degrade to the escape hatch, never throw' contract (lines 199-205) holds.","status":"rejected"}
{"angle":"correctness-logic","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":254,"severity":"low","finding":"REJECTED — TEMPLATE_RE regex statefulness. The literal has no `g`/`y` flag, so RegExp.prototype.test carries no lastIndex across calls; isTemplateValue is deterministic per input despite the shared module-level regex. Also checked the lazy `[\\s\\S]*?` against a value containing many `{{`: no catastrophic backtracking (no nested quantifier).","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":59,"severity":"low","finding":"REJECTED — 'a failed fetch is cached forever'. The cache-hit guard requires `existing.failure == null` (line 59), so a failed OR no-permission entry always falls through and re-fetches on the next load(). Also checked for a permanently-loading entry: inflight is cleared in `finally` (102-104) and both the success and catch paths set loading:false, so no path leaves loading:true. What actually re-triggers the retry is a remount or a serverId change (ToolStepForm.tsx:136-139 deps), which per the confirmed lifecycle row happens on every step switch — so retries are frequent, not absent.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":78,"severity":"low","finding":"REJECTED — set() after unmount writing to a dead consumer. defineLocalStore (store-kit.ts:632-658) holds the zustand api in a useRef and its unmount cleanup only runs __destroy__, which detaches EventBus listeners; it does not tear down the store api. A late set() therefore writes to a store with no remaining subscribers — no React state-update-on-unmounted warning, no leak beyond the instance itself being GC'd with the component.","status":"rejected"}
{"angle":"concurrency-lifecycle","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":57,"severity":"low","finding":"REJECTED — cache-hit check running BEFORE the permission check (57-59 vs 61). Order does matter in principle: an entry fetched while permitted is still served after a mid-session permission loss. But the entry was legitimately fetched, contains only tool names/descriptions the user was already shown, and the whole store instance is destroyed on the next step switch. No leak of anything not already disclosed; not worth a change.","status":"rejected"}
{"angle":"performance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":209,"severity":"low","finding":"REJECTED — attributeFindings scanning `steps` twice per finding (steps.find inside resolveFindingStep at 192, then steps.findIndex at 211). O(findings x steps), and the backend caps a workflow at 50 steps (validate.rs:610), so the worst case is a few thousand string comparisons per validation round, re-run on a 400ms debounce. Measurably free; a Map<id,index> would be premature.","status":"rejected"}
{"angle":"api-contract","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":78,"severity":"low","finding":"REJECTED — response-shape mismatch. Checked against the generated contract: ListToolsResponse.tools is NON-optional (api-client/types.ts:3082-3084), so `res.tools ?? []` (line 80) is redundant but harmless, not a nullable-treated-as-non-null bug. Tool.input_schema is `unknown` (types.ts:6542-6546) and describeToolSchema's parameter is `unknown` — an exact match, and every narrowing is done defensively inside. Tool.description is optional and is handled with a ternary at ToolStepForm.tsx:145. No contract violation found in these three files.","status":"rejected"}
{"angle":"modularity-coupling","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":1,"severity":"low","finding":"REJECTED — vocabulary duplication in toolSchemaForm. It imports FieldSchema/TitledChoice from ../workflowElicitSchema rather than declaring a second JSON-Schema type set, imports nothing outside the workflow module, and has no React/store dependency (it is pure and directly unit-tested). The one real consequence of that reuse is that FieldSchema.type is `string` and so cannot express a type ARRAY — logged separately under correctness-logic, not as a coupling defect.","status":"rejected"}
{"angle":"i18n-copy","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":58,"severity":"low","finding":"REJECTED — mixed voice in HUMAN_COPY. Entries alternate between declarative ('This step needs a prompt.') and imperative ('Choose the server this step should call.'), but the split tracks a real distinction — describing a broken state vs. naming the single action that fixes it — and every entry stays one sentence with a consistent em-dash-before-the-remedy pattern. Style preference, not a defect. Also checked every entry for a leaked machine code or YAML/Rust key: none found except WORKFLOW_TEMPLATE_SYNTAX, logged separately.","status":"rejected"}
{"angle":"extensibility","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":58,"severity":"low","finding":"REJECTED — HUMAN_COPY as a closed map every new code must edit. Verified this is the intended, well-supported design, not drift-prone: adding a code is a ONE-place edit here, an unmapped code degrades to the backend message (humaniseFinding:172-174) rather than to nothing, and validate.rs:2004 fails the backend suite in BOTH directions (emitted-but-unregistered, and registered-but-unhumanised). Cross-checked all 41 registered VALIDATION_CODES against HUMAN_COPY's 41 keys and the /validate-def handler (handlers/dev.rs:938-963, which only calls validate_collecting, i.e. validate.rs + ref_check.rs): every code this surface can emit is mapped. No gap.","status":"rejected"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":42,"severity":"low","finding":"REJECTED — SCHEMA_BUDGET / ToolFormSpec / ToolField as unused exports. SCHEMA_BUDGET is exported for toolSchemaForm.test.ts:129-135 (which derives the overflow case from it rather than hardcoding 24) — a legitimate test seam, and it is read on the production path at lines 226-227 and 129. ToolField/ToolFormSpec are imported by ToolArgumentsForm.tsx:16-17. isTemplateValue, splitArguments, valueToText and describeToolSchema all have production callers (ToolArgumentsForm.tsx, ToolStepForm.tsx:15/152/174). Only the coerceToDeclared arms and overflowNames' contents are genuinely unreached, logged separately.","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":160,"severity":"high","finding":"INV-6 promises the free-text fallback is 'never the default and never silent'; in the state 'server chosen, reachable, serves ZERO tools' the fetch SUCCEEDS so entry.failure stays null, blockingFailure is null (no Alert), and usePicker is false purely because toolOptions.length === 0 — so the code silently renders the free-text Tool Input (line 269) with the generic description 'The exact name of the tool to call on that server.' and no stated reason. The author cannot tell an empty-catalog server from a working picker.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":292,"severity":"high","finding":"INV-6 names 'a tool may declare no schema' as one of the TWO cases that must carry a documented, visible reason; in the state 'tool chosen but describeToolSchema returns null' (absent schema, non-object schema, or zero declared properties — toolSchemaForm.ts:206-212) the code sets useGenerated=false and falls back to the generic key/value 'Arguments' editor with description 'Key/value pairs passed to the tool…' and NO explanation of why the generated form is absent. The unreachable-server case gets an Alert (line 239); the no-schema case gets nothing.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":258,"severity":"high","finding":"INV-3 makes the Tool field a picker and INV-6 forbids silent degradation; in the state 'saved workflow whose step.tool is NOT among the fetched tools' (renamed/removed upstream tool) the kit Combobox resolves selection as `byValue.get(current) ?? null` (sdk/packages/kit/src/kit/combobox.tsx:73), so the field renders EMPTY showing the placeholder while step.arguments/step.tool still hold the stale name. The stored value is neither shown nor flagged, and validation cannot catch it (WORKFLOW_TOOL_NO_TOOL only fires on an EMPTY tool), so the author sees a blank Tool field on a workflow that will still call the old tool.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/WorkflowBuilderPage.tsx","line":83,"severity":"high","finding":"INV-1 promises 'no raw schema/YAML key language ever reaches the person building a workflow'; the humanisation is applied ONLY inside BuilderValidationPanel. On the Save path the server runs validate_for_install (validate.rs:501-524) and returns AppError message '[semantic/WORKFLOW_PROMPT_MISSING] agent_1: step has neither prompt: nor prompt_file:', which the page surfaces verbatim via message.error(e.message) (and the store stores it raw at WorkflowBuilder.store.ts:338-343). Reachable whenever store.validation is null or stale — never-validated, a transient /validate-def failure (the store deliberately keeps the prior validation, line 106-109), the 400ms debounce race, or the prompt_file divergence the /validate handler documents (handlers/dev.rs:98-105 resolves prompt_file against a throwaway temp dir while install resolves against the real bundle root). humaniseFinding is never called on this path.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":147,"severity":"medium","finding":"INV-6 requires the fallback's stated reason to be TRUE; entryForServerName resolves against McpServer.servers, whose initial state is `[]` (modules/mcp/stores/mcpServer/state.ts:6). While that list is still loading, an existing tool step's valid server resolves to the `unknown-server` failure and the author is shown 'This step points at a server called \"X\", which isn't one of the servers available to you' (line 163) plus the free-text fallback — a false, alarming reason in a transient state that then flips to the picker. The store already distinguishes a loading catalog (entry.loading); it does not distinguish a loading SERVER LIST.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":63,"severity":"medium","finding":"INV-1's rule for HUMAN_COPY is 'say what the PERSON must do'; the new copy for WORKFLOW_SANDBOX_FLAVOR_REQUIRED ('the workflow needs a sandbox environment chosen for it') and WORKFLOW_UNKNOWN_FLAVOR ('pick one of the installed environments') direct an action the builder cannot perform: BuilderDef has no `sandbox` field and toWorkflowDef never emits one (WorkflowBuilder.store.ts:25-30, 51-63), while AddStepMenu still offers the 'Run code' step (stepForms.ts:59). Every builder workflow containing a sandbox step therefore validates with a blocking error whose humanised instruction points at a control that does not exist, permanently blocking Save. Root cause is pre-existing; the branch's new copy asserts the missing affordance as if it were present.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":158,"severity":"medium","finding":"INV-6 says the fallback is 'never the default'; in the ordinary INITIAL state (no server chosen) blockingFailure deliberately excludes the 'no-server' failure, so no Alert renders and toolOptions is empty — the author's FIRST view of the Tool field is the free-text Input, i.e. the escape hatch IS the default. The store already authored the correct copy for this state ('Pick a server first to see the tools it offers.', ToolCatalog.store.ts:161) but the component never renders it; a disabled picker carrying that sentence would satisfy the invariant.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":295,"severity":"medium","finding":"INV-4 promises one typed field per declared property and INV-6 requires the fallback's reason to be stated correctly; in the state 'tool declares more than SCHEMA_BUDGET.maxFields (24) properties', the overflow properties spill into the section labelled 'Additional arguments' whose description reads 'Extra values this tool did not declare. Usually empty.' — which is false for those keys, since the tool DID declare them. The overflow note in ToolArgumentsForm.tsx:248-251 says 'the rest can be set below', contradicting the section's own description.","status":"confirmed"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":168,"severity":"rejected","finding":"Candidate: humaniseFinding falls back to the raw backend message for an unknown code (line 173), leaking wire vocabulary. REJECTED as a finding in its own right — the design explicitly accepts this as the test-prevented case, and the backend drift guard (validate.rs:1985-2062) fails the suite when a registered/emitted code has no copy. The residual hole is logged separately under wired-and-behaving (the scanner's layer filter).","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":69,"severity":"rejected","finding":"Candidate: INV-5 (templating survives) not upheld in typed controls. REJECTED — verified upheld in every generated kind: isTemplateValue (toolSchemaForm.ts:263) is type-agnostic, a templated value renders as a text Input for switch/number/select/multiselect/json alike (lines 111-120), coerceToDeclared passes a template through untouched (toolSchemaForm.ts:300), a compact RefInsertMenu is attached to every field's label row, and the switch back to a typed control is one click (lines 83-94).","status":"rejected"}
{"angle":"design-conformance","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":275,"severity":"rejected","finding":"Candidate: a saved workflow with schema-undeclared arguments loses them on edit. REJECTED — splitArguments preserves them in `extra`, the free rows are seeded from `extra` when a schema exists (ToolStepForm.tsx:178-180), and both commit paths merge known+rows so neither half clobbers the other (lines 203-216). State verified upheld.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":111,"severity":"medium","finding":"`markUnknown` and the `unknownNames` state it writes (line 47) have NO production caller and no test caller — a repo-wide grep finds only the definition. The 'unknown server name' case is instead handled purely by entryForServerName's derived failure, so this action and its state are dead.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":118,"severity":"medium","finding":"`invalidate` is documented as 'used by \"try again\"' but has no caller: the unreachable-server Alert (ToolStepForm.tsx:239-246) is a bare warning with no retry action, and the entry is cached only for the component's lifetime. The design's INV-6 escape hatch therefore has no recovery path — an author whose server was momentarily down must unmount and remount the step form to get the picker back.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/stores/ToolCatalog.store.ts","line":12,"severity":"medium","finding":"The store documents itself as a 'PRIVATE, per-builder catalog' whose id-keying means 'two names for one server share a fetch' and whose `inflight` guard exists because 'several fields read the same server in one render pass'. But defineLocalStore().use() allocates a store per COMPONENT INSTANCE (sdk/packages/framework/src/store-kit.ts:629-645) and StepConfigPanel remounts the step form on every selection via `key={step.id}` (StepConfigPanel.tsx:88) — so the catalog is per-tool-step-mount, not per-builder: listTools refires every time the author clicks between steps, and no cache or in-flight coalescing ever spans two steps.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/server/src/modules/workflow/validate.rs","line":1965,"severity":"medium","finding":"The design calls the humanisation drift guard 'the guard that makes this real'. The emit-site scanner only records a code when the FIRST string literal in the call is one of \"schema\"|\"semantic\"|\"security\"; a future ValidationError emitted on any other layer contributes nothing and fails no assertion, so it would ship with the raw wire message reaching the author. The test asserts the scanner found >0 codes overall, which does not detect a single silently-skipped emit site.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":265,"severity":"low","finding":"`loading={entry.loading}` on the Combobox is unreachable: usePicker (line 160) already requires `!entry.loading`, so the picker only renders when loading is false. The loading state is handled entirely by the fallback Input's disabled/placeholder branch (lines 274-277), leaving the Combobox's loading skeleton dead.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/validationCopy.ts","line":161,"severity":"low","finding":"`HUMANISED_CODES` is exported as 'the drift-guard surface' but has no production consumer; the actual guard is the Rust test's TEXTUAL scan of the file for `\"CODE\"` literals (validate.rs:2040-2046), which never reads this export. Its only consumer is validationCopy.test.ts.","status":"confirmed"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":76,"severity":"rejected","finding":"Candidate: the per-step invalid markers are computed but never reached. REJECTED — verified wired: StepList reads store.validation, derives counts through the same attributeFindings/indexFindingsByStep the panel uses, and renders them at lines 115-138; the populated + problems gallery cells exercise them.","status":"rejected"}
{"angle":"wired-and-behaving","file":"src-app/ui/src/dev/gallery/coverage.ts","line":487,"severity":"rejected","finding":"Candidate: the coverage reasons cite builder-tool-picker.spec.ts, which is absent from the committed diff. REJECTED — the file exists on disk (src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts, currently untracked alongside builder-validation-attribution.spec.ts and the tools mock-server helper), so the claim is true of the working tree; committing them is a process matter outside this audit's product-diff scope.","status":"rejected"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":236,"severity":"low","finding":"DESIGN_SYSTEM.md's titled-section convention (SectionHeader for a titled block; the module's own StepList uses it) is applied to one half of the arguments UI only: the generated group is a bare `div className=\"flex flex-col gap-4\"` with no title, while its sibling fallback block is a titled LabeledControl. The two halves of the same concept render at different structural levels.","status":"confirmed"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":1,"severity":"rejected","finding":"Candidate: hardcoded colors / raw Tailwind hues / arbitrary values / inline style colors in the new UI. REJECTED — a sweep of ToolStepForm, ToolArgumentsForm, BuilderValidationPanel, StepList, RefInsertMenu and gallery.tsx finds zero raw hues, zero hex literals, zero arbitrary bracket values and zero inline style colors; every color is a semantic token (text-destructive / text-warning / text-success / text-muted-foreground / bg-muted / border-primary / bg-accent).","status":"rejected"}
{"angle":"design-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":99,"severity":"rejected","finding":"Candidate: physical direction properties / off-grid spacing. REJECTED — the same sweep finds no pl-/pr-/ml-/mr-/text-left/text-right/left-/right- utilities (StepList uses ps-0, the finding rows use text-start), and every spacing utility is on the 4px scale or the kit's sanctioned 2px half-step (mt-0.5).","status":"rejected"}
{"angle":"design-in-context","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":293,"severity":"medium","finding":"In the schema-driven state the author reads: untitled typed fields, then a section titled 'Additional arguments'. 'Additional' presupposes a preceding section called 'Arguments' that is never rendered (ToolArgumentsForm.tsx:236 has no heading) — the label is stranded from the title it refers to, and the same block silently renames itself to 'Arguments' in the fallback state, so the two states of one surface disagree about what the section is.","status":"confirmed"}
{"angle":"design-in-context","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":334,"severity":"low","finding":"One form offers two different templating affordances side by side: every generated field carries a compact RefInsertMenu on its label row (ToolArgumentsForm.tsx:95-105), while the key/value rows immediately below — which take the same {{ }} references — offer only a placeholder hint ('value or {{ reference }}') and no menu. In the fallback state the menu disappears entirely, so the discoverable way to insert a reference exists exactly where the schema happened to be readable.","status":"confirmed"}
{"angle":"design-in-context","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":39,"severity":"rejected","finding":"Candidate: the finding rows duplicate the step-list's identity chrome (index + label), so a count/label belongs in the container title instead. REJECTED — the design explicitly requires each finding to NAME its step because the panel sits away from the step list (DESIGN §2.2); repeating 'Step 3 · <label>' is the invariant, not duplication.","status":"rejected"}
{"angle":"precedent-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/toolSchemaForm.ts","line":152,"severity":"medium","finding":"The design names WorkflowElicitForm + workflowElicitSchema as the generated form's twin. Only the FieldSchema/TitledChoice TYPES were reused; the schema→control derivation was re-implemented: fieldKind (lines 152-195), choiceOptions (141) and enumOptions (145) duplicate the already-shipped isMultiSelectField / isTitledSelectField / selectOptions in WorkflowElicitForm.tsx (lines ~35-63), which handle the identical enum / anyOf / oneOf / items-enum cases. Two independent readings of the same JSON-Schema subset now exist in one module and can drift.","status":"confirmed"}
{"angle":"precedent-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":141,"severity":"low","finding":"The design names CapabilitySelect (capabilities.tsx) as the tool picker's twin. The Tool picker was instead inlined in ToolStepForm — options built at lines 141-148 and the control at 258-268 — rather than added as a sibling export next to CapabilitySelect. The two entity pickers stacked on the same form are also different primitives with different label conventions: Server is a Select labelled `display_name || name`, Tool is a Combobox labelled `name — description`, so adjacent fields for the same class of value read and behave differently.","status":"confirmed"}
{"angle":"precedent-fidelity","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":54,"severity":"rejected","finding":"Candidate: the findings list hand-rolls a clickable row instead of mirroring StepList's rows. REJECTED — it is a real kit Button (semantic, keyboard-reachable), which is if anything stricter than the twin: StepList's rows are `li` elements with an onClick (StepList.tsx:71-88). Diverging toward the accessible primitive is not a precedent violation.","status":"rejected"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":463,"severity":"high","finding":"The cell's own comment claims 'Seeded through the tool CATALOG store so no network call is made — the same shape listTools returns', but the surface never touches ToolCatalogStoreDef: it imports describeToolSchema/splitArguments and renders ToolArgumentsForm in isolation with a hand-derived spec (lines 465-497). Consequence for a design review: the schema-driven state of the ACTUAL tool-step surface (picker + generated form + 'Additional arguments' together) has no gallery cell, and INV-3's Tool PICKER is never rendered in the gallery at all — the only ToolStepForm cell is the unreachable-server fallback (line 506), which deliberately renders the free-text branch. The gallery therefore shows the escape hatch and never the primary path the design exists to establish.","status":"confirmed"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/dev/gallery/stateMatrix.generated.ts","line":4086,"severity":"medium","finding":"New conditional render states registered by this branch have no gallery cell: ToolStepForm's `usePicker` fork (signal at ToolStepForm.tsx:258 — the picker itself), its `entry.loading` fallback (lines 274-277, not even signalled), and ToolArgumentsForm's `spec.overflowNames.length > 0` note (signal at line 247). Every one of them is a state the design's invariants turn on, and none can be seen in a design review.","status":"confirmed"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":365,"severity":"low","finding":"The two `errors[]` entries omit `severity`, which the backend ALWAYS serializes (ValidationError.severity is a plain field, validate.rs:429-430; ::at/::err set Severity::Error). The panel's severityOf defaults correctly so nothing renders wrong, but the fixture is still not byte-shaped like a real /validate-def response — the same class of drift the branch's own comment says it is fixing.","status":"confirmed"}
{"angle":"gallery-fidelity","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":355,"severity":"rejected","finding":"Candidate: the validation fixtures carry invented codes / pre-humanised prose. REJECTED — verified faithful against the backend: WORKFLOW_PROMPT_MISSING + 'step has neither prompt: nor prompt_file:' at location `agent_1` matches validate.rs:683-687 (`&s.id`); WORKFLOW_TOOL_NO_TOOL + 'tool step has empty tool:' matches validate.rs:728-732; WORKFLOW_REF_FIELD_UNRESOLVED's message and warning severity match ref_check.rs:362-370 (::warn) with a `<step>.<field>` location. The branch removed the previously-invented `unresolved_reference`/layer `graph` fixture.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/dev/gallery/coverage.ts","line":487,"severity":"info","finding":"SCOPE NOTE — codes/layers/messages of the two error fixtures were cross-checked line-by-line against src-app/server/src/modules/workflow/validate.rs and ref_check.rs; results in the rows below.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":366,"severity":"low","finding":"VERIFIED REAL: fixture {code:'WORKFLOW_PROMPT_MISSING', layer:'semantic', message:'step has neither prompt: nor prompt_file:'} matches validate.rs:682-687 verbatim (ValidationError::at(\"semantic\",\"WORKFLOW_PROMPT_MISSING\",\"step has neither prompt: nor prompt_file:\",&s.id)); code is registered in VALIDATION_CODES validate.rs:368. No finding.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":372,"severity":"low","finding":"VERIFIED REAL: fixture {code:'WORKFLOW_TOOL_NO_TOOL', layer:'semantic', message:'tool step has empty tool:'} matches validate.rs:726-733 verbatim; code registered validate.rs:377. Location 'tool_1' is a bare step id, which is what ValidationError::at(&s.id) emits. No finding.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":382,"severity":"low","finding":"VERIFIED REAL: warning location 'summarize.prompt' matches the loc format ref_check.rs:139/149/174 `format!(\"{}.prompt\", s.id)`; code WORKFLOW_REF_FIELD_UNRESOLVED registered validate.rs:403 and emitted with layer 'semantic' + severity Warning at ref_check.rs:362-371. No finding on code/layer/location.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":384,"severity":"low","finding":"Warning message is NOT verbatim despite the doc-comment claim at gallery.tsx:355-356 ('copied verbatim from validate.rs + ref_check.rs'). Fixture: \"'agent_1.output' accesses field '.title' but the object shape is unknown; ...\". The backend's first interpolation is render_expr(expr) (ref_check.rs:366-367), and render_expr (ref_check.rs:434-450) renders the FULL expression including every access — so for {{ agent_1.output.title }} the server emits \"'agent_1.output.title' accesses field '.title' ...\". Impact is nil on screen (WORKFLOW_REF_FIELD_UNRESOLVED has HUMAN_COPY at validationCopy.ts:150, so finding.message is never rendered), but the 'verbatim' claim is false and this fixture is the template others will copy.","status":"confirmed"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":366,"severity":"medium","finding":"Seeded def CONTRADICTS the findings it is paired with, so the combination is one the backend cannot produce. errorValidation asserts WORKFLOW_PROMPT_MISSING ('step has neither prompt: nor prompt_file:') on 'agent_1', but agentStepFixture carries a full prompt (gallery.tsx:239-240); it asserts WORKFLOW_TOOL_NO_TOOL ('tool step has empty tool:') on 'tool_1', but toolStepFixture sets tool:'search_trials' (gallery.tsx:257). validate.rs:681 and :726 emit these ONLY when the field is empty/whitespace. Consequence: seeded-wf-builder-problems marks agent_1/tool_1 broken in the step list while their real config is complete — a reviewer who opens either step (or wires StepConfigPanel into the surface) sees a fully-filled form under a red marker. Only defensible as a 'validation is stale relative to the edit' transient, which nothing in the fixture says.","status":"confirmed"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":384,"severity":"medium","finding":"The warning references a field access that does not exist in the seeded def. Fixture message says 'agent_1.output' accesses field '.title'; the seeded summarize step's prompt is 'Summarise the key points from {{ agent_1.output }} in five bullets.' (gallery.tsx:315) — no .title access anywhere in builderFourStepDef. ref_check.rs:343-372 emits WORKFLOW_REF_FIELD_UNRESOLVED only from an actual Access::Field walk over a parsed reference, so this finding could never be produced for this def.","status":"confirmed"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":365,"severity":"low","finding":"REJECTED as a finding: the two error entries omit `severity`, while the Rust ValidationError serializes it unconditionally (validate.rs:429-430 uses serde `default` for DESERIALIZATION only). TS `severity?: Severity` (types.ts:7443) makes it legal, and severityOf() (validationCopy.ts:196-198) treats absent as 'error' — identical rendering. Not worth a change.","status":"rejected"}
{"angle":"fixture-reality","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":268,"severity":"low","finding":"REJECTED: toolInputSchema (required string / integer+default / boolean / closed enum) is a plausible MCP `Tool.input_schema` and exercises describeToolSchema's text/integer/switch/select arms (toolSchemaForm.ts:157-194). No invented construct; nothing to compare it against on the backend since input_schema is an opaque serde_json::Value.","status":"rejected"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/coverage.ts","line":487,"severity":"high","finding":"FALSE coverage claim the gate cannot detect. The reason says ToolStepForm is rendered '...AND, via StepConfigPanel, by seeded-wf-builder-populated whose def now contains a tool step'. StepConfigPanel renders ONLY the selected step (StepConfigPanel.tsx:47 `steps.find(s => s.id === selectedStepId)`, dispatched at :88), and populatedBuilderSurface seeds selectedStepId:'agent_1' (gallery.tsx:440) — so that surface renders AgentStepForm, never ToolStepForm. The gallery comment at gallery.tsx:294-297 states the same wrong premise ('without one, ToolStepForm is never rendered by the surface whose coverage entry claims it'): adding the tool step to the def is necessary but NOT sufficient; it must also be selected. gen-gallery-coverage.mjs only checks that a KEY exists, never the reason prose, so this stands unchallenged.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/RUNTIME_FINDINGS.md","line":9,"severity":"high","finding":"The committed runtime-health record is a garbage run and now asserts a false product state. Base said '🔴 HIGH (gating) | 0' + 'None — every surface is runtime-clean of gating HIGH findings'; the branch commits '🔴 HIGH (gating) | 2393' and a 16-row 'Surfaces with HIGH findings (gate-failing)' table. Every listed cause is the stale-Vite harness artifact — 'net::ERR_NETWORK_CHANGED' (31 occurrences) and 'Failed to fetch dynamically imported module: http://localhost/modules/...' — including a `crash` finding on deep-chat-tool-group caused by ConversationPage failing to import. request-failed 2620/2635 were auto-muted as harness noise while their console-error twins were not, which is the signature of a dev-server restart mid-run, not a regression. Additionally the entire 'Baselined (documented pre-existing — non-gating)' section and its triage note were DELETED (2 → 0), losing the seeded-s3-group-widget-error triage. Committing this both slanders 16 unrelated surfaces and destroys the baseline the gate subtracts from, so the next real regression has nothing to be measured against. Re-run on a quiet worktree Vite and recommit.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/coverage.ts","line":488,"severity":"medium","finding":"Both new/changed coverage reasons (lines 487 and 488) cite 'builder-tool-picker.spec.ts' as supporting evidence, but that file is NOT committed on this branch — `git ls-files --error-unmatch src-app/ui/tests/e2e/workflows/builder-tool-picker.spec.ts` fails and `git status` lists it as untracked (alongside builder-validation-attribution.spec.ts and helpers/builder-tools-mock-server.ts). As committed, the coverage record points at a test that does not exist in the repository. (The file does exist in the working tree and does exercise TEST-4/5/6/7 over the picker + generated form, so the substance is real — only the commit is missing.)","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":461,"severity":"medium","finding":"The toolStepSchemaSurface doc comment describes a surface that is not what renders. It claims 'the Tool field is a picker over the server's real tools' and 'Seeded through the tool CATALOG store so no network call is made — the same shape `listTools` returns'. The surface renders ONLY <ToolArgumentsForm> (gallery.tsx:489-495); ToolArgumentsForm.tsx has no Tool field at all (it maps spec.fields, :237-246), and neither ToolCatalogStoreDef nor listTools is imported or touched anywhere in the surface — the schema is the local literal toolInputSchema (gallery.tsx:268). A reader trusting the comment would believe INV-3 (the picker) has a gallery cell; it does not. The registered `note` at gallery.tsx:924 is, by contrast, accurate.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":933,"severity":"low","finding":"Title/slug say 'server unreachable' but the rendered failure is a DIFFERENT kind. The step's server is 'a_server_that_is_not_available' (gallery.tsx:515), which no entry in McpServer.servers matches, so entryForServerName returns {kind:'unknown-server'} (ToolCatalog.store.ts:148-153) and the Alert shows the unknown-server copy ('...which isn't one of the servers available to you...', :163) — not the 'unreachable' copy ('Couldn't reach \"X\" (detail)...', :167). The store's own comment (:86-89) calls `unreachable` the COMMON case, and it has no gallery cell.","status":"confirmed"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/stateMatrix.generated.ts","line":4041,"severity":"low","finding":"REJECTED: spot-checked every changed signal line number against source and all match — BuilderValidationPanel !finding.stepId:46 / validating:90 / cost:122; StepList from==null:47, steps.length===0:59, problems>0:115, problems===0&&cautions>0:128; ToolArgumentsForm templated:83,111,203 + overflowNames:247; ToolStepForm blockingFailure:239, usePicker:258, useGenerated&&spec:282, rows.length===0:301. Generated files are faithful, not hand-edited.","status":"rejected"}
{"angle":"coverage-honesty","file":"src-app/ui/src/dev/gallery/galleryCoverage.generated.ts","line":419,"severity":"low","finding":"REJECTED: the added GALLERY_SURFACES key 'modules/workflow/components/builder/ToolArgumentsForm' and the 419→420 count are exactly what gen-gallery-coverage.mjs's collectSurfaces() produces for the one new .tsx under src/modules; STATE_MATRIX.md summary (338→339 surfaces, 2067→2077 signals) is consistent with the ToolArgumentsForm addition + the 2 new StepList branches + the RefInsertMenu compact branch.","status":"rejected"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":258,"severity":"medium","finding":"The tool step's PRIMARY state has no gallery cell — only its degraded fallback does. `usePicker` (:258, the Combobox over the server's real tools) and `useGenerated && spec` (:282, the embedded generated arguments form) both require a resolvable server whose catalog carries the selected tool. seeded-wf-builder-tool-fallback deliberately uses an unresolvable server, so both are false there; seeded-wf-builder-populated never selects the tool step; seeded-wf-builder-tool-schema-form renders ToolArgumentsForm standalone, bypassing ToolStepForm entirely. Net: no screenshot in the gallery ever shows what a correctly-configured tool step looks like — every design review of this form sees the escape hatch. (Functionally covered by the untracked builder-tool-picker.spec.ts TEST-4/TEST-5; this is a visual-review gap, not a behavioural one.)","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/BuilderValidationPanel.tsx","line":46,"severity":"medium","finding":"The panel's whole-workflow finding branch has no gallery cell. `if (!finding.stepId)` (:46) renders the finding as plain <li> text with the title 'Whole workflow' (validationCopy.ts:252) instead of a clickable Button — the component's doc comment (:21-22) calls this out as a designed state. All three fixture findings in errorValidation resolve to real steps (agent_1 / tool_1 / summarize), in BOTH surfaces that use it (seeded-wf-builder-validation-error and seeded-wf-builder-problems), so the unattributed rendering is never shown. Real backend codes routinely land here: WORKFLOW_NO_STEPS, WORKFLOW_CYCLE, WORKFLOW_TOO_MANY_STEPS and every `outputs[...]` location (validationCopy.ts:190) have no step.","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/ToolStepForm.tsx","line":301,"severity":"medium","finding":"ToolStepForm's declared required state `empty` (`rows.length === 0` → 'No arguments', :301-303) is still unreached by any gallery cell even though the branch added a ToolStepForm surface. seeded-wf-builder-tool-fallback seeds toolStepFixture.arguments with three keys (gallery.tsx:258-262) and, with spec null, argsToRows consumes all of them (:178-180) → 3 rows. stateCoverage.ts:459 still excuses this as 'via surface — rendered within its page; empty branch proven by Part 2 runtime coverage', which was written before any ToolStepForm cell existed and is now a stale excuse next to a real cell that could have delivered it (arguments: {}).","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/ToolArgumentsForm.tsx","line":247,"severity":"low","finding":"The overflow notice `spec.overflowNames.length > 0` (:247-252, 'Showing N of M arguments this tool accepts') has no gallery cell: toolInputSchema declares 4 properties (gallery.tsx:271-291) against SCHEMA_BUDGET.maxFields = 24 (toolSchemaForm.ts:48), so overflowNames is always []. A 25+-property schema fixture would cover it in the same surface.","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":485,"severity":"low","finding":"The schema-driven form is only HALF populated, so representative-data layout is only half proven. splitArguments puts {query, limit} in `known` (legacy_flag goes to `extra`), leaving include_completed (Switch) and phase (Select) with no value — they render unchecked / placeholder. A cell with all four fields carrying values (including a long enum label and a long string) is what would expose real-data layout bugs in the label row, which already carries two action buttons in template mode (ToolArgumentsForm.tsx:81-107).","status":"confirmed"}
{"angle":"state-completeness","file":"src-app/ui/src/modules/workflow/components/builder/StepList.tsx","line":115,"severity":"low","finding":"REJECTED: both new StepList branches ARE covered by seeded-wf-builder-problems. `problems > 0` (:115) lights on agent_1 and tool_1 (two errors); `problems === 0 && cautions > 0` (:128) lights on summarize (the sole warning, location 'summarize.prompt' resolving via resolveFindingStep's head split). RefInsertMenu's new `compact` branch (:66) is covered by the schema-form cell (ToolArgumentsForm.tsx:104 passes compact) and non-compact by the existing builder cells; ToolArgumentsForm's three `templated` branches (:83,:111,:203) are covered by limit:'{{ inputs.since_year }}' in the integer field.","status":"rejected"}
{"angle":"patterns-conformance","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":911,"severity":"low","finding":"REJECTED: the three new seeded entries conform to SeededSurfaceEntry (sdk/packages/gallery/src/registry/types.ts:62-81) and to the house pattern seen in e.g. src/modules/citations/gallery.tsx — slug/title/note/path:'/'/initialPath:'/'/component, no `surface` field (that is overlay-only), unique `seeded-wf-*` slugs (assertUniqueSlugs, registry-core.ts:62). The hand-rolled `lazy(async () => ...)` store-seeding wrappers match the pre-existing agentFormSurface/populatedBuilderSurface/validationErrorSurface in the same file rather than the lazyNamed/lazyProps helpers, which is correct for store-seeded surfaces. `fullHeight: true` on the two small max-w-xl form cells matches the pre-existing seeded-wf-builder-agent-form. No new module gallery.tsx is needed (gen-gallery-seed-registry gates module ownership, already satisfied).","status":"rejected"}
{"angle":"maintainability","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":298,"severity":"low","finding":"Identifier `builderFourStepDef` now holds FIVE steps (toolStepFixture appended at :340). The doc comment (:294) and the surface title (:903, '(5 steps)') were both updated; the identifier was not, and it is referenced from three surfaces (:439, :549, :584). A stale count in a name is exactly the thing the next author will trust instead of counting.","status":"confirmed"}
{"angle":"maintainability","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":251,"severity":"low","finding":"toolStepFixture (:251) and toolInputSchema (:268) are untyped object literals, unlike the file's other fixtures which are pinned to generated wire types (cannedTestResult: TestRunResponse :57, cannedDryRunResult: DryRunResult :81, errorValidation: ValidateDefResponse :363). Nothing ties toolInputSchema to `Tool.input_schema` or toolStepFixture to the tool StepDef, so a wire-shape change will not fail tsc here — the fixture just silently stops resembling reality. (builderFourStepDef's `as unknown as BuilderDef` at :342 has the same effect for the step, and predates this branch.)","status":"confirmed"}
{"angle":"maintainability","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":363,"severity":"low","finding":"errorValidation is an INLINE fixture that no gate checks against the backend. check-gallery-fixtures.mjs validates only src/dev/gallery/fixtures/recorded/*.json against openapi.json (its MANIFEST, scripts/check-gallery-fixtures.mjs:28-33), and the Rust drift guard validation_codes_are_registered_and_humanised reads validationCopy.ts, not this file — so a backend message rewording or a code rename leaves this fixture silently wrong (which is precisely how the pre-branch invented codes survived). Cheapest durable fix: assert these three codes against HUMANISED_CODES (validationCopy.ts:161) in a unit test, so a renamed code fails.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":486,"severity":"low","finding":"`if (!spec) return null` is unreachable. describeToolSchema returns null only for a non-record schema, a missing/non-record `properties`, or zero properties (toolSchemaForm.ts:206-212); toolInputSchema is a record with four properties, so spec is always truthy. It is also placed AFTER `splitArguments(toolStepFixture.arguments, spec)` at :485, which already accepts null — so the guard protects nothing even in principle.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":494,"severity":"low","finding":"`onChange={noop}` makes every control on the schema-form surface inert: typing in `query`, toggling `include_completed`, picking a `phase`, the RefInsertMenu insert, and the 'Use a value instead' undo (ToolArgumentsForm.tsx:81-107) all no-op. DEC-5's reversible template mode — the property those two buttons exist to provide — therefore cannot be demonstrated or interaction-tested from this cell, and a runtime interaction recipe pointed at it would silently observe nothing. The sibling fallback cell (:519-526) wires the real store instead; wiring commitField the same way here costs one closure.","status":"confirmed"}
{"angle":"dead-code","file":"src-app/ui/src/modules/workflow/gallery.tsx","line":900,"severity":"low","finding":"REJECTED: no dead registrations. All three new seeded entries have unique slugs and reachable lazy components; every new module-level fixture (toolStepFixture, toolInputSchema) has at least one consumer; `noop` and `cleanValidation` remain used; gallery.tsx exports only `gallery`, consumed by the runtime registry (@/dev/gallery/support), and gallery.tsx is excluded from the surface denominator by gen-gallery-coverage.mjs's walk() filter.","status":"rejected"}
