# TESTS — control-mcp-e2e-coverage

Every ITEM is covered; every `INV-N` is pinned by an `[acceptance]` test that
would FAIL if the invariant were violated.

## FIX 1 — multi-word capability search

- **TEST-1** (tier: unit) [acceptance] [invariant: INV-7] [covers: ITEM-1, ITEM-2, ITEM-3] file: `src-app/server/src/modules/control_mcp/handlers.rs` — asserts: the EXACT live-session query — `op_match_score` over a fixture catalog containing `Project.create`/`Project.list`/`Assistant.create`/… ranks `Project.create` FIRST for `"create project"` and the match set is NON-EMPTY. Fails on the shipped whole-phrase `.contains` matcher (which scores 0 matches), so it is a genuine regression proof, not a restatement of the new code.
- **TEST-2** (tier: unit) [covers: ITEM-1, ITEM-3] file: `src-app/server/src/modules/control_mcp/handlers.rs` — asserts: ALL-terms semantics — an op matching only SOME terms (`Project.list` for `"create project"`) is excluded, and a term matching via a DIFFERENT field than its sibling (one term hits `tags`, the other hits `summary`) still matches.
- **TEST-3** (tier: unit) [covers: ITEM-1, ITEM-3] file: `src-app/server/src/modules/control_mcp/handlers.rs` — asserts: single-term parity — every op the old whole-phrase predicate matched for `"project"` / `"create"` / `"CREATE"` is still matched (case-insensitive), plus empty / whitespace-only query returns everything (passthrough) and a term matching nothing yields zero.
- **TEST-4** (tier: integration) [covers: ITEM-4] file: `src-app/server/tests/control_mcp/mod.rs` — asserts: through the REAL JSON-RPC surface against the REAL 300+-op catalog, `list_capabilities{query:"create project"}` returns `total > 0` and `operations[0].operation_id == "Project.create"`; and the pre-existing single-term counts (`"project"`, `"create"`) are non-zero.

## FIX 2 — title generation never runs

- **TEST-5** (tier: unit) [covers: ITEM-5] file: `src-app/server/src/modules/chat/extensions/title/title.rs` — asserts: `build_title_request` carries the reasoning-safe budget (≥ 4096, i.e. > the starving 512 that the measured bridge run exhausted), no tools, and `thinking.mode == Disabled`.
- **TEST-6** (tier: unit) [covers: ITEM-6] file: `src-app/server/src/modules/chat/extensions/title/title.rs` — asserts: the escalated-retry decision — retry ONLY when the text is empty AND `finish_reason` is budget-exhausted (`length`/`max_tokens`); never on an empty `stop` completion (which must stay a single call), never when text was produced, and never twice.
- **TEST-7** (tier: integration) [covers: ITEM-7] file: `src-app/server/tests/chat/title_test.rs` — asserts: the budget that reaches the WIRE is the new constant (updated `title_request_carries_the_reasoning_safe_token_budget`), and the pre-existing empty-generation test still records exactly ONE title call (the retry must not fire on `finish_reason: "stop"`).
- **TEST-8** (tier: integration) [covers: ITEM-6, ITEM-7] file: `src-app/server/tests/chat/title_test.rs` — asserts: with a stub whose first title call ends budget-exhausted (`finish_reason: "length"`, no text) and whose second returns text, the extension issues TWO title calls, the second at the larger budget, and the conversation ends TITLED.
- **TEST-9** (tier: integration) [acceptance] [invariant: INV-8] [covers: ITEM-8] file: `src-app/server/tests/chat/title_real_llm_test.rs` — asserts: driving a real first exchange through the CONFIGURED test LLM (the local reasoning bridge), the conversation ends up with a non-empty title. This is the test that fails if titles stop being generated; it FAILS on the pre-fix 512-token budget against that same model (verified reproduction, DESIGN §6).

## BUILD — real e2e coverage of the control surface

- **TEST-10** (tier: unit) [covers: ITEM-9] file: `src-app/server/tests/chat/helpers.rs` — asserts: `configured_test_llm()` resolution order — the OpenAI seam, the Anthropic seam, and the global `ZIEE_TEST_LLM_BASE_URL` each resolve; `None` ONLY when nothing is configured; an Anthropic-absent-but-OpenAI-present environment resolves (never skips).
- **TEST-11** (tier: integration) [acceptance] [invariant: INV-2] [covers: ITEM-10] file: `src-app/server/tests/control_mcp/real_llm_test.rs` — asserts: the Tier-4 control real-LLM tests run against whatever LLM is configured (they execute, and report the resolved provider/model in their output) and skip ONLY when `configured_test_llm()` is `None`. Would fail if the gate were narrowed back to `ANTHROPIC_API_KEY`, since the run environment sets the OpenAI seam.
- **TEST-12** (tier: e2e) [acceptance] [invariant: INV-1] [covers: ITEM-17] file: `src-app/ui/tests/e2e/control/control-spec-gating.spec.ts` — asserts: no spec under `tests/e2e/control/` gates itself on a single vendor's key — every `test.skip(...)` gate in that directory resolves through the shared `configuredTestLlm()` seam, and the string `ANTHROPIC_API_KEY` appears in no gate expression. This is the executable form of the design's guiding rule; it fails the moment someone reintroduces the "covered by tests that never execute" state.
- **TEST-13** (tier: e2e) [acceptance] [invariant: INV-3] [covers: ITEM-11, ITEM-12, ITEM-13] file: `src-app/ui/tests/e2e/control/control-tool-in-chat.spec.ts` — asserts: the natural-language DISCOVERY journey — the prompt "create a new project called <Foo>" never names an operation id, and the recorded MCP tool-call history for that conversation contains a `list_capabilities` call. Fails on the pre-fix search (the live session's 0-result path leaves the model unable to reach the mutating invoke, so the downstream approval/creation assertions never satisfy).
- **TEST-14** (tier: e2e) [acceptance] [invariant: INV-4] [covers: ITEM-13, ITEM-14] file: `src-app/ui/tests/e2e/control/control-tool-in-chat.spec.ts` — asserts: in a FRESH (auto-approve) chat the mutating `invoke_capability` still raises the approval card — the card is visible before anything is created, for each table row.
- **TEST-15** (tier: e2e) [acceptance] [invariant: INV-5] [covers: ITEM-13, ITEM-14] file: `src-app/ui/tests/e2e/control/control-tool-in-chat.spec.ts` — asserts: approve → the entity REALLY exists via the REST API, table-driven over `Project.create` (`GET /api/projects`), `Assistant.create` (`GET /api/assistants`) and `MemorySettings.update` (`GET /api/memory/settings` reflects the requested change) — never asserted through the chat DOM.
- **TEST-16** (tier: e2e) [acceptance] [invariant: INV-5] [covers: ITEM-15] file: `src-app/ui/tests/e2e/control/control-tool-in-chat.spec.ts` — asserts: deny → nothing created, table-driven over `Project.create` and `Assistant.create` (the pre-existing `denying the control write leaves nothing created` test survives as the `Assistant.create` row, strengthened with a total-count baseline rather than a name-only match).
- **TEST-17** (tier: e2e) [negative-perm] [acceptance] [invariant: INV-6] [covers: ITEM-16] file: `src-app/ui/tests/e2e/control/control-negative-perm.spec.ts` — asserts: a user holding `control::use` but NOT the op's permission — (a) NOT OFFERED: `list_capabilities`/`describe_capability` omit/refuse `MemorySettings.update` for a user lacking `memory::write`; (b) DENIED: the same user driving the natural-language "create a project" prompt ends with NO project created (`GET /api/projects` empty) even after approving.
- **TEST-18** (tier: e2e) [covers: ITEM-11, ITEM-12] file: `src-app/ui/tests/e2e/control/control-tool-in-chat.spec.ts` — asserts: the chat reflects the mutation — after approval the conversation's assistant turn reports the created entity (the design's "and the chat reflects it"), proving the tool result was fed back to the model rather than the write happening invisibly.
