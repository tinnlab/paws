# HUMAN_FEEDBACK — chat-boot-fetch-hygiene

- **FB-1** [status: resolved] — "I told you to 're-run the enumerated tests.' That phrasing has been causing agents on this campaign to re-run entire suites after every fix iteration. One branch spent ~17 × 32-minute serial Playwright runs that way; it was the dominant cost of the branch and proved almost nothing new each time. […] during iteration, run ONLY the tests covering the files you actually touched, plus a typecheck. Run the full enumerated set exactly ONCE, at the end, before declaring READY." → Adopted for the remainder of the work. Iteration used single-test runs (`-g "TEST-N"`) and, after the production change, the scoped regression set for the two touched files; the full enumerated set was run once at the end and is what TEST_RESULTS.md records. No standard was relaxed — every enumerated test has an observed PASS from that final run. [generalizable: yes — during a fix/iterate loop run only the tests covering the touched files plus a typecheck; run the full enumerated set once, at the end, as the phase-8 record. A prior PASS is not invalidated by unrelated edits, so re-running the whole set per iteration buys nothing and can dominate a branch's cost.]

- **FB-2** [status: resolved] — "But 'addressed' does not mean 'changed'. If the serialization is genuinely intentional and load-bearing […] then the correct outcome is to document why and leave it alone — write that up in DECISIONS.md with the evidence, and do not manufacture a change." → Rank 4 was investigated and left unchanged. The dependency is real and stronger than the triage described: `hasPermissionNow` reads permissions that only `/api/auth/me` populates, and for `server-update` the permission also gates whether the MODULE LOADS AT ALL (`shouldLoad: ctx => … && ctx.can(...)`), so there is no local reordering available. Recorded in DECISIONS.md DEC-2/DEC-3 with file:line evidence, and — going beyond documentation — pinned by TEST-8, whose falsifiability was verified by breaking the gate and observing red. No production code was changed for Rank 4.

No other human feedback has been received on this branch.
