# BASE — conflict surface vs the branch base

Base ref for this branch and for every lifecycle gate: **`origin/feat/agent-core`**
(NOT `origin/main` — this work never merges to main; the default base would
misattribute the whole agent-core diff to this branch).

Branch: `fix/control-describe-schema`, cut from `origin/feat/agent-core`
at `b29adbad5`.

## Migrations

Migrations in this repo are **per-module** (`src-app/server/src/modules/*/migrations/`),
not one flat dir. Highest existing by filename across `src-app`:
`202607191300_agent_delegate_enabled.sql`. The control module's own migration is
`202607146025_control_mcp_grant_permissions.sql`.

**This branch adds NO migration**, so there is no number to collide.

## Files this branch touches that the base is also moving

- `src-app/server/src/modules/control_mcp/**` — the control feature landed on
  `feat/agent-core` recently (`handlers.rs`, the e2e specs, the SDK extraction of
  `catalog`/`policy`/`tools`). Active area; expect the base to move under us.
  Mitigation: re-merge `origin/feat/agent-core` before handing off.
- `sdk` submodule pointer — the base bumps it routinely
  (`chore(sdk): bump submodule → …`, `ee665ee34 chore: rebase sdk commits …`).
  This branch bumps it again, so a pointer conflict is likely and is resolved by
  re-cutting the SDK commit onto the newest `sdk/agent-core-and-perf` tip.
- `src-app/server/tests/control_mcp/mod.rs` — appended to; base also edits it.
- `src-app/ui/tests/e2e/control/` — a NEW spec file, so no textual conflict.

## OpenAPI regen

**Not implied.** No `#[derive(JsonSchema)]` type changes, no new/changed REST
handler signature. The control MCP surface is JSON-RPC served from an untyped
`axum::response::Response`, so its payload shape is not in `openapi.json`.
`src-app/ui/openapi/openapi.json` is only READ (as a test/measurement input), never
regenerated by this branch.

## Cross-repo note

`sdk/` is a git submodule (`github.com/ziee-ai/sdk`, branch
`sdk/agent-core-and-perf`). ITEM-5 and ITEM-6 land inside it, so this branch
carries a submodule pointer bump to a **locally-committed, unpushed** SDK commit.
The orchestrator must push the SDK commit before (or with) the ziee branch, or
the pointer will be unresolvable for anyone else.
