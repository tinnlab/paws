# TESTS — enumerated up front

No user-facing permission is introduced (pure build/test/dev infra), so no A9
backend-deny / A10 restricted-user e2e applies. The diff touches the `ui` and
`desktop/ui` workspaces (vite/playwright/gallery configs + tests), so the
frontend gate applies: ≥1 `tier: e2e` test (TEST-13) + phase-8 `npm run check` /
`gate:ui` per workspace.

- **TEST-1** (tier: unit) [covers: ITEM-1] file: `sdk/crates/ziee-build-support/src/worktree_db.rs` — asserts: `port_base(key, floor, span)` is deterministic, in `[floor, floor+span)`, and stable across calls; `worktree_key_for_cwd`/`worktree_root` strips `/src-app` consistently.
- **TEST-2** (tier: unit) [covers: ITEM-2] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: `worktreeKey(root)` = FNV-1a 8-hex; `portBase(key,floor,span)` matches the Rust math; `pickBindablePort(start)` returns a bindable port and skips a held one.
- **TEST-3** (tier: unit) [covers: ITEM-1, ITEM-2] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: CROSS-LANGUAGE parity — for a shared fixture root string the TS `worktreeKey` equals the value the Rust `worktree_key` test pins (byte-identical vector), and `portBase` agrees.
- **TEST-4** (tier: unit) [covers: ITEM-3] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: the no-foreign-reuse predicate `serverIsThisWorktree(sentinelRoot, ownRoot)` returns true only when roots match; a foreign root → false (→ gate-ui must boot its own port).
- **TEST-5** (tier: unit) [covers: ITEM-4] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: the sentinel payload builder emits the worktree root; the vite sentinel middleware returns it on `/__worktree`.
- **TEST-6** (tier: unit) [covers: ITEM-6] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: `resolveGalleryPort({env, cfgPort})` resolves env > cfg > key-derived, and `cfgPort=null` → key-derived (never `NaN`).
- **TEST-7** (tier: unit) [covers: ITEM-5] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: the shared port resolver used by gate-ui/runtime-health/visual returns the SAME port for the same env+cwd (so gate-ui and its runtime-health child agree), and honors an explicit `GALLERY_PORT`.
- **TEST-8** (tier: unit) [covers: ITEM-8] file: `src-app/ui/tests/fixtures/e2e-data-dir.test.mjs` — asserts: the yaml-fragment builder sets `app.data_dir` to a per-testId dir under `<worktree>/.ziee-cache/e2e-app-data` (never `~/.ziee`) and symlinks the read-only `bin/` cache.
- **TEST-9** (tier: unit) [covers: ITEM-9] file: `src-app/server/src/modules/file/utils/embedded.rs` — asserts: extract writes a temp file then atomic-renames; two concurrent extracts under an flock produce a byte-/sha256-intact final file (never a torn partial); an already-present intact file is not re-written.
- **TEST-10** (tier: unit) [covers: ITEM-10] file: `src-app/desktop/ui/tests/fixtures/reaper-filter.test.mjs` — asserts: the desktop reaper's container-name filter is namespaced by the run's port-base (never the bare un-namespaced prefix) and a container whose runId is in the shared live set is KEPT.
- **TEST-11** (tier: unit) [covers: ITEM-11] file: `src-app/desktop/ui/tests/fixtures/reaper-filter.test.mjs` — asserts: the desktop port allocator bind-verifies (skips a held port), uses a base disjoint from the web-e2e `9100`, and reads a key/env-overridable lock dir.
- **TEST-12** (tier: unit) [covers: ITEM-12] file: `src-app/ui/tests/fixtures/e2e-data-dir.test.mjs` — asserts: when `ZIEE_E2E_BASE_*`/`ZIEE_E2E_LOCK_DIR` are unset, the defaults derive from `worktreeKey()` (base ports from `portBase`, per-worktree lock dir) — lock dir and port base move together; an explicit env still wins.
- **TEST-14** (tier: unit) [covers: ITEM-7] file: `sdk/packages/gallery/scripts/lib/run-key.test.mjs` — asserts: the desktop port resolver (same `resolveGalleryPort` used by desktop scripts/configs) derives a key-based port when neither env nor `gallery.config.json` `port` is forced, and desktop's floor is disjoint from web's floor (no cross-workspace 1420 collision).
- **TEST-13** (tier: e2e) [covers: ITEM-13, ITEM-3, ITEM-4, ITEM-8, ITEM-9, ITEM-12] file: `scripts/prove-worktree-isolation.sh` — asserts: at K worktrees launched simultaneously, ports are pairwise-disjoint, zero `"reusing gallery dev server"` foreign hits, each gate:ui sees only its own sentinel root, zero `ERR_ABORTED/ECONNREFUSED/EADDRINUSE/port already allocated/ENOENT-config/55006/3D000/42P04`, no run's docker reaped by another, sha256-intact extracted binaries, each e2e wrote to its own data-dir not `~/.ziee`. Green at K=8 cold = exit condition. Runs runId-scoped only.
